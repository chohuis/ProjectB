- 파일 먼저 읽고 완성된 솔루션 작성
- 불필요한 설명 없이 코드만
- 한 번에 테스트

## 작업 대상 (2026-08-13 개정 — Godot 이주)

**개발은 `04.GodotOnePitch/` 하나뿐이다.** 코드 규칙은 `04.GodotOnePitch/CLAUDE.md`.

| 폴더 | 성격 |
|---|---|
| `04.GodotOnePitch/` | **유일한 개발 대상** (Godot 4.6 · GDScript 단독) |
| `02.SvelteElectron/` | **동결 — 참조용.** 이주가 끝날 때까지 **읽기 전용**으로 둔다 |
| `03.OnePitch/` · `00.OneF/` · `03.App/` · `_archive/` | 아카이브. 수정하지 않는다 |
| `docs/` | 02 시절 문서. 이주 문서는 `04.GodotOnePitch/docs/` |

### `02.SvelteElectron/`을 어떻게 쓰나

**고치지 않는다. 읽는다.** 그 안에 이주에 필요한 게 두 가지 있다:

1. **동작 명세** — 어떤 규칙이 왜 그런 값인지가 커밋 메시지와 주석에 있다.
   드래프트 앵커·수상 자격선·잠재력 범위·교체 규칙이 전부 실측 근거와 함께
   적혀 있다. 옮길 때 근거를 다시 찾을 필요가 없다.
2. **검사 53개** — 비싸게 배운 것이 코드로 박혀 있다. **로직보다 먼저 옮긴다.**

⚠ **밸런스 작업은 동결됐다** (2026-08-13). 이주 중에 게임 수치를 바꾸지
않는다 — 두 변수를 동시에 움직이면 무엇이 원인인지 못 가린다. 이주 중에
떠오른 개선은 목록으로 쌓아두고 이주 후에 처리한다.

- `03.OnePitch/`의 설계 문서를 근거로 **새 코드를 작성하지 않는다.** 그쪽 스택(Flutter + Rust frb)은 폐기됐다.
- `03.OnePitch/03_설계/12_구현확정_결정요약.md`이 그 프로젝트에서 살릴 결정의 단일 창구다.
