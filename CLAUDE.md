- 파일 먼저 읽고 완성된 솔루션 작성
- 불필요한 설명 없이 코드만
- 한 번에 테스트

## 작업 대상 (2026-08-20 개정 — 02로 되돌림)

**개발은 `02.SvelteElectron/` 하나뿐이다.** 코드 규칙은 `02.SvelteElectron/CLAUDE.md`.

| 폴더 | 성격 |
|---|---|
| `02.SvelteElectron/` | **유일한 개발 대상** (Svelte + Electron + Rust napi) |
| `04.GodotOnePitch/` | **동결 — 참조용.** 지우지 않는다 |
| `03.OnePitch/` · `00.OneF/` · `03.App/` · `_archive/` | 아카이브. 수정하지 않는다 |
| `docs/` | 02 시절 문서 |

### 왜 되돌렸나 (2026-08-20)

Godot 이주(2026-08-13 시작)를 멈추고 02로 돌아온다. **화면 완성도가 02가
앞선다**는 사용자 판단이다 — 02는 화면 57개·기능 폴더 21개이고, 04는 그
절반을 못 채웠다.

### `04.GodotOnePitch/`를 어떻게 쓰나

**고치지 않는다. 읽는다. 지우지 않는다.** 그 안에 두 가지가 있다:

1. **02 동작 명세** — 02 코드에 근거가 안 적힌 값들이 04 주석에 **실측
   근거와 함께** 있다. 드래프트 앵커·수상 자격선·잠재력 범위·NPC 대응표·
   구장 좌표가 그렇다. 02를 고칠 때 그걸 본다.
2. **검사 4,141개** — 이주하면서 02 결함 여럿을 여기가 찾아냈다.

⚠ **04 문서를 그대로 믿지 않는다.** "02가 이랬다"고 적힌 것 상당수가
**고치기 전 상태의 기록**이다(02 마지막 커밋 08-17 · 04 시작 08-13 — 겹친다).
2026-08-20에 훑어보니 대부분 이미 02에서 고쳐져 있었다:
구단 성향 호출 · 부상 완치 복구 · NPC 성장 `t.tier` · 관계 주 인덱스 ·
주인공 지명(`draftDrafted`) · 주인공 은퇴 · 시즌 종료 통합 · 자동 진행 분류.
**옮기기 전에 02 코드에서 아직 그런지 확인한다.**

⚠ 04 문서의 "02 검사 53개"는 **파일 수**다. 실제 검사는 **604건**이다.

### 02에 남아 있는 것 — 넷 (2026-08-20 실측)

| # | 결함 | 02 위치 | 비용 |
|---|---|---|---|
| 1 | 난수 `thread_rng` 31곳 — 같은 세이브·같은 시드라도 결과가 매번 다르다 | `packages/engine-native/src/*.rs` | 큼 |
| 2 | ~~이벤트 로더 0건~~ — **틀렸다(2026-08-23 실측).** 로더도 호출부도 있고 `events/` 553건이 실린다. `02/CLAUDE.md` 참고 | — | — |
| 3 | 상무 Phase 1이 한 번도 안 돈다 — 호출부가 `&[]`를 넘긴다 | `npc_sim.rs:2567` | 중간 |
| 4 | 구장 좌표가 세 티어 모두 그림과 어긋난다 | `scripts/fit-park-anchors.cjs` + refs | 작음 — `04/data/parks.json`에 실측값이 있다 |

⚠ **밸런스는 여전히 동결이다.** 두 변수를 동시에 움직이면 원인을 못 가린다.
04에서 실측한 값을 02에 넣을지는 **건마다 사용자에게 묻는다.**

- `03.OnePitch/`의 설계 문서를 근거로 **새 코드를 작성하지 않는다.** 그쪽 스택(Flutter + Rust frb)은 폐기됐다.
- `03.OnePitch/03_설계/12_구현확정_결정요약.md`이 그 프로젝트에서 살릴 결정의 단일 창구다.
