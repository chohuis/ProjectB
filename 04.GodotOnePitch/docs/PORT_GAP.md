# 02 → 04 이식 격차 — 전수 조사

**2026-08-17** · 이 문서가 **남은 이식의 정본**이다.
남은 일 전반은 `docs/QUEUE.md`, 왜 그렇게 했는지는 `docs/RESUME.md`,
경기 화면 대조는 `docs/MATCH_PARITY.md`, 02 대조 아홉 축은 `docs/PARITY.md`.

> **목표는 하나다** — 02에서 동작하는 모든 기능이 04에서도 동작한다.
> UI·UX·화면·팝업·설정·연출까지.

⚠ **"안 옮기기로 했다"는 주석은 결정 기록이지 금지가 아니다.**
`settings_screen.gd:9-11`이 그 예다 — 사용자가 그 결정을 뒤집었다(2026-08-17).
같은 형태를 만나면 **금지가 아니라 후보로 본다.**

---

## 세어 본 결과

| | 개수 |
|---|---|
| 02 화면 파일 (`pages` 14 + `features/**/ui` 42) | **56** |
| 04 화면·부품 (`ui/screens` 14 + `ui/*_vm` 22 + `ui/parts` 11) | **47** |
| 판정 `있다` | **34** |
| 판정 `부분` | **11** |
| 판정 `없다` | **5** (첫 조사 8 → 병역 셋을 `부분`으로 정정) |
| 판정 `일부러 안 옮김` (→ 후보로 재검토) | **3** |
| 04에만 있는 것 (지우지 않는다) | **2** |

⚠ **개수는 파일 대응이지 기능 대응이 아니다.** 아래 "화면이 아닌 기능"에
설정 여섯 · i18n 등이 따로 있다.

### 다시 센 결과 (2026-08-18, P-38까지 닫고)

**위 표는 첫 조사 때 값이다.** 그 뒤로 닫은 것을 항목 단위로 다시 셌다.

| 닫은 묶음 | 항목 |
|---|---|
| 🟢 **결정 화면 아홉 갈래** | P-21 · P-21b · P-22 · P-24 · P-24b · P-25 · P-25b · P-26 · P-27 · P-28 · P-29 |
| 🟢 **병역 넷** | P-18 · P-18b · P-18c · P-18d |
| 🟢 **소식** | 본문 상세 · 월간 부상 리포트 · 다이제스트 · 유망주 TOP 10 · **이름표**(P-33) |
| 🟢 **부상 치료 · 훈련 결과** | P-30 · P-31 |
| 🟢 **재정** | 투자 절(P-32) · 투자 정산(P-32b) |
| 🟢 **새 게임** | 권역 2단 선택(P-34) · 능력치 프리셋 4택(P-35) |
| 🟢 **훈련** | 경고 룰 · 훈련 이력(P-36) |
| 🟢 **학사** | 이번 학기 진행(P-37) |
| 🟢 **리그** | 스탯 순위(P-38) |
| 🟢 설정 | 화면 톤 셋 + 나머지 셋(실측으로 판정) |

**남은 `부분` — 셋**(넷은 "다시 세야 한다"였고 넷 다 열어서 닫았다)

| 02 화면 | 무엇이 남았나 |
|---|---|
| `main/MainPage` | 우측 고정 패널이 없다 → **UI 방향 U-4** |
| `league/LeaguePage` | 팀 마크(U-1) · **대회 · 포스트시즌 · 리그 기록 탭** |
| `schedule/SchedulePage` | 팀 마크(U-1) |

⚠ **리그의 남은 세 탭은 "엔진이 없다"가 아니다** — 대회는 소식으로 오고
(`TournamentNews`), 포스트시즌은 `Postseason.run_background`가 시즌말에 돌아
`SeasonHistory`에 남으며, 이적·방출도 소식으로 온다. **표로 모아 보는 자리가
없을 뿐이다.**

**남은 `없다` — 셋**(둘은 UI 방향으로 옮겼다)

| 02 | 어디로 |
|---|---|
| `main-layout/TopHeader` (188) | **U-3** |
| `main-layout/RightPanel` (266) | **U-4** |
| `team/TeamMark` (31) | **U-1** |

⚠ **`InjuryTreatmentModal`은 `없다`에서 빠졌다** — P-30에서 닫았다.

⚠ **"다시 세야 한다"고 적어 둔 넷을 다 열었다** — `new-game` · `training` ·
`academics` · `league`. **넷 다 실제로 빠진 것이 있었다**(프리셋 · 경고 룰 ·
학기 진행 · 스탯 순위). `finance`도 그랬다(투자 절). **첫 조사가 파일 이름으로
대응시킨 것이 원인이다** — 항목 단위로 세야 보인다.

---

## 1. 02 페이지 14개

| 02 페이지 | 줄 | 04 대응 | 판정 |
|---|---|---|---|
| `main/MainPage` | 859 | `ui/screens/main_screen` + `main_vm` | **부분** — 우측 고정 패널이 없다 |
| `match/MatchPage` | 3,220 | `match_screen` + `match_vm` + `pitch_vm` + `briefing_vm` | **있다** (M-1~M-7로 아홉 패널 다 참) |
| `new-game/NewGamePage` | 1,749 | `new_game_screen` + `new_game_vm` | **부분** — 시작 프리셋·상세 입력 재확인 필요 |
| `league/LeaguePage` | 1,605 | `league_vm` (리그 탭) | **부분** — 팀 마크·정렬/필터 재확인 필요 |
| `training/TrainingPage` | 1,393 | `training_screen` + `training_vm` | **부분** |
| `status/StatusPage` | 1,004 | `status_screen` + `status_vm` (하위 탭 6) | **있다** |
| `schedule/SchedulePage` | 954 | `schedule_vm` (일정 탭) | **부분** — 팀 마크 없음 |
| `news/NewsPage` | 540 | `news_vm` (소식 탭) | **있다** |
| `academics/AcademicsPage` | 503 | `academics_vm` (하위 탭) | **부분** |
| `finance/FinancePage` | 436 | `finance_vm` (하위 탭) | **부분** |
| `team/TeamPage` | 304 | `team_vm` (팀 탭) + `team_detail_screen` | **있다** |
| `people/PeoplePage` | 290 | `people_screen` + `people_vm` | **있다** |
| `achievements/AchievementsPage` | 236 | `achievements_vm` (하위 탭) | **있다** |
| `me/MePage` | 123 | "나" 탭 자체 | **있다** |

---

## 2. 02 부품·모달 42개

### 2-1. 04에 전용 화면으로 있다 (10)

| 02 | 04 |
|---|---|
| `player/PlayerDetailModal` (1,602줄) | `player_detail_screen` + `player_detail_vm` |
| `team/TeamDetailModal` (979) | `team_detail_screen` + `team_detail_vm` |
| `season-end/SeasonEndModal` (1,195) | `season_end_screen` + `season_end_vm` |
| `career/DraftBoardModal` (690) | `draft_board_screen` + `draft_board_vm` |
| `pre-game-briefing/PreGameBriefingModal` (720) | `briefing_vm` (경기 화면 안) |
| `retirement/CareerEndScreen` (299) | `retirement_screen` + `retirement_vm` |
| `retirement/RetirementAskModal` (98) | 같은 화면 (`_open_retirement`) |
| `settings/SettingsModal` (276) | `settings_screen` — **항목이 둘뿐** (아래 3절) |
| `save-slots/SaveSlotScreen` (281) | `ui/app.gd` 슬롯 |
| `intro/IntroScreen` (161) | `title_screen` |

### 2-2. 결정 화면 하나로 합쳐져 있다 (9) — **부분**

04 `decision_screen` + `decision_vm`이 아홉 갈래를 한 화면으로 받는다:
`career_results` · `draft_observe` · `draft_notification` · `career_choice_hub` ·
`career_choice` · `fa_market` · `salary_negotiation` · `option_clause` · `trade`.

| 02 모달 | 04 |
|---|---|
| `contract/ContractNegotiationModal` (372) | `salary_negotiation` 갈래 |
| `contract/TradeModal` (196) | `trade` 갈래 |
| `contract/FaMarketModal` (105) | `fa_market` 갈래 |
| `contract/OptionClauseModal` (44) | `option_clause` 갈래 |
| `contract/DraftNotificationModal` (112) | `draft_notification` 갈래 |
| `career/DraftObserveModal` (141) | `draft_observe` 갈래 |
| `career/CareerChoiceHubModal` (181) | `career_choice_hub` 갈래 |
| `career/CareerResultsModal` (242) · `CareerResultModal` (133) | `career_results` 갈래 |
| `career/UniversityApplyModal` (214) · `IndependentApplyModal` (167) | `career_choice` 갈래의 선택지 |

⚠ **"합쳐 놓은 것"이 곧 "옮긴 것"은 아니다.** 02 모달마다 있던 표·비교·
근거 표시가 한 줄 선택지로 눌렸을 수 있다 — **모달 하나씩 열어 무엇을
보여주는지 세야 한다.** 지금은 `부분`으로 둔다.

### 2-3. 없다 (8)

| 02 | 04 엔진 | 값 |
|---|---|---|
| `main-layout/TopHeader` (188) | — | 화면만. **UI 방향 U-3** |
| `main-layout/RightPanel` (266) | — | 화면만. **UI 방향 U-4** |
| `team/TeamMark` (31) | — | 화면만. **UI 방향 U-1** |
| `injury/InjuryTreatmentModal` (134) | ❓ `injury_treat` 검색 0건 | **엔진부터 확인** |

### 2-3b. 병역 셋 — **"없다"가 아니라 "부분"이었다** (2026-08-18 정정)

🔴 **처음 조사에서 셋 다 "화면만 없다"로 적었는데 틀렸다.** 04엔
`StatusVm.military_of`가 이미 있고 `status_screen`이 그린다(U-2에서 넣었다).
**파일 이름으로 대응시킨 탓이다** — 이 문서가 스스로 경고한 "부분이 제일
위험하다"에 내가 걸렸다.

| 02가 보여주는 것 | 04 | |
|---|---|---|
| 부대(체육/일반) | ✅ `unit_label` | |
| 남은 기간(주) | ✅ `weeks_left` | |
| 입대 연도 | ✅ `enlist_year` (02엔 없다) | 04에만 |
| **계급** (이병 ≤8주 · 일병 ≤34 · 상병 ≤60 · 병장) | ❌ | **없다** |
| **복무 진행 %** | ❌ (`weeks_served`/`weeks_total`은 있다) | 계산만 하면 된다 |
| 계약 +2년 배지 | ❌ | 04에 그 축이 있는지 확인 |
| 컨디션·피로 | ✅ 상태 화면 위쪽에 이미 있다 | 자리만 다르다 |

⚠ **계급 문턱 넷은 02 `MilitaryStatusPanel.svelte:12-18`에 있다** — 지어낼 게 없다.

### 🔴 체육부대가 도달 불가다 — **열여덟 번째 죽은 배선** (2026-08-18)

`sim/military.gd`는 체육부대를 **네 곳**에서 갈라 쓴다:
`unit_label`(`:65`) · 입대 소식 문구(`:130`) · **복귀 적응 주차**
(`RECOVERY_SPORTS`, `:179`) · 전역 소식(`:232`).
그런데 **`"sports"`로 입대시키는 곳이 하나도 없다** —
`career_decision.gd:413`이 `Military.enlist(state, "general", at_day)`
**하나만** 부른다. 게다가 그 자리는 "갈 곳이 없을 때의 마지막 갈래"다.

| 02 | 04 |
|---|---|
| `SportsUnitApplicationModal` — 상무에 **지원한다** | ❌ 지원하는 자리가 없다 |
| `MilitaryEnlistAskModal` — 입대할지 **묻는다** | ❌ 조용히 정한다 |

⚠ **02가 사용자에게 묻던 것을 04가 대신 정하고 있다.** 화면이 없는 게 아니라
**선택 자체가 없다.** 그래서 체육부대의 이점(복귀 적응이 빠르다)이 게임에
한 번도 안 나타난다.

⚠ **여기는 "화면만 붙이면 되는" 일이 아니다.** 자격 조건(성적·나이·`career_stage`)을
02에서 읽어 와야 하고 `decision_screen`에 갈래가 둘 는다.

### 🔴 어떻게 고칠지 — **사용자 확정** (2026-08-18)

> **04도 02와 똑같이 물어야 한다.**

**자동으로 정하는 지금 동작은 결함이다.** 아래 둘을 `Pending`에 올리고
`decision_screen`이 받는다 — 다른 진로 결정 아홉과 같은 방식이다.

| | 언제 묻나 | 선택지 |
|---|---|---|
| **입대 확인** (`MilitaryEnlistAskModal`) | 입대 시기가 왔을 때 | 입대한다 / 미룬다 |
| **상무 지원** (`SportsUnitApplicationModal`) | 자격이 될 때 | 상무에 지원한다 / 현역으로 간다 |

⚠ **`career_decision.gd:413`의 자동 `Military.enlist(state, "general", …)`는
남기지 않는다.** 물어 놓고 답을 안 기다리면 그 물음이 장식이 된다 —
`retirement_ask`가 04에서 딱 그 상태였다(**밀어넣는 코드는 있는데 받는 자리가
없어 자동 진행이 거기서 멈춘 채 안 풀렸다**, `app_root.gd:440`).

⚠ **자격 조건·확률·기간은 02에서 그대로 읽어 온다.** 지어내지 않는다.

#### 02에서 읽어 온 조건 (`advanceWeek.ts:1952-2135`)

공통 전제 — `militaryStatus == "미필"` **그리고** `career_stage`가
`military`도 `highschool`도 아니다 (`:1959-1961`).

| | 언제 | 조건 |
|---|---|---|
| **체육부대 후보 공개 → 지원** | **W50** | `age <= 27` · `sportsUnitPromptedYear != seasonYear` |
| 체육부대 선발 결과 | **W52** | `sportsUnitApplied` |
| **입영 기간 만료 → 입대 확인** | **W52** | `age >= 28` · 미신청 · `militaryAskedYear` 가드 |

#### 🔴 **W50·W52를 그대로 옮기지 않는다 — 날짜로 푼다** (사용자 지적)

02는 `weekInYear = ((weekNum - 1) % 52) + 1`로 잰다. **04엔 진짜 달력이 있다** —
`Calendar.date_of(season_year, day)`가 연·월·일을 준다. 시즌 시작이
`SEASON_START_MONTH/DAY`이고 달마다 길이가 다르므로 **주차와 날짜가 1:1이 아니다.**

⚠ **"52주 중 50번째 주"는 04에 없는 개념이다.** 그대로 박으면 04가 안 쓰는
축을 하나 새로 만드는 것이고, 달력이 있는데 주차로 재면 **어느 날인지 화면이
말할 수 없다**("W50에 무슨 일이 있었나"를 사용자가 못 읽는다).

**이미 같은 판단을 한 선례가 있다** — P-4(인시즌 승강)에서 02의 "월 첫 주"를
`Calendar.date_of`의 **달 변화**로 갈랐다. 주석: *"주기는 근사 없이"*.

**옮기는 방법**: W50·W52가 02에서 무엇을 뜻하는지 먼저 정하고(시즌 막바지 ·
시즌 종료 직전), 04에서는 **그 뜻에 해당하는 날짜 조건**으로 쓴다.
`Calendar.week_of(day)`가 있으니 주차로 재는 것도 가능하지만,
**월·일로 말할 수 있는 조건을 먼저 찾는다.**

⚠ **어느 쪽을 고르든 근거를 적는다.** 02가 W50을 고른 이유가 "시즌이 끝나 갈
무렵"이면 04에서도 그 뜻이 유지되는 날짜여야 한다 — **주차 숫자를 옮기는 게
아니라 뜻을 옮긴다.**

**환산해 봤다** (04 시즌은 **3월 1일 시작 · 364일**):

| 02 | 04 달력 | 뜻 |
|---|---|---|
| **W50** | 344일차 · **2월 7~13일** | 시즌 종료 **약 3주 전** |
| W51 | 351일차 · 2월 14~20일 | |
| **W52** | 358일차 · **2월 21~27일** | **시즌 마지막 주** |

**그래서 04에서는 이렇게 쓴다:**

| 02 | 04 조건 | 왜 |
|---|---|---|
| W50 (후보 공개 → 지원) | **2월이 시작될 때** (`Calendar.date_of`의 **달 변화**) | P-4의 선례 그대로. "시즌 막바지"라는 뜻이 달 이름으로 읽힌다 |
| W52 (선발 결과 · 입대 확인) | **시즌 마지막 주** (`day > DAYS_PER_SEASON - 7`) | 시즌 종료 직전이라는 뜻. 04는 364일차에 롤오버한다 |

⚠ **주차 숫자(50·52)를 04 코드에 적지 않는다.** 적는 순간 시즌 길이가
바뀌면 조용히 어긋난다 — **날짜와 시즌 끝에서 파생한다.**

⚠🔴 **가드 둘이 없으면 게임이 얼어붙는다. 02가 실측으로 두 번 겪었다.**
02 주석 그대로:

> `sportsUnitPromptedYear` 가드가 **반드시 있어야 한다.** 이 블록은 주를 안
> 넘기고 pending만 밀어넣은 채 반환한다 — 사용자가 신청/거절 어느 쪽을 눌러도
> 주차가 그대로라 다음 진행에서 조건이 또 참이 된다. 그러면 미필·비고교·27세
> 이하는 **매년 여기서 게임이 멈춘다**(실측 확인).

> `militaryAskedYear` 가드 필수 — W50 체육부대 공개와 **같은 결함**이다.
> 모달의 "연기"는 상태를 안 바꾸므로 다음 진행에서 조건이 또 참이 된다.
> 실측: **2038 W51에서 자동 진행이 1000회 반복 상한에 걸려 멈췄고, 수동
> 진행이면 영영 W51이다.**

⚠ **04에 옮길 때 이 가드 둘을 같이 옮긴다.** "물었다"를 해마다 기억하는
자리(`sports_unit_prompted_year` · `military_asked_year`)가 없으면
**04에서도 같은 증상이 난다** — 04는 자동 진행이 "다음 결정까지" 가므로
거기서 무한히 선다.

⚠ **입대 처리는 `Military`가 정본이다.** 02도 그 자리에 적어 뒀다 —
"네 경로가 각자 적고 있었고 그중 둘이 오프시즌 처리를 빠뜨렸다"(`:2083-2084`).

⚠ **자동 진행(`AutoAdvance`)이 이 물음을 어떻게 넘기는지도 정해야 한다** —
사용자가 "다음 결정까지"를 눌렀을 때 여기서 멈춰야 한다. **멈추는 게 맞다**
(진로 결정이므로). 검사로 못 박는다.
| `game-status/GameStatusModal` (599) | ❓ 검색 0건 | **무엇을 보여주는지부터 읽는다** |

### 2-4. 소식 패널 — 🔴 **적어 놓은 게 틀렸다** (5)

**04 화면을 띄워 보고 02 원본을 열어 보니 둘 다 달랐다.**

#### 틀린 것 ① — `DigestCards`는 독립 패널이 아니다

`InjuryPanel`·`OffseasonPanel`이 **같이 쓰는 부품**이다
(`InjuryPanel.svelte:16,90` · `OffseasonPanel.svelte:13,97`).
"패널 넷"이 아니라 **패널 셋 + 공용 카드 띠**다.

#### 틀린 것 ② 🔴 — **04는 본문을 아예 못 읽었다**

02의 저 다섯은 전부 **`NewsPage.svelte:243-258`의 본문 상세 갈래**다.
`selected.metadata.type`으로 갈라 붙고, 아무것도 안 걸리면 본문을 줄째로
찍는다. **04엔 그 상세가 통째로 없었다** — `news_vm.build`가 `preview`만
싣고 `body`는 화면까지 넘어가지도 않았다.

**`sim/` 여덟 파일 열여섯 자리가 여러 줄 본문을 쓴다** —
`digest` · `coach_report` · `body_report` · `contract_decision` ·
`military` · `national_runner` · `retirement` · `tournament_news`.
**전부 묻혀 있었다.** 지난 세션에 넣은 체육부대 후보 30인 명단도
서른세 줄을 쓰고 아무도 못 읽었다. **바로 앞 항목에서 죽은 출력을 만든 것이다.**

✅ **고쳤다** — 목록 줄을 누르면 본문이 펼쳐진다.
`news_vm`이 `body`와 `detail`을 싣고, `main_screen`이 머리(‹ 목록 · 분류) ·
제목 · 보낸이·날짜 · **본문을 줄째로** 그린다. 여는 것이 읽음 표시다.
**상세에서도 답할 수 있다**(04에서 소식 선택지는 답하는 유일한 입구다).

검사 16(`test/news_detail_test.gd`) · **변이 7/7 + 5/5 + 2/2** ·
캡처 `news-detail`(새 갈래).

⚠ **겪은 것 셋** — ① 검사에서 `render()`라는 **없는 함수를 지어냈다**
(진짜는 `set_view_model` + `MainVm.build`). ② 선택지 키를 `options`로
지어냈다(진짜는 `choices`). ③ **GdUnit은 실패가 나면 그 파일의 남은 검사를
안 돈다** — 15개 중 8개만 돌았는데 개수를 안 봤으면 못 알아챘다.

⚠ **글자가 다 있는지로만 재면 안 된다.** 본문을 통째로 한 `Label`에 넣는
변이가 **살아남았다**(글자는 다 있으니까). 본문 줄을 `NewsBody` 상자에 따로
담아 **줄 수와 자리**를 보게 고쳤다.

#### 🔴 그 아래서 **죽은 출력을 둘 더 찾았다**

##### ② 월간 부상 리포트 — `injury_log`를 아무도 안 읽었다 ✅

`InjuryRunner.build_news`가 줄·집계·미리보기를 다 만들어
`state["injury_log"]`에 넣는데 **`injury_log`를 읽는 코드가 하나도 없었다**
(쓰는 자리 하나뿐). 바로 아래 세 줄에서 `BodyReport`는 `mailbox`로 가는데
이것만 빠졌다.
→ `InjuryRunner.news_message`를 만들어 소식함에 넣는다. 긴 부상이 위다.

⚠ **죽은 가드를 하나 만들 뻔했다.** 소식을 다시 만들며 `if not
msg.is_empty()`를 뒀는데 **도달할 수가 없어 변이가 안 잡혔다.** 가드를
두는 대신 **이미 만든 것을 넘기고, 버퍼를 읽은 자리에서 바로 비워**
갈릴 수 없게 했다.

검사 9(`test/injury_news_mail_test.gd`) · **변이 8/8**.

##### ③ 다이제스트 — `Digest.build`를 아무도 안 불렀다 ✅

`sim/digest.gd`는 **362줄**이고 검사도 있는데 부르는 곳이 **자기 검사
하나뿐**이었다.

**왜 안 붙어 있었는지 알아냈다** — `Digest.build`가 바라는 입력이 04
상태에 없다. 04는 `state["standings"]`도 `league_state"`도 없고
**순위표를 일정에서 파생한다**(`Standings.from_schedule`). 02 모양 그대로
옮겨 놓고 먹일 것을 못 만들어 굳은 것이다.
→ `sim/digest_runner.gd`(번역기) 신설 · `WeekRunner`가 부른다.

🔴 **처음엔 `[내 자리]`가 통째로 안 나왔다.** 권역(`regions`)을 안 먹였고,
고교 갈래는 그게 비면 **섹션 자체를 안 만든다.** 그래서 미리보기가
`[다른 무대]`로 떨어졌는데 — **그건 02가 고쳐 둔 결함이다**
("남의 리그가 먼저 떠서 열어볼 이유가 안 보였다"). 되풀이할 뻔했다.
**04에 권역이 있다** — `Tournament.regions_of`가 구장에서 파생한다.

```
고치기 전  미리보기: [다른 무대]
고친 뒤    미리보기: HALLA 1위 · 전국 1위
           [내 자리] · [내 무대] · [다른 무대] · [나를 보는 눈]
```

검사 10(`test/digest_mail_test.gd`) · **변이 10/10**.

⚠ **픽스처를 두 번 틀렸다.** ① 04는 순위표를 상태에 안 들고 **일정에서**
파생하는데 `standings`를 직접 넣었다. ② `from_schedule`은 점수가 아니라
**`winner_id`/`loser_id`**로 세는데 점수만 넣어 **전부 무승부**가 됐다
(승률이 다 `.000`). ③ 지어낸 팀 id는 권역에 없어 진짜 id로 바꿨다.

##### 이번 루프의 형태 — **죽은 출력 셋**

| | 쓰는 쪽 | 읽는 쪽 |
|---|---|---|
| 소식 본문 | `sim/` **열여섯 자리** | ✗ 화면이 안 그렸다 |
| 월간 부상 리포트 | `injury_log` | ✗ 아무도 안 읽었다 |
| 다이제스트 | — | ✗ **조립기를 아무도 안 불렀다** |

**쓰는 쪽을 만들면 읽는 쪽이 있는지 센다.**

##### ④ 오프시즌 결산 — **죽은 출력이 아니었다** (성급했다)

`SeasonRunner`가 **소식함에 한 통도 안 넣는다**(`mailbox`가 그 파일에
한 번도 안 나온다)기에 넷째인 줄 알았는데, **끝까지 따라가 보니 아니었다** —
`finish_season`의 결과가 `AppRoot.last_season_end`로 가고 **결산 화면**
(`ui/screens/season_end_screen.tscn`)이 그걸 읽는다(`app_root.gd:391-400`).

**04는 오프시즌 결산을 소식이 아니라 전용 화면으로 낸다.** 02의
`OffseasonPanel`에 대응하는 자리가 이미 있는 것이고, PORT_GAP이 이걸
"화면만 없다"로 적어 둔 것이 **틀렸다**.

⚠ **"소식함에 안 넣는다"가 곧 "죽었다"는 아니다.** 04엔 소식 말고도
읽는 자리가 있다 — **읽는 쪽을 셀 때 화면까지 센다.**

성능 — 주간 경로를 또 건드려 단독으로 셋: **0.956 · 0.952 · 0.980초**
(붙이기 전 0.899~0.912). 다이제스트가 **약 +0.05초**. 자체 보고선 3초 안이다.

#### 남은 것 — 패널 셋

이제 본문이 열리므로 **패널은 그 위에 얹는 층**이다. 04 소식은 아직
`metadata`를 안 싣는다.

| 02 | 04 엔진 | 판정 |
|---|---|---|
| `messages/InjuryPanel` (183) | ✅ `injury_runner` | ⬜ 본문은 열린다. 카드형은 없다 |
| `messages/ProspectTop10Panel` (181) | ❌ **없었다** | ✅ 아래 — 소식·효과 둘 다 새로 만들었다 |
| `messages/OffseasonPanel` (194) | ✅ `sim/offseason.gd` | ✅ **04는 화면으로 낸다** — 아래 |
| `messages/DigestCards` (89) | ✅ `sim/digest.gd` | 위 둘의 부품 |
| `messages/TrainingStatBars` (152) | ✅ `training_vm` | **부분** |

### 2-5. 개발자 도구 — **일부러 안 옮김** (4)

`devtools/DevToolsHubModal` · `AutoAdvancePanel` · `ScenarioPanel` ·
`events/EventManagerModal` · `match-engine-lab/MatchEngineLabModal`.
04는 CLI 계측(`tools/run.gd`)으로 대체했다. **후보로만 남긴다** — 게임 기능이
아니라 개발 도구다.

### 2-6. 04에만 있다 — **지우지 않는다** (2)

- **경기 전 브리핑** (F-5) — 상대 선발 구위·무브먼트·커맨드 + 상대 타선 9명
- **구장 그림 27장** — 02는 프로 구장 하나가 하드코딩이라 고교 경기도 프로
  구장에서 열렸다

### 2-7. 이미 대응 (6)

`navigation/SidebarNav` → 04 좌측 탭 · `match-view/BaseballField` →
`ui/parts/baseball_field` · `achievements/AchievementManagerModal` →
`achievements_vm` 하위 탭.

---

## 3. 화면이 아닌 기능

### 3-1. 🔴 설정 — 02 여섯 줄 vs 04 두 줄

| 항목 | 02 | 04 | 할 일 |
|---|---|---|---|
| **테마 (밝게/어둡게/시스템 따름)** | ✅ radiogroup 셋 | **✅ 2026-08-18** | `AppTheme`의 색을 `const` → `static var`로. **읽는 231곳은 한 글자도 안 바뀌었다** |
| **언어** | ✅ 키 기반 | ❌ | **04에 하드코딩 한글 문구가 736개다**(`grep` 실측). 표로 빼는 게 먼저고, 04는 한국어 단일이라 지금 스위치를 만들면 **한 칸짜리 목록**이다 |
| **연출 속도** | ✅ 화면엔 있다 | ❌ | 🔴 **02에서도 죽은 스위치다.** `effectSpeed`를 읽는 곳이 `SettingsModal` 밖에 **0곳**이다(실측) — 저장만 하고 아무도 안 본다. **옮기면 02의 죽은 배선을 물려받는 것**이라 안 옮긴다(P-11 원칙) |
| **모션 줄이기** | ✅ `App.svelte:46`이 CSS로 적용 | ❌ | **04엔 끌 모션이 없다.** `Tween` · `AnimationPlayer` · `create_timer`가 `ui/` 전체에 **0건**(실측). 애니메이션을 먼저 만들어야 뜻이 생긴다 |
| 창 크기 | ✅ | ✅ | |
| 볼륨 | ✅ (소리는 아직 없다고 화면이 밝힌다) | ❌ | 04도 소리가 없다 |
| 전체화면 | ❌ | ✅ | 04에만 있다 |

### 3-2. 그 밖

| 축 | 02 | 04 | 판정 |
|---|---|---|---|
| i18n (`$t`) | ✅ 키 기반 | ❌ 문구가 코드에 박혀 있다 | 언어 설정의 전제 |
| 팀 색 주입 (`applyTeamTokens`) | ✅ 소속팀 색이 문서 루트에 | ❌ **238팀 `colors`를 안 쓴다** | UI 방향 U-2 |
| 배지 (안 읽은 소식 · 미확인 업적) | ✅ | ✅ | |
| 정렬·필터·페이지 넘김 | ✅ 페이지별 | 부분 | 화면마다 세야 한다 |
| 자동 진행 정책 | ✅ | ✅ (`auto_advance`) | |
| 저장·불러오기 | ✅ 슬롯 | ✅ 슬롯 | |

---

## 4. 순서 — 값싼 것부터

**막힌 것부터 푼다.** 아래 넷은 커리어 자체가 안 굴러가는 자리라
화면 이식보다 앞선다 (`QUEUE.md`의 P-13~P-17).

| | 항목 | 왜 먼저 |
|---|---|---|
| 🔴 1 | **P-14 대학 진학 뒤 주인공이 로스터에서 사라진다** | P-15·P-16·P-13이 이 위에 설 수 있다 |
| 🔴 2 | P-15 대학 학년이 18해째 1학년 | 졸업이 없으니 프로에 못 간다 |
| 🔴 3 | P-16 20해에 성장한 주가 1주 | 훈련은 도는데 능력치가 안 오른다 |
| 🔴 4 | P-13 라이벌 값이 전부 0 | 잡히기만 하고 안 쌓인다 |

그다음 **화면만 없는 것**(엔진이 이미 돈다) → 병역 셋 · 소식 패널 넷 ·
설정 테마 → **엔진부터 확인해야 하는 것** 둘(부상 치료 · 경기 상태) →
**UI 방향**(U-1~U-5).

---

## 5. 진행

| 항목 | 상태 | 검사 · 변이 |
|---|---|---|
| 0단계 전수 조사 | ✅ 2026-08-17 | — |
| 🔴 P-14 로스터 이동 | ✅ 2026-08-18 | 검사 7 · **변이 7/7** |
| 🔴 P-15 대학 학년 | ✅ **P-14가 원인이었다** | (같은 검사) |
| P-13 라이벌 값 | ~ 움직이기 시작 (−5~4) | 20해 재측정 필요 |
| P-16 성장 1주 | ✅ **결함 아님** — 계측이 틀린 질문 | P-16b로 뗌 |
| 설정 · 화면 톤 셋 | ✅ 2026-08-18 | 검사 16 · **변이 7/7** |
| `shot.gd`가 톤을 안 걸던 것 | ✅ 같이 고침 | — |
| 설정 · 연출 속도 | ✅ **안 옮긴다** — 02에서도 죽은 스위치 | 실측 0곳 |
| 설정 · 모션 줄이기 | ⬜ 애니메이션이 먼저 | 실측 0건 |
| 설정 · 언어 | ⬜ 문구 736개를 표로 빼는 게 먼저 | 실측 736 |

| 병역 · 계급 | ✅ 2026-08-18 | 검사 6 · **변이 4/4** |
| 병역 · 복무 진행 막대 | ✅ 이미 있었다 (조사표가 틀렸다) | — |
| 🔴 체육부대 도달 불가 | ⬜ **새로 찾음** — 열여덟 번째 죽은 배선 | — |

### ⚠ 함정 — 캡처가 매달릴 수 있다 (2026-08-18)

`godot --script tools/shot.gd -- <갈래>`는 **창을 띄우는 도구라 파스 오류가
나면 아무것도 못 하고 매달린다.** 실제로 `theme_changed` 이름 충돌 때 걸어 둔
캡처가 **51분간 CPU를 먹으며 살아 있었다** — 그 뒤 이름을 고치고 새 캡처를
따로 돌려 결과를 봤기 때문에 **먼저 것을 잊었다.**

⚠ **성능은 단독으로 재라는 규칙을 그 사이에 어겼다.** `bench:day`를 잰
시점 일부가 이 프로세스와 겹쳤을 수 있다(0.899~0.905초 — 게이트 1.0 안이고
여유가 10%라 판정이 뒤집힐 값은 아니다).

**캡처가 안 끝나면 `Get-Process`로 확인하고 거둔다.** 파스 오류가 난 뒤
캡처를 다시 걸 때는 **앞엣것이 살아 있는지 먼저 본다.**

### 🔴 병역 물음 — **여기까지 했다** (2026-08-18)

| 단계 | 상태 |
|---|---|
| ① 02 원본 읽기 | ✅ 조건·가드를 위에 적었다 |
| 04 달력 환산 | ✅ W50 → 2월 첫 주 · W52 → 시즌 마지막 주 |
| ② 검사 15개 | ✅ `test/military_ask_test.gd` |
| ③ 빨간불 | ✅ |
| ④ 로직 | ✅ `Military.should_ask_sports_unit` · `should_ask_enlist` · `mark_sports_unit_asked` · `mark_enlist_asked` (커밋 `3410dc7a8`) |
| 자동 `enlist` 걷어내기 | ✅ `career_decision.gd`가 `military_pending`을 돌려준다 |
| ⑤ **배선** | ✅ 넷 다 (커밋 `2372f2ff2`) |
| ⑥ 실측 | ✅ **체육부대 2주 · 일반병 6주** — `RECOVERY_SPORTS`가 처음으로 걸렸다 |
| ⑦ 변이 | ✅ `military.gd` **14/14** · `career_runner.gd` **7/7** |
| ⑧ 전체 검사 | ✅ 3,529개 · 실패 0 · 고아 0 |

**⑤에서 정본 목록 둘을 빠뜨릴 뻔했다.** `DecisionVm.build`의 `match`에만
갈래를 넣고 **`HANDLED`에 안 넣으면** `blocking`이 안 골라 **화면이 영영
안 뜬다** — `retirement_ask`와 똑같은 모양이다. 기존 검사가 **`STOPPING`**
(자동 진행 정지)도 같이 넣어야 한다고 잡았다. **`HANDLED` · `STOPPING` ·
`LABELS` 셋이 같이 가는 자리다.**

#### ⬜ 상무 선발 결과 — 02 값을 다 읽어 왔다

02 `advanceWeek.ts:2034-2090` · `utils/militaryRules.ts:34-43`.
**시즌 마지막 주에 `sports_unit_applied`인 사람의 당락을 가른다.**

| 값 | 02 | 근거 |
|---|---|---|
| 연간 선발 | **13명** | `rosterSize 26 / serviceYears 2`(24개월) |
| 한 팀 최대 | **3명** | `maxPerTeam` |
| 지원자 풀 | 주인공 + NPC 후보 상위 30 | `militaryCalcCandidates` |
| 붙으면 | `Military.enlist(state, "sports", day)` | |
| 떨어지면 | **곧바로 입대 확인을 묻는다**(`reason: "rejected"`) + `mark_enlist_asked` | |

⚠ **02가 그 자리에 누수를 적어 뒀다** — 주인공 선발(W52)과 NPC 선발(오프시즌)이
**별개 추첨**이라 둘 다 뽑히면 그해 입대가 정원 +1이고, 상무는 로스터 캡이
안 걸려 **해마다 쌓인다.** 04는 한 자리에서 뽑아 이 누수를 안 물려받는다.

⚠ **떨어진 뒤 곧바로 묻는 흐름을 빠뜨리지 마라.** 안 이으면 지원했다가
떨어진 사람은 그해에 아무 일도 안 일어난다.

**⑤ 배선에 남은 것 넷:**

1. **대기줄에 올리는 자리** — 주간 처리에서 `Military.should_ask_*`를 보고
   `Pending.push_once`. **답을 받으면 어느 쪽이든 `mark_*_asked`를 부른다**
2. **`decision_vm`에 갈래 둘** — `sports_unit_apply`(신청하기 / 이번엔 아니오) ·
   `military_enlist_ask`(입대한다 / 미룬다). 다른 아홉과 같은 꼴
3. **답 처리** — 상무는 `sports_unit_applied` 표시 후 **시즌 마지막 주에 결과**,
   입대는 `Military.enlist(state, unit, day)`. **입대 처리는 `Military`가 정본**
4. **자동 진행이 여기서 멈추는지 검사로 못 박는다**

⚠ **`career_decision.gd`가 지금 `military_pending`만 돌려주고 아무도 안 받는다.**
배선을 끝내기 전까지는 **"갈 곳이 없는" 커리어가 병역 없이 떠 있다** — 이 상태로
두면 `retirement_ask`와 같은 자리가 된다. **배선이 이 항목의 절반이다.**

✅ **실측했다 (2026-08-18)** — 전역까지 굴려 보니 **체육부대 2주 · 일반병 6주**.
`RECOVERY_SPORTS`가 **이 프로젝트에서 처음으로 실제로 걸린 값**이다. 지금까지
코드는 네 곳에 있었는데 `"sports"`로 입대시키는 입구가 없어 한 번도 안 걸렸다.
이제 상무를 고르면 복귀가 **3분의 1**로 짧아진다 — 지원할 이유가 생겼다.

⚠ **복귀 적응은 입대가 아니라 전역에서 붙는다**(`military.gd:314`). 입대 직후에
재면 `-1`(없음)이 나온다 — 처음에 그렇게 재고 "안 걸렸나" 했다.

### ✅ 화면 확인 — 눈으로 보니 결함이 하나 나왔다

`tools/shot.gd`에 갈래 둘(`sports-unit` · `enlist`)을 새로 만들어 띄웠다.

🔴 **문구에 `**복귀 적응이 짧습니다**`라고 썼는데 04 결정 화면은 `Label`이라
별표가 글자 그대로 찍혔다.** 문서 습관이 화면으로 샌 것이다. 고치면서
**실제 값을 넣었다** — "복귀 적응이 2주로, 일반병(6주)보다 짧습니다".
그냥 "짧습니다"보다 고를 근거가 된다.

⚠ **같은 자리가 다른 갈래에도 있을 수 있어 검사로 막았다** —
`decision_vm.gd` 전체에 `**`가 없는지 본다.

### 🔴 닫고 나서 사용자가 짚은 것 둘 (2026-08-18)

**① 포지션이 선발에 영향을 준다** — 02 `npc_sim.rs:2014-2048`
`select_sports_unit_ids`는 **두 단계**다:

| | 무엇 | 팀당 상한 |
|---|---|---|
| **Phase 1** | **그해 전역자가 비운 포지션**을 채운다 | **무시** |
| Phase 2 | 남은 자리를 OVR 순 | 적용 |

04가 구현한 건 **Phase 2뿐**이고, 그건 **02의 주인공 경로와 같다** —
02도 주인공을 뽑을 때 `vacating_positions`에 빈 배열을 넘긴다
(`:2566` 주석: *"주인공 선발 시에는 vacating_positions 없이 OVR 순 Phase2만
동작"*). **그 점에서는 04가 02와 어긋나지 않는다.**

### 🔴 P-18 — **04에서 입대하는 건 주인공뿐이다** (사용자 지적, 확정)

> **NPC도 상무 영향을 받아야 한다. 그래야 주인공이 신청하는 해에 경쟁이
> 빡세기도 하고 아니기도 하다.**

실측: `Military.enlist`를 부르는 곳이 **주인공 경로 셋뿐**이다
(`career_decision.gd:274` · `:448` · `career_runner.gd:120`).
`national_runner`가 면제를 주는 자리 하나가 있을 뿐 **NPC는 아무도 입대하지
않는다.**

**그래서 지금 선발은 뜻이 없다:**
- 미필 풀이 **영영 안 줄어든다** — 자격자가 해마다 쌓이기만 한다
- 경쟁 상대가 늘 같으므로 **당락이 내 OVR 순위 하나로 고정**된다.
  "올해는 빡세네"가 없다
- 전역 공백이 안 생기니 **Phase 1(포지션 채우기)이 영영 안 걸린다**

**02에 있는 NPC 경로 셋** (`lib.rs:766-798`):

| 02 함수 | 무엇 |
|---|---|
| `calc_sports_unit_selection` | 상무 선발. **NPC 경로는 `vacating_positions`를 넘겨 Phase 1이 돈다** |
| `pick_general_enlistees` | 현역 입대자를 뽑는다 |
| `calc_early_enlist_decisions` | 조기 입대 결정 (`ovr_rank_pct < 0.20` 같은 조건) |

**해야 할 것:**
1. NPC 현역 입대 — 미필 풀이 줄어들게
2. NPC 상무 선발 — **주인공과 정원을 나눠 쓴다**(02의 누수를 안 물려받는다)
3. NPC 전역 — 공백 포지션이 생기게. 그래야 Phase 1이 뜻을 갖는다
4. Phase 1을 `select_sports_unit_ids`에 붙인다

⚠ **밸런스에 닿는다.** 미필 풀이 줄면 주인공의 당락이 바뀐다 —
붙인 뒤 **여러 해를 굴려 연도별 경쟁 강도가 실제로 흔들리는지** 잰다.
"빡세기도 하고 아니기도 하다"가 실측으로 보여야 끝난 것이다.

#### 진행 (2026-08-18)

✅ **조기 입대 확률**을 02 그대로 옮겼다(`npc_sim.rs:2590-2624`) —
나이(27세 0.20 · 26세 0.12 · 25세 0.05) + 순위 하위(20% 미만 0.35 ·
35% 미만 0.20) + 출장 부족(0.30 / 0.15) + 계약 만료 임박(0.20),
**더한 뒤 0.85에서 자른다.** `sim/npc_military.gd` · `WeekRunner`가 부른다.
검사 12.

🔴 **처음엔 입대만 붙였다가 리그를 말렸다.** 실측에서 자격자가 8해에
**7,336 → 3,026**으로 줄기만 했다 — 들어간 사람이 안 돌아왔다.
04는 **전역도 주인공 경로 하나뿐**이었다(`career_runner.gd:161`).
`_serve_year`를 붙여 고쳤다. 복무중이 1,514까지 올랐다 2년 주기로 도로 준다.

✅ **게임 경로로 다시 쟀다 — 결과가 달랐다.**

처음 계측은 `NpcMilitary.run`만 부르고 시즌 롤오버를 안 돌려 **신인이 안
들어왔다**(12해에 7,336 → 1,975로 줄기만). `SeasonRunner.finish_season`을
같이 돌리니:

| 해 | 입대 | 복무중 | 미필 |
|---|---|---|---|
| 2027 | 820 | 807 | 6,779 |
| 2028 | **1,041** | 1,761 | 5,700 |
| 2029 | 970 | 1,844 | 4,792 |
| 2030 | 823 | 1,658 | 4,061 |
| 2031 | 634 | 1,273 | 3,732 |
| 2032 | 556 | 1,025 | 3,537 |
| 2033 | 499 | 932 | 3,476 |
| 2034 | **490** | 897 | **3,389** |

**미필이 평형에 든다**(3,732 → 3,537 → 3,476 → 3,389 — 감소가 잦아든다).
신인이 실제로 들어온다. **입대 수도 820 → 1,041 → … → 490으로 흔들린다** —
"빡세기도 하고 아니기도"의 바탕이 생겼다.

⚠ **이번 루프에서 "계측이 게임 경로를 안 탄다"를 네 번째 만났다**
(`measure:growth` · `fa_measure` · `shot.gd` · 이것).

✅ **성능** — `WeekRunner`에 붙였으므로 단독으로 세 번 쟀다:
**0.902 · 0.909 · 0.930초**. 게이트 안이다.

---

### 성능 게이트를 1.0 → **30초**로 늘렸다 (2026-08-18 · 사용자 확정)

⚠ **30초는 한계지 목표가 아니다.** 하루가 30초면 사실상 못 논다 —
**3초를 넘기면 게이트 안이라도 보고한다**고 `bench/day_bench.gd`에 적었다.
(처음엔 3초를 받았고 곧 30초로 늘려 줬다.)

⚠ **그런데 이유로 댄 첫 항목이 틀렸다** — 아래 P-18b를 보라.

1.0에서는 여유가 7%(0.930초)뿐이라 **02에 있는 것을 못 옮기고 있었다.**
사용자가 "제한시간 때문에 못하는 게 있나"를 묻기에 셋을 댔고 3초를 받았다:

| 막혀 있던 것 | 무엇이 필요한가 |
|---|---|
| **P-18b 출장 시간 축** | NPC 9,600명의 주간 출장을 쌓아야 02 확률식의 네 항 중 하나가 산다. 지금 중립 1.0으로 **죽어 있다** |
| **NPC 복무를 주 단위로** | 지금은 "한 해에 `WEEKS_PER_SEASON`"으로 뭉쳐 세서 **시즌 도중 전역이 없다** |
| `NpcGrowth`의 좁은 창 | 넓히면 02 실측에 더 붙는다 |

늘려도 안 되는 것 — 04의 NPC 경기를 02 **투구 단위**(`match_engine.rs`)로
바꾸는 것. 2,124경기 × 투구는 3초로도 어림없고 **02도 안 했다**(02도 NPC는
시즌 모델 `npc_sim.rs`다).

⚠ **한 번 진행 25초는 그대로다.** 고친 자리: `bench/day_bench.gd` ·
`bench/game_bench.gd` · `docs/UX_REPORT.md`.

---

## P-18c — 체육부대 선발이 02와 세 군데 달랐다 ✅

② 항목(NPC 상무 선발)을 하면서 **02 대조로 셋을 찾았다.**

### ① 지원자 풀이 수천 명이었다 🔴

02 `advanceWeek.ts:2052`는 `topN: 29`로 자르고 주인공을 더해 **30명 풀에서
13명**(43%)을 뽑는다. 04 `_sports_rivals`는 **미필 프로 전원**을 세웠다 —
**7,000명에서 13명**(0.2%). 04의 주인공은 사실상 못 붙었다.
→ `Military.SPORTS_APPLICANT_TOP = 29` · `Military.sports_candidates()`.

### ② Phase 1이 아예 없었다

02 `select_sports_unit_ids`는 두 단계다 — **Phase 1**이 그해 전역자가 비운
포지션을 **팀당 상한을 무시하고** 먼저 채우고, **Phase 2**가 남은 자리를
OVR 순으로(여기서만 팀당 상한) 채운다. 04엔 Phase 2만 있었다.

⚠ **02에서 Phase 1은 한 번도 안 돈다.** 호출부가 `npc_sim.rs:2567` 하나뿐이고
거기서 `&[]`를 넘긴다. **죽은 코드였다.**

🔴 **그래서 04가 진짜 공백을 먹이자 되먹임이 생겼다** — 전역 26 → 공백 26 →
Phase 1이 26 선발 → 두 해 뒤 전역 26. 실측에서 상무 복무가 정원(13×2 = **26**)의
배가 넘는 **58명**이 됐다. **02에 없던 결함을 02를 충실히 옮겨서 만든 것이다.**
→ Phase 1이 보는 공백을 `max_total`로 자른다. 빈자리 우선은 남고 정원은 지킨다.

### ③ 정원이 갈라져 있었다

주인공 경로와 NPC 경로가 **따로 뽑았다.** 02도 그렇다 — `selectedIds`를 받아
놓고 NPC에는 안 쓴다(PORT_GAP의 "02의 명백한 결함까지 물려받지 않는다").
→ `Military.resolve_sports_unit(state, vacating)`이 **한 해에 한 번만** 뽑고,
먼저 부른 쪽(=`NpcMilitary`, `WeekRunner` 96행)이 뽑으면 `CareerRunner`(138행)는
그 명단을 읽기만 한다. 해를 같이 적어 이듬해에 작년 명단을 안 읽는다.

### 실측 — "빡세기도 하고 아니기도"가 나오나

```
        커트라인  상무복무  입대   미필
2027년    91.0      13      827   6,772
2028년    90.0      26    1,054   5,676
2029년    89.0      26      986   4,757
2030년    86.0      26      815   4,037
2031년    83.0      26      632   3,716
2032년    79.0      25      555   3,522
2033년    70.0      24      498   3,459
2034년    70.0      25      501   3,361
2035년    70.0      26      436   3,300
```

**커트라인이 91 → 70으로 흔들린다.** 첫 몇 해는 빡세고 뒤로 갈수록 붙기
쉬워진다. 상무 복무는 정원(26)에 잠겼다.

❓ **다만 단조 감소지 오르내림이 아니었다.** 2033년부터 풀 최고가 70.0에
눌러앉기에 "세계 전체가 마른다 → D-3에서 볼 일"로 적었는데 **그 판단이
틀렸다.** 원인은 바로 아래 P-18d였다.

검사 19(`test/sports_unit_quota_test.gd`) · **변이 12/12**.

### 병역 넷을 닫고 잰 값 (아무것도 안 도는 상태)

**전체 검사 3,571개 · 실패 0 · 고아 0.** 직전 3,486에서 **85 늘었다**
(신설 19 + 13, 나머지는 기존 갈래가 새 경로를 타면서 늘어난 몫).

**`bench:day` 단독 3회** — 일하는 시간 **0.899 · 0.912 · 0.902초**,
기다리는 시간 0.905 · 0.918 · 0.908초(프레임 84번 넘김).
게이트(30 / 60) 안이고 **자체 보고선 3초도 안 넘는다.**

⚠ 병역 넷을 통째로 붙였는데 **주간 비용이 안 늘었다**(붙이기 전 0.899~0.905).
NPC 병역이 **시즌 마지막 주에만** 돌기 때문이다.

### 이번에 또 어긴 것 둘

⚠ **성능을 검사와 같이 돌리면서 쟀다.** 앞서 이 자리에 적었던
0.902 · 0.960 · 0.915초는 **전체 검사가 백그라운드로 도는 중에 잰 값**이다.
`docs/RESUME.md:52`에 "성능은 검사와 같이 돌리면 못 믿는다"고 **내가 적어
놓고** 같은 걸 또 했다. 위 값으로 갈아끼웠다.

⚠ **검사 출력을 `| tail -4`로 잘라 개수 줄이 사라졌다.**
`reports/`는 **20개만 남기므로** 먼저 돈 전체 검사 보고서까지 밀려나
결과를 영영 못 읽었다. **개수를 봐야 파스 오류를 잡는다**(3,486 → 16 사건).
→ 앞으로 개수는 `reports/`의 최신 `results.xml` 루트 `tests=`에서 읽고,
**끝나자마자** 읽는다.

---

## P-18b — 출장 시간 축은 **차이가 아니었다** ✅

3초 게이트를 받아 든 첫 항목이 이것이었는데, 02를 열어 보니
**02도 상수를 박아 넣는다:**

```ts
// game.ts:2740
playingTimePct: 0.5,
```

0.5는 `TIME_MID`(0.35)보다 크므로 **02에서도 그 항은 한 번도 안 걸린다.**
04의 중립 1.0과 **결과가 완전히 같다.** 축을 만들 필요가 없다 —
`sim/npc_military.gd`의 "⬜ P-18b" 주석은 이 근거로 닫는다.

⚠ **게이트를 늘려 달라고 대면서 든 이유 셋 중 첫째가 틀렸다.**
"02에 있는데 04에 없다"고 생각한 것이 **02에도 없었다.** 남은 둘(주 단위
복무 · `NpcGrowth` 창)은 여전히 유효하다.

---

## P-18d — 순위 백분위가 **뒤집혀 있었다** 🔴 ✅

P-18b를 확인하러 02 호출부를 여는 김에 바로 위 줄에서 찾았다.

```ts
// game.ts:2717-2725  — 오름차순이다
const kblOvrs = ….sort((a, b) => a - b);
const idx = kblOvrs.findIndex(v => v >= ovr);
const ovrRankPct = idx / kblOvrs.length;
```

**02는 못하는 사람이 0에 가깝다.** 그래야 `rank < 0.20 → +0.35`가
`npc_military.gd` 상수 주석대로 **"주전 경쟁에서 밀린 사람"**에게 붙는다.

04는 내림차순으로 세워 놓고 같은 식을 썼다 —
**리그 최고 선수가 조기 입대 확률이 제일 높았다.**

### 고치기 전후 (게임 경로 · 같은 시드)

| | 2027 풀최고 | 2035 풀최고 | 2035 커트라인 | 2035 미필 |
|---|---|---|---|---|
| 뒤집힌 채 | 92.0 | **70.0** | 70.0 | 3,300 |
| 고친 뒤 | 92.0 | **85.0** | 83.0 | **4,058** |

리그가 더 이상 상위권을 잃지 않는다. **"세계가 마른다"는 진단이 틀렸고
원인은 이 뒤집힘이었다** — 표를 읽어 세운 인과는 굴리기 전까지 가설이다.

⚠ **첫 검사가 이걸 못 잡았다**(변이 0/2). 픽스처가 두 무리 **크기**를
갈라놨다 — 체육부대가 상위 13명을 먼저 데려가서 아래 무리가 그냥 더 컸다.
**개수 대신 평균**으로 바꾸고, 나이를 27 → 24로 내려 **순위 말고는 아무
항도 안 걸리게** 했다(27이면 나이 항 0.20이 확률을 받쳐 줘서 순위 항을
통째로 죽여도 사람이 계속 입대한다). 그러고 **5/5**.

검사 13(`test/npc_military_test.gd`) · **변이 5/5**.

⬜ **남은 것**: 출장 시간 축(04에 없어 지금은 중립 1.0으로 둬 그 항이 안
걸린다 — **P-18b**) · NPC 상무 선발(Phase 1 포지션 채우기) · 변이 · 화면.

**② 02는 소식도 같이 넣는다** — `advanceWeek.ts:2012-2025`
`gameStore.addMessage`로 **후보 30인 명단을 소식함에 넣고** 나서 모달을 띄운다.
04는 물음만 띄우고 **소식을 안 넣었다.** 아래에서 고쳤다.

## ✅ ① 병역 물음 — **닫혔다** (2026-08-18)

리듬 열 단계를 다 지났다. 검사 33 · 변이 `military.gd` 14/14 ·
`career_runner.gd` 7/7 · 전체 3,540개 통과 · 고아 0.

### 다음에 할 것 (값싼 순서)

1. 🔴 **P-18 NPC 병역** — 지금은 주인공만 입대해 경쟁이 고정이다
2. **소식 패널 넷** — 다이제스트 · 부상 · 유망주 Top10 · 오프시즌.
   **엔진이 이미 돈다**(`sim/digest.gd` · `injury_runner` · `sim/offseason.gd`)
3. **결정 화면 아홉 갈래를 02 모달과 하나씩 대조** — 표·비교·근거가
   한 줄 선택지로 눌렸는지 본다
4. **부상 치료 · 경기 상태** — 엔진부터 확인
5. **UI 방향 U-1~U-5** (팀 마크 · 팀 색 주입 · 헤더 · 우측 패널 · 목록 마크)
6. **밝은 톤 다듬기** — 하위 탭 버튼이 다소 무겁다. 02도 U5로 뒤집은 뒤
   U7·U9·U10으로 남은 어두운 섬을 차례로 옮겼다
7. **P-13 20해 재측정** · **P-16b 계측 고치기** · **P-17 대학 일정**

**한 원인이 셋을 붙들고 있었다.** `_move_to`가 로스터를 안 건드려서 주인공이
어느 로스터에도 없었고, `all_players`가 로스터만 훑으므로 **진급·성적 집계·
관계가 전부 주인공을 못 봤다.** 20해 계측이 아니었으면 못 찾았다 —
4해로는 고교 구간만 돌아 멀쩡해 보였다.

---

## P-19 — 고교 유망주 TOP 10: **04엔 통째로 없었다** ✅

02는 고교 시절 **4주마다** 유망주 랭킹을 소식으로 보내고, 그 순위가
**인기·스카우트 점수·사기에 실제로 닿는다**(`rankEffect`).
04는 고교 3년 내내 자기 위치를 알 방법이 없었고 그 효과도 없었다.

→ `sim/prospect_top10.gd`(산식) · `sim/prospect_runner.gd`(배선) 신설.
`WeekRunner`가 부른다.

### 옮기며 갈라진 것 넷 — 전부 **실측에서 걸렸다**

🔴 **① `career_stage`가 새 게임 주인공에 없다.**
`World.new_game`이 그 칸을 안 채우고 `career_decision`이 **처음 진로를
정할 때 비로소** 넣는다(`career_decision.gd:534`). `!= "highschool"`로 막았더니
**한 통도 안 왔다.** `digest_runner`도 같은 키를 읽고 있어 실제 게임에선
계층이 `hs1`/`hs23`이 아닌 쪽으로 갈렸다.
→ `CareerPath.stage_of(p)` 신설 — 비면 **리그로 되찾는다.** 둘 다 그걸 쓴다.

🔴 **② 04는 학년을 `grade`로 든다.** `hs_grade`는 `Digest.build`의 입력
이름일 뿐인데 주인공에서 그 이름으로 읽어 **늘 0**이었다.

🔴 **③ 04 고교 NPC에 `scout_score`가 없다.** 전원 0이라 점수가 `ovr*0.8`뿐이었고
**상위 열 명이 전부 56.0으로 똑같이** 나왔다 — 순위가 사실상 넣은 순서였다.
**"04는 갖고 있다"고 확인 없이 적었다.** 02도 이 축이 없어 **일부러 지어낸다**
(`simNpcScout`, 결정론적 10~70). 04도 같은 범위를 `Rng`로 짓는다.
02 산식을 글자 그대로는 못 옮긴다 — 02는 `id.slice(-3)`을 정수로 읽는데
04 id는 숫자 꼬리가 아니다. **옮기는 것은 값**(범위와 결정성)이다.

🔴 **④ 1학년은 통합 10위에 못 든다.** 실측에서 3,061명 중 명단 밖이었다 —
**02가 학년별 칸을 내는 이유가 그것이다.** 통합 + 내 학년 둘을 낸다
(02는 넷을 내지만 04 본문은 글자라 40줄이면 안 읽힌다).

### 02와 일부러 다르게 한 것 둘

- **본문에 명단을 적는다.** 02는 `body: subject`이고 내용이 전부 `metadata`에
  있어 패널이 없으면 빈 소식이다. 04는 본문 상세가 생겼으니 거기 적는다.
- **id를 해·주차로 짓는다.** 02는 `Date.now()`를 써서 같은 주를 두 번 굴리면
  두 통이 된다 — 04의 재현 가능 규칙에도 어긋난다.
- **상한(100)을 둔다.** 02는 안 두는데 고교가 3년이라 4주마다 최대 10씩
  열두 번이면 인기가 120이 된다. 축이 0~100이다.

⚠ **학년 칸이 성적을 따로 다시 읽어 변이가 가려졌다** — 통합이 성적을 잃어도
학년 칸이 가려서 "성적을 안 먹인다"가 안 잡혔다. **부상 때와 같은 처방** —
`message_of`에 성적을 넘겨 **갈릴 수 없게** 했다.

⚠ **픽스처가 또 검사를 가렸다** — "잘 던지면 순위가 오른다"를 나와 같은
OVR 70 NPC로 세웠더니 **성적 없이도 이미 1위**라 오를 자리가 없었다.

검사 31(`test/prospect_top10_test.gd`) · **변이 11/11 + 8/8** ·
`bench:day` **단독 0.889 · 0.921 · 0.897초**(붙이기 전 0.956~0.980 — 늘지 않았다).

---

## P-20 — 부상 리포트가 **원본 ID를 찍고 있었다** ✅

`InjuryPanel`(02의 마지막 남은 패널)을 옮기려고 04 소식을 **게임 경로로
한 시즌 굴려 찍어 봤더니** 이렇게 나왔다:

```
수술  GEN_TEAM_KBL_DAEJEON_PHANTOMS_1_20270101_Y2027_025  37주
수술  GEN_TEAM_ABL_COASTALRAYS_1_2_20270101_Y2027_025     34주
… 모두 114줄
```

🔴 **`_push_news`가 이름을 안 담았다.** 사건에 `player_id`·`injury_type`·
`severity`·`weeks`·`day`·`team_id`뿐이라 본문이 id로 떨어졌다.
**02도 똑같은 결함을 겪고 고쳤다** — `top10Engine.ts:62-66`:
*"표에 없는 팀이 `?? npc.teamId`로 떨어져 문구에 `TEAM_HS_AEWOL` 같은 ID가
그대로 찍혔다."*

⚠ **되찾지 말고 그때 담는다.** 나중에 id로 로스터를 뒤지면 복무·은퇴로
떠난 사람을 못 찾는다. `_push_news`가 부르는 자리에 그 사람(`p`)이 이미 있다.

### 02 `InjuryPanel`이 하던 일을 04 본문으로

| 02 | 04 |
|---|---|
| `DigestCards` 등급 카드 (누르면 그 목록) | 미리보기에 집계 (`수술 2 · 장기 20`) |
| **`내 팀` 칩** | **내 팀을 위로 올리고 `●` 표시** |
| `아는 사람` 칩 | ⬜ 04에 관계 축을 아직 안 이었다 |
| 표 — 이름·나이·POS·팀·부상·복귀 | 줄 — 등급·이름·POS·팀·주 |
| `PAGE = 100`으로 끊기 | **`BODY_ROWS = 20` + "그 밖 N명"** |

⚠ **04 본문은 글자라 거르개가 없다.** 02는 100줄이 견디지만 04는
**실측 114줄**이 통째로 나왔다 — 상한을 두고 **나머지를 센다**(조용히
사라지지 않게).

```
  수술  윤동찬  3B  대전 팬텀스 2군  37주
  장기  전지민  SP  부산 웨이브스  19주
  …
그 밖 92명.
```

검사 13(`test/injury_news_mail_test.gd`) · **변이 7/7** ·
`bench:day` **단독 0.903 · 0.896 · 0.900초**.

⚠ **계측이 또 게임 경로를 안 탔다** — 처음엔 주 하나에 하루씩만 밟아
**부상이 한 건도 안 났다**(경기를 안 치렀으니). 일정대로 `GameSim.play`를
돌리고 주 경계에서 `WeekRunner`를 부르니 그제야 나왔다. **이번 루프 여섯 번째.**

---

## P-21 — 진로 지원에 **고를 근거가 하나도 없었다** ✅

결정 화면 아홉 갈래 대조를 시작하며 제일 큰 격차부터 열었다.

02 `UniversityApplyModal`(214줄)은 대학마다 이걸 낸다:
**자격 충족**(성적 `meetsAcademic` · 야구 `meetsBaseball`, 색으로 갈림) ·
**팀 프로필**(스타일 · 난이도 · 재정 · 강점 · 설명) ·
**로스터**(총 인원 · 선수 수 · 명단).

04 `_hub`는 **`"○○ 지원"`이라는 한 줄 버튼**이 전부였다.
**아무 데나 찍는 것과 같았다** — "합쳐 놓은 것"이 곧 "옮긴 것"은 아니다.

### 🔴 그리고 **44곳이 도달 불가**였다

`APPLY_SHOWN = 6`으로 잘랐는데 대학이 **50곳**이다. 게다가 가나다순 앞에서
잘려서 **뒤쪽 대학은 게임 내내 한 번도 지원할 수 없었다.**
결정 화면에 **스크롤이 없어서** 그렇게 둔 것이다.

→ `decision_screen.tscn`에 `ScrollContainer`를 넣고 `APPLY_SHOWN`을 **지웠다**
(죽은 상수를 남기지 않는다). **전력 순**으로 세운다 — 가나다순은 근거가 아니다.

### 04 화면이 이제 내는 것

```
내 학업 5등급 (1이 최상) · 야구 점수 0

  한류대   전력 S · 합격  8% · 스카우트 +15  (요구 학업 4등급 · 야구 40)
  백제대   전력 A · 합격 28% · 스카우트  +8  (요구 학업 5등급 · 야구 25)
  비슬대   전력 B · 합격 28% · 스카우트  +3  (요구 학업 6등급 · 야구 12)
  …50곳
  부산 씨라이온스  입단 58%
```

⚠ **산식을 화면이 다시 세지 않는다.** `CareerPath.university_chance` ·
`independent_chance` · `requirement_of_power` · `scout_bonus_of_power`가
정본이다 — 다시 세면 뜬 확률과 실제 판정이 갈린다(협상 화면이 같은 이유로
`Negotiation`을 그대로 쓴다).

⚠ **전력과 스카우트 가산을 같이 적는다.** 확률만 보면 약한 대학이 늘 유리해
보인다 — 강한 곳일수록 가산이 크다(S +15 / A +8 / B +3 / C 0).

### 다른 갈래는 어땠나 — **`salary_negotiation`은 이미 두껍다**

02 `ContractNegotiationModal`(372줄)과 대조했더니 04가 이미 낸다:
요구 슬라이더 · 계약 기간 · 구단/선수 옵션 · 논트레이드 · **수락 확률** ·
시장가 대비. **판정을 `있다`로 올린다.**

검사 15(`test/career_apply_info_test.gd`) · **변이 7/7** ·
캡처 `career-apply`(새 갈래).

⚠ **`.tscn` 경로를 바꿔 기존 검사 둘이 깨졌다** — 알던 함정을 또 밟았다
(`decision_vm_test.gd`의 `get_node("Pad/Center/Col/Choices")`).

⚠ **"글자가 있나"로만 재면 값이 0이어도 통과한다** — 변이 넷이 살아남았다.
**정본과 같은 값인지**를 보게 고쳐 7/7.

✅ **남은 것도 닫았다** — 아래 P-21b. (원래 적힌 것:) 02의 팀 프로필·로스터 명단은 아직 없다.
04 대학 데이터에 `power`·`resource`·`stadium`은 있고 `style`·`desc`는 없다.
로스터는 `world.rosters`에 있으니 붙일 수 있다 — **두 칸 구조(목록 + 상세)**가
필요해 별도 항목으로 둔다.

---

## P-22 — 진로 결과가 **지명 결과를 안 보여줬다** ✅

02 `CareerResultsModal`(242줄)은 **네 칸**을 낸다 — 드래프트(팀 / N라운드
M순위 + 계약금, 또는 미지명) · 대학 지원(합격한 **이름들**) · 독립리그 · 체육부대.

04 `_results`는 세 줄이었다:

```
대학 합격 2곳
독립리그 합격 1곳
드래프트 신청이 받아들여졌습니다.
```

🔴 **`"신청이 받아들여졌습니다"`는 결과가 아니라 신청 수리다.** 몇 라운드에
어느 팀이 뽑았는지가 없었다 — `results`에 `drafted`·`draft_team_id`·
`draft_round`·`draft_pick`이 **다 있는데 화면이 안 읽었다.**

🔴 **대학은 개수만 냈다.** 바로 다음 화면(`career_choice`)에서 골라야 하는데
어디 붙었는지 모르면 고를 수가 없다.

고친 뒤:

```
[드래프트]
  서울 로열스  2라운드 21순위

[대학 지원]
  아산대 합격
  백제대 합격

[독립리그 지원]
  서울 코메츠 합격
```

검사 10(`test/career_results_info_test.gd`) · **변이 7/7**.

⚠ **변이 둘이 살아남았다** — 지명 팀 이름과 "전원 불합격"을 **두 칸 다**
보는 검사가 없었다. 한 칸만 보면 다른 쪽을 지워도 안 잡힌다.

---

## P-23 — 결정 갈래 **둘이 도달 불가**다 🔴 ⬜

`DecisionVm`은 아홉 갈래를 받고 `AutoAdvance`도 아홉을 세는데,
**대기줄에 올리는 곳이 없는 갈래**가 있다.

| 갈래 | 받는 쪽 | 해소하는 쪽 | **올리는 쪽** |
|---|---|---|---|
| `draft_observe` | ✅ `_observe` | ✅ `apply` | ❌ **없다** |
| `option_clause` | ✅ `_option` | ✅ `Pending.resolve` | ❌ **없다** |
| `trade` | ✅ `_trade` | ✅ `accept_trade`/`reject_trade` | ❓ |

⚠ **체육부대와 똑같은 모양이다**(열여덟 번째 죽은 배선) — 코드가 여러 곳에
흩어져 있는데 입구가 없어 게임에 한 번도 안 나타난다.
`retirement_ask`는 **반대 모양**이었다(올리는 쪽만 있고 받는 쪽이 없어
자동 진행이 멈췄다). **양쪽을 다 세는 검사**를 두었다
(`test/decision_reachable_test.gd`) — 고치면 `NO_ENTRY`에서 뺀다.

❓ **`trade`는 못 가렸다.** `"type": "trade"`가 소스에 둘 있는데 **둘 다
`career_events`에 쌓는 기록**이지 대기줄 입구가 아니다. 읽어서는 입구가
없는 것으로 보이나 **문자열로는 못 가른다.**

⬜ **굴려서 확인해야 한다.** 시도했는데 **계측이 또 게임 경로를 안 탔다** —
경기를 안 치고 주간·커리어 처리만 돌리니 16해에 `sports_unit_apply` 하나만
떴다(진로가 진행이 안 됐다). **이번 루프 일곱 번째.**
경기를 실제로 치는 계측(`GameSim.play` → 주 경계 `WeekRunner`+`CareerRunner`)은
**10분을 넘겨 죽었다** — 시드 하나·짧은 해로 줄여 다시 잰다.

⚠ **02에도 없을 수 있다.** `OptionClauseModal`은 44줄뿐이고
`DraftObserveModal`은 관전 연출이다 — **02에서 실제로 뜨는지부터 본다**(P-18b 선례).

---

## P-24 — 주인공이 **트레이드 대상이 아니었다** ✅

P-23의 `trade` ❓를 **읽어서 확정했다** — `TradeRunner.assets_of`가
주인공을 **명시적으로 뺐다**(`if p.get("is_protagonist", false): continue`).
제안에 실릴 수가 없으니 `trade` 결정은 **도달 불가**였다.
화면(`_trade`) · 받는 코드(`accept_trade`/`reject_trade`) · `AutoAdvance`
항목이 다 있는데 게임에 한 번도 안 나타났다 — **체육부대와 같은 모양.**

**02는 주인공을 자산으로 넣는다**(`market.ts:351-368` `protagonistAsset`)
그리고 걸리면 `pushPendingAction({type:"trade"})`로 **묻는다**
(`:543-560`) — **받아오는 선수의 이름·OVR·포지션·연봉**까지 실어서.

### 고친 것 넷

1. `assets_of`가 주인공을 싣는다. **노트레이드 조항이 있으면 뺀다** —
   조항을 따 놓고 팔려 가면 협상 화면의 그 토글이 장식이 된다.
2. `TradeRunner.offer_protagonist` 신설 — **조용히 옮기지 않고 묻는다.**
   답하기 전에는 팀이 안 바뀐다.
3. `_trade` 화면이 **받아오는 선수**를 보여준다(이름·포지션·OVR·연봉·이유).
   뭘 받는지 모르면 받아들일지 정할 수가 없다.
4. 🔴 **`accept_trade`가 로스터를 안 옮기고 있었다 — P-14와 똑같은 자리다.**
   `team_id`만 바꾸면 `relink_protagonist`가 새 팀에서 못 찾고,
   못 찾으면 성장·관계·기록이 전부 그 위에 선다.
   **주인공이 트레이드될 길이 없어서 이 결함이 안 드러나 있었다.**
   → `CareerDecision.move_roster`(P-14가 만든 정본)를 쓴다(`_move_roster` → 공개).

### 실측 — 드물다

```
시드 8개 · 각 한 해 · 프로 로스터로 옮겨 놓고 TradeRunner.run
  자산에 나: 예 (8/8)   리그 성사: 15건   주인공 물음: 0/8
```

리그 전체 성사가 한 해 15건이라 9,600명 중 걸릴 확률이 낮다. **드문 게
결함은 아니다** — 02도 같은 제안 생성에서 갈린다.

⚠ **계측이 P-14 함정을 그대로 밟았다** — 처음엔 `team_id`만 바꿔 놓고
쟀더니 "자산에 나: 아니오"가 나왔다. **로스터가 안 따라온 것**이고,
`move_roster`로 옮기니 실렸다. 고친 결함과 **같은 실수를 계측에서 했다.**

검사 11(`test/protagonist_trade_test.gd`) · **변이 6/7** ·
`bench:day` **단독 0.950 · 0.945 · 0.954초**.

⬜ **변이 하나가 남는다** — `TradeRunner.run` 안의 주인공 갈래를 타는
픽스처를 못 세웠다(계약 만료 선점 조건을 맞춰도, 진짜 팀 id를 써도 제안이
안 섰다 — 순위표·`TeamProfile`·payroll이 더 걸린다).
**`Trade.propose`를 직접 불러 만든 제안으로 `run` 안쪽을 타는 검사**가 필요하다.

⚠ **P-23의 `draft_observe` · `option_clause`는 아직 도달 불가다.**
`decision_reachable_test.gd`의 `NO_ENTRY`가 그 둘을 세고 있다.

---

## P-25 — 옵션 조항이 **계약에 남지 않았다** ✅

P-23의 `option_clause` 도달 불가를 잡으러 갔더니 **뿌리가 더 깊었다.**

협상 화면에서 구단 옵션·선수 옵션을 고를 수 있고 `Negotiation`이 연봉
배수까지 곱하는데(`TEAM_OPTION_MULT`·`PLAYER_OPTION_MULT`),
🔴 **`_apply_contract`가 그 둘을 선수에게 안 옮겼다.**
계약이 끝날 때 볼 근거가 사라지니 `option_clause`가 뜰 수가 없었다.

**노트레이드도 같은 자리였다** — 협상에서 따 놓고 선수에게 안 남아서
P-24에서 만든 트레이드 가드가 영영 안 걸릴 뻔했다.

### 고친 것 셋

1. `_apply_contract`가 `team_option_years`·`player_option_years`·`no_trade`를
   남긴다.
2. `ContractDecision.check_option_clause` 신설 —
   **계약 마지막 해**(`contract_years == 1`)에 옵션이 있으면 묻는다.
   `SeasonRunner`가 재계약 물음 다음에 부른다. 한 해에 한 번만.
3. 🔴 **화면이 구단 옵션을 사용자에게 고르게 하고 있었다.**
   **그건 구단이 정한다** — 02는 시즌 평점이 문턱을 넘는지로 갈라
   **통보**하고(`advanceWeek.ts:1060-1070`), 사용자가 고르는 건 **선수 옵션**뿐이다.
   통보에 선택지를 두면 "고를 수 있다"는 거짓말이 된다 — 무엇을 눌러도
   같은 일이 일어난다. 이제 구단 옵션은 확인 하나, 선수 옵션은 둘이다.

⚠ **문턱은 가운데(75)로 뒀다.** 02는 `75 - winNowPressure/100*25`인데
04 `TeamProfile`에 그 축이 있는지 아직 안 봤다 — **없는 축을 지어내지
않는다.** ⬜ 찾으면 거기만 고친다.
평점도 04엔 `calcSeasonRating`이 없어 **OVR**로 뒀다. ⬜ 같이.

검사 16(`test/option_clause_test.gd`) · **변이 7/7** ·
`bench:day` **단독 0.973 · 0.991 · 0.950초**.

⚠ **픽스처가 또 실제 경로를 몰랐다** — `renewal` 계약은 즉시 발효가 아니라
`NEXT_KEY`에 쌓였다가 롤오버가 옮긴다. 바로 읽으려다 헛짚었다.

⚠ **변이 둘이 살아남았다** — 연도 가드는 `push_once`가 중복을 막아 줘서
**답해서 대기줄이 빈 뒤**를 봐야 갈렸고, 구단 옵션 문턱은 **못하는 선수**를
안 세워서 안 걸렸다.

### 🔴 옛 검사가 옛 약속을 못 박고 있었다 (P-24 뒤처리)

전체 검사에서 **실패 1건**이 나왔다 — `test_the_protagonist_is_not_an_asset`.
*"주인공은 자산 풀에서 뺀다 — 진로는 사용자가 정한다"*였는데,
**이유는 맞고 방법이 틀렸다.** 빼 버리니 `trade`가 도달 불가가 됐다.
**빼는 게 아니라 묻는 것**이 사용자가 정하는 방법이다 —
새 약속(`..._is_an_asset_but_gets_asked`)으로 갈아끼웠다.

⚠ **바꾼 함수를 쓰는 검사 파일을 다 안 돌렸다.** `trade_test.gd`는 돌렸는데
`trade_runner_test.gd`를 안 돌려서 전체 검사가 잡았다.

### 남은 것

⬜ **`draft_observe`만 도달 불가로 남았다**(`NO_ENTRY`).
02는 **W47에 올린다**(`advanceWeek.ts:948-956`) — 고교/대학 결과 주가
아니고 진로 대기가 없을 때. 04 달력으로 환산해 붙인다.

---

## P-26 — 드래프트 관전이 도달 불가였다 ✅ · **도달 불가 갈래가 0이 됐다**

`option_clause`(P-25) 다음, **마지막 하나**였다. 화면(`_observe`) ·
해소하는 쪽(`apply`) · `AutoAdvance` 항목이 다 있는데 **대기줄에 올리는 곳이
하나도 없었다** — 체육부대와 같은 모양.

02는 **W47**에 올린다(`advanceWeek.ts:948-956`) — 결과 주가 아니고 진로
대기가 없을 때. **04는 `CareerRunner.RESULT_WEEK`가 이미 그 주라 주차를
새로 박지 않았다.**

⚠ **드래프트를 신청한 사람에게만.** 안 낸 사람에게 "오늘 드래프트가
열립니다"는 남의 일이다 — 02는 진로 대기가 없을 때만 올려 같은 뜻이 된다.

⚠ **결과보다 먼저 올린다.** 결과를 보여준 뒤 관전을 띄우면 순서가 거꾸로다.

⚠ **한 해에 한 번.** `RESULT_WEEK` 이후 매주 도는 자리라 안 막으면 여러 번 뜬다.

검사 7(`test/draft_observe_test.gd`) · **변이 5/5** ·
`bench:day` **단독 0.922 · 0.945 · 0.931초**.

### 🟢 `decision_reachable_test.gd`의 `NO_ENTRY`가 **비었다**

| 갈래 | 올리는 쪽 |
|---|---|
| `career_results` · `career_choice_hub` · `career_choice` | ✅ |
| `draft_notification` · `salary_negotiation` · `fa_market` | ✅ |
| `sports_unit_apply` · `military_enlist_ask` | ✅ (병역 항목) |
| `trade` | ✅ **P-24** |
| `option_clause` | ✅ **P-25** |
| `draft_observe` | ✅ **P-26** |

**아홉 갈래가 전부 실제로 뜬다.** 검사가 양쪽(올리는 쪽·받는 쪽·`AutoAdvance`)을
같이 세므로, 앞으로 한쪽만 만들면 바로 걸린다.

### 🔴 옛 검사가 **또** 옛 약속을 못 박았다 (P-25 뒤처리)

전체 검사에서 실패 2건 — 둘 다 `decision_vm_test.gd`였다.
`test_an_option_clause_can_go_either_way`가 `option_type` 없이 올려 놓고
**"행사/미행사를 사용자가 고른다"**를 봤다. 그게 P-25에서 바꾼 지점이다.
뜻("두 갈래로 갈린다")은 **선수 옵션**에서 그대로 살고, 구단 옵션은
**통보**를 보는 검사를 따로 뒀다.

⚠ **두 번 연속 같은 자리에서 걸렸다** — 바꾼 함수를 쓰는 검사 파일을
`grep -rln`으로 먼저 세는 습관이 아직 안 붙었다.

---

## P-25b — **"04엔 없다"고 적은 축 둘이 04에 있었다** ✅

P-25에서 옵션 문턱과 시즌 평점을 *"04에 대응 축이 있는지 아직 안 봤다"*며
가운데 값으로 두고 ⬜로 적었다. **찾아보니 둘 다 있었다.**

| 02 | 04 | 처음에 뭘 했나 |
|---|---|---|
| `winNowPressure` (옵션 문턱) | ✅ `TeamProfile.DEFAULT["win_now_pressure"]` | 상수 75로 굳혔다 |
| `calcSeasonRating` | ✅ `Contract.season_rating(stats)` | **OVR**로 뒀다 |

🔴 **OVR로 재면 한 해 부진해도 옵션이 그대로 행사된다.** `season_rating`은
그 해 **성적**(ERA·WHIP)으로 재고 재계약 제시액도 그걸 쓴다 — 정본이 따로
있었다.

02 식을 그대로 넣었다 — `75 - win_now_pressure/100*25`.
압박 0인 팀은 문턱 75, 100인 팀은 50이다.

⚠ **"없다"고 적기 전에 먼저 센다.** `grep -rn "<축>" sim/`이면 끝나는 일을
안 하고 ⬜로 미뤘다 — **P-18b(02에도 없었다)의 반대 실수**다.

⚠ **문턱을 따로 재는 검사만으론 부족했다** — 그 값을 안 쓰고 상수로 굳히는
변이가 살아남았다. **같은 성적인데 팀만 다를 때 갈리는지**를 봐야 잡힌다.

⚠ **검사 기대값을 지어내지 않았다** — `tools/_tmp_*.gd`로 찍어 보고
ERA 4.60(평점 68.7)을 골랐다. 처음 쓴 3.60은 80.4라 두 팀 다 잡아서
검사가 아무것도 안 봤다.

검사 20(`test/option_clause_test.gd`) · **변이 5/5 + 2/2** ·
`bench:day` **단독 0.953 · 0.973 · 0.976초**.

---

## P-24b — `run` 안의 주인공 갈래 ✅ **닫았다** (아래는 헤맨 기록)

P-24의 남은 변이 하나("안 묻고 조용히 옮긴다")를 잡으러 파고들었다.
**`Trade.between`은 제안을 낸다** — 계약 만료 선점 조건(내 계약 1년 +
상대 유망주 OVR 66 · 연차 2)을 맞추면 **아홉 가지 mode 조합 전부에서**
`offering_ids: ["ME"]`인 제안이 하나씩 나온다(찍어서 확인).

**그런데 `run_league`를 굴리면 `proposed: 0`이다.** 같은 입력을 손으로
조립해 짝 루프를 그대로 흉내 내도 **합계 0**이 나온다.

확인한 것 — 전부 정상이었다:
- `World.teams_of("LEAGUE_KBL")` = 10팀, 쓰려던 두 팀이 **둘 다 그 안에 있다**
  (`_1`은 1군 `_2`는 2군인데 **리그 id가 같다**)
- `World.roster_of(손으로 만든 world, 팀)` = 사람 수가 맞게 나온다
- `TradeRunner.assets_of`가 낸 자산 사전이 `Trade.between`이 먹는 모양과 같다
- `mode_of` · `TeamProfile.of`가 값을 낸다

❓ **그래서 짝 루프에서만 빠지는 이유를 아직 모른다.**
⬜ 다음에 볼 것: `run_league` 안에서 `ctx`가 실제로 어떤 팀을 담는지 찍는다
(손으로 흉내 내지 말고 **그 함수 안에서** 찍는다 — 흉내가 이미 두 번
빗나갔다).

⚠ **드문 것과 검사가 못 보는 것은 다르다.** 실측에서 여덟 시드 0회는
"리그 성사가 한 해 15건"으로 설명되지만, **합성 픽스처에서도 0**인 것은
설명이 안 된다. 그 차이가 남아 있는 한 이 갈래는 **검사 밖**이다.

⚠ **시간을 정해 두고 물러났다** — 한 변이에 매달려 남은 항목을 미루는 것보다
정확히 적고 넘어가는 편이 낫다.

---

## P-27 — FA 제안 생성기가 04에 **없다** 🔴 ⬜ (다음 항목)

04 `decision_vm.gd`의 주석이 *"제안을 만드는 곳이 04에 없다"*고 적어 뒀는데
**맞았다.** 그리고 **02에는 있다** — 없는 게 아니라 안 옮긴 것이다.

| | 02 | 04 |
|---|---|---|
| 리그 전체 FA(NPC) | ✅ | ✅ `FaMarket.resolve` · `FaRunner` |
| **주인공에게 오는 제안** | ✅ `generateFaOffers` → Rust `generate_fa_offers` | ❌ **없다** |

그래서 `fa_market` 화면이 **"기다린다" 하나만** 준다 — 04는 FA가 돼도
고를 게 없다.

### 02 산식 (`player_engine.rs:354-410`) — 옮길 값

```
base   = 1800 + max(ovr - 50, 0) * 220 + fame * 28
market = base * league_salary_mult(league)      # ABL 3.5 · JBL 2.0 · 독립 0.35 · 그 밖 1.0
drop   = max(1 - 미계약주 * 0.04, 0.72)

팀마다:
  win_mult   = 1 + (win_now_pressure - 50)/100 * 0.30
               ※ 여유(cap-payroll)/cap < 0.15면 × 0.6
  year_bias  = stability > 65 ? +1 : stability < 35 ? -1 : 0
  bonus_mult = 0.08 + market_appeal/100 * 0.12
  noise 폭   = (100 - scouting_quality)/100 * 0.25
  salary     = market * (0.85 + r*0.35) * win_mult * (1 + noise) * drop
  duration   = clamp(rand(1..4) + year_bias, 1, 5)
  계약금     = salary * (bonus_mult + r*0.08)
  구단옵션 35% · 선수옵션 25% · 노트레이드 20%

제안 수 = rand(3..5), 같은 리그 · 자기 팀 제외
```

⚠ **04 축이 다 있다** — `TeamProfile`에 `win_now_pressure` · `stability` ·
`market_appeal` · `scouting_quality`가 있고 `payroll_of`도 있다.
(P-25b에서 **"없다"고 적기 전에 세라**를 배웠다.)

🔴 **02가 실측으로 겪은 함정을 같이 옮긴다** — `faEngine.ts:36-41`:
*"KBL은 1군(`_1`)과 2군(`_2`)이 **같은 leagueId**를 쓴다. 그대로 넘기면
FA 제안에 2군이 섞이고 실제로 그리로 이적한다(실측: `..._STARS_2`와 3년 계약)."*
**04도 `_1`/`_2`가 같은 `league_id`다**(이번에 확인했다). 1군만 거른다.

⚠ **`rand::thread_rng()`를 그대로 옮기지 않는다** — 04는 `Rng`를 거친다.
같은 세이브를 다시 열면 같은 제안이 와야 한다.

---

## P-27 — FA 제안 생성기 ✅ **옮겼다**

🔴 **주인공에게 오는 FA 제안을 만드는 코드가 04에 없었다.** 리그 전체
FA(`FaMarket`·`FaRunner`)는 도는데 주인공 몫이 없어 화면이
**"한 해 더 기다린다" 하나만** 줬다 — FA가 돼도 고를 게 없었다.

→ `sim/fa_offers.gd` 신설(02 `player_engine.rs:354-410` 그대로).
`_fa`가 만들어 상태에 남기고, `apply`의 `offer:` 갈래가 이미 받는다.

### 실측 — 다섯 팀이 부른다

```
=== FA 시장 ===  제안 5건
  광주 팬서스   · 연봉 1.10억 · 4년      노트레이드
  서울 로열스   · 연봉 1.10억 · 4년      노트레이드
  대구 세이버스 · 연봉 1.01억 · 3년      선수옵션 1
  대전 팬텀스   · 연봉 9628만 · 4년      구단옵션 1 · 선수옵션 1
  서울 코브라스 · 연봉 8928만 · 1년
```

### ✅ 02가 겪은 함정이 **04엔 없었다**

02는 실측으로 당했다(`faEngine.ts:36-41`): *"KBL은 1군과 2군이 같은
leagueId를 쓴다. 그대로 넘기면 FA 제안에 2군이 섞이고 실제로 그리로
이적한다(실측: `..._STARS_2`와 3년 계약)."*

**04는 `World.teams_of`가 이미 거른다**(`world.gd:101-106`) — 2군은
`LEAGUE_*_FARM`이라는 별도 리그로 파생된다.

⚠ **처음엔 02를 그대로 옮겨 `_1` 필터를 넣었다.** **늘 참인 죽은 가드**였고
검사가 "KBL에 2군이 없다"로 잡았다 — **함정만 보고 옮기지 말고 04 구조를
먼저 본다.** (P-25b의 "없다고 적기 전에 세라"와 짝을 이루는 실수다.)

### 04에 없는 축 하나 — 지어내지 않았다

02는 여유(`salary_cap - payroll`)가 15% 아래면 압박에 0.6을 곱한다.
**04엔 급여 상한 축이 없다**(`data/*.json`을 세어 봤다).
02도 상한을 모르면 여유를 0.5로 두므로(`player_engine.rs:380`) **안 걸린다** —
`if 0.5 < 0.15`를 적으면 죽은 가드가 되므로 **갈래 자체를 안 넣고** 적어 뒀다.
⬜ 상한 축이 생기면 그때 넣는다.

### 같이 고친 것 둘

- `Contract.league_mult` 신설 — **리그 배수 표를 두 벌 적지 않는다**
  (ABL 3.5 · JBL 2.0이 `salary_rules.json`에 이미 있었다).
- `sign_fa_offer`가 **옵션·노트레이드를 계약으로 옮긴다** — P-25가 협상
  쪽에서 찾은 것과 같은 자리다. 제안에 붙여 놓고 안 옮기면 계약 끝에
  볼 근거가 사라진다.

⚠ **`thread_rng`를 그대로 안 옮겼다** — `Rng`를 거친다. 씨앗에 **미계약 주**를
섞어서 기다리면 부르는 팀도 값도 달라진다(안 섞으면 기다릴 이유가 없다).

⚠ **`generate` 둘을 견주는 검사가 흔들렸다** — 씨앗이 달라 부르는 팀이
바뀌므로 금액 비교가 성립하지 않는다. `offer_of`에 **같은 팀·같은 난수**를
주고 `drop`만 바꾸고, `generate` 쪽은 **두 번 다 부른 팀끼리** 견줬다.

검사 22(`test/fa_offers_test.gd`) · **변이 8/8** ·
`bench:day` **단독 0.917 · 0.929 · 0.939초**.

### ✅ P-24b 해결 — **상대 팀이 2군이어서였다**

`run_league` **안에서 찍어 보니 곧바로 나왔다:**

```
[PROBE] teams=10 ctx=10
[PROBE] TEAM_KBL_BUSAN_WAVES_1   assets=2 mode=seller
[PROBE] TEAM_KBL_CHANGWON_STARS_1 assets=2 mode=seller
[PROBE] 결과 {proposed: 1, done: 1, moved: 0} · 물음 예
```

🔴 **앞서 세 번 빗나간 이유** — 상대를 `..._WAVES_2`로 잡았다.
04는 2군을 `LEAGUE_*_FARM`으로 파생하므로 **`World.teams_of("LEAGUE_KBL")`에
`_2`가 아예 없다**(P-27에서 알아낸 바로 그 구조다). 짝이 설 수가 없었다.
**다른 구단의 1군**(`..._CHANGWON_STARS_1`)을 쓰니 한 번에 섰다.

⚠ **흉내 낸 계측이 세 번 다 틀렸고, 그 함수 안에서 한 번 찍으니 끝났다.**
`data/teams.json`엔 `_2`가 있어서 "리그 id가 같으니 짝이 선다"고 읽었는데,
**데이터와 `teams_of`가 다른 답을 준다.** 읽어서 세운 인과는 굴리기
전까지 가설이다.

검사 12(`test/protagonist_trade_test.gd`) · **변이 5/5**
("안 묻고 조용히 옮긴다" · "묻고도 계속 옮긴다" 둘 다 잡힌다).

### 🔴 옛 검사가 **세 번째로** 옛 약속을 못 박았다 (P-27 뒤처리)

전체 검사 실패 2건 — 둘 다 `decision_vm_test.gd`.
`test_an_empty_fa_market_still_lets_me_move_on`이
*"제안을 만드는 곳이 04에 없다 — 지금은 기다리는 길만 준다"*를 보고 있었다.
**P-27이 바꾼 바로 그 전제다.** 뜻을 갈라서 옮겼다:
- "제안이 없어도 대기줄은 풀린다" → **부를 팀이 없는 리그**로 그대로 산다
- "본문에 '없습니다'" · "선택지가 `wait` 하나" → **이제 틀린 약속**이라
  제안이 서는 경우를 보는 검사를 새로 뒀다

---

## P-21b — 진로 지원에 **어떤 팀인지**를 붙였다 ✅

P-21에서 자격·확률·요구조건까지 붙였는데 **"그래서 어떤 팀인가"가 없었다.**
02 `UniversityApplyModal`은 거기에 **프로필**(스타일·난이도·재정·강점·설명)과
**로스터**(총 인원·선수 수·명단)를 더한다.

### 04에 있는 것만 냈다

| 02 | 04 | |
|---|---|---|
| 재정(`funding`) | ✅ `resource`(`부유`·`안정`·`알뜰`·`궁핍`) | 붙였다 |
| 로스터 총 인원 | ✅ `world.rosters` | 붙였다 |
| 로스터 명단(포지션) | ✅ | **같은 자리 경쟁자 수**로 줄였다 |
| 난이도 | ✅ 전력 등급이 이미 있다(P-21) | 중복 |
| 스타일 · 강점 · 설명 | ❌ **04 데이터에 없다** | 지어내지 않았다 |

```
한류대  전력 S · 합격 8% · 스카우트 +15  (요구 학업 4등급 · 야구 40)  [부유 · 32명 · 같은 자리 14]
치악대  전력 B · 합격 28% · 스카우트 +3  (요구 학업 6등급 · 야구 12)  [궁핍 · 32명 · 같은 자리 16]
```

⚠ **명단을 다 적지 않고 "같은 자리 N"으로 줄였다.** 02는 두 칸 구조라
명단이 들어가지만 04는 한 줄 버튼이다. **인원보다 같은 포지션 경쟁자 수가
"가면 뛸 수 있나"를 가른다** — 32명은 어디나 같은데 투수는 12~17로 갈린다.

⚠ **로스터가 없으면 아무 말도 안 한다** — `0명 · 같은 자리 0`은 정보가
아니라 잡음이다. **실제 세계엔 빈 팀이 없어 변이가 살아남았고**, 빈 세계로
재는 검사를 더해 6/6이 됐다.

⚠ **없는 축을 읽지 않는지 검사가 본다** — `style`·`desc`·`strengths`·
`difficulty`를 `decision_vm.gd`가 읽으면 실패한다. 읽어 봐야 늘 빈칸이다.

검사 7(`test/career_apply_detail_test.gd`) · **변이 6/6** ·
캡처 `career-apply`로 눈으로 확인.

---

## P-28 — 지명 통보가 **거부하면 어디로 가는지 안 알려줬다** ✅

02 `DraftNotificationModal`(112줄)과 대조했다.

| 02가 내는 것 | 04(전) |
|---|---|
| 팀 · N라운드 M순위 | ✅ |
| 연봉 · 계약금 | ✅ |
| **계약 기간** | ❌ |
| **총 계약액** | ❌ |
| "협상은 불가합니다" | ❌ |
| 🔴 **거부 시 어디로** (대학 / 독립 / 대안 없음) | ❌ |

🔴 **되돌릴 수 없는 선택인데 모르고 누르게 했다.**
`alt_university_team_id`·`alt_independent_team_id`가 **액션에 이미 실려
있었고** 엔진(`reject_draft_offer`)이 그걸로 갈리는데 **화면만 안 읽었다.**

```
부산 웨이브스가 2라운드 21순위로 지명했습니다.
연봉 6000만 · 3년 · 계약금 1.50억
총 계약액 3.30억

구단 제시 조건입니다. 협상은 할 수 없습니다.

거부하면 아산대로 진학합니다.        ← 대학이 있을 때
거부하면 서울 코메츠에 입단합니다.    ← 독립만 있을 때
거부하면 갈 곳이 없습니다. …          ← 둘 다 없을 때
```

⚠ **엔진과 같은 순서로 본다**(대학 → 독립 → 병역). **대학 대안은 고교생만** —
대학 재학생에게 말하면 두 번 입학이고 엔진은 그 갈래를 안 탄다.
**화면이 엔진보다 관대하면 거짓말이 된다.**

⚠ **04는 갈 곳이 없어도 조용히 입대시키지 않고 묻는다**(병역 항목) —
그래도 "갈 곳이 없다"는 걸 알려야 고를 수 있다.

검사 9(`test/draft_notification_test.gd`) · **변이 8/8**.

---

## P-29 — 진로 최종 선택 ✅ · **결정 화면 아홉 갈래 대조가 끝났다**

02 `CareerResultModal`(133줄) 대조. **되돌릴 수 없는 선택인데** 04는
`"프로에 간다"` · `"지금 자리에 남는다"`처럼 **뜻이 비어 있었다.**

| 02 | 04(전) |
|---|---|
| `드래프트 지명: {팀} / {N}R {M}순위` | `"프로에 간다"` 🔴 |
| `대학 합격: {팀}` | ✅ 이름만 |
| `다음 학년 진급 (N학년)` / `독립리그 계속` | `"지금 자리에 남는다"` 🔴 |

고친 뒤 — **지원 화면(P-21b)과 같은 팀 정보를 붙였다.** 되돌릴 수 없이
정하는 자리라 오히려 더 필요하다.

```
프로에 간다 — 부산 웨이브스  2라운드 21순위
아산대에 진학한다  [안정 · 32명 · 같은 자리 14]
다음 학년으로 진급한다 (3학년)
```

검사 8(`test/career_choice_test.gd`) · **변이 6/6**.

### 🟢 결정 화면 아홉 갈래 — **전수 대조 끝**

| 갈래 | 결과 |
|---|---|
| `career_choice_hub` | ✅ P-21 · P-21b (자격·확률·요구·재정·인원·같은 자리) |
| `career_results` | ✅ P-22 (지명 결과 · 합격 이름) |
| `career_choice` | ✅ **P-29** |
| `draft_notification` | ✅ P-28 (기간·총액·거부 시 행선지) |
| `draft_observe` | ✅ P-26 (도달 불가였다) |
| `salary_negotiation` | ✅ 이미 두꺼웠다(슬라이더·옵션·수락 확률) |
| `option_clause` | ✅ P-25 · P-25b (계약에 안 남던 것 · 축 둘) |
| `fa_market` | ✅ P-27 (제안 생성기가 없었다) |
| `trade` | ✅ P-24 · P-24b (주인공이 대상이 아니었다) |

**PORT_GAP의 `부분` 14개 중 아홉이 여기 몰려 있었고, 아홉 다 닫혔다.**

⚠ **대조에서 나온 형태 셋:**
1. **액션에 실린 값을 화면이 안 읽는다**(P-28의 `alt_*`, P-22의 `draft_*`)
2. **도달 불가**(P-24 · P-25 · P-26 — 받는 쪽만 있고 올리는 쪽이 없다)
3. **고른 것이 남지 않는다**(P-25 옵션 · 노트레이드, P-27 제안 조항)

---

## P-30 — 부상 치료 선택 ✅ · **PORT_GAP이 또 틀렸다**

PORT_GAP은 이 자리를 *"`injury_treat` 검색 0건 — **엔진부터 확인**"*으로
적어 뒀는데 **틀렸다. 04에 산식이 다 있었다:**

| 04에 이미 있던 것 | 상태 |
|---|---|
| `data/injury_rules.json`의 `penalty_by_treatment` | ✅ 표가 있다 |
| `Injury.permanent_penalty(type, **treatment_choice**)` | ✅ 갈린다 |
| `InjuryRunner._heal_protagonist`가 `cur["treatment_choice"]`를 읽는다 | ✅ |
| 재정 화면의 **"부상 치료비"** 줄(`treatment_weekly`) | ✅ |
| `Injury.trigger_chance`가 읽는 `prior_steroid_used` | ✅ |

🔴 **그런데 다섯 다 읽기만 했다. 채우는 곳이 하나도 없었다.**
**결정 화면 대조에서 본 형태 ②③이 그대로 있었다** —
받는 쪽만 있고 올리는 쪽이 없고, 고른 것이 남지 않는다.
그래서 후유증은 늘 `default`로 떨어졌고 **스테로이드의 대가가 게임에
한 번도 안 나타났다.**

### 02 값 그대로 옮겼다 (`InjuryTreatmentModal:19-35`)

```
=== 부상 치료 ===
입스. 회복까지 12주 남았습니다.
  [ 자가 극복 — 기간 그대로 · 비용 없음 · 완치율 40% … 영구 손실 제구·커맨드 -3 ]
  [ 스포츠 심리 상담 — 기간 그대로 · 주당 80만 · 완치율 75% … -1 ]

어깨 염증
  [ 보존 치료 — 기간 그대로 · 주당 30만 · 영구 손실 없음 ]
  [ 스테로이드 주사 — 3주 단축 · 일시금 200만 · 재발 시 중증으로 악화… ]
```

⚠ **02가 가르는 부상 둘만이다.** 나머지는 빈 배열 —
**없는 선택을 지어내면 그게 두 번째 정본이 된다.**

⚠ **세 자리를 같이 넣었다**(`HANDLED` · `match` · `AutoAdvance`) —
하나라도 빠지면 도달 불가이거나 자동 진행이 그냥 지나간다.

⚠ **완치하면 치료비를 끊는다.** 안 끊으면 다 나은 뒤에도 재정 화면에서
주마다 계속 빠진다 — **화면이 읽는 값이라 조용히 새는 자리다.**

검사 24(`test/injury_treatment_test.gd`) · **변이 9/9** ·
`bench:day` **단독 0.962 · 0.910 · 0.898초**.

---

## P-31 — 훈련 결과를 볼 자리가 없었다 ✅ (`TrainingStatBars`)

`week_runner.gd:228`과 `match_outcome.gd:82`가 `training_log`에 쌓으면서
주석에 *"소식이 `training_log`를 읽는다"*고 **적어 뒀는데 읽는 곳이
하나도 없었다.** 무엇이 얼마나 늘었는지 플레이어가 볼 자리가 없었다.

⚠ **이번 루프에서 같은 형태를 네 번째 만났다** — 소식 본문 · 월간 부상
리포트 · 다이제스트 · 부상 치료. **주석이 "읽는다"고 해도 세어 봐야 한다.**

### ✅ 04도 02와 같은 모형이었다 — 지어낼 게 없었다

02 `TrainingStatBars`는 능력치마다 **진척 막대(pct)** · 현재값 ·
**레벨업(+1 ★)**, 그리고 컨디션/피로도/사기를 낸다.
04는 능력치가 실수라 그 개념이 없는 줄 알았는데 — **`Growth.try_level_up`이
정수 레벨과 `xp`를 가르고 `xp_threshold`가 다음 레벨까지의 양을 준다.**

```
=== 훈련 결과 ===   미리보기: 2개 능력이 올랐습니다.
velocity +1  현재 71  다음까지 35%
control +2   현재 68  다음까지 80%
슬라이더를 익혔다

컨디션 74 · 피로 41 · 사기 63
```

⚠ **02는 막대로 그리고 04 소식은 글자다** — 같은 값을 다르게 그리는 것이지
없는 값을 지어내는 게 아니다.

⚠ **이름을 못 찾은 줄도 안 버린다** — 구종 개발 로그(`슬라이더를 익혔다`)가
그렇다. 능력치 표에 없다고 버리면 그 소식이 조용히 사라진다.

⚠ **한 주에 두 번 안 넣는다** — 경기 성장도 같은 로그에 쌓으므로 `send`가
두 번 불릴 수 있다. 겹친 id는 소식함에서 하나가 조용히 사라진다.

검사 11(`test/training_report_test.gd`) · **변이 7/7** ·
`bench:day` **단독 0.951 · 0.939 · 0.930초**.

### 🔴 옛 검사가 **네 번째로** 옛 약속을 못 박았다 (P-30 뒤처리)

전체 검사에서 실패 1건 — `auto_advance_test`의
`test_a_routine_notice_does_not_stop_the_world`가
`injury_treatment`를 **"알림성"** 목록에 넣고 있었다.

⚠ **그 목록이 쓰인 때(`b3f7f4cd0`)에는 그런 갈래가 아예 없었다** —
이름만 예시로 적어 둔 것이다. P-30에서 실제로 만들고 보니
**치료 선택은 결과가 상태에 남는다**: 후유증(제구 -3 vs -1) · 회복 주차 ·
치료비 · 스테로이드의 재발 확률이 다 갈린다.

**안 멈추면 자동 진행이 지나가고 물음은 대기줄에 영영 남는다** —
`retirement_ask`가 04에서 딱 그 상태였다. 목록에서 빼고
**멈춘다는 것을 못 박는 검사**를 따로 뒀다.

⚠ **네 번 다 "이유는 맞고 방법이 틀렸다"였다.** 옛 검사가 지키려던 뜻은
살아 있고, 그 뜻을 지키는 **방법**이 바뀐 것이다.

---

## P-32 — 재정: **투자 절이 화면에 없었다** ✅

남은 `부분`을 항목 단위로 세다가 `finance`(02 436줄)에서 나왔다.

02 `FinancePage`의 절 다섯 — 주간 수입·지출 / 계약 중 / 받은 제안 /
개인 트레이닝 구독 / **투자**. 04 `finance_vm.build`가 내는 것:
`kpi` · `ledger` · `trend` · `sponsor` · `training`. **투자가 없다.**

🔴 **엔진은 다 있었다** — `Finance.investment_options` · `investment_option` ·
`can_invest` · `resolve_investment`, 그리고 `data/finance_rules.json`에
**예금(+3% 확정) · 펀드(+8% / 최대 −35%) · 사업(+15% / 최대 −80%)**까지.
**넷 다 아무도 안 불렀다** — 형태 ②를 **다섯 번째** 만났다.

```
=== 현금 5000만 · 프로 ===
  예금       기대  +3% · 확정      — 확정 수익. 물가만큼은 지킨다
  펀드       기대  +8% · 최대 -35% — 평균은 예금보다 낫지만 마이너스 해도 있다
  사업·주식  기대 +15% · 최대 -80% — 크게 벌거나 크게 잃는다

=== 현금 100만 ===  못 함: 현금이 500만 이상이어야 합니다.
```

⚠ **위험을 같이 적는다.** 기대 수익만 보면 사업이 늘 나아 보인다 —
`floor`가 최대 손실률이다. 흔들림이 0이면 `-0%` 대신 **"확정"**이라 적는다.

⚠ **못 하면 이유를 말한다.** 빈 칸은 고장으로 보인다.

⚠ **최소 현금을 화면이 다시 적지 않는다** — `Finance.invest_min_cash()`를
새로 내서 숫자가 두 곳에 안 생기게 했다.

⚠ **화면은 `career_stage`가 아니라 리그로 무대를 가른다**(`_stage_of`).
엔진만 재는 검사로는 그 차이를 못 봐서 **화면 경로로도 막히는지** 보는
검사를 따로 뒀다.

✅ **정산까지 닫았다** — 아래 P-32b.

검사 14(`test/investment_test.gd`) · **변이 8/8**(`sim/finance.gd`).

⚠ **04에만 있는 것 하나** — `Finance`의 **사치품**(`LUXURY_UNIT` 등)은
02 `FinancePage`에 없다. 04 고유라 그대로 둔다.


## P-32b — 투자 정산: **굴릴 자리가 없었다** ✅

P-32에서 갈래·값·화면까지 냈는데 **`resolve_investment`를 부르는 곳이
여전히 없었다.** 굴릴 자리가 없으니 이력도 안 쌓이고, 방금 만든 재정
화면의 투자 절도 영원히 비어 있었을 것이다.

🔴 **02를 다시 열어 보니 내가 절을 잘못 옮겼다.** 02 `FinancePage`의
투자 절은 **고르는 자리가 아니다** — `<p class="sub">투자는 **시즌 종료
화면**에서 한 번만 선택합니다. 여기서는 지금까지의 결과만 봅니다.</p>`
그리고 `SeasonEndModal.svelte:30`에 이유가 적혀 있다:

> **여기가 유일한 투자 시점이다.** 상시 화면에 두면 매주 눌러보는 도박이
> 되고, DESIGN §7.3의 "가계부 없이 상시 잔액만" 원칙과도 어긋난다.

그래서 두 화면을 02대로 갈랐다:
 · **결산 화면**(`SeasonEndVm` + `season_end_screen`) — 금액 셋 · 갈래 셋 ·
   결과. **한 해에 한 번**
 · **재정 화면**(`FinanceVm`) — **이력과 누적 손익만**. 어디서 고르는지 안내

⚠ **탭 밖에 둔다.** 02는 모달이 한 줄 스크롤이라 투자 절이 자연히 보이는데
04 결산은 탭이 셋이다 — **한 해에 한 번뿐인 선택을 탭 안에 숨기면 못 보고
넘긴다.** 씬에 `Invest` 칸을 따로 냈고 검사가 탭 본문에 없음을 본다.

02 그대로 가져온 것:
 · 금액 세 갈래 `1/4 · 1/2 · 전액`, **100원 단위로 내림**, 최소 현금이 하한
 · **기본은 1/4** — 02 주석: "전액을 기본값으로 두면 실수로 다 넣는다"
 · **원금은 그대로 있고 손익만 자산에 얹는다**(`money + profit`)
 · 결과 문구 `{원금} 투자 → {손익} ({수익률}%)` · 자산 반영/차감 문구

02와 다르게 한 것 하나:
 · 🔴 **씨앗을 해와 갈래로 만든다**(`Rng.mix(["invest", year, option_id])`).
   02는 `thread_rng`라 **같은 세이브를 다시 열면 결과가 달랐다** — 04는
   재현된다. 04 규칙("난수는 `Rng`를 거친다")이 그것을 이미 금하고 있다

⚠ **없는 갈래를 id로 못 가른다.** `resolve_investment`는 못 찾은 갈래에도
**그 id를 그대로 되돌려준다**(이름만 `-`) — 돌려받은 id로 판정하는 첫 가드가
검사에 걸렸다. `investment_option(...).is_empty()`로 앞에서 막는다.

⚠ **같은 이름의 함수가 이미 있었다** — `Finance.stage_of(career_stage)`는
무대 **규칙 사전**을 낸다. 무대 **이름**을 리그에서 내는 새 함수는
`career_stage_of(p)`로 뒀고, **화면과 엔진이 이 하나만 본다** — 재정 화면이
리그로 가르는데 엔진이 `career_stage`를 보면 "화면엔 안 뜨는데 눌리는
자리"가 생긴다.

⚠ **못 하는 사람에겐 결산에서 아예 안 보인다.** 다른 화면은 "왜 못 하나"를
적지만 여기는 규칙 파일이 적어 둔 대로다 — **생활비도 빠듯한 신인에게 투자
화면을 띄우면 조롱이다.** 이유는 재정 화면이 말한다.

실측 — 사업·주식 1200만, 2027년:
```
투자 결과
사업·주식  1200만 투자 → +467만 (+39.0%)
자산에 반영됐습니다.
```
손실도 실제로 난다(30해를 굴려 확인 — 검사가 그것을 본다).

검사 30(`test/season_end_investment_test.gd`) · **변이 14/14**
(`sim/finance.gd` 8 · `ui/season_end_vm.gd` 3 · `season_end_screen.gd` 3).
캡처 `season-invest` · `season-invest-done`.

⚠ **변이 하나가 처음엔 살았다** — 고른 금액의 강조 표시를 지웠는데 아무
검사도 안 봤다. **어느 쪽을 굴리는지 안 보이면 못 고른다** — 눌린 칸과
안내 문구를 보는 검사를 더해 14/14.

🔴 **같은 항목 안에서 형태 ①을 또 만났다.** P-32에서 `finance_vm`에 투자
절을 냈는데 **재정 화면이 그 칸을 안 읽었다** — `_build_finance`가 카드
넷만 붙이고 있었다. 뷰모델 검사만 보면 통과라 안 보인다. **화면을 띄워
눈으로 볼 때까지 몰랐을 것이다.**

`_investment_card`를 붙였다 — 안내 · 이력 줄 · 누적 손익 · 못 하는 이유.
검사 4(`test/finance_screen_test.gd`) · **변이 4/4**. 캡처 `finance-bottom`.

⚠ **캡처 픽스처가 세 번째로 같은 자리에서 헛돌았다** — `AppRoot.set_state`가
사전을 **복제한다**(`s.duplicate(true)`). 넘긴 뒤에 상태를 고치면 화면은
옛 사전을 본다. **넘기기 전에** 넣어야 한다.


⚠ **02에도 죽은 코드가 있다** — `finance.ts:321 finalAssets`(은퇴 화면용
총 투자·총 손익·납세액)를 **어느 `.svelte`도 안 부른다.** 04도 은퇴 화면에
없다. 물려받지 않는다.


## P-33 — 다이제스트: **소식 본문에 id가 그대로 찍혔다** ✅

남은 `부분` 넷을 세러 `new-game`을 열다가 권역 이름이 필요해 뒤지던 중에
나왔다. 실측 — 고치기 전 본문:

```
[내 자리]
  HS_AEWOL
  HALLA   1위 / 6팀
[내 무대] HALLA
  1위  HS_AEWOL  10승 10패 .500  ← 우리
  2위  HS_HALLA  10승 10패 .500
```

🔴 `Digest`는 **일부러 표를 안 갖는다.** `digest.gd:104`가 적어 뒀다:

> **손으로 표를 만들지 않는다.** 원본에서 구장ID→지역명 맵을 적었다가
> 8개 중 하나를 빠뜨려 화면에 `YEONGSAN권역`이 그대로 찍혔다. 이름은
> 호출부가 넘긴다.

**넘기는 쪽이 없었다.** `DigestRunner.input_of`가 `team_names`를 아예 안
담고 `region_names`는 **아무도 안 채우는 상태 키**를 읽고 있었다
(`state.get("region_names", {})` — 04에서 그 키를 쓰는 곳이 0이다).
02가 고쳐 둔 결함을 04가 다시 만든 것이다.

⚠ **둘 다 04에 있었다** — 팀 이름은 `World.team_names()`(238개), 구장
이름은 `ParkVm.name_of`(`parks.json` 27개). **없어서가 아니라 안 이어서**다.

고친 뒤:
```
[내 자리]
  애월고
  한라구장   1위 / 6팀
[내 무대] 한라구장
  1위  애월고  10승 10패 .500  ← 우리
  2위  한라고  10승 10패 .500
```

⚠ **상태에 든 `team_names`를 안 쓰고 데이터에서 다시 낸다.** 손으로 만든
상태(검사·계측)가 그 키를 빠뜨리면 소식이 조용히 id로 나온다.

⚠ **다른 소식은 멀쩡하다** — 대회·부상·유망주·국가대표 소식은
`state["team_names"]`를 읽고 그 키는 `World.new_game`이 채운다. 셌다.

검사 3(`test/digest_mail_test.gd`) · **변이 3/3**.


## P-34 — 새 게임: **102개를 드롭다운 하나에 넣고 있었다** ✅

02 `NewGamePage`는 4단계다(① 기본 정보 ② 팀 선택 ③ 능력치 프리셋 ④ 확인).
04는 한 화면에 이름·학교·손·폼·생일뿐이었고, **학교는 `OptionButton` 하나에
102개**였다.

02가 그러지 않은 이유가 주석에 있다(`NewGamePage:52`):

> 고교는 8권역 주말리그라 **어느 지역에서 시작하느냐가 라이벌·일정을
> 정한다** — 102개를 한 줄로 늘어놓으면 그 구조가 안 보이고 고르기도 어렵다.

**04에 권역이 있었다** — `Tournament.regions_of`가 구장에서 파생한다.
실측 8권역: 무지개 20 · 한강 16 · 낙동 16 · 영산 14 · 계룡 12 · 팔공 12 ·
한라 6 · 설악 6.

붙인 것:
 · **권역 → 학교 2단.** 권역은 이름 순, 학교는 **전력 내림차순**
   (02는 난이도 내림차순 — 04엔 `difficulty`가 없어 같은 축인 `power`로)
 · **학교 상세** — 연고 · 구장(+성향) · 권역과 학교 수 · 전력 · 재정
 · 권역을 바꾸면 그 권역의 첫 학교로 따라간다. **빈 칸으로 두지 않는다** —
   시작 버튼이 잠기고 왜 잠겼는지 안 보인다

⚠ **전력은 등급이 아니라 눈금이다.** 02의 난이도 다섯 칸(최하~최상)과 달리
04는 2~5의 숫자다(실측 102팀: 2가 43 · 3이 45 · 4가 11 · 5가 3).
**최대를 같이 적는다** — `4 / 5`. 안 적으면 3이 센지 약한지 모른다.

⚠ **04에 없는 데이터는 안 지어냈다** — 02는 여기에 창단연도 · 운영예산 ·
과거 5시즌 성적 · 우승 이력 · 스타일 배지 · 태그 · 강점까지 붙인다.
`teams.json`엔 id·이름·도시·색·구장·전력·재정뿐이다. 02 `refs.json`에는
그 값들이 있으니 **옮기려면 데이터 이주가 따로 필요하다** — 여기 적어 둔다.

🔴 **옛 검사 셋이 옛 약속을 못 박았다**(형태 ④ — **다섯 번째**).
`vm["teams"]`에 102개가 한 줄로 있는지 보던 검사들이다. **셋 다 이유는
맞았다** — "102개를 다 고를 수 있어야 한다" · "찾을 수 있는 순서여야 한다" ·
"학교 없이 시작 못 한다". 세는 자리만 옮겼다.

⚠ **`can_start`의 학교 가드가 죽지 않았다** — 없는 권역을 주면 목록이
비어 `team_id`가 빈 채로 남는다. 그 갈래를 검사가 세운다.

검사 12(`new_game_vm_test` 8 · `new_game_screen_test` 4) · **변이 7/7**.
캡처 `new-game`.

✅ **P-35에서 닫았다 — 능력치 프리셋 4택.** 02는 균형형·파워피처·제구형·
체력형 넷 중 하나를 고르게 하고(`NewGamePage:162-197`) 04는 `PlayerGen`이
무작위로 만든다. **02는 두 구종으로 시작하는데 04는 직구 하나다** —
04 주석은 "02 그대로"라고 적혀 있는데 틀렸다. **다음 항목.**


## P-35 — 새 게임: **능력치 프리셋 4택이 통째로 없었다** ✅

02는 새 게임 3단계에서 유형 넷 중 하나를 고르게 한다(균형형 · 파워피처 ·
제구형 · 체력형). 04는 그 단계가 없어 `PlayerGen.roster`가 리그 범위에서
**무작위로 만든 능력치**를 그대로 썼다 — 어떤 투수로 시작할지 고를 수도,
시작 전에 볼 수도 없었다.

**값은 02 그대로 옮겼다**(`NewGamePage:162-197`). 넷 다 OVR 68이다 —
유형이 갈리는 것이지 세기가 갈리는 게 아니다. 한쪽이 세면 고르는 게 아니라
정답이 된다.

```
균형형     구위 70 · 커맨드 70 · 제구 68 · 무브먼트 66 · 멘탈 68 · 스태미나 68   포심 1 · 싱커 1
파워피처   구위 78 · 커맨드 64 · 제구 60 · 무브먼트 66 · 멘탈 68 · 스태미나 70   포심 2 · 커터 1
제구형     구위 57 · 커맨드 78 · 제구 75 · 무브먼트 66 · 멘탈 68 · 스태미나 62   포심 1 · 체인지업 1
체력형     구위 67 · 커맨드 65 · 제구 63 · 무브먼트 62 · 멘탈 77 · 스태미나 78   포심 1 · 싱커 1
```

🔴 **"02 그대로"라고 적힌 주석이 틀렸다.** `world.gd`가 직구 하나(`grade 3`)를
주면서 "02 그대로 — 나머지는 훈련으로 배운다"고 적어 뒀는데, **02는 넷 다 두
구종으로 시작한다.** 02가 그 자리에 근거까지 적어 뒀다:

> 하나면 타자가 같은 공만 봐서 contact_q가 48까지 내려가고 BABIP이 44.7%가
> 된다 — 실측(60경기)에서 ERA 9.07이었다. 둘이면 4.52로 정상권이다.

⚠ **숙련도 축은 둘이 같다** — 02 `save.ts:114`가 `1=습득중 2=기초 3=보통
4=능숙 5=마스터`, 04 `PitchDev.MAX_GRADE`도 5다. 그래서 02의 1·2를 그대로
옮겼다. **04는 직구를 3으로 주고 있었다 — 02보다 후했다**(그 3은
`PitchStep.DEFAULT_GRADE`, 즉 "안 적혀 있을 때의 기준값"이지 02가 준 값이 아니다).

⚠ **04에선 02의 한 구종 붕괴가 재현되지 않는다.** 같은 프리셋에 구종 수만
바꿔 **12시즌 224이닝**을 굴렸다:

```
구종 2 — 224이닝 합산 ERA 5.41 · K 240 · BB 85
구종 1 — 226이닝 합산 ERA 5.28 · K 206 · BB 74
```

ERA는 사실상 같고 **탈삼진만 갈린다**(240 대 206). 02의 9.07 대 4.52는
02 엔진 이야기다 — 04 `PitchStep`은 같은 공만 던져도 그만큼 안 벌한다.
**그래도 02 값을 옮긴다**(규칙: 값은 02에서 그대로). 04에서도 두 구종이
탈삼진을 늘려 유형 차이가 실제로 드러난다.

프리셋별 첫 시즌 실측(씨앗 셋 평균 · 애월고):
```
균형형 ERA 5.53 · 파워피처 2.82 · 제구형 4.82 · 체력형 4.43
```

⚠ **유형을 안 고르면 균형형이다.** 02에도 "고르지 않음"은 없다 —
무작위로 두면 화면에서 고른 뜻이 없어진다.

🔴 **옛 검사가 OVR로 씨앗 차이를 쟀다**(형태 ④ — **여섯 번째**).
이제 OVR은 프리셋이 정한다(넷 다 68). **뜻은 맞다** — 씨앗이 다르면 다른
주인공이 나와야 한다. 재는 축을 잠재력·성장률로 옮기고, **시작 능력치는
씨앗이 흔들면 안 된다**는 것을 같은 검사에 넣었다.

검사 15(`new_game_vm_test` 11 · `new_game_screen_test` 4) · **변이 7/7**
(`ui/new_game_vm.gd` 5 · `sim/world.gd` 2). 캡처 `new-game`.

⚠ **02의 확인 단계(4단계)는 안 옮겼다** — 카드 한 장에 고른 것을 다시
보여주는 화면이다. 04는 한 화면이라 고른 것이 늘 보인다. 단계를 넷으로
쪼개는 것 자체가 02의 모달 구조에서 온 것이라 04에선 뜻이 없다.

⚠ **전체 검사가 옛 약속 셋을 더 잡았다**(형태 ④ — 일곱 번째).
· pitch_vm_test: 주인공이 `직구 하나·숙련도 3 — 02 그대로`라고 못 박고 있었다.
  **검사와 주석이 같이 틀렸다.**
· pitch_vm_test: 경기까지 가는 숙련도를 3으로 봤다 — 이제 1이다.
  **둘째 구종도 경기에 가는지**를 같이 보게 고쳤다(전엔 안 봤다)
· pitcher_rotation_in_game_test: 씨앗 4242가 불펜 주인공을 만든다고 봤는데,
  프리셋으로 OVR이 68로 굳으면서 **애월고에선 늘 선발**이 된다.
  **한성고(전력 5)에서 그 씨앗이 불펜**이다 — 찍어서 골랐다

⚠ **한 파일에서 두 번째 실패는 첫 실패를 고친 뒤에야 보인다** — GdUnit이
실패가 나면 그 파일의 남은 검사를 안 돈다. 세 번째는 그래서 늦게 나왔다.


## P-36 — 훈련: **경고 룰과 훈련 이력이 없었다** ✅

02 `TrainingPage`(1,393줄)의 절을 04와 하나씩 대조했다. 슬롯 · 예상 결과 ·
보유 구종 · 신규 습득 · 해금 가능 · 조건 미충족 · 현재 상태까지는 04에 다
있었고, **경고 룰 · 훈련 히스토리 · 최근 활동** 셋이 없었다.

🔴 **훈련은 문턱을 피하는 게임인데 문턱이 안 보였다.** 04 `Growth.week_xp`가
피로 85 이상이면 **0.35배**, 70 이상이면 **0.65배**로 깎는데 화면 어디에도
그 숫자가 없었다. 사기가 오래 바닥이면 입스가 오는 것(`Injury.yips_chance`)도
마찬가지다.

```
경고 룰
  피로 85 이상        훈련 효율 35%
  피로 70 이상        훈련 효율 65%
  사기 저하 8주 연속   입스 위험 12%
  사기 저하 5주 연속   입스 위험 3%
  정상 — 입스 위험 없음
```

⚠ **숫자를 화면이 다시 적지 않는다.** `Growth.week_xp` 안에 흩어져 있던
문턱을 `Growth.FATIGUE_BANDS` 표로 빼고 화면이 그 표를 읽는다. 입스는
`Injury.yips_bands()`를 새로 내서 규칙 파일을 그대로 읽는다 — **규칙이
바뀌면 안내도 같이 바뀐다.**

⚠ **02와 값이 다르다.** 02 화면은 `피로 70+: XP ×0.70 / 85+: ×0.50`이라고
적어 뒀는데 04 엔진은 0.65 · 0.35다. **04 엔진 값을 적는다** — 화면 문구를
02에서 베끼면 화면이 거짓말을 한다(02 화면 문구가 02 엔진과 맞는지는 확인
안 했다. 04는 화면이 엔진 표를 직접 읽으므로 어긋날 수 없다).

⚠ **규칙만 적으면 남 얘기로 읽힌다** — 지금 내가 몇 주째인지, 위험이
몇 %인지 같이 적는다. 02도 그 줄이 있다(`슬럼프 진행중 (N주 연속)`).

⚠ **02의 "슬럼프"는 04에 없다** — 02는 사기 35 미만 3주 연속이면 성장
−30%였고, 04는 같은 축을 **입스**(부상)로 다룬다. 04 축을 적는다.

**훈련 이력** — `training_log`는 이미 쌓이고 있었다(P-31에서 소식이 읽게
했다). 훈련 화면에서도 최근 여섯 줄을 보여준다. 최근 것이 위다.

⚠ **02의 "최근 활동"은 안 옮겼다** — 그건 훈련이 아니라 전체 로그다.
04는 소식함이 그 자리다(P-30·P-31에서 채웠다). **같은 것을 두 화면에
두지 않는다.**

검사 11(`training_vm_test` 7 · `training_screen_test` 4) · **변이 8/8**
(`ui/training_vm.gd` 6 · `training_screen.gd` 2). 캡처 `training`.


## P-37 — 학사: **이번 학기가 어떻게 가는지 볼 수 없었다** ✅

02 `AcademicsPage`(503줄)의 절 셋을 04와 대조했다 — 과목별 현황 · 주간 학업
선택 · 시험 준비 현황.

 · **주간 학업 선택** · **다음 시험**은 04에 있다
 · **과목별 현황**(국영수사과 · 석차백분율 · 출석 · 과제)은 **04에 데이터가
   없다.** 04 학사의 축은 학점 → 졸업 자격이다(`academics_vm.gd:12`에 이미
   그 판단이 적혀 있었고, 다시 세어 확인했다)
 · 🔴 **시험 준비 현황의 누적 게이지가 없었다**

`Academics.study_week`이 매주 `study_quality_sum`·`study_weeks`를 쌓는데
**읽는 곳이 `semester_gpa` 하나뿐이었고 그건 학기가 끝나야 돈다.**
학기 중엔 지금 몇 점으로 가고 있는지 볼 방법이 전혀 없었다 —
**형태 ③을 여섯 번째로 만난 자리다.**

```
이번 학기   이번 학기 예상 학점 2.48  (1주 수강)
            경고선 1.75 · 졸업선 2.00   직전 학기 2.47
```

⚠ **02와 축이 다르다.** 02는 0~100 누적 점수 게이지고 04는 학점(0~4.5)이다.
**04 축으로 적는다** — 02 게이지를 흉내 내면 화면과 엔진이 갈린다.

⚠ **문턱을 같이 적는다.** 경고선(1.75)·졸업선(2.0)을 모르면 2.48이 좋은
건지 나쁜 건지 알 수 없다. 규칙 파일에서 읽는다.

⚠ **"예상"이라고 적는다** — 남은 주에 따라 바뀐다. 수업이 한 주도 없으면
0.00이 아니라 "아직 수업이 없습니다"다(0.00은 낙제로 보인다).

🔴 **캡처 갈래가 엉뚱한 화면을 찍고 있었다.** `shot.gd`의 `academics`가
`select_tab(3)`으로 번호를 박아 뒀는데 탭이 늘면서 3번이 **재정**이 됐다 —
학업 화면을 찍는다면서 재정을 찍고 있었다. 이름으로 찾게 고쳤다
(`StatusVm.TABS`엔 학업이 없다 — 학교에 다닐 때만 붙는다).

검사 7(`academics_vm_test` 6 · `academics_screen_test` 1) · **변이 7/7**
(`ui/academics_vm.gd` 5 · `status_screen.gd` 2). 캡처 `academics`.


## P-38 — 리그: **스탯 순위가 통째로 없었다** ✅

02 `LeaguePage`(1,605줄)는 탭이 다섯이다 — 리그 순위 · **스탯 순위** · 대회 ·
포스트시즌 · 리그 기록. 04 리그 탭은 **순위표 하나**였다.

🔴 **`sim/leaderboard.gd` 147줄을 아무도 안 불렀다.** 부문 표 스무 개
(평균자책점·다승·탈삼진·세이브·홀드·WHIP·이닝 / 타율·홈런·타점·도루·OPS…),
규정 이닝·타석 자격 판정, 서식(`.545` · `1.27`)까지 다 있는데 **화면이
하나도 안 썼다.** 게임을 아무리 굴려도 **리그 1위가 누구인지 볼 수 없었다** —
형태 ②를 일곱 번째로 만난 자리다.

실측(고교 300경기):
```
[투수] 평균자책점        [타자] 타율
  1 강동혁 기흥고 1.27      1 황민욱 마산만고 .545
  2 서준우 강진고 1.33      2 임우호 백호고   .500
  3 이정찬 광교고 1.40      3 신상훈 중원고   .500
  4 이우완 태종고 1.42      4 박영욱 애월고   .476
```

⚠ **자격을 같이 적는다.** 비율 부문은 규정 이닝·타석을 넘은 선수만 든다 —
안 적으면 "1이닝 던지고 0.00으로 1위"가 왜 없는지 설명할 방법이 없다.

⚠ **내가 끼면 눈에 띈다.** 그게 이 표를 여는 이유다.

🔴 **캡처가 두 결함을 한꺼번에 드러냈다** — 띄워 보지 않았으면 둘 다 몰랐다:
 · **픽스처가 게임 경로를 안 탔다.** `Fixtures.played_state`가
   `MatchDay.play_day`를 직접 불러서 **시즌 성적이 안 쌓인다**(그걸 쌓는 것은
   `GameSim.play`다). 그래서 화면이 "아직 기록이 없습니다"만 찍었다.
   **계측·픽스처가 게임 경로를 안 타는 것을 아홉 번째로 만났다**
 · **순위표 상자가 화면 바닥까지 늘어나** 스탯 순위를 밀어냈다.
   `_list_box(expand)`를 갈라 한 탭에 상자가 둘일 때를 만들었다

🔴 **중단된 변이 실행이 소스에 변이를 남겼다.** 변이 도구가 2분 제한에 걸려
죽으면서 `for t in teams:`를 바꾼 채로 끝냈고, 다음 실행이 "대상 문자열
없음"으로 나왔다. **되돌리고 다시 돌렸다** — 변이 도구를 백그라운드로 돌릴 때
중간에 죽으면 소스를 반드시 확인한다.

⚠ **첫 자격 검사가 아무것도 안 봤다** — 두 부문 다 열 명으로 잘려 크기가
같았다. **아무도 규정 이닝을 못 채운 시즌 초반(6경기)**을 세워 갈랐다.

⬜ **남은 세 탭** — 대회 · 포스트시즌 · 리그 기록.
 · **대회**: `sim/tournament.gd` · `tournament_news.gd`가 돌고 **소식으로
   온다**(대진·라운드·우승). 02는 표로도 보여준다
 · **포스트시즌**: `Postseason.run_background`가 시즌말에 돌고
   `SeasonHistory`가 `ps_result`를 남긴다 — **전용 화면은 없다**
 · **리그 기록**: 04는 이적·방출을 소식으로 낸다
 셋 다 **엔진은 돌고 결과가 어딘가에 닿는다** — 02처럼 표로 모아 보는
 자리가 없을 뿐이다. 다음 묶음으로 적는다.

검사 10(`test/league_vm_test.gd`) · **변이 6/6**. 캡처 `league`.

⚠ **"다른 무대가 섞이나" 검사를 세 번 고쳐서야 붙었다** — ① 고교만 돌린
픽스처엔 다른 리그 성적이 아예 없어 섞일 것이 없었다 ② KBL 표로 보니
섞이는 쪽도 KBL이라 표가 그대로였다 ③ 고교로 보니 그 픽스처 30일 안에
고교 경기가 **한 판도 없었다**(찍어서 확인: 대학 100 · ABL 96 · KBL 60 ·
고교 0). **대학 · 누적 부문**으로 보니 갈렸다.
**"검사가 통과한다"와 "검사가 무언가를 본다"는 다른 말이다.**


## P-39 — 리그: **포스트시즌·대회를 볼 자리가 없었다** ✅

02 `LeaguePage`의 다섯 탭 중 04에 없던 셋을 셌다.

🔴 **`state["postseason"]`과 `state["tournament_log"]`가 쌓이는데 화면에서
읽는 곳이 0이었다**(형태 ③ — **여덟 번째**). `Bracket.to_rounds`가 화면용
모양까지 내주는데 아무도 안 불렀다.

실측 — 한 시즌을 끝까지 굴린 뒤(`SeasonRunner.finish_season`):
```
postseason 리그 ["LEAGUE_KBL", "LEAGUE_KBL_FARM"]
  와일드카드    부산 웨이브스 vs 창원 스타스   1 - 2   3전 2선승
  준플레이오프  대전 팬텀스   vs 창원 스타스   3 - 0   5전 3선승
  플레이오프    서울 로열스   vs 대전 팬텀스   2 - 3   5전 3선승
  한국시리즈    광주 팬서스   vs 대전 팬텀스   4 - 1   7전 4선승
  우승 광주 팬서스
```

붙인 것 — 리그 탭 아래에 **포스트시즌**(라운드별 대진 · 시리즈 형식 ·
내 팀 강조 · 우승)과 **대회**(최근 여덟 줄 · 우승 팀 · **우리가 어디까지
갔는지**)를 낸다.

⚠ **안 붙은 시리즈는 "대기"다** — 0-0으로 찍으면 이미 진 것처럼 보인다.
⚠ **우리가 어디까지 갔는지가 요점이다** — 우승 팀만 적으면 남의 기록이다.

🔴 **내가 배선을 엉뚱한 함수에 붙였다.** 리그 탭인 줄 알고 **일정 탭**에
붙였다 — 빈 목록 문구가 `"아직 치른 경기가 없습니다"`(리그)와
`"아직 잡힌 경기가 없습니다"`(일정)로 **한 글자 차이**다. **검사가 잡았다**
(4 실패 → 0). 배선을 붙였으면 **그 함수가 진짜 그 화면인지 확인한다.**

🔴 **형태 ⑦을 열 번째로 밟았다.** 대회 로그가 0건이길래 결함인 줄 알았는데,
계측이 `MatchDay.play_day`를 직접 불러 **`DayEngine`을 안 탄** 탓이었다 —
대회는 거기서 돈다(`day_engine.gd:179`). 긴 계측은
**`DayEngine.advance_day`** 같은 게임 입구를 쓴다.

⚠ **캡처도 엔진으로 만든다** — `league-postseason` 갈래가 대진을 손으로
안 만들고 `Postseason.build` + `resolve_others`를 부른다. 손으로 만들면
라운드 이름·시드가 실제와 달라지고 화면만 보고 "됐다"고 믿게 된다.

⬜ **리그 기록(이적·방출) 탭은 안 옮긴다.** 04는 이적·방출·FA를 **소식으로**
낸다(`state["logs"]`는 경기 중계 로그지 리그 기록이 아니다 — 세어서 확인).
소식함엔 이미 분류 칩이 있다. **같은 것을 두 화면에 두지 않는다**는 원칙대로
소식함이 그 자리다.

검사 11(`league_vm_test` 8 · `main_screen_test` 3) · **변이 10/10**
(`ui/league_vm.gd` 7 · `main_screen.gd` 3). 캡처 `league-postseason`.

### 대회 기록 — 실측 (2026-08-18)

게임 경로로 **9주(63일)**까지 굴렸다(`DayEngine.advance_day` · 등판일엔
경기를 직접 치르고 넘어간다):

```
63일까지 · 반복 62
진행 중 대회: ["TOUR_HS_GAENARI"]
tournament_log 0건
```

**결함이 아니다** — 개나리기가 **아직 진행 중**이고 로그는 **우승이 나야**
쌓인다(`tournament_runner.gd:242-253`). 화면·검사·변이는 픽스처로 이미
확인했다. **끝까지 굴린 실측은 다음 긴 계측에서 확인한다.**

🔴 **무한 루프를 두 번 돌린 이유를 찾았다** — `DayEngine.advance_day`는
**새 사전을 돌려주고 원본을 안 바꾼다**(`s.duplicate(true)`). 반환을 안 받아
날짜가 영영 그대로였다. 게임은 `DayRunner`가 그 반환으로 상태를 갈아끼운다.
**계측이 게임 경로를 안 탄 것이 아니라, 게임 경로의 계약을 몰랐다.**

⬜ **진행 중 대회 대진은 아직 안 보여준다** — `state["tournaments"]`에
대진이 있고 02는 대진표를 그린다. 04는 **끝난 대회 기록만** 낸다.
다음 항목으로 적어 둔다.


## U-1 팀 마크 — 조사 (2026-08-18)

02 `utils/teamMark.ts`를 읽었다. **그림 파일이 아니라 데이터로 조립하는
SVG**다 — 주석이 이유를 적어 뒀다:

> 238팀에 각각 로고를 그리는 건 현실적이지 않다(고교만 102팀이고 전부 가상
> 학교다). 대신 이미 있는 것으로 만든다 — **팀 색 두 개와 이름.**
> 외곽 5종 × 문양 12종 × 패턴 4종

배정 규칙도 근거가 있다:
 · **문양을 해시로만 고르면 같은 권역에서 겹친다** → 그룹(리그·권역) 안에서
   **자리 번호로 배정**한다. 가장 큰 그룹이 대학 50팀이고 `lcm(5,12)=60`이라
   (외곽, 문양) 짝이 그룹 안에서 절대 안 겹친다
 · 이름에 뜻이 있으면 그게 이긴다(웨이브스=파도 · 로열스=왕관)
 · 1군과 2군은 **같은 마크**를 쓴다(`_1`/`_2`를 떼고 같은 키)
 · **문양은 언제나 흰색**이고, 팀 색 위에서 흰 글씨가 읽히는 것을 02가
   전수 검사로 보증한다(`check:teamcolors` — 주색 L* 상한 · 대비 4.5:1)

**04에 있는 것**: `teams.json`의 `colors` 두 개(238팀 전부). **없는 것**:
외곽·문양 벡터, 배정표, 이름 뜻 표.

### 옮기는 순서 (다음 항목)

1. **`ui/parts/team_mark.gd`** — `Control._draw`로 외곽 5종을 그린다.
   `situation_board.gd`가 선례다. 색은 `colors[0]`·`colors[1]`
2. **띠 4종** — 가로띠 · 사선 · 세로분할 · 없음. 외곽으로 잘라낸다
3. **배정** — 02처럼 **그룹 안 자리 번호**로. `Rng.mix` 해시는 폴백으로만
4. **문양 12종은 마지막** — 벡터를 다시 그려야 하므로 양이 크다.
   **1~3까지만 해도 목록에서 팀이 구분된다** — 거기서 한 번 끊고 눈으로 본다
5. **대비 검사** — 02가 전수로 보증한 것을 04도 검사로 세운다
   (`colors[0]` 위에 흰색이 읽히는가)

⚠ **문양을 지어내지 않는다.** 02 SVG 경로를 읽어 옮기거나, 04에서 그릴 수
있는 만큼만 그린다.

### U-1 1단계 — 부품과 배정 (2026-08-18)

`ui/parts/team_mark.gd`(외곽 5종 + 띠 4종) · `ui/team_mark_vm.gd`(배정)를
만들었다. **외곽 좌표는 02 `SHELL`의 SVG path 그대로**다.

⚠ **Godot엔 `clip-path`가 없다** — 02는 SVG로 띠를 잘라내는데, 04는
`Geometry2D.intersect_polygons`로 외곽과 겹치는 부분만 남긴다.

🔴 **1군·2군이 다른 마크로 나왔다.** `World.teams_of`가 2군을 걸러서
자리 번호를 못 찾고 폴백 해시로 떨어졌다 — **실측이 잡았다**.
데이터를 찾을 땐 `_ref_id`로 1군 id를 쓴다.

⚠ **대학 50팀은 겹친다** — 외곽 5 × 띠 4 = **20조합**뿐이다. 실측으로
30개가 겹친다. **문양 12종을 옮기면 60으로 늘어난다**(02가 `lcm(5,12)=60`을
근거로 잡아 둔 이유가 그것이다). 고교 권역은 최대 20팀이라 지금도 안 겹친다 —
검사가 둘 다 못 박는다.

❓ **대비 검사는 세우지 못했다.** 02 주석이 "주색 L* 상한 · **보조색** 대비
4.5:1"이라고 적혀 있는데, 04 데이터로 **주색에 4.5:1을 걸어 보니 101팀이
미달**이다(예: 광주 팬서스 `f49530` 2.29 · 서울 코브라스 `30c52b` 2.30).
**02가 무엇을 어떤 문턱으로 재는지 `check:teamcolors`를 읽고 나서 세운다** —
지금 세우면 04 데이터를 02 규칙이라고 착각한 검사가 된다.
**문양(흰색)을 얹기 전에 반드시 확인한다.**

검사 10(`test/team_mark_test.gd`).
⬜ 다음: 화면에 붙이기(U-5 목록 마크) · 문양 12종 · 대비 규칙 확인

### U-1 2단계 — 리그표에 붙였다 (2026-08-18)

`StandingRow`에 마크 자리를 내고 `LeagueVm`이 줄마다 `mark` 사전을 얹는다.
**화면이 색을 고르지 않는다.** 캡처(`league`)로 확인 — 20px에서 팀마다
색과 모양이 갈린다.

성능: `bench:day` **0.896초**(게이트 30초). 마크는 화면 그리기라 시뮬에
영향이 없다 — 그래도 붙일 때마다 잰다.

⚠ **변이 하나가 안 잡혔는데 "동등 변이"였다** — `band`를
`(slot / 5) % 4` 대신 `slot % 4`로 바꿔도 **`lcm(5,4)=20`이라 같은 20조합**이
나온다. 행동이 같으니 검사가 잡을 것이 없다. **검사를 억지로 만들지 않고
근거를 적어 둔다**(4/5가 아니라 4/4로 읽는다).

⬜ 남은 것: 일정·인물 목록에도 마크 · 문양 12종 · 대비 규칙 확인 · U-2~U-4

### U-1 3단계 — 대비 규칙 (2026-08-18)

🔴 **02 규칙을 잘못 읽고 검사를 세울 뻔했다**(형태 ⑧).
`scripts/check-teamcolors.cjs:15`가 정본이다 —
**"보조색 위에 흰 글씨 — 대비 4.5:1 이상"**. 주색이 아니다.

```
주색에 4.5:1 (내 오해)     238팀 중 101팀 미달
보조색에 4.5:1 (02 규칙)   국내 172팀 전부 통과 · 해외 50팀만 미달
```

⚠ **해외(ABL·JBL)는 안 잰다** — 실제 구단 색이라 밝은 것이 섞여 있다.
02도 그렇다. **그 사실을 검사가 적어 둔다** — 모르고 규칙을 넓히면
데이터를 고치게 된다.

**문양(흰색)을 얹어도 국내는 읽힌다**는 것이 이걸로 확인됐다.

검사 12(`test/team_mark_test.gd`).
⬜ 남은 것: 일정·인물 목록 마크 · 문양 12종 · U-2~U-4

### U-1 4단계 — 문양 열둘 · 일정 목록 (2026-08-18)

**일정 목록에도 상대 마크**를 붙였다(`ScheduleVm` → `ScheduleRow`).
캡처로 확인 — 같은 상대는 같은 마크다.

**문양** — 02 `MOTIF` SVG 좌표를 그대로 옮겼다. **채운 도형은 다각형으로,
선 도형은 선으로** 그린다(02가 그렇게 나눠 뒀고 Godot도 그 둘이 따로다 —
선을 다각형으로 흉내 내면 굵기가 크기마다 어긋난다).

⚠ **여섯만 그린다** — `star` · `bolt` · `mount` · `arrow` · `wing` ·
`bats`(선) · `anchor`(선 일부). 02 SVG에 **곡선(`C`·`Q`)이 있는 것들**
(`seam` · `wave` · `flame` · `ring` · `crown`)은 다각형으로 바꾸면 형태가
달라진다. **지어내는 대신 비워 둔다** — 그 자리는 외곽·띠·색으로 갈린다.
**배정 눈금에는 열둘을 다 남긴다** — 나중에 채워도 팀마다 문양이 안 바뀐다.

**대학 겹침 30 → 0.** 5 × 12 × 4 = **240조합**이다(실측: 50팀이 slot 0~49로
서로 다르다).

🔴 **옛 검사가 (외곽,띠)만 보고 있었다**(형태 ④ — 여덟 번째). 문양이
들어오자 "겹쳤다"고 62번 잡았다. **뜻은 맞고 재는 축이 늘었다** — 문양을
넣어 고쳤다.

성능 `bench:day` 그대로. 검사 12 · 실패 0.
⬜ 남은 것: 인물 목록 마크 · 곡선 문양 다섯 · U-2~U-4

### U-1 5단계 — 팀 고르기 · 어디에 붙일지 다시 셈 (2026-08-18)

**새 게임 팀 고르기**에 마크를 붙였다(02도 여기에 96px로 띄운다).

⚠ **인물 목록에는 안 붙인다** — 02의 `player_row`가 아니라 04 인물 탭은
**관계(사람) 목록**이다(감독·동료·라이벌). 팀 마크 자리가 아니다.
**붙일 자리를 다시 세어 판정했다** — 리그표 · 일정 · 팀 고르기 셋이다.

⬜ **헤더(내 팀)** — 02 `TopHeader`가 소속팀 마크를 띄운다. U-3에서 같이 본다.

검사 43(새 게임) · 실패 0. 캡처 `new-game`.

## U-2 — 팀 색 주입 ✅ (사용자 확정: 02와 같게 헤더·강조까지)

02 `applyTeamTokens`가 소속팀 색을 문서 루트에 넣어 **화면 143곳**이 그 색을
쓴다. 02 주석이 뜻을 적어 뒀다:

> 유니폼 안의 핵심은 **소속팀이 바뀌면 화면 색이 바뀐다**는 것이다.
> 색이 **한 곳에서만** 계산돼야 공짜로 얻어진다.

🔴 **팀 주색을 헤더에 그대로 쓰면 안 된다.** 02 실측 — 238팀 주색 L* 13~83,
**130팀(55%)이 L* > 45**. 광주 팬서스(`#F49530`)는 L*=70이라 흰 글씨가 죽는다.
**헤더용은 항상 어둡게 보정한다**(밝은 톤 L*26 · 어두운 톤 L*32).

04로 옮긴 것 — `ui/team_theme.gd`(파생) · `AppTheme`의 팀 색 슬롯 다섯 ·
`app_root._refresh`가 소속팀이 바뀔 때만 다시 뽑는다 · 헤더에 팀 색 띠 +
팀 이름은 강조색.

⚠ **04 규칙("색은 `AppTheme`에서만")을 안 깼다** — `TeamTheme`은 계산만 하고
바르는 것은 `AppTheme`이다. 화면은 여전히 테마만 본다.

실측:
```
광주 팬서스  주색 L*70  →  헤더 5b340b L*26 (밝은 톤) · 6e4010 L*32 (어두운 톤)
애월고       →  헤더 433371 L*26 · 강조 be96ff L*69 · accent c1500f(흰글씨 4.75)
소속 없음    →  기본색 283d64
```
**국내 전 팀이 헤더에서 흰 글씨 4.5:1을 넘는다**(검사가 전수로 본다).

⚠ **톤을 바꾸면 팀 색도 다시 뽑는다** — 헤더 목표 명도가 톤마다 다르다.
`apply_tone`이 `apply_team`을 부른다.

검사 11(`test/team_theme_test.gd`) · `bench:day` 그대로.
⬜ 남은 것: 강조색을 쓰는 자리 더 늘리기(02는 143곳) · U-3 · U-4

## U-4 — 우측 패널: **내 팀 순위가 없었다** ✅ (1단계)

02 `RightPanel`(266줄)의 항목을 셌다 — 다음 경기(상대·홈/원정·언제) ·
게이지 · OVR · **순위(전국·권역 · 승패무 · 승률 · 연승)** · 태그 · 최근 로그.

04 우측 패널: OVR · 컨디션 · 피로 · 다음 등판 · 진행 · 훈련 계획.
🔴 **순위가 없었다** — 리그 탭을 열어야만 내 팀이 몇 위인지 알 수 있었다.
02는 그걸 **늘 보이는 자리**에 뒀다.

붙인 것 — `MainVm._my_rank`가 `Standings.from_schedule`로 순위를,
고교면 `Tournament.regions_of`로 권역 순위를 낸다. 화면은 팀 강조색으로
찍는다(U-2와 이어진다).

🔴 **실측이 둘을 잡았다**:
 · `Standings`는 **`pct_label`을 안 낸다**(키: team_id·wins·losses·draws·
   win_pct·runs_for·runs_against·rank). 있는 줄 알고 빈 문자열을 냈다 —
   `win_pct`로 만든다
 · **프로에도 권역 줄이 붙었다** — 구장이 팀마다 하나라 `웨이브스타디움
   1위 / 1팀`이 떴다. **권역은 고교만이다**

⚠ **아직 안 치렀으면 안 만든다** — 0승 0패를 띄우면 꼴찌로 보인다.

⬜ 남은 것: 다음 경기 상대·홈/원정 · 연승 · 태그 · 최근 로그 ·
**U-3 TopHeader 대조**

검사 3(`test/main_vm_test.gd` · 36개 실패 0).

## U-3 — TopHeader 대조 ✅

02 `TopHeader`(188줄) 항목: **팀 마크** · **등번호** · 선수 이름 · meta 조각 ·
날짜 · 주차 · 진행 버튼.

04 헤더에 있던 것: 날짜 · 요일 · 주차 · 선수 이름 · 팀 이름(+U-2에서 팀 색 띠).

붙인 것 — **소속팀 마크**(이름 앞 26px). 캡처 `me`로 확인.

⚠ **등번호는 안 만든다** — `sim/`·`data/`를 다 뒤져도 04엔 등번호가 없다
(`jersey`·`back_number` 검색 0건). **지어내지 않는다.**

🔴 **검사가 두 줄로 잘려 들어갔다** — `node -e` 치환이 긴 줄을 자르면서
`)`가 사라졌고, **검사 파일이 조용히 두 개 적게 돌았다**(38개 중 36개).
문법 오류가 아니라 **함수 하나가 통째로 사라진 모양**이라 실패도 안 났다.
**검사 개수를 세지 않았으면 못 봤다** — GdUnit 개수 확인이 또 한 번 살렸다.

검사 38 · 실패 0.
⬜ 남은 것: 우측 패널 나머지(다음 상대·연승·태그·로그) · 곡선 문양 다섯

### U-4 2단계 — 연승은 그 경로에 없었다 (2026-08-18)

우측 패널에 연승·최근10을 붙이려다 **`Standings.from_schedule`이 그 둘을
안 낸다**는 것을 실측으로 확인했다.

```
from_schedule 키: team_id · wins · losses · draws · win_pct ·
                  runs_for · runs_against · rank
```

`standings.gd`엔 `streak`("W3")·`last10`("WWLWL")과 `update_streak`가 있지만
그건 **누적 갱신 경로**의 것이고, 04는 순위표를 **일정에서 파생**한다.
🔴 **있는 줄 알고 키를 읽었다가 빈 값이 나왔다** — 실측이 잡았다.
(같은 실수를 `pct_label`에서도 했다. **찍어 보고 쓴다.**)

**지금은 값이 오면 그리고 없으면 안 그린다** — 빈 줄을 지어내지 않는다.
`_streak_label`("W3" → "3연승")은 만들어 뒀으니 엔진이 내주면 바로 붙는다.

⬜ **다음**: `from_schedule`이 `streak`·`last10`도 세게 한다(sim 변경이라
따로 연다). 그때 이 검사를 갱신한다 — 검사에 그렇게 적어 뒀다.

검사 40 · 실패 0. 전체 검사 **3,935 · 실패 0**(U-3 검사 둘은 그 뒤).

### U-4 3단계 — 연승을 엔진이 세게 했다 (2026-08-18)

`Standings.from_schedule`이 `streak`("W3")·`last10`("WWLWL")을 같이 낸다.
**같은 순회 안에서 센다** — 일정이 날짜순이라 이어 붙이면 되고, 따로
정렬하면 하루 83경기에서 비용이 는다.

실측:
```
광주 팬서스  12승 5패  연속 W2  최근 WWLWWLWLWW
창원 스타스  12승 5패  연속 L1  최근 LLWWWWWLWL
```

⚠ **연승 표기를 `update_streak`과 같은 꼴로 맞췄다**("W3"·"L2") — 두 경로가
다른 꼴을 내면 화면이 둘 다 처리해야 한다.

🔴 **내가 만든 옛 검사를 내가 갱신했다**(형태 ④). 전 단계에서
`test_연승은_아직_그_경로에_없다`로 "지금은 안 나온다"를 못 박아 뒀고,
엔진을 고치면서 **검사 주석에 적어 둔 대로** 바꿨다. **일부러 남긴 표지가
제 몫을 했다.**

성능 `bench:day` **0.925초**(뜨거운 자리라 꼭 쟀다 · 게이트 30초).
`from_schedule`을 쓰는 검사 전부(214개) 통과.

### U-1 6단계 — 문양 열둘을 다 그린다 (2026-08-18)

곡선 문양 다섯을 마저 옮겼다. **02 좌표 그대로**다:
 · `ring` · `seam` — 원은 `draw_arc`로(다각형 근사는 22px에서 각이 진다)
 · `seam`의 실밥 두 줄 · `flame` — SVG의 `C`를 **3차 베지어 점 열 개**로
 · `wave` — `Q` 반복을 사인 곡선으로

🔴 **`crown`은 곡선이 아니었다** — 02를 열어 보니 `<path fill>` 다각형이다.
"곡선이라 못 옮긴다"고 적어 둔 다섯 중 하나가 사실은 아니었다.
**목록을 만들 때 원본을 한 줄씩 안 보고 뭉뚱그린 탓이다.**

이제 **열둘을 다 그린다** — 캡처 `team-marks`(64px 줄)로 확인했다:
야구공 · X · 별 · 번개 · 산 · 물결 · 링 · 화살 · 날개 · 불꽃 · 닻 · 왕관.
실제 목록(`league` 캡처)에서도 팀마다 다르게 나온다.

⚠ **캡처 격자의 22px 행은 눌린다** — `GridContainer`가 셀 크기를 다시 잡아
`size`가 덮인다. **진짜 화면(리그표)에서는 정상**이다 — 그걸로 판정했다.

성능 `bench:day` **0.937초** · 검사 12 · 실패 0.

## P-40 — 리그: **진행 중 대회 대진이 없었다** ✅

02 대회 탭은 대진표를 그린다. 04는 **끝난 대회 기록만** 냈다(P-39).
`state["tournaments"]`에 대진이 살아 있는데(라운드·경기·승자) 보여주는
자리가 없었다 — **형태 ③을 아홉 번째로 만난 자리다.**

⚠ **포스트시즌 대진과 구조가 다르다** — 그쪽은 `Bracket`의 시리즈 배열이고
대회는 `{bracket_size, total_rounds, matches[]}`다. **찍어 보고 맞췄다**
(안 찍었으면 `Bracket.to_rounds`를 그대로 부르다 빈 화면이 됐다).

⚠ **내 경기만 낸다** — 32강 대진을 다 그리면 못 읽는다. 02도 내 팀 줄을
강조한다. 이긴 경기는 강조색, 아직 안 붙은 경기는 "예정".

실측 — 게임 경로로 6주까지 굴리니 `TOUR_HS_GAENARI`가 진행 중이고
대진에 32강 · 5라운드 · 내 첫 경기(애월고 vs 김포고)가 있다.

검사 4(`league_vm_test` 41개 · 실패 0).

## P-5c — 계측이 게임 경로를 복제하던 것 ✅

`bench/fa_measure.gd`가 `FaRunner.run_league`의 **시장 조립을 손으로 다시
지었다**(세계를 안 바꾸려고). 그래서 P-5에서 게임 쪽 분모를 고쳤을 때
**계측이 안 따라와 "고쳤는데 그대로"로 한 번 읽혔다.**

`market_players_of` · `market_teams_of`를 `FaRunner`에 내고 **둘이 같이
부른다** — 갈릴 수가 없다.

⚠ **문서가 낡아 있었다** — QUEUE는 `tools/fa_measure.gd:70-93`이라 적었는데
실제 파일은 `bench/fa_measure.gd`다. **찾아서 셌다.**

실측(같은 씨앗 · 게임 함수를 쓴 뒤):
```
계약 8명 · 미계약 0명 · 등급 B:7 C:1
이적 6명 중 보상선수 1명 (이적 75% · 보상 17%)
```
바뀌기 전과 같은 값이다 — **복제가 지금은 맞게 베껴져 있었다는 뜻**이고,
그래서 더 위험했다(다음에 게임을 고치면 또 갈린다).

⚠ 계측 주석에 있던 경고("`budget_index`·`win_now_pressure`가 빠지면 모든
팀이 똑같이 불러 이적 0%가 된다")를 **검사로 옮겼다** — 조립 함수가 그 축을
빠뜨리면 검사가 잡는다.

검사 2(`fa_runner_test` 24개 · 실패 0).

## P-16b — 계측이 성장을 오해하게 내놓던 것 ✅

계측이 `_grew_weeks`("한 주에 core OVR이 1칸 넘게 오른 주")만 냈다. 매주
0.9씩 꾸준히 올라도 **그 숫자는 0**이다. 그 0을 "성장이 막혔다"로 읽어
P-16을 잘못 세웠다.

**사람이 궁금한 것(OVR 시작→끝 · 해마다의 OVR)을 내게 고쳤다.** 그러자
줄이 **스스로 두 가지를 더 드러냈다**:

### ① 계측이 `potential`을 읽고 있었다 — 키는 `potential_hidden`이다

`OVR 69.0/0` — 잠재력이 6해 내내 0으로 찍혔다. `player_gen.gd:243`에 그
경고가 이미 적혀 있었다("`potential`로 넣으면 아무도 못 읽는다"). 고치니 76.

### 🔴 ② 계측이 훈련 계획을 **한 번만** 세우고 굴리고 있었다

게임의 자동 진행은 매 걸음 `AutoTraining.apply`를 부른다(`auto_advance.gd:205`,
02 `runAutoAdvance`와 같다). 계측은 첫 주 계획을 박아만 뒀다. 그래서 피로가
95를 넘어도 회복 프로그램으로 안 갈아탔다.

| | 계획을 안 갱신(전) | 게임과 같게(후) |
|---|---|---|
| 부상으로 쉰 주 | **106 / 156 (68%)** | **26 / 156 (17%)** |
| OVR 3해 | 68 → 65 (**−1.0/해**) | 68 → **71** (**+1.0/해**) |
| 등판 게이트 | `skip_injury` (매해) | `ask` · `play` |
| 훈련 영역 | 투수 156 | 투수 129 · 컨디셔닝 27 |
| 임계 넘은 주 | 1 | 3 |

**04의 성장·부상 산식은 멀쩡했다.** 계측이 게임 경로를 안 탄 것이다
(형태 ⑦ — **열두 번째**).

### 그래서 검사로 박았다 — `test/bench_paths_test.gd`

계측이 게임 함수를 부르는지 소스로 본다(계측은 십 분 넘게 돌아 검사에서
못 굴린다). 검사 4 · **변이 6/6**.

⚠ **처음 만든 검사는 주석을 읽고 있었다.** 이 파일들은 그 함수를 주석에서도
이름으로 부른다 — 변이로 호출을 지웠더니 주석이 남아 통과했다(1/2 검출).
`_code()`가 `#` 줄을 걷어낸 뒤에 본다.

### 🔴 그리고 P-5c가 반쪽이었다

`market_players_of`·`market_teams_of`를 냈고 계측은 그걸 부르게 했는데
**`run_league`는 여전히 제 손으로 짓고 있었다.** 직전 커밋 메시지에
"run_league가 그 둘을 부른다"고 적었는데 **거짓이었다**(형태 ⑥).
같은 검사가 그것을 잡았다 — 지금은 정말 한 곳이다.


## P-17 — 대학 리그가 해마다 9경기 ✅ (오해였다)

**"내 경기 9"는 팀 경기 수가 아니라 등판 수였다.** 계측이 둘을 안 갈라
내서 리그 크기 문제로 읽혔다. 팀 경기를 같이 내니:

```
고교  팀 23경기 중 등판 10   (리그 20 + 대회)
대학  팀 16경기 중 등판 10   (리그  9 + 대회)
```

02도 **대학은 9경기**다 — `leagueScheduler.ts:55` `UNIV_TARGET_GAMES = 9`
(고교 `HS_TARGET_GAMES = 20`). 04 `schedule.gd`가 그대로 20·9다.
**값이 맞고, 등판 10은 3인 로테이션(7) + 불펜의 결과다**(`rotation_test`가
6~8로 못 박아 둔 그 값).

## D-3 — NPC 구종 성장 ✅

04의 NPC는 `pitches`가 없어 마운드에서 **늘 포심만** 던졌다. 그래서
`pitch_pattern_modifier`(같은 구종 반복 벌점)가 NPC 경기 내내 걸렸다.

⚠ **초기 생성이 문제가 아니었다.** 02의 NPC도 `pitches`가 빈 채로 시작한다
— 04와 같다. **갈리는 건 주간 성장**이고 04엔 그 자리가 통째로 없었다.

`sim/npc_pitch_dev.gd` — 02 `npc_sim.rs` 값 그대로:

| | 02 | 자리 |
|---|---|---|
| 목표 구종 수 | SP ≥70:4/5 · CP ≥70:2 ≥60:3/4 · RP ≥65:3/4 | 2813 |
| 주당 진행 | 발견→1 8주 · 1→2 8주 · 2→3 12주 · 3→4 24주 · 4→5 36주 | 2822 |
| 자격 | 33세 미만 · `ovr/potential ≥ 0.70` · 새 구종은 29세 미만 | 2843 |
| 대상 | 모자라면 새 구종(무작위) · 채웠으면 최저 숙련도 | 2849 |
| 상한 | 구종 5개 · 숙련도 5 | 3130 |

⚠ **목표 표는 주인공(`AutoTraining.pitch_target`)에 위임한다.** 두 벌로 두면
갈리고, 그 격차가 그대로 성적이 된다. 검사가 둘이 같은 값인지 본다.

⚠ **대상은 오프시즌에만 정한다**(02와 같다). 시즌 중에 갈아타면 8주짜리를
못 끝낸다. 진행은 매주.

⚠ **`randf()`를 안 쓴다.** 02는 `npc_id` 해시 + 가진 구종 수로 LCG를 돌린다
— 04는 `Rng.value_for(["npc_pitch", id, len])`이라 호출 순서와 무관하다.

⚠ **열을 안 늘렸다.** NPC는 사전이고 `save_game.gd`가 통째로 저장한다 —
`NpcStore`의 열 배열을 건드릴 이유가 없었다.

### 실측 (52주 · 씨앗 20270101)

```
NPC 구종 (투수 3317명)
  0개 1193명 · 1개 108명 · 2개 391명 · 3개 924명 · 4개 701명 · 5개 0명
  숙련도1 5634 · 숙련도2 832 · 숙련도3 0 · 숙련도4 0 · 숙련도5 0
  익히는 중 2246명 · 29세 미만인데 0개 645명
```

⚠ **29세 이상으로 시작한 투수는 영영 0개다** — 새 구종은 나이가 막고 올릴
것도 없다. **02도 그렇다.** 그런 투수는 엔진에서 포심 하나로 떨어진다.
검사가 그 사실을 적어 둔다(값을 만지고 싶어지면 거기가 "02가 그렇다"고
말한다 — 밸런스는 동결이다).

성능: 주간 성장 평균 **87ms · 최악 128ms**(계측 기준 300ms) · `bench:day`
0.915초. 뜨거운 자리인데 안 무거워졌다.

계측에 구종 줄을 붙였다 — **안 내면 도는지 알 수가 없다**(형태 ③).
전원이 0개면 계측이 실패로 떨어진다.

검사 22 · **변이 14/14**.

⚠ **검사 넷이 처음엔 틀렸다.** 셋은 fixture 탓(SP·구속 65는 목표가 5인데
넷으로 "채웠다"고 봤고, 32세에게 빈 손을 줬고, `{ovr, velocity}`만 줘서
`pitching_ovr`이 70을 20으로 다시 만들었다). **넷째는 아무것도 안 봤다** —
타자 fixture에 `batting`이 없어 `grow_one`이 구종 자리에 닿기도 전에
돌아갔고, "타자도 배운다" 변이가 안 잡혀서 알았다.

⚠ **숙련도 2→3은 12주가 아니라 13주다.** `100/12`를 열두 번 더해도 부동
소수 누적이라 100에 아주 조금 못 미친다 — **02도 f64라 같다.** 주석의
"12주"는 뜻이고 도는 값은 `100.0 / 12.0`이다. 검사를 실측에 맞췄다.

## P-5b — FA 시장이 04는 리그별 세 판이었다 ✅

02 `market.ts:1241`은 `activeProLeagues()`의 1군을 **한 판에** 넣는다.
그 자리에 이유도 적혀 있다:

> KBL 팀만 넘기고 있었다. Rust FA 오퍼에는 ABL(OVR 70·명성 30)·JBL(62·15)
> 경로가 있는데 목적지 팀을 안 주니 **NPC가 해외로 갈 방법이 없었다.**
> 확장팩을 열어도 32팀이 관전 대상일 뿐이었다.

04는 `for lid in PRO_LEAGUES: run_league`라 **04가 그 상태 그대로였다.**

→ `run_league` → **`run_market(state, rng)`** 한 판. 흐름도 하나다.

함께 걸린 것 넷 (QUEUE가 미리 적어 둔 그대로):

1. **`_move`의 `to_league`** — 목적지 팀의 리그로 바꾼다. 팀만 바꾸면 그
   선수는 새 팀에 있으면서 옛 리그 순위·정원에 잡힌다
2. **정원** — 목적지 리그 것으로 잰다(JBL만 32, KBL·ABL은 34)
3. 🔴 **보상선수 국적 판정** — `is_foreign`("그 리그에서 외국인인가")는
   **JBL 팀의 일본 선수를 내국인으로 통과시킨다.** 한 판이 되면서 한국
   선수가 JBL 팀과 계약할 수 있게 됐고, 그러면 그 팀의 일본 선수가
   **보상선수로 KBL에 온다.** 02가 같은 함정에 다섯 번 걸렸다고 적어 뒀다.
   → `Foreign.is_foreign_in_quota_league(nationality_of(p))`
4. **상무는 따로 안 뺀다** — 04는 독립 리그 소속이라 `PRO_LEAGUES`에 없다

### 실측 (씨앗 20270101 · 6해)

```
계약 387명 · 미계약 0명 · 등급 A:164 B:153 C:70
FA 이적 311명 · 보상선수 이동 11명 (합 398)
(비율 — 이적 80% · 이적 중 보상 4%)
```

⚠ **P-5c에서 적은 "계약 8명 · B:7 C:1"은 게임의 값이 아니었다.**
계측이 warmup 6해를 돌린 **뒤에** 제 손으로 시장을 다시 돌렸는데, 게임이
이미 계약을 마쳐 자격자가 남지 않았다 — **끝난 시장을 재고 있었다.**
한 판으로 바꾸자 그 자리가 "계약 0명"으로 드러났다.

→ `run_market`이 `grades`·`transfers`·`compensations`를 같이 내고,
`SeasonRunner`가 `summary`에 담고, **계측은 게임이 낸 값을 읽는다.**
조립을 같이 쓰는 것보다 강한 약속이다(형태 ⑦ — **열세 번째**).

⚠ **`summary`가 안에 있었다.** `SeasonRunner.run`은 `{ran, year, phases,
summary}`를 낸다 — 겉을 읽어 조용히 0이 나왔다. **찍어 보고 썼다.**

⚠ **`moved`는 옮김 처리 수다** — 보상선수 이동도 세고 원소속 재계약도 센다.
그대로 나누어 "이적 103%"·"이적 100%"가 찍혔다. `transfers`를 따로 낸다.

검사 30 · **변이 4/5**.

⚠ **다섯째("정원을 KBL로 고정")는 표면화되지 않는다.** `market_teams_of`가
`open_slots`를 **정확한 리그 정원**으로 이미 계산해 모형이 먼저 막는다 —
`run_market`의 `limit` 가드는 "모형과 세계가 벌어진 경우"의 두 번째
방어선이고, 그 상황은 보상선수가 자리를 채운 특정 순서에서만 온다.
**억지 검사를 만들지 않고 근거를 적는다.**

⚠ **옛 검사 다섯이 옛 약속을 지키고 있었다**(형태 ④):
KBL만 채워 "자리 없음"을 만든 것 셋 · `is_foreign` 플래그로 외국인을
표시한 것 하나 · **"리그마다 다른 흐름"** 하나. 마지막은 **04 자기 규칙**
이었고 02는 한 판이다 — 재현성 검사로 옮기고, "한 판이라 리그가 서로에게
닿는다"를 새로 세웠다.


## P-13 — 라이벌 20해 재측정 ⬜ (아직 못 잰다)

20해 실측(씨앗 20270101):

```
OVR 68.0 → 80.0 (+12.0 · 해마다 +0.6) · 부상으로 쉰 주 26 / 1040주
teammate 233명  값 -4 ~ 99 (평균 40.4)
rival     22명  값  0 ~  0 (평균  0.0)
coach      7명  값  0 ~ 100 (평균 34.9)
```

🔴 **그런데 주인공이 2032년부터 15해를 대학 4학년에 멈춰 있다.**
대기줄에 `draft_observe` · `sports_unit_apply` · `military_enlist_ask`가
계속 떠 있는데 계측이 그 셋을 안 답한다 — `career_choice_hub` ·
`career_results` · `career_choice`만 답하게 짜여 있다.

**그 상태의 라이벌 0.0은 판정에 못 쓴다.** 6해 실측에서는 −7~8이 나왔다.
계측이 커리어를 프로까지 밀어야 20해가 뜻을 갖는다 — **P-16b와 같은
계열이다**(계측이 게임 경로를 안 탄다).

## P-13 — 계측이 대기줄을 막고 있던 것 (부분)

20해 실측에서 주인공이 15해를 대학 4학년에 멈춰 있었다. 계측이
`career_choice_hub` · `career_results` · `career_choice`만 답하고
`draft_observe` · `draft_notification` · `sports_unit_apply` ·
`military_enlist_ask`는 안 답했다 — 대기줄이 안 풀리면 진행이 선다.

**화면과 같은 함수로 답하게 고쳤다**(`DecisionVm.apply`). 계측이 제 손으로
대기줄을 지우면 그게 두 번째 정본이 된다.

9해 실측 — **대기줄은 풀렸다**(2029년부터 "대기 없음"):

```
OVR 68.0 → 74.0 (+0.7/해) · 부상으로 쉰 주 26 / 468주
teammate 126명  값 -1 ~ 61 (평균 20.1)
rival     21명  값 -3 ~  2 (평균 -0.4)
coach      7명  값  0 ~ 100 (평균 30.6)
manager    2명  값  9 ~  15 (평균 12.0)
```

🔴 **그래도 주인공이 대학 4학년에 머문다**(2032~2035). 대기줄이 빈 채로
멈춰 있으니 **원인은 물음이 아니라 졸업 뒤 진로가 안 열리는 것**이다.
→ **P-42**로 세웠다. **P-13은 그것을 고친 뒤에 다시 잰다.**

## P-42 — 대학 4학년을 마쳐도 프로로 안 가던 것 ✅ (게임)

계측 20해에서 주인공이 **2032년부터 15해를 대학 4학년에 멈춰** 있었다.
대기줄은 비어 있었다 — **아무도 다음 진로를 안 연 것이다.**

원인: `state["career"]`가 **한 번 만들어지면 안 지워졌다.** 고교에서 채운
`results`가 그대로 남아 `CareerRunner._should_open`의

```gdscript
if not c.get("results", {}).is_empty():
    return false
```

에 **매년 걸렸다.** `submitted`는 `choose_*`가 풀어 줬지만 `results`는 아무도
안 비웠다.

⚠ **02가 같은 증상을 겪고 그 자리에 적어 뒀다**(`game.ts:1768`):

> 실측: 2032 진학 → 2038까지 7년째 대학생(29세), 매년 W42 진로 허브만 반복.

02는 무대를 옮길 때 진로 상태를 **통째로 비운다** — `careerApplications` ·
`careerResults` · `careerApplicationsSubmitted` · `careerFinalChoice` 넷.
04도 같은 자리(`CareerDecision._move_to`)에서 `reset_decision(state)`을 부른다.

⚠ **`_move_to`가 정본이다.** 무대가 바뀌는 길이 셋(지명 수락·진학·독립)인데
각자 비우면 하나를 빠뜨린다 — **02가 그 실수를 했다.**

⚠ **사전을 갈아끼우지 않고 안을 비운다.** 화면·계측이 `of(state)`로 받은
참조를 들고 있을 수 있다.

⚠ **`final_choice`는 비운 뒤에 다시 적는다** — 순서가 뜻이다.

실측(12해): **진로가 다시 열린다** — 2030년부터 `career_choice`가 뜬다
(전에는 12해 내내 대기줄이 비어 있었다).

검사 9 · **변이 9/9** (`career_decision` 5 + `career_runner` 4).

⚠ **"결과가 있으면 안 연다" 갈래를 아무 검사도 안 보고 있었다.** 변이가 안
잡혀서 알았다 — 있던 검사는 `submitted`만 봤다.


## ⬜ P-43 — 계측이 진로를 우회하고 있었다 (새로 드러남)

P-42를 고치고 나니 계측이 `career_choice`에 **답을 못 했다.**
`choose_school_or_independent(s, "university", "TEAM_UNIV_BAEKJE")`를
결과와 무관하게 불렀는데, 대학 4학년이 다시 대학에 가는 것이라 **학적 역행
가드**에 막혔다.

→ 계측이 `DecisionVm.build`가 내는 **실제 선택지**를 쓰게 고쳤다(화면과 같은
경로). 그러자 **게임 규칙대로 굴러가고, 그래서 새 사실이 드러났다**:

```
12해 내내 highschool/학년 3 · 대기 ["career_choice", "draft_observe"]
teammate 136명  값 13 ~ 100 (평균 84.8)   ← 전에는 평균 25.5
rival     21명  값 -70 ~ 42 (평균 -5.1)   ← 전에는 0 ~ 0
coach      3명  값 11 ~ 100 (평균 43.0)
manager    1명  값 98 · owner 1명 값 67
```

🔴 **관계 값이 처음으로 02 규모로 움직였다**(teammate 평균 84.8 · rival
−70~42 · manager 98). 예전 숫자는 **계측이 우회로 만든 세계의 값**이었다.

⬜ **그런데 주인공이 고교 3학년에 12해 머문다.** 원서를 백제대 하나만 넣어
떨어지고, 그러면 남는 선택지가 `continue`("지금 자리에 남는다")뿐이다.
두 가지를 갈라 봐야 한다:
  1. 계측 fixture가 대학 셋(`MAX_CHOICES`)을 다 지원해야 하는 것인지
  2. **고교 3학년이 `continue`로 무한히 남을 수 있는 것이 맞는지** —
     02에 그 갈래가 있는지 `_stay_label`·02 원본으로 확인한다

**P-13(라이벌 20해)은 이것까지 정리한 뒤에 잰다.**

## P-43 — 04가 02에 없는 갈래를 갖고 있던 것 ✅

`DecisionVm._choice`가 `continue`("지금 자리에 남는다")를 **무대와 무관하게
항상** 붙였다.

🔴 **그런데 엔진은 이미 무대를 가른다.** `CareerDecision.continue_current_stage`
가 대학·독립이 아니면 `false`를 돌려주고 **대기줄도 안 푼다.** 그래서 고교
졸업반이 그걸 고르면 아무 일도 안 일어나고 같은 물음이 다음 해에 또 떴다 —
실측 **12해 내내 고교 3학년**이었다. **화면이 엔진이 받을 수 없는 것을
내고 있었다**(형태 ② — 일곱 번째).

02가 무엇을 주는지는 `CareerResultModal.svelte:75-113`이 정본이다:

| 조건 | 02가 주는 것 |
|---|---|
| `canContinue` | **대학생만** — 다음 학년 진급 |
| `canContinueIndie` | **독립리그만** — 계속 |
| 합격·지명 | 그것들 |
| **전원 탈락 + 미필** | **"전원 탈락: 현역 입대"** |

⚠ **04의 "조용히 입대시키지 않는다"(사용자 확정 2026-08-18)와 안 부딪힌다** —
02도 조용히 안 보낸다. **사용자가 그 선택지를 눌러서** 간다.
`Military.should_ask_enlist`는 여기 못 온다 — `_open`이 고교를 막고
`ENLIST_AGE`가 28이라 열여덟에는 안 뜬다.

⚠ **군필에게는 안 권한다.** 02가 그 자리에 적어 뒀다 — "60회 조사에서 한
커리어가 군 복무를 **세 번** 했다". `Military.enlist`가 자격을 다시 본다.

⚠ **선택지가 비는 상태를 안 만든다** — 빈 물음은 화면을 얼린다.

### 실측 (씨앗 20270101 · 12해)

**커리어가 드디어 움직인다:**

```
2027~2029  고교 (1→3학년)
2030       병역 (LEAGUE_MILITARY)
2031~2038  독립리그 (팀 35경기 중 등판 10)
OVR 68.0 → 76.0 (+0.7/해) · 잠재력 76에 도달
teammate 102명  값 3 ~ 98 (평균 46.8)
owner      2명  값 0 ~ 47
```

검사 11 · **변이 5/5**.

⚠ **옛 검사가 옛 약속을 지키고 있었다**(형태 ④ — 열다섯 번째).
`decision_vm_test.test_there_is_always_a_way_forward`가 `["continue"]`를
기대했다("그게 재수다"). **뜻은 맞고**(길이 하나는 있어야 한다) **값이
틀렸다** — 02는 재수를 안 준다.


## ⬜ P-44 — 병역·전역 경로가 로스터를 안 옮긴다 (새로 드러남)

실측 트레이스에서 **병역 이후 계속 "로스터 속 나: 못 찾음"**이다.

```
2030년 military/LEAGUE_MILITARY  → 로스터 속 나: 못 찾음
2031년 independent/LEAGUE_INDEPENDENT → 로스터 속 나: 못 찾음
...  2038년까지 계속
```

**P-14와 같은 계열이다** — 그때는 `CareerDecision._move_to`가 `team_id`만
바꾸고 로스터를 안 건드렸다. 이번엔 **병역/전역 경로**(`Military.enlist` ·
`Military.discharge`)가 그렇다.

⚠ **04는 주인공도 로스터에 산다**(`world.gd:280`). 로스터에 없으면 동료·
라이벌·성장이 그 위에 서고, 실측에서 **rival이 −2~0(평균 −0.3)으로 여전히
작다.**

**P-13(라이벌 20해)은 P-44를 고친 뒤에 잰다.**

## P-44 — 병역·전역 경로가 로스터를 안 옮기던 것 ✅

**P-14와 같은 결함이 이 경로에 남아 있었다.** 그때는 `CareerDecision._move_to`
가 `team_id`만 바꾸고 로스터를 안 건드렸다 — 이번엔 `Military.enlist`와
`Military.discharge`다.

실측 트레이스가 **병역 이후 12해 내내 "로스터 속 나: 못 찾음"**이었다
(military → independent 전부).

⚠ **04는 주인공도 로스터에 산다**(`world.gd:280`). 없으면
`relink_protagonist`가 못 찾고 동료·라이벌·성장이 전부 그 위에 선다.
**02엔 대응 코드가 없다** — 02는 주인공을 로스터 밖에 둔다. **04 자기 규칙을
04가 안 지키던 것**이다(P-14와 같은 말).

→ 두 자리에서 `CareerDecision.move_roster`를 부른다.

⚠ **`move_roster`가 빈 팀 칸을 안 만들게 보강했다.** 입대는 `team_id`가 ""라
그대로 넣으면 **빈 이름의 유령 팀**이 생기고 로스터를 훑는 코드가 그걸 본다.

### 실측 (씨앗 20270101 · 12해) — 커리어가 처음으로 프로까지 갔다

```
2027~2029  고교          로스터 속 나: 있음
2030       병역           (로스터 밖 — 맞다)
2031~2036  독립리그        로스터 속 나: 있음   ← 전에는 "못 찾음"
2037~2038  프로 KBL       팀 144경기 중 등판 10
OVR 68.0 → 76.0 (잠재력 76 도달)
teammate 144명 · coach 10명 · manager 3명 · owner 3명
```

**`manager`·`owner`가 셋으로 늘었다** — 프로 관계가 처음 생겼다.

검사 6 · **변이 4/5**.

⚠ **다섯째("전역이 옛 자리를 안 비운다")는 표면화되지 않는다.** 입대가 이미
로스터를 비웠으므로 전역 시 `from_team`이 늘 빈 상태다. 그 인자는
**입대 경로를 안 거친 세이브를 위한 방어**로 남긴다 — 억지 검사를 만들지
않고 근거를 적는다.

### ⬜ 라이벌은 아직 작다 (P-13)

`rival 21명 · 값 −2 ~ 0 · 평균 −0.3`. teammate(평균 21.4)와 견줄 크기가
아니다. **이제 표본을 제대로 만들 수 있으니**(커리어가 프로까지 간다)
P-13을 20해로 다시 잰다.

## P-13 — 라이벌 20해 재측정 (P-42·43·44 뒤) ⬜ 아직 열려 있다

**커리어가 프로에서 끝까지 굴러간 첫 20해다** — 이제야 판정할 수 있는 표본이다.

```
2027~2029 고교 · 2030 병역 · 2031~2036 독립 · 2037~2046 프로 KBL
OVR 68.0 → 81.0 (+0.7/해) · 부상으로 쉰 주 26 / 1040주
teammate 238명  값 0 ~ 100 (평균 51.6)
rival     21명  값 0 ~   0 (평균  0.0)
coach     10명  값 -4 ~ 97 (평균 11.8)
manager    3명  값 -27 ~ 6 (평균 -7.0)
owner      3명  값 0 ~   8 (평균  3.3)
```

🔴 **라이벌이 20해를 굴려도 0~0이다.** teammate는 평균 51.6까지 간다.

### 배선은 다 맞다 — 찍어서 하나씩 지웠다

| 의심 | 실측 |
|---|---|
| 규칙 표가 비었나 | `weekly.rival = {faced_and_won: 3, faced_and_lost: -1}` **있다** |
| `weekly`가 rival 분기를 안 타나 | `relationship.gd:321` **탄다** |
| 행을 만드는 자리가 없나 | `_meet_rivals`가 21명을 만든다 **돈다** |
| 만든 뒤에 갱신하나 | `reconcile → _meet_rivals → weekly` **순서 맞다** |

### 남은 후보 — **감쇠가 0으로 되돌린다**

12해에서는 `−2 ~ 0`이었는데 20해에서 `0 ~ 0`이다. **값이 붙었다가 시즌
감쇠로 0에 수렴한다.** 맞대결은 한 해 몇 번뿐이라 붙는 양이 감쇠를 못 이긴다.

⚠ **여기서 값을 만지지 않는다** — 밸런스는 동결이고, 02의 라이벌 평균이
얼마인지 모르면 "작다"고 판정할 수 없다.

**다음에 할 일: 02를 돌려서 라이벌 값을 잰다.**
02 `scripts/measure-relations.cjs`가 그 자리다. 02가 내는 라이벌 분포와
나란히 놓은 뒤에야 04를 고칠지 정한다.


## ⬜ P-45 — OVR이 잠재력을 넘는다 (새로 드러남)

같은 20해 실측에서 **OVR 81 / 잠재력 76**이다. 2038년에 76(천장)에 닿은 뒤
2046년까지 계속 올라 **다섯을 넘겼다.**

`Growth.potential_cap_factor(current, potential)`가 천장 근처에서 0으로
가야 하는데 안 막는 것으로 보인다 — **찍어서 확인한다.**
⚠ 02 `npc_sim.rs`의 `potential_cap`이 정본이다.

## P-45 — OVR이 잠재력을 넘는다 ✅ **결함이 아니었다 (내 오독)**

20해 실측에서 주인공이 **OVR 81 / 잠재력 76**이 됐다. 나는 02 `npc_sim.rs`의
`potential_cap`(천장에서 **0.00**)과 04 `growth.gd`의 `potential_cap_factor`
(천장에서 **0.10**)를 나란히 놓고 "04가 02 값을 바꿔 놓았다"고 적었다.

🔴 **틀렸다. 주인공 표는 `npc_sim.rs`가 아니다.**

02는 성장 엔진이 **둘로 나뉘어 있다**:

| | 02 원본 | 천장(`ratio >= 1.00`) |
|---|---|---|
| **주인공** | `growth_engine.rs:186` `potential_cap_factor` | **갈래가 없다 — 0.10** |
| **NPC** | `npc_sim.rs:2676` `potential_cap` | **0.00** |

04도 정확히 그렇게 갈라 놓았다(`Growth.potential_cap_factor` ·
`NpcGrowth.potential_cap`). **처음부터 맞게 옮겨져 있었다.**

⚠ **02 검사가 그 전제 위에 서 있다** — `startPresets.test.ts:71`:
"잠재력을 넘긴 채 시작하면 그 커리어는 **1주차부터 성장이 0.10배**가 된다".
0.00이면 그 문장이 성립하지 않는다.

⚠ **04 검사에도 이미 그 약속이 있었다** — `growth_test.gd:50`
`potential_cap_factor(99, 99) > 0.0`. **내가 그걸 안 읽고 02만 봤다.**

### 그래서 검사로 박았다

두 표가 **일부러 다르다**는 것과 **천장 아래에서는 같다**는 것을 검사가
말한다. 다음에 "통일하자"는 생각이 들면 거기서 막힌다.
검사 3 · **변이 4/4**(주인공에 hard cap 걸기 · 천장 값 바꾸기 · NPC에서
hard cap 빼기 · 문턱 옮기기).

### 형태 ⑧이 하나 늘었다 — 이번엔 내가 저질렀다

**02 규칙을 잘못 읽고 04를 결함이라 판정했다.** 값을 고쳤다면 02와 갈렸다.
⚠ **같은 이름의 함수가 02에 둘 있으면 어느 쪽이 주인공용인지 먼저 가른다.**

## P-13 — 라이벌 값 ✅ **판정: 04는 02와 같다. 값은 안 바꾼다**

20해 실측(커리어가 프로까지 굴러간 첫 표본): `rival 21명 · 0~0 · 평균 0.0`
(teammate 평균 51.6).

### 배선을 찍어서 하나씩 지웠다

| 의심 | 실측 |
|---|---|
| 규칙 표가 비었나 | `weekly.rival = {faced_and_won: 3, faced_and_lost: -1}` **있다** |
| `weekly`가 rival 분기를 안 타나 | `relationship.gd:321` **탄다** |
| 행을 만드는 자리가 없나 | `_meet_rivals`가 21명을 만든다 **돈다** |
| 만든 뒤에 갱신하나 | `reconcile → _meet_rivals → weekly` **순서 맞다** |

### 그래서 02와 규칙 파일을 통째로 대조했다

`02.SvelteElectron/resource/data/master/players/relationship_rules.json`
↔ `04.GodotOnePitch/data/relationship_rules.json`

**차이가 하나도 없다.** `weekly.rival`도 `decay`도 글자 그대로 같다:

```
decay  02 {on_move: 0.6, apart_per_season: 0.9, apart_floor: 2}
       04 {on_move: 0.6, apart_per_season: 0.9, apart_floor: 2}
```

02 엔진(`relationship.rs:415`)의 라이벌 갱신도 04와 같은 모양이다 —
**맞대결이 없는 주는 안 움직이고**, 성향이 부호를 뒤집는다.

### 결론

**라이벌이 안 크는 것은 04의 결함이 아니라 02에서 물려받은 성질이다.**
맞대결은 한 해 몇 번뿐인데 시즌마다 0.9배로 식으니, 20해면 0에 수렴한다.

⚠ **02 계측도 라이벌을 안 낸다**(`scripts/measure-relations.cjs`는 동료
중심이다) — **02에도 실측 기준값이 없다.** 그래서 "02보다 작다"고 말할
근거 자체가 없다.

⚠ **여기서 값을 바꾸면 그건 이주가 아니라 밸런스 결정이다.** 이주 중
밸런스는 동결이므로 **바꾸지 않는다.** 라이벌을 키우고 싶다면 이주가
끝난 뒤 사용자 결정으로 다룬다 — 목록에 남긴다.

## E — 02 `refs.json` 팀 역사 이주 ✅

04는 **238팀을 다 갖고도 `history`가 하나도 없었다.**
`team_detail_vm.gd:8`이 그 사실을 적어 뒀다 — "02의 '팀 평가'는 이걸로 못
옮긴다. 04엔 그 데이터가 없다".

원본: `02.SvelteElectron/resource/data/master/entities/refs.json`의
`teams[].history` — `foundedYear` · `budget` · `seasonRanks`(S-1~S-5) ·
`titles` · `rivals`.

### 옮긴 만큼이 02와 정확히 같다

| | 02 | 04 |
|---|---|---|
| 창단연도 | 30팀 | **30팀** |
| 운영예산 | 171팀 | **171팀** |
| 과거 5시즌 | 172팀 | **172팀** |
| 우승 이력 | 22팀 | **22팀** |
| 라이벌 | 38팀 | **38팀** |

⚠ **02에 없는 것을 지어내지 않았다.** 해외(ABL 32 · JBL 24)는 02 원본에
`history`가 통째로 없다 — 04도 그대로다. **검사가 그 사실을 적어 둔다**
("해외에 역사가 생겼으면 지어낸 것이다").

⚠ **빈 칸을 0으로 채우지 않았다.** 채우면 화면이 그 0을 진짜 값으로 읽어
"창단 0년"이 뜬다.

### FA가 예산을 실제로 읽는다 (형태 ③ 방지)

02 `market.ts:1265`: `budgetIndex = 그 팀 예산 / 평균 예산`.
04는 구단주 씀씀이를 `clampf(…, 0.8, 1.35)`로 죄어 쓰고 있었고
`fa_runner.gd`가 "재정이 붙으면 값만 바뀐다"고 미리 적어 뒀다 — **그 자리를
이었다.**

⚠ **clamp를 안 건다.** 02엔 그 상한이 없다 — 씌우면 부자 구단이 부자답게
못 부른다. ⚠ **예산이 없는 팀은 평균으로 친다**(02의 `?? avgBudget`).
0으로 두면 그 팀이 FA를 한 명도 못 부른다. ⚠ **평균의 분모에서는 뺀다**
(02도 `filter(b => b > 0)`) — 넣으면 평균이 반토막 난다.

실측(씨앗 20270101 · 6해):

| | 구단주 씀씀이(전) | 예산(후) |
|---|---|---|
| 계약 | 387명 | **373명** |
| **미계약** | **0명** | **26명** |
| 등급 | A:164 B:153 C:70 | A:174 B:144 C:55 |
| 이적 | 311명 (80%) | 297명 (80%) |

**미계약자가 처음으로 생겼다** — 예산이 낮은 구단이 못 부르면서 남는
선수가 나온다. 02도 미계약자를 진로 배정으로 보낸다.

### 화면이 읽는다

`TeamDetailVm._history` → `team_detail_screen._build_history`.
검사가 **화면 소스에 `_build_history()` 호출이 있는지**까지 본다.

검사 12 · **변이 4/4**.

### 🔴 캡처를 눈으로 보고 둘을 고쳤다

첫 캡처에 **"운영예산 운영예산 350억"**이 찍혔다 — 뷰모델이 라벨을 값에
넣었는데 화면도 라벨을 붙인다. 그리고 **준우승이 "우승" 라벨**로 나왔다
(02 `titles`엔 준우승도 담긴다). 뷰모델은 값만 내고 **결과를 라벨로
옮겼다** — 지금은 "우승 / S-5 한국시리즈", "준우승 / S-3 한국시리즈"다.

⚠ **고교 예산이 "0억"으로 접혔다.** 02 실측 고교 0.40~5.00억 · 프로
120~350억인데 `%.0f억`으로 냈다. **소수점은 필요할 때만 붙인다**
(210.0억은 어색하다).

⚠ **검사도 두 번 틀렸다** — `not_contains("0억")`이 "210억"을,
`not_contains("0년 창단")`이 "2000년 창단"을 잡았다. **부분 일치로 0을
막으면 안 된다.**
