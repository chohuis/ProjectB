# 02 ↔ 04 출력 대조

**이 문서가 바통이다.** 축 하나씩 02를 돌려 재고, 04를 돌려 재고, 나란히 놓는다.

## 왜 하나

이주 내내 한 것은 **입력 대조**였다 — 02의 값·산식을 옮기고 검사로 못 박고
변이로 검사가 진짜 보는지 확인했다(검사 2,970개). 하지만 **02와 04를 같은
조건으로 돌려 분포를 비교한 적이 없다.**

같은 상수를 써도 **호출 순서 · 경계(`>=` vs `>`) · 반올림 시점 · 도는 횟수**가
다르면 분포가 갈린다. 그건 검사로 안 잡힌다.

## 판정 세 가지

| | 뜻 |
|---|---|
| ○ | 같다 |
| △ | **의도한 차이** — 왜 다른지 적는다 |
| ✗ | 설명 안 되는 차이 — 원인을 찾는다 |

⚠ **다른 게 맞는 것이 있다.** 04는 02의 결함을 여럿 고쳤다(주인공이 지명될
수 없던 것 · 은퇴가 없던 것 · 관계가 안 움직이던 것 · 대학 49개 무조건 합격 …).
그 자리는 04가 달라야 한다.

⚠ **02는 경기 엔진이 `thread_rng`라 같은 시드에도 결과가 매번 달랐다.**
값 대 값이 아니라 **분포 대 분포**로만 비교할 수 있다.

## 명령

```bash
# 02 (읽기만 — 02 소스는 고치지 않는다)
cd 02.SvelteElectron
npx electron scripts/audit-engine.cjs      # 경기 엔진   ELECTRON_RUN_AS_NODE=1
npm run measure:draft                      # 드래프트
npm run measure:promotion                  # 진급
npm run measure:finance                    # 재정
npm run test:injury / test:fa / test:staff / test:relationship

# 04
godot --headless --script tools/run.gd -- measure:engine
godot --headless --script tools/run.gd -- measure:career
godot --headless --script tools/run.gd -- measure:growth
godot --headless --script tools/run.gd -- measure:offseason
```

---

# 축 1. 경기 엔진 — ✗ **원인 둘 중 하나 고침**

## 결론 먼저

| | |
|---|---|
| **고쳤다** | `zone_to_target(0)`이 **존 밖을 한가운데로** 보내고 있었다 |
| **안 고쳤다** | NPC 투구 선택에 **존 밖을 던지는 동작이 아예 없다** |

두 번째는 없는 기능을 새로 만드는 일이고 비율을 지어내야 해서 **동결 규칙에
걸린다**(이 단계는 재는 단계다). `docs/QUEUE.md`에 올렸다.

## ① 고친 것 — 존 밖이 한가운데였다

`PitchOutcome.zone_to_target`이 1~9 범위 밖을 전부 `(0, 0)`으로 떨어뜨렸다.
`0`은 **존 밖(거르기)** 약속인데(`PitchVm.BALL_ZONE`), 그걸 **정중앙**으로
겨냥했다.

⚠ **주인공 경기에도 걸려 있었다.** 화면에 "존 밖 (거르기)" 버튼이 있고
누르면 한복판 공이 갔다 — 화면은 거른다고 말하는데 실제로는 한가운데다.

## ② 안 고친 것 — NPC가 존 밖을 안 던진다

`MatchDay._decide`가 `location`을 **1~9에서만** 고른다. 벤치마크용
자리표시자(`GameBench._decide`와 같은 코드)가 **리그 경기 수천 개에
그대로 쓰이고 있다.** 주석도 "성능을 재는 게 목적이라 전술은 안 넣는다"다.

존 밖을 안 던지니 볼이 4개 쌓일 일이 없다. **제구 흔들림만으로는 볼넷이
안 난다**(실측: 존 밖 비율 0에서 9이닝당 볼넷 0.0).

## 실측 세 번

| 항목 | 02 | 04 원래 | 04 (존밖 수정만) | 04 (+ 26% 시험) |
|---|---|---|---|---|
| 경기당 득점 | 7.9 | 7.4 | 7.0 | 9.8 |
| 리그 ERA | 4.06 | 4.13 | 3.95 | 5.69 |
| **9이닝당 볼넷** | **3.3** | 0.0 | **0.0** | 1.2 |
| 9이닝당 탈삼진 | 7.4 | 11.3 | 11.3 | 10.6 |
| 타율 | .230 | .255 | .251 | .285 |
| 경기당 홈런 | 2.7 | 1.6 | — | 1.9 |

⚠ **26%는 시험해 보고 되돌렸다.** 볼넷은 늘지만(0 → 1.2) ERA·타율이 같이
치솟는다 — 지어낸 비율을 코드에 남기지 않는다. 지금 `OUT_OF_ZONE_RATE`는
0이고, 그건 "아직 이 동작이 없다"는 뜻이다.

## 남은 어긋남

- **9이닝당 탈삼진 11.3 vs 7.4** — 존 밖과 별개로 04가 삼진을 많이 잡는다.
  볼넷이 붙어도 다 설명되진 않는다. 별도 축으로 봐야 한다
- **타율 .251 vs .230** · **홈런 1.6 vs 2.7** — 04는 안타가 많고 장타가 적다

## 덤으로 잡은 것

`achievements_test`에 **"고교는 리그 승이 0"이라는 잘못된 단정**이 있었다.
그 실행에서 우연히 0이었을 뿐이고 고교에도 주말리그가 있다. 값이 아니라
**읽는 자리**(순위표를 일정에서 파생하는가)를 보도록 고쳤다.

---

# (원래 기록) 축 1 첫 대조 — 볼넷이 0이다

02 `audit-engine.cjs`(NPC 시즌 모델) vs 04 `measure:engine`(400경기, 진짜 로스터).

| 항목 | 02 | 04 | 판정 |
|---|---|---|---|
| 경기당 이닝 | 17.4 | 16.0 | △ 02는 연장 포함으로 보인다(확인 필요) |
| 경기당 득점(양팀) | 7.9 | 7.4 | ○ |
| 리그 ERA | 4.06 | 4.13 | ○ |
| 9이닝당 피안타 | 8.1 | 10.2 | ⚠ 04가 높다 |
| 9이닝당 탈삼진 | 7.4 | 11.3 | ✗ 04가 크게 높다 |
| **9이닝당 볼넷** | **3.3** | **0.0** | ✗✗ |
| 타율 | .230 | .255 | ⚠ 04가 높다 |
| 타석당 삼진율 | 0.19 | 0.28 | ✗ |
| **타석당 볼넷율** | **0.09** | **0.00** | ✗✗ |
| 경기당 홈런(양팀) | 2.7 | 1.6 | ⚠ 04가 낮다 |

## 무엇을 봤나

**04의 리그 경기에서 볼넷이 한 개도 안 나온다.** 투수 줄(`bb`)도 타자
줄(`bb`)도 전부 0이다 — 기록이 새는 게 아니라 **볼넷 자체가 안 난다.**

나머지 어긋남이 전부 여기서 설명된다. 볼넷으로 끝날 타석이 삼진이나
인플레이로 흘러가므로 **삼진율이 0.19 → 0.28로 뛰고**, 출루가 볼넷 대신
안타로 몰려 **타율이 .230 → .255로 오른다.** 주자가 덜 쌓이니 **홈런의
타점 가치가 줄고**, 이닝은 짧아진다.

⚠ **검사 2,970개가 전부 통과하는데 이렇다.** 볼넷 규칙 자체는 코드에 있다
(`sim/pitch_step.gd:154` — `count["balls"] >= 4` → `WALK`, 줄에 `bb` 누적,
타석은 타수로 안 센다). 조각 검사는 그 함수를 직접 불러 통과한다.
**한 경기를 끝까지 돌렸을 때 안 나는 것**이라 조각 검사로는 안 잡힌다.

## 다음에 볼 곳

1. `PitchStep`에서 **볼 카운트가 투구 사이에 이어지는가** — 스트라이크는
   쌓여서 삼진이 나는데 볼만 안 쌓이면 그 자리다
2. `MatchDay._decide`의 코스 선택이 **존 밖을 아예 안 던지는가**
3. `GameLoop`가 타석을 넘길 때 `count`를 어디서 비우는가

## 아직 안 한 것

- 02의 "경기당 이닝 17.4"가 연장 포함인지 확인 (04는 9이닝 고정)
- 02 주인공 모델(`match_engine.rs`)과의 대조 — 02는 모델이 **둘**이고
  04는 **하나**다. 04 값은 두 모델 사이에 떨어지는 게 맞다
  (02 피안타 비중 NPC 0.52 · 주인공 0.55 / 볼넷 비중 0.18 · 0.13)

---

# 축 2. 드래프트 — ○ / △

02 `measure:draft` vs 04 `measure:career`.

| 항목 | 02 | 04 | 판정 |
|---|---|---|---|
| 지명 인원 | 110 (11R × 10팀) | 110 (11R × 10팀) | ○ |
| 신인 연봉 | 3,000만 균일 | 3,000만 균일 | ○ |
| 1순위 계약금 | 45,100만 | 50,000만 × 구단지수(0.85~1.15) | ○ 지수 곱하면 겹친다 |
| 110순위 계약금 | 2,000만 | 2,000만 | ○ |
| 후보 수 | 1,555~1,686 | 1,420 | △ 풀 구성이 다르다(아래) |
| 계약 연수 | — | 3년 | — |

## 04가 고친 것 (의도한 차이)

`career_rules.json` 주석: 02의 **옛 TS 표는 KBO 규정 위반**이었다 —
1순위 연봉 9,000만(규정 위반)에 하위 지명 1,500만(최저연봉 3,000 미달).
04는 **연봉을 최저로 균일**하게 두고 차등을 계약금이 지게 했다.
지금 02를 돌리면 연봉 3,000만 균일로 나오므로 **02도 그 뒤에 고친 것으로
보인다** — 두 쪽이 같은 결론에 왔다.

## "계약금 최고=중앙=4,000만"은 결함이 아니다

04 계측이 뽑은 60커리어의 지명 라운드가 5~10(순번 41~110)에 몰려 있어
전부 `until_pick: 70` 구간(4,000만)에 떨어진다. 표는 순번별로 제대로
갈린다(50,000 / 30,000 / 20,000 / 12,000 / 7,000 / 4,000 / 2,000).

## 아직 못 맞춘 것

- **후보 수 1,420 vs 1,555~1,686** — 02는 고졸1,020 + 대졸400 + 대학재학207
  + 독립32로 센다. 04의 풀 구성을 같은 기준으로 갈라 봐야 한다
- **진로 분포는 분모가 다르다.** 02의 "미지명→대학 298~481"은 **드래프트 풀
  전체**(대졸 포함) 기준이고, 04의 "진학 75%"는 **주인공 60커리어** 기준이다.
  같은 분모로 다시 재야 비교가 된다

---

# 축 3. 성장 — ❓ **02에 대응 계측이 없다**

## 무엇을 해봤나

| 돌린 것 | 나온 것 | 쓸 수 있나 |
|---|---|---|
| 02 `test:synthetic` | 규칙 검사(단조성·결정성·분산) — **분포가 아니다** | ✗ |
| 02 `diag-roster.cjs` | 리그별 **인원**·투수율 — OVR이 아니다 | ✗ (축 다름) |
| 04 `measure:growth` | 리그별·나이대별 **평균 OVR** | ○ |

**02에는 "NPC가 한 해에 얼마나 크는가"를 분포로 내는 스크립트가 없다.**
있는 것은 성질 검사(OVR 높을수록 ERA 낮다 · 같은 입력이면 같은 출력)뿐이다.

## 04 실측 (52주 뒤)

```
리그별 평균 OVR   KBL 71.7 · KBL_FARM 65.4 · JBL 76.5 · JBL_FARM 63.2
                 UNIVERSITY 67.9 · INDEPENDENT 63.3
나이대별 평균 OVR 15~19 63.5 / 20~24 68.5 / 25~29 69.0 /
                 30~34 69.2 / 35~39 67.7
30세 이상 1,015명 · 노화 빚이 쌓인 사람 1,015명
```

⚠ **나이 곡선이 늦게 꺾인다.** 30~34가 69.2로 최고고 25~29(69.0)보다 높다.
야구 상식으로는 26~28이 정점이다. `Aging.CURVE`는 32세부터 깎기 시작하는데
(velocity 1.00 · stamina 0.80), 그 감쇠가 성장을 못 이기는 것으로 보인다.
**단, 이건 02와 대조한 게 아니라 04만 보고 한 말이다.**

## 그래서 어떻게 하나

02에 계측이 없으므로 **대조 방법을 바꾼다** — 02의 성장·노화 **산식을 읽어**
04와 나란히 놓는다(코드 대조). 02를 고칠 수 없으니 02 쪽 계측을 새로
만들 수는 없다.

읽을 것: 02 `npc_sim.rs`의 주간 성장 · 나이 계수 · 천장 감쇠 ↔
04 `sim/npc_growth.gd` · `sim/growth.gd` · `sim/aging.gd`

⚠ **QUEUE D-7이 이미 그 자리를 가리킨다** — "주인공 경로와 NPC 경로의 성장
표가 둘이다(천장 감쇠 0.10 vs 0.00 · 나이 계수의 뜻이 다르다)". 04 안에서도
표가 둘인데 02와 대조하기 전에 그것부터 정리해야 한다. **이주가 끝난 뒤**로
미뤄 둔 항목이라 여기서 손대지 않는다.

**판정: ❓ 보류.** 코드 대조는 D-7과 함께 동결 해제 후에 한다.

---

# 축 4. 부상 — ○ **전부 일치**

02 `test:injury` vs 04 `measure:injury`(새로 만들었다, 표본 20,000회).

| 항목 | 02 | 04 | 판정 |
|---|---|---|---|
| 피로 82 (예고/실측) | 5.0 / 5.1 | 5.0 / 4.9 | ○ |
| 피로 87 | 15.0 / 15.4 | 15.0 / 15.0 | ○ |
| 피로 92 | 35.0 / 34.4 | 35.0 / 35.2 | ○ |
| 피로 97 | 60.0 / 60.5 | 60.0 / 59.6 | ○ |
| 유예 주 · 쉼 | 0.00% | 0.0% | ○ |
| 유예 주 · 고강도+저컨디션 | 20.5% | 20.0% | ○ |
| 유예 지난 뒤 (피로 92) | 35.1% | 35.2% | ○ |
| 회복 주차 하한 | 1주 | 1주 | ○ |
| 시설이 복귀를 앞당기나 | 8.21 < 9.21주 | boost 1.2 → 8주 · 0.8 → 13주 | ○ 방향 같다 |

**규칙 표도 같다.** 04 `injury_rules.json`의 `fatigue_bands`가
`[[95,0.60],[90,0.35],[85,0.15],[80,0.05]]`로 02의 60/35/15/5와 정확히 같다.

## 이 축이 확인해 준 것

**예고와 실측이 맞는다.** 화면이 "이대로면 35%"라고 말하면 실제로 35%가
난다 — 두 쪽 다. 표만 옮기고 실제 발생 경로가 어긋나면 화면이 거짓말을
하는데, 그게 아니었다.

## 계측을 만들며 걸린 것 (04 결함 아님)

처음에 컨디션을 **60.0(경계값)** 으로 재서 04가 9.7%로 나왔다(02는 20.5%).
깊은 과부하 조건이 `condition <`(미만)이라 정확히 문턱이면 안 걸린다.
55로 다시 재니 20.0%였다. **계측 fixture 문제였지 코드 차이가 아니다** —
경계값으로 재면 어느 쪽이 맞는지 알 수 없다.

---

# 남은 축

| 축 | 02 | 04 | 상태 |
|---|---|---|---|
| 경기 엔진 | `audit-engine.cjs` | `measure:engine` | ✗ 위 참조 |
| 드래프트 | `measure:draft` | `measure:career` | 안 함 |
| 진로 | `study-careers.cjs` | `measure:career` | 안 함 |
| 성장 | `test:synthetic` | `measure:growth` | 안 함 |
| 부상 | `test:injury` | **없음 — 만들어야** | 안 함 |
| 재정 | `measure:finance` | **없음 — 만들어야** | 안 함 |
| 진급 | `measure:promotion` | `measure:offseason` | 안 함 |
| FA·계약 | `test:fa` | **없음 — 만들어야** | 안 함 |
| 스태프 | `test:staff` | **없음 — 만들어야** | 안 함 |
| 관계 | `test:relationship` | **없음 — 만들어야** | 안 함 |

⚠ **밸런스 동결은 유지한다.** 이 단계는 재는 단계다. ✗가 나오면
"04가 02와 다르게 계산한다"를 고치는 것만 하고, 수치를 더 좋게 만들지 않는다.
