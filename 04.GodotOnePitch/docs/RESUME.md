# 인수인계 — Godot 이주

**이 문서가 진행 현황의 정본이다.** 코드 규칙은 `04.GodotOnePitch/CLAUDE.md`.

마지막 갱신: 2026-08-14 · 커밋 46342dd4f

---

## 한 줄

`02.SvelteElectron`(Svelte + Electron + Rust)에서 **Godot 4.6 · GDScript 단독**으로
이주 중. **M1~M6·M8·M9 뼈대·D1~D3 끝났다.** **게임이 해를 넘긴다** —
새 게임(팀 238·선수 7,337·일정 6,396경기) → 하루씩 진행 → 경기를 손으로
던지고 → 시즌이 끝나면 진급·드래프트·은퇴·신입생이 돌고 다음 해가 열린다.
**진행 단위가 주 → 일로 바뀌었다** (사용자 결정).
**창은 1440×900** — 02와 같다. 화면은 좌 탭 · 가운데 내용 · 우 패널 3단.

---

## 확정된 결정

사용자가 정한 것들이다. 다시 묻지 않는다.

| | |
|---|---|
| 엔진 | **Godot 4.6.1** (`~/Godot/Godot_v4.6.1-stable_win64_console.exe`) |
| 언어 | **GDScript 단독** — Rust GDExtension·C# 안 씀 |
| 1차 목표 | **PC / Steam.** 안드로이드는 나중 |
| 화면 | 지금 구조 **그대로** 옮긴다 (픽셀이 아니라 구조) |
| 경기 화면 | 현 구조 유지 · 구장 이미지 27장 재사용 |
| 저장 | Godot 네이티브. **기존 세이브 호환 포기** |
| 검사 틀 | **GdUnit4** 6.2.1 |
| UI | **씬(.tscn)** — Godot 관례 방식 |
| 폰트 | **Pretendard** 임베드 (OFL 1.1) |
| 밸런스 | **동결.** 이주 중 게임 수치 안 바꾼다 |
| 막히면 | **멈추고 묻는다** (설계 결정·수치·화면 구조·게이트 실패) |

### 왜 GDScript 단독인가

Rust 코어 24k줄을 gdext로 살리는 안이 처음 권고였으나, 사용자가 모바일과
생태계 표준을 이유로 GDScript를 택했다. **성능 게이트를 실측으로 통과**해서
근거가 생겼다 — 시즌 시뮬 2,124경기가 0.659초(기준 2초).

---

## 끝난 것

```
2aa67b6ec  P0 게이트     시즌 0.659초 — ⚠ 뼈대로 잰 값이라 뜻이 없었다
3c82555eb  저장          열 배열+zstd — 220배 · 18MB → 1.1MB
bca71f885  GdUnit4       Godot 4.6 호환 확인 (웹 검색으로 검증)
54903c0ce  시드 RNG      해시 기반 — 호출 순서와 무관
4259a7dc3  계측 러너      진입점 하나 · 종료 코드 판정
3960b3953  화면 규약      ViewModel 사전 하나
2d41218b6  폰트          Pretendard 임베드
94548dac6  씬 전환        .tscn 방식
a8ac7a6c0  문서          개발 대상을 04로 · 02는 동결
92f2782be  M1 순위표      standings — 승패·연승·최근10 (변이 5건)
92afd43aa  M1 시즌 누적    season_stats — 타석은 누적이 아니라 파생 (변이 7건)
d618952b8  M1 투구 결과    match_result — 네 군데 표를 하나로 (변이 9건)
6a58bfd5e  M1 대진        bracket — 순서는 이름이 아니라 깊이 (변이 8건)
62b388427  M1 스탯 순위    leaderboard — 비율과 누적은 자격이 다르다 (변이 11건)
34a8c7396  M1 통산 기록    career_summary — 합은 평균이 아니다 (변이 18건)
f2a88b976  M1 야구계 소식  digest — 네 갈래를 월 1통으로 (변이 18건)
2909a63cf  M2 주루        baserunning + Tuning — 계수를 두 모델이 나눈다 (27)
6650c69b4  M2 타구·수비    batted_ball — 코드 좁히기는 MatchResult 하나 (23)
e2eb679c2  M2 투구 품질    pitch_outcome — 항 열다섯, 하나가 지배하면 안 된다 (53)
07068c864  M2 투수 교체    pitcher_switch — 예산 식은 하나다 (25)
0669909c2  M2 경기 진행    match_state + pitch_step — 조각을 엮는다 (45)
5828624fc  M2 결과 어댑터  match_report — 계약 검사가 무승부 어긋남을 잡았다 (28)
28f3dd992  M2 계측        game_loop — 진짜 엔진으로 재니 게이트 27배 초과
cb83364c4  M2 성능        제자리 수정 — 게이트 통과, 게이트를 다시 잡았다
bff4d5599  D1 달력        일 단위 시간 축 (변이 16)
24607869d  M3-1 로스터     로테이션 고정 · 회전은 한 곳 (변이 25)
e17eb7b54  D2 일정        실제 리그 일정 · 해외 포함 (변이 16)
a2e9bb9d9  D2 되돌림      압축 취소 — 일 단위만으로 목적 달성
dc34be6fa  피로 두 축      freshness / fatigue 로 갈랐다 (변이 41)
1554b49c9  M4-1 성장       XP·잠재력·나이·피로 (변이 21)
3ddfb02da  M4-2 훈련 계획   미리보기와 실제가 같은 함수 (변이 15)
c1d44fb80  M4-3 훈련 성장   슬롯 배수·천장은 스탯마다 (변이 22)
509bb69e3  M4-4 경기 성장   레벨업 규칙을 훈련과 합쳤다 (변이 24)
b5a7a01b7  M4-5 에이징      자기관리는 비율로 본다 (변이 22)
7792a345a  M5-1 수상        자격선이 없으면 0이 1위 (변이 22)
91d73192c  M5-2 드래프트    앵커를 NPC 실측 위에 (변이 26)
1b57fef0c  M6-1 진행기      하루씩 간 것과 한 번에 간 것이 같다 (변이 35)
1ee3e76fb  M6-2 시즌 종료    순서 자체가 정본 (변이 23)
3aacd4d37  M6-3 로스터 정리  리그를 코드에 적지 않는다 (변이 39)
c81a85914  M7-1,2 진행 화면  날짜와 "다음 등판까지"가 D4 (변이 28)
81ec9c7e8  M7-3 프레임 쪼개기 최악의 날을 진행 표시로 (변이 20)
736da09d6  M7-4 앱 루트      조각들이 처음으로 이어졌다 (변이 24)
fbebcf29a  M7-5a 일정 탭     띄우니 fixture가 뒤집혀 있었다 (변이 25)
cdd7b0b0c  M7-5b 소식 탭     막힌 이유를 화면이 말한다 (변이 26)
4d411f17d  M8-1 선수 생성    검사가 자기 자신과 비교했다 (변이 37)
82a4e8270  M8-2 이름         짝이 어긋나면 김씨가 Lee (변이 26)
6c2fe00f9  M8-3 세계 조립    20경기를 전부 남의 구장에서 (변이 22)
fff5c2a7b  M8-4 세이브       02보다 15배 빠르고 30배 작다 (변이 19)
9b5527b34  M7-6a 진짜 경기   엔진과 어댑터의 모양이 달랐다 (변이 23)
085ebf2c7  M7-6b 리그·팀 탭  진짜 순위표가 떴다 (변이 39)
8d5d99bde  M7-6c 나 탭       P1 화면을 안 고치고 끼웠다 (변이 18)
64eec18c8  M3-2 로테이션     주인공이 다 던지던 것 (변이 47)
48452f54d  M7-6d 타이틀·슬롯  이제 게임을 켤 수 있다 (변이 15)
0d9e7c2c8  M7-6e1 경기 뼈대  손으로 던진 경기 = 자동 시뮬 (변이 —)
66b299011  M7-6e2 투구 선택  숙련도가 결과에 안 닿고 있었다 (변이 52)
fb7d321ca  M7-6e3 구장 27장  고교 경기가 프로 구장에서 열렸다 (변이 19)
85c5131d5  창 1440×900      세로 한 줄로 짜 온 걸 가로로 다시
f5e16252b  M9-1~5 오프시즌   세계가 해를 넘긴다 (변이 54)
46342dd4f  M9-6 롤오버      앞 화면에서 다음 해로 (변이 28)
e71c227bd  M9-7a 시즌 성적   아무도 season_stats를 안 쌓고 있었다
912c905b3  M2-6 투수 교체    불펜이 한 경기도 못 던졌다 (변이 7)
(다음)     M9-7b 기록·수상   화면을 열어야만 쌓이던 것 (변이 15)
```

**검사 1,404개 통과 · 변이 전부 검출 · 게이트 통과.**
**M1·M2·M3·M4·M5·M6·M8·M9 뼈대·D1·D2·D3 끝났다** — `sim/`에 33개 모듈.

## 성능 게이트 (실측으로 다시 잡음)

```
최악의 날 · 일하는 시간    0.887초 기준 1.0초   ← bench:day (진짜 일정·엔진)
최악의 날 · 기다리는 시간  1.376초 기준 2.0초   ← 프레임 쪼개기 값 포함
합성 벤치 (98경기)        0.80초  기준 1.0초   ← bench:game
국내 한 시즌              22.4초  기준 25초    ← 회귀 감지
```

⚠ **`bench:day`는 창을 띄워 잰다.** 헤드리스는 렌더가 없어
`process_frame`이 거의 즉시라 기다리는 시간이 0.70초로 나온다 — 실제보다
0.4초 짧다. 게이트를 그 값으로 잡으면 창에서 넘는다.

```bash
godot --script tools/run.gd -- bench:day     # --headless 안 붙인다
```

⚠ **P0의 "2,124경기 2초"는 두 군데가 틀렸다.** 경기 수를 적게 잡았고,
**게임은 시즌을 통째로 안 돌린다** — 이제 일 단위로 진행한다.

**해외까지 전부 풀 시뮬한다**(`radiusGate` 안 옮김). 9개 리그 합쳐 시즌
6,396경기이고 최악의 날이 118일차 83경기다.

⚠ **여유가 얇다** — 하루 1.13배 · 시즌 1.12배. P0 때는 3배였다.
**뭘 얹을 때마다 `bench:day`를 돌린다.** 한참 뒤에 27배를 발견하는 일이 없게.

⚠ **상태는 제자리에서 고친다.** 투구마다 복사하면 그것만으로 2.4배 느리다
(54.3 → 22.4초). 복사본이 필요하면 **호출부가 직접 복사한다.**
제자리로 바꿀 때 "이전 값" 비교가 조용히 무너지므로 스칼라를 먼저 붙잡는다.

---

## 남은 것

### 진행 단위가 **일 단위**로 바뀌었다 (사용자 결정)

프로는 경기가 거의 매일 있는데 주 단위로 진행하면 **한 번에 여러 경기가
한꺼번에 끝나서** 자기 등판을 골라 볼 수 없다. 02 설계 문서도 원래
"경기일·이벤트 발생 시 진행 정지"였다(DESIGN.md §209) — 구현만 주 단위였다.

```
D1 달력   sim/calendar.gd    시즌 일차 ↔ 날짜 · 주차 파생 · 요일   ✔
D2 일정   sim/schedule.gd    라운드 로빈 → 요일 규칙으로 날짜 배정  ✔
D3 진행기  sim/day_engine.gd  하루씩 / 다음 이벤트 전날까지        ✔
D4 화면   M7에서            날짜 표시 · "다음 경기까지"
```

⚠ **시간 축만 일 단위다.** 성장·훈련·재정·관계는 7일마다 그대로 돈다 —
밸런스를 안 건드려야 02 실측값과 대조할 수 있다.

⚠ **일정을 압축하지 않는다.** 02처럼 프로를 1~50주에 편다. 압축(24주)은
밀도와 진짜 오프시즌을 주지만 **투수 피로 밸런스를 바꾼다** — 등판 간격이
12일 → 5.8일이 되면 등판 간 회복이 48 → 23으로 반토막인데 손실(약 57)은
그대로다. 시즌 내내 피로 바닥에 붙는다. 되돌리려면 `Schedule.LEAGUES`의
기간만 바꾼다.

⚠ **`radiusGate`를 안 옮긴다** (사용자 결정). 해외까지 매일 풀 시뮬한다.
실측 최악의 날 **118일차 83경기 = 0.71초** (게이트 1초). 압축했을 때(77)보다 오히려
많다 — 성기게 펴면 리그들이 어떤 날엔 몰린다.

### 지금: M7 화면 (진행 중)

**끝난 것 — 게임이 실제로 돈다:**

```
진입점         타이틀 → 새 게임 / 이어하기 → 게임 (슬롯 3개)
새 게임        팀 238 · 선수 7,337 · 일정 6,396경기 · 542ms
진행           하루씩 / 다음 등판 전날까지 · 프레임 쪼개기
경기           진짜 로스터로 시뮬 → 순위표 · 기록
로테이션        보직 배정 · 선발 순번 · 불펜 등판
저장·불러오기   0.57MB · 저장 77ms · 로드 92ms · 목록 0ms
탭 5개         소식 · 나 · 팀 · 리그 · 일정
경기 화면       구장 27장 · 수비 아홉 · 존 3×3 · 구종·전략·힘
시즌 종료       진급·드래프트·은퇴·신입생 → 다음 해
```

**남은 것:**

```
인물 탭        관계도 — 데이터가 없다(미이관 usecases 쪽)
나머지 화면     결산 · 드래프트 보드 · 훈련 · 재정 · 업적 …
```

⚠ **02 좌표의 마운드·1루·3루가 구장 그림보다 30~50px 위를 가리킨다**
(홈플레이트와 2루는 맞다). `parkAnchors.ts`가 "구장 27장이 각자 티어 기준
그림을 따랐다"고 전제하는데 실제로는 다이아몬드 세로 비율이 조금 다르다.
02 정본 그대로 옮긴 것이라 **이주 중에는 안 건드린다.**

⚠ **1~68일차가 비어 있다.** 고교 개막이 64일차(첫 경기 69일차)인데 훈련·
이벤트·학사·대회가 아직 없어서 멈출 이유가 없다 — "68일 진행" 버튼이 그
때문이다. **설계가 아니라 미이관의 부작용**이고, 콘텐츠가 붙으면 사라진다.

원본: `apps/ui/src/pages/**`

### M6 끝났다 (시즌 진행·오프시즌)

```
day_engine.gd          하루 진행 · 정지 조건 · 주 경계
season_end.gd          시즌 종료 순서 — 순서 자체가 정본
roster_maintenance.gd  상한 · 승강 판정 · 충원할 자리
```

⚠ **`Calendar.week_ends_between`은 닫힌 구간이다** (양 끝 포함).
`DayEngine`은 `day`가 아직 안 산 오늘이라 반열린 구간이 필요해서 한 칸
옮겨 부른다. 그대로 부르면 **주 경계에서 쪼갤 때마다 한 번씩 더 센다** —
하루씩 30번 간 합이 8인데 30일을 한 번에 가면 4였다.

⚠ **리그를 코드에 적지 않는다.** `RosterMaintenance.active_pro_leagues()`가
유일한 창구다. 02는 세 리그를 여섯 군데 적어 놓고 정작 운영은 KBL 전용이라
해외 1군이 41명(상한 34)까지 부풀었다.

~~⚠ M6는 순서와 판정만 옮겼다~~ — **M9에서 실제 처리를 붙였다.** 아래 참고.

### M5 끝났다 (드래프트·수상)

```
awards.gd   부문 1위·MVP — 자격선이 없으면 0이 1위가 된다
draft.gd    점수·라운드 앵커 — 앵커를 NPC 실측 위에 놓는다
```

⚠ 드래프트 산식은 지난 세션에 **네 번** 고쳤고 그 넷이 전부 검사로 박혀
있다 — 부상 감점이 산식을 지배 · 대회 항이 아무도 안 가름 · 수상 항 없음 ·
상한이 순서를 지움. 고칠 때 검사부터 본다.

### M4 끝났다 (성장·훈련)

```
growth.gd           XP 문턱·잠재력·나이·주간 XP·레벨업·피로 승수·apply_gains
training.gd         훈련 계획의 피로·컨디션 · 미리보기
training_growth.gd  한 주 훈련이 능력을 얼마나 올리나
game_growth.gd      한 경기가 능력·사기·명성에 남기는 것
aging.gd            한 시즌 나이 먹기
```

⚠ **일정 압축 판단에 쓸 근거가 하나 나왔다.** 자기관리 판정이 **비율**
기반이라(시즌 대비 위기 주 비율) **시즌을 24주로 압축해도 안 흔들린다.**
반대로 투수 피로는 등판 간격에 직접 걸려서 압축에 민감하다.

### M4 남은 것

`calc_game_growth`의 OVR 재계산(`calc_pitching_ovr`)은 안 옮겼다 — 능력치
정의를 가진 쪽(M5 이후)과 짝이다. NPC 성장(`npc_sim`의 주간 성장)도 남았다.

### 옛 메모 — M3 로스터·로테이션

원본: `02.SvelteElectron/apps/ui/src/shared/utils/rosterEngine.ts`
검사: `rotationIndex.test.ts` — **인자 자리가 밀린 결함**이 여기서 나왔다.
소스 정규식으로는 못 잡았고 함수를 직접 부른 검사가 잡았다.

M2가 남긴 붙일 자리:
- `PitcherSwitch`가 큐(`pitchers`·`max_outs`·`lines`)를 이미 다룬다 —
  로스터가 오면 `GameLoop`에 교체를 끼운다
- `GameLoop.current_pitcher`가 지금은 한 팀에 투수 하나다

### M2가 남긴 숙제

- ~~프레임 쪼개기~~ — M7-3에서 했다 (`ui/day_runner.gd`)
- 시즌 통째로 도는 자리(오프시즌·건너뛰기)는 22초다. 진행 표시가 필요하다
- `sim/match_sim.gd`는 P0 뼈대다 — 지금은 `bench:season`이 아직 쓴다

### M9 뼈대가 돌아간다 — 게임이 해를 넘긴다 (2026-08-14)

`SeasonEnd`가 순서만 갖고 **아무도 실행하지 않았다.** 이제 돈다.

```
promotion.gd      진급·졸업·나이 — 고교 3년 · 대학 4년 · 졸업생은 드래프트 풀로
offseason.gd      부상 회복 · 은퇴 (35세부터, 저능력 벌점, 상한 0.72)
npc_draft.gd      후보를 순번에 앉힌다 — 보드 2배 좁히기 · 편차 ±4 · 노이즈 상한 ±6
season_runner.gd  위를 PHASES 순서대로 + 로스터 상한 · 진로 배정 · 신입생 · 롤오버
```

**실측 15년** (`measure:offseason --years 15`) — 총원 7,376 안정 ·
고교 학년 1021/1020/1019 · 졸업 1,420 → 지명 110 · 대학 400 · 나머지는
독립·포기 · 2035년부터 은퇴가 시작돼 2041년 6,237로 꺾인다.

⚠ **실측이 검사가 못 본 것을 넷 잡았다.** 전원 1학년으로 시작(졸업생 0명) ·
신입생 충원 없음(고교가 5년에 텅 빔) · 미지명자 4,221명이 소속 없이 떠다님 ·
로스터 상한 없음(프로가 매년 110명씩 증가). **`is_pro_league`는 1군만 참이라**
2군 상한이 영영 안 걸렸다 — 02의 "KBL 700명"과 같은 형태다.

⚠ **주인공은 NPC 배정에서 뺀다.** 능력치 순으로 밀려 자리를 못 잡으면
은퇴하는데, 그러면 **게임이 조용히 끝난다** — 실측 3년차에 그랬다.

### M2-6 투수 교체가 붙었다 — 불펜이 실제로 나온다 (2026-08-14)

`GameLoop`에 **투수 교체가 아예 안 붙어 있었다.** 선발 한 명이 끝까지
던지므로 불펜은 영영 등판이 없었는데, `is_my_start`는 불펜을 확률로
내보내서 화면엔 "오늘 등판"이 떴다.

```
              고치기 전                    고친 뒤
씨앗 4242    RP · 화면 9 · 실제 0경기  →  실제 8경기 · 9.7이닝
씨앗 777     SP · 화면 7 · 실제 7경기  →  실제 7경기 · 49.3이닝
씨앗 20270101 RP · 화면 7 · 실제 0경기  →  실제 6경기 · 7.3이닝
```

경기당 투수 4.4~5.1명. M3-2가 "OVR 51 RP → 5~6등판"이라고 잰 건
**판정만** 센 값이었다.

같이 고친 것:
- **스태미나가 양 팀 공유였다** — 한 팀이 지치면 상대도 지쳤다.
  02는 `npc_pitcher_stamina.my`/`.opponent`로 나눈다
- **스태미나가 경기 내내 82로 고정이었다** — 감소 식이 아예 없었고,
  그래서 `pitch_quality`의 `stamina_penalty`가 영영 0이었다
- **투수 줄이 팀별이었다** — 교체가 붙으면 구원 자책점이 선발 것이 된다.
  이제 큐의 `lines[]`에 투수별로 쌓인다
- **큐 생성 난수를 경기 흐름과 분리했다** — 같은 난수기를 쓰니 흐름이
  밀려 손으로 던진 경기와 자동 시뮬이 갈렸다

⚠ **구원 스태미나를 100으로 리셋하면 안 된다.** 02 실측으로 주인공 완투
ERA 3.83 vs 투수진 3명 교체 1.65 — `min(cap, 82)`로 들어온다.

⚠ **성능 여유가 얇아졌다.** 최악의 날 0.731 → 0.887초(기준 1.0).

### M9에 남은 것

- 미지명자 **12단계 배정**(재수·군·해외). 지금은 대학·독립·포기 셋뿐이라
  매년 900명 안팎이 야구를 그만둔다
- **FA·트레이드·2단계 방출** — 02의 `free_agency.rs` · `release_second_stage`
- **구단 성향 갱신**(`team_profiles`) — 02에서 구현돼 있는데 아무도 안 불러
  전 팀이 50이었고 트레이드 buyer가 구조적으로 0팀이었다
- **시즌 기록·수상·주인공 기록** — `season_history` · `awards` ·
  `protagonist_record` 단계가 아직 비어 있다(`Awards`·`CareerSummary`는 있다)

### 그 다음

```
M7-7  결산 화면 · 훈련 화면
M9    위 남은 것
P6    Steam 빌드·패키징
```

---

## 작업 리듬 (M1 7모듈에서 확인됨)

**모듈 하나마다:**

```
① 검사를 GDScript로 옮긴다
② 빨간불인지 확인한다     ← 통과하면 그 검사가 아무것도 안 보는 것이다
③ 로직을 옮긴다
④ 변이 검증 — 일부러 깨뜨려 잡히는지
⑤ 커밋
```

②를 건너뛰지 않는다. 이 저장소에서 "통과하는데 아무것도 안 보는 검사"가
실제로 여러 번 나왔다.

### 변이 검증

도구와 목록 17개가 **`tools/mutation/`에 있다.** 쓰는 법과 도구가 거짓말한
세 가지는 거기 README에.

```bash
node tools/mutation/mutate.cjs sim/<모듈>.gd test/<모듈>_test.gd tools/mutation/mut_<모듈>.json
```

**모듈을 고치면 목록도 같이 고친다.** 안 붙은 변이가 있으면 크게 알리고
종료 코드 1을 낸다 — CRLF 때문에 28건 중 20건이 조용히 건너뛰어졌는데
요약은 "8/8 검출"이던 적이 있다.

### 변이가 안 잡힌 자리 (검사가 부실했던 곳)

M1·M2에서 **16건이 처음에 안 잡혔다.** 거의 전부 같은 형태다 —
**검사 표본이 그 분기를 안 건드렸다:**

| 형태 | 예 |
|---|---|
| 능력치를 묶어서 봄 | 선구안·눈을 같이 움직여서 한 항을 지워도 다른 항이 가림 |
| 문턱보다 한참 아래·위 표본 | 3루 도루 문턱 88인데 표본이 80이라 문턱을 없애도 결과가 같음 |
| 이미 다른 항으로 문턱을 넘음 | 후반 득점권 항을 빼도 이미 강판 판정이 남 |
| 우연히 맞는 fixture | 결승이 마침 목록 첫 칸이라 "깊이로 찾는가"를 안 봄 |
| 동등 변이 | 고쳐도 동작이 진짜 같음 — 변이 쪽을 바꿈 (3건) |

**"검사가 통과한다"와 "검사가 그걸 본다"는 다르다.**

---

## 검사 53개 분류 (P2-1 결과)

```
A 로직    17파일 · 260검사   순수 함수 — 모듈과 짝지어 옮긴다
B+ 혼합   27파일 · 233검사   TS 소스 정규식이 섞여 있다
C 화면     9파일 · 102검사   M7 몫
```

⚠ **B+의 소스 정규식은 실제 호출로 바꾼다.** 근거: 이번 세션의 로테이션
결함이 **인자 자리가 밀린 것**이라 소스 문자열은 그대로였다. 정규식으로는
못 잡았고 함수를 직접 부르는 검사가 잡았다.

바꿀 수 없는 것만 소스 검사로 남긴다 (예: "이 함수가 삭제된 상태여야 한다").

---

## 명령

```bash
G=~/Godot/Godot_v4.6.1-stable_win64_console.exe

# 검사 (전체 / 한 파일)
$G --headless -s addons/gdUnit4/bin/GdUnitCmdTool.gd --ignoreHeadlessMode -a test
$G --headless -s addons/gdUnit4/bin/GdUnitCmdTool.gd --ignoreHeadlessMode -a test/standings_test.gd

# 계측·벤치
$G --headless --script tools/run.gd -- list
$G --headless --script tools/run.gd -- bench:game     # 성능 게이트 — 뭘 얹을 때마다
$G --headless --script tools/run.gd -- bench:save

# 변이 검증 (모듈 하나)
node tools/mutation/mutate.cjs sim/bracket.gd test/bracket_test.gd tools/mutation/mut_bracket.json

# 화면 스크린샷 (창을 띄운다 — 헤드리스로는 못 찍는다)
$G --script tools/shot.gd -- status
$G --script tools/shot.gd -- status-empty

# 새 class_name을 만든 뒤엔 한 번 돌려야 다른 스크립트가 찾는다
$G --headless --import
```

⚠ `--ignoreHeadlessMode`가 필요하다. GdUnit4가 헤드리스에서 `InputEvent`가
안 간다고 막는데 지금 검사는 UI 조작을 안 쓴다. **M7에서 씬 조작 검사를
만들면 이 전제가 깨진다.**

---

## 겪은 함정 (같은 데서 두 번 막히지 않게)

- **`Control`을 `Window`에 직접 붙이면 앵커가 자동으로 안 먹는다.** 루트가
  0×0으로 남아 회색 판만 찍힌다 — 안쪽 컨테이너는 멀쩡해서 더 헷갈린다.
  씬으로 만들면 안 겪는다
- **스크린샷은 헤드리스로 못 찍는다.** 창을 띄우고 프레임을 돌린 뒤
  `RenderingServer.force_draw()`를 불러야 한다
- **정수 리터럴이 2⁶³을 넘으면 파싱이 실패한다.** FNV-1a의 `0xcbf29ce484222325`가
  그렇다. 오류가 콘솔에만 나고 값은 나와서 조용히 넘어간다
- **`PackedStringArray`엔 `filter()`가 없다.** `Array`와 다르다
- **`var x := dict.duplicate()`는 타입 추론이 안 된다.** `var x: Dictionary =`로 적는다
- **GDScript 주석은 `#`다.** `//`를 쓰면 파싱이 깨진다
- 셸에서 백틱·`${}`가 든 GDScript를 `node -e`로 넣으면 셸이 먹는다.
  스크래치패드에 `.cjs`를 쓰거나 Edit 도구를 쓴다

---

## 02.SvelteElectron에서 무엇을 읽나

**고치지 않는다. 읽는다.**

**① 동작 명세** — 어떤 규칙이 왜 그 값인지가 커밋 메시지와 주석에 있다.
실측 근거까지 같이 있어서 옮길 때 다시 찾을 필요가 없다:

```
드래프트 앵커   round = 11 - (score-78)*0.40 · 문턱 78
                (백분위 93 = score 88 → 7R · NPC 실측 근거)
수상 자격선     다승·탈삼진 40 · 방어율 45
                (45일 때 이닝 중앙 44.3이라 분포 한가운데 앉아 있었다)
잠재력 범위     random(80~99)
                (프리셋 최고 스탯 78보다 위여야 성장이 안 막힌다)
교체 규칙       max_outs = 12 + (스태미나/99)*15
                (NPC와 같은 식. 주인공만 스태미나 문턱을 쓰던 걸 고쳤다)
```

**② 검사 53개** — 비싸게 배운 게 코드로 박혀 있다.

### 02에 미해결로 남긴 것 (이주 후에 다시 볼 목록)

- `MainPage` 등판 회피가 선수 기록 없는 점수만 만든다
- `syncProtagonistLeagueUpdate`가 로테이션·피로를 안 건드린다 (주인공 팀 한정)
- `test:rosterbalance` 4건 — 시즌말 포수 0명
- 이야기 이벤트 87% 유실
- 동료가 떠난 뒤에도 `together`
- 소식함 200통 포화

---

## 지금 파일 구조

```
04.GodotOnePitch/
  CLAUDE.md          코드 규칙 (금지 사항·성능 규칙·함정)
  docs/RESUME.md     이 문서 — 진행 현황 정본
  docs/shots/        화면 스크린샷
  fonts/             Pretendard + OFL.txt
  sim/               npc_store · rng · match_sim (P0 뼈대 — M3에서 지운다)
                     standings · season_stats · match_result · bracket
                     leaderboard · career_summary · digest        (M1)
                     tuning · baserunning · batted_ball · pitch_outcome
                     pitcher_switch · match_state · pitch_step
                     game_loop · match_report                     (M2)
  ui/                theme · fixtures · parts/*.tscn · screens/*.tscn
  test/              GdUnit4 검사 · support/scripted_rng.gd
  tools/             run.gd(계측) · shot.gd(스크린샷) · mutation/(변이 검증)
  bench/             fixtures · game_bench · save_bench · rng_probe
  addons/gdUnit4/    6.2.1
```
