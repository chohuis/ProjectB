# 경기 화면 02 ↔ 04 대조 (M-0)

**2026-08-17** · 02 `pages/match/MatchPage.svelte`(3,220줄) +
`features/match-view/ui/BaseballField.svelte`(262) **= 3,482줄**
vs 04 `ui/screens/match_screen.gd`(254) + `match_vm.gd`(181) +
`pitch_vm.gd`(111) + `briefing_vm.gd`(119) **= 665줄**

> **고치기 전에 센다.** F-4c에서 "구장 이름 데이터가 없다"고 판단했다가
> 02 `refs.json`에 27개가 다 있는 걸 뒤늦게 찾았다 — **없는 것과 안 옮긴
> 것을 먼저 가른다.**

---

## 대조표

| 02 패널 | 무엇을 보여주나 | 04 상태 | 04에 데이터가 있나 | 근거 |
|---|---|---|---|---|
| `scene-panel` | 장면 제목 · 이닝 배지 · 선택한 수비 위치 | **부분** — 이닝만 | 이닝 ○ / 수비 위치 선택은 **기능 자체가 없다** | `MatchPage:1505-1512` · `match_screen.gd:76` |
| `lineup-panel` ×2 | **양 팀 타순 9명** · 현재 타자 · 다음 타자 강조 | **없음** | ○ `away_lineup`/`home_lineup` + `away_index`/`home_index` | `MatchPage:1515-1573` · `game_loop.gd:20-26` |
| `play-text-panel` | 중계 텍스트 (한 줄씩 쌓임) | **있음** | ○ `ctx.log` | `MatchPage:1576-1583` · `match_screen.gd:186` |
| `situation-panel` | **베이스 다이아몬드** + **S/B/O 램프** | **부분** — 글자로만("2·3루" · "카운트 1-2" · "2아웃") | ○ 전부 | `MatchPage:1591-1631` · `match_screen.gd:88-93` |
| `zone-panel` | 존 캔버스 · **마지막 투구가 떨어진 점** · 호버 · 타깃 | **있음**(M-7) — 버튼 3×3 + 존 밖 + 착탄 점. 호버·타깃 미리보기는 04에 없다(마우스 추적 대신 칸 선택이다) | ○ `state["last_landing"]` | `MatchPage:1641-1663` · `pitch_step.gd:115-122` |
| `pitch-select-panel` | 구종 슬롯 + **숙련도 막대** + **빈 칸(잠금)** + **선택별 소모/남은 구수** + 전략·세기 | **있음**(M-6) — 숙련도는 점 다섯 칸(자리가 좁다) | ○ 전부 | `MatchPage:1666-1770` · `pitch_vm.gd` |
| `watch-panel` | **관전 중** — "아직 등판하지 않았다" / "교체돼 벤치에 있다" + 점수 | **없음** — 선택 화면만 숨긴다 | ○ `is_my_pitch` · 교체 여부는 `_my_line`이 안다 | `MatchPage:1776-1783` · `match_screen.gd:121-124` |
| `info-panel`(타자) | 타자 **능력치 막대** ↔ **시즌 성적** (뒤집기) | **부분** — 이름만. 브리핑에 OVR·태그가 있으나 **첫 공 전에만** | ○ 능력치 / 시즌 성적은 `season_stats`에 있다 | `MatchPage:1790-1822` · `match_screen.gd:96` |
| `info-panel`(투수) | **체력·멘탈 막대** + 구속 + 투구수 ↔ 시즌 성적 | **부분** — "6.0이닝 5K 1BB 4H 1자책"뿐. **체력·멘탈이 없다** | ○ `home_stamina`/`home_mental` (`game_loop.gd:149-151`) | `MatchPage:1824-1860` · `match_vm.gd:104` |

### 04에만 있는 것 — **맞춘다고 지우지 않는다**

| | 무엇 | 왜 |
|---|---|---|
| **브리핑** (F-5) | 상대 선발 구위·무브먼트·커맨드 + 상대 타선 9명의 OVR·위협 태그 | 02엔 없다. 04는 "상대가 누구인지" 첫 공 전에 보여준다 |
| **구장 그림 27장** | 홈 팀 구장에서 경기한다 | 02는 **프로 구장 하나가 하드코딩**이라 고교·대학 경기도 프로 구장에서 열렸다 |

---

## 무엇이 제일 급한가

기준은 **"보기 좋은 것"이 아니라 "못 보고 결정하는 것"**이다.

### 다시 센 결과 (2026-08-18)

⚠ **이 표가 낡아 있었다.** M-1~M-5를 "급하다"로 적어 뒀는데 **다섯 다 이미
붙어 있다.** 캡처(`shot.gd -- match`)로 눈으로 확인했다 — 문서를 믿고
다시 만들 뻔했다. **적힌 판정을 믿지 않는다**는 규칙이 여기서도 맞았다.

| 순위 | 항목 | 상태 | 어디에 |
|---|---|---|---|
| **M-1** | 투수 체력·멘탈 | ✅ 닫음 | `match_screen._build_vitals` · `has_pitcher_vitals` |
| **M-2** | 타순 + 현재 타자 | ✅ 닫음 | `MatchVm.lineup_rows` · 양 팀 9명 · 현재/다음 강조 |
| **M-3** | 베이스 다이아몬드 + S/B/O 램프 | ✅ 닫음 | `ui/parts/situation_board.gd` · `_build_situation` |
| **M-4** | 관전 패널 | ✅ 닫음 | "교체돼 벤치에 있다" · 이닝·점수 |
| **M-5** | 타자 능력치 ↔ 시즌 성적 | ✅ 닫음 | `batter_bars` · `batter_season` · 뒤집기 버튼 |
| **M-6** | 구종 슬롯 숙련도·빈 칸·남은 구수 | ✅ 닫음 | `PitchVm.slots_of` |
| **M-7** | 마지막 투구 착탄 점 | ✅ 닫음 | `state["last_landing"]` |

⚠ **성능 줄도 낡았다.** "게이트 1.0초에 0.979로 여유 2%"라고 적혀 있었는데
**게이트는 30초**(사용자 확정)이고 실측은 **0.898~0.902초**다. 여유가 33배다.
그래도 **패널을 늘릴 때마다 `bench:day`를 단독으로 다시 잰다** — 경기 화면은
성능 경로다.

### 남은 것

대조표에서 아직 `부분`인 것은 **장면 제목·수비 위치**(`scene-panel`)다.
그 밖은 02가 스크롤로 늘어놓은 것을 04가 한 화면에 접은 차이라
**픽셀이 아니라 구조를 맞춘다**는 원칙대로 두 번 옮기지 않는다.

⚠ **02 화면을 그대로 베끼지 않는다.** 04는 1440×900 한 화면이고 02는
스크롤이 있다. **픽셀이 아니라 구조를 맞춘다.**

⚠ **경기 화면은 성능 경로다.** 패널을 늘릴 때마다 `bench:day`를 다시 잰다
— 게이트 1.0초에 지금 0.979로 **여유가 2%뿐이다.**
