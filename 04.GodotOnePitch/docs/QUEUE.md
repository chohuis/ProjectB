# 이주 작업 큐

**위에서부터 하나씩.** 모듈 하나를 끝낼 때마다 여기 체크하고 커밋한다.

⚠ **이 파일이 이어달리기의 바통이다.** 세션이 끊기거나 컨텍스트가 요약돼도
여기만 읽으면 정확히 이어진다. `RESUME.md`는 **왜 그렇게 했는지**를 적고
여기는 **무엇이 남았는지**만 적는다.

---

## U — UI/UX 보고서에서 나온 것 (2026-08-16)

근거는 `UX_REPORT.md`(제안) · `UX_02_SCREENS.md`(02 대조) · `UX_SHOTS.md`(캡처).
**밸런스에 안 닿으므로 지금 해도 된다.**

| # | 무엇 | 손질 범위 | 검사 |
|---|---|---|---|
| ✅ **U-1** | **`injury_history`를 아무도 안 채웠다.** 화면이 그 키로 카드를 만드는데 쓰는 곳이 `fixtures.gd`뿐이라 **진짜 게임에선 카드가 안 붙었다.** → `StatusVm._injury_history`가 `body_log`를 읽는다. `injury_runner`가 로그에 **해**를 같이 남긴다(날짜는 시즌마다 1로 돌아간다). 화면이 **후유증**도 말한다 | `status_vm.gd` +33 · `injury_runner.gd` +4 · `status_screen.gd` +5 | 검사 6 · **변이 6/6** · 캡처 `status-injury` |
| ✅ **U-2** | **병역이 하나도 안 보였다.** `sim/military.gd`가 매주 도는데 화면에 키가 없어 **전역이 언제인지 알 길이 없었다.** → "나" 탭에 병역 카드(현역/군필 알약 · 복무 형태 · 입대 연도 · **복무 막대 + "N주 남음"**). `Military.unit_label`을 만들어 이름의 정본을 하나로 뒀다 | `status_vm.gd` +27 · `status_screen.gd` +36 · `military.gd` +10 | 검사 8 · 캡처 `status-military` |
| ✅ **U-2b** | 복무 중인데 계약 카드가 **옛 소속(애월고)을 그대로 띄웠다.** `Military.enlist`가 `team_id`를 비우는데 `team_name`은 안 지운다 → `StatusVm.team_name_of`가 복무 중이면 부대명을 준다. **헤더도 같은 함수를 쓴다** — 두 자리가 다른 말을 하면 어느 쪽이 맞는지 알 수 없다. `LEAGUE_SHORT`에 `LEAGUE_MILITARY`("복무")도 넣었다 — 없으면 id가 원문 그대로 샌다. **이름을 상태에서 지우지는 않는다**(전역할 때 돌아갈 곳의 이름이 사라진다) | `status_vm.gd` +14 · `main_vm.gd` +5 | 검사 4 |
| ✅ **U-3** | **OVR을 어디에도 안 보여줬다.** 내 능력치를 보려면 팀 탭 로스터에서 내 줄을 찾아야 했고 피로는 훈련 화면에만 있었다 → 오른쪽 칸 맨 위에 **OVR · 컨디션 · 피로**를 상시로. 투수는 투구 OVR로 가른다. **피로 구간 이름표는 `TrainingVm`이 정본**이다 — 두 곳이 각자 정하면 훈련 화면과 껍데기가 다른 말을 한다 | `main_vm.gd` +25 · `main_screen.gd` +36 · `.tscn` | 검사 3 |
| ✅ **U-4** | **설정이 아예 없었다** — 창 크기도 못 바꿨다(PC가 1차 목표). → `Settings`(`user://settings.cfg`) + 설정 화면 + 타이틀 입구. 켤 때 `App._ready`가 물린다. **테마·언어·연출 속도·모션 줄이기는 안 넣었다** — 04는 한국어 단일·톤 하나이고 **"연출 속도"가 볼 값이 어디에도 없다**(`theme.gd:55`의 1.4초는 주석뿐). 스위치만 만들면 그게 또 죽은 배선이다 | `settings.gd` 신규 · `settings_screen` 신규 · `app.gd` +10 · `title_screen` +6 | 검사 8 · **변이 4/4** · 캡처 `settings` |
| | ⚠ **곁가지**: `Settings.apply()`가 `shot.gd`의 창 크기를 덮어 캡처가 1600×900으로 찍혔다. 프레임을 돌린 **뒤에** 크기를 정하도록 고쳤다 — 캡처 크기가 사용자 설정을 타면 **바이트 대조로 결함을 잡아 온 방식이 무너진다** | `shot.gd` | — |
| ✅ **U-5** | **키보드가 0건이었다** — `_unhandled_input`·`InputEventKey`·`KEY_SPACE`가 하나도 없었다. 02는 스페이스로 주를 넘긴다 → `MainScreen.handle_key`. 글자 치는 중이면 안 먹고, 버튼이 막혔으면 키로도 안 된다(두 입구가 다른 말을 하면 안 된다). 등판일엔 경기로 간다 | `main_screen.gd` +28 | 검사 5 · **변이 4/4**. ⚠ **`--ignoreHeadlessMode` 전제는 안 깼다** — 판정을 함수로 뽑아 검사가 `InputEvent` 없이 본다. 화면 검사를 헤드리스 밖으로 내보내면 CI가 갈린다 |
| ✅ **U-6** | 탭에 "나 / 세계" 구분선. **어디서 가를지는 사전이 정한다**(`MainVm.NAV_BREAK_AFTER`) — 화면이 다시 판정하면 탭 순서를 바꿨을 때 선만 옛 자리에 남는다. 마지막 탭 뒤엔 안 긋는다 | `main_vm.gd` +8 · `main_screen.gd` +8 | 검사 1 |
| ✅ **U-7** | **시작이 곧 덮어쓰기인데 버튼이 "시작"이라고만 했다.** 04에서 되돌릴 수 없는 유일한 동작이다. 타이틀은 "덮어쓰기"라고 말하는데 새 게임 화면에 들어오면 그 말이 사라졌다 → 찬 슬롯이면 **"덮어쓰고 시작"**(경고색). 깨진 슬롯도 찬 것으로 센다 | `new_game_vm.gd` +9 · `new_game_screen.gd` +12 · `app.gd` +14 | 검사 5 · **변이 3/3** |
| ✅ **U-8** | **관계 7색이 이웃끼리 안 갈렸다** (우호↔신뢰 1.07 · 신뢰↔각별 **1.06** — 1.0이 같은 밝기다). 02는 배지를 배경+글자 쌍으로 갈랐지만(밝은 테마) 04는 색 하나에서 배경·테두리·글자를 파생하므로 **그 색 자체가 갈려야 한다.** 색상 배치(나쁜=빨강 · 중립=무채색 · 좋은=파랑→초록)는 02 그대로 두고 **명도만 계단으로 다시 잡았다** — `(휘도+0.05)`가 한 칸마다 정확히 1.30배 | `theme.gd:45-70` | 검사 6 · **변이 8/8** · 캡처 `people-tones` |
| ✅ **U-9** | 빈 상태 문구. **거의 다 이미 맞았다** — `people_vm.gd:19-22`는 "한 주를 진행하면 관계가 생깁니다"까지 말하고 `finance_vm.gd:193`은 문턱 숫자를 말한다. 하나만 고쳤다: "수상자가 없습니다" → **"자격선을 넘은 선수가 없습니다"**. `awards.gd:149`가 "아무도 두 부문을 못 채우는 해가 있다"고 적어 뒀듯 **이게 정상인 해가 있는데** 옛 문구로는 버그인지 아직인지를 못 가른다 | `season_end_screen.gd` 1줄 | 없다 |
| ✅ **U-10** | **줄 정렬이 안 맞았다.** `BarRow`의 라벨 칸이 44px이라 네 글자 라벨("주자견제"·"무브먼트"·"위기관리")이 넘쳐 **막대 시작점이 줄마다 어긋났다.** 값 칸엔 폭도 정렬도 없어 자릿수가 바뀌면 막대 끝이 움직였다. → 라벨 76 · 값 52 우측 정렬. `InfoRow`도 같이 | `bar_row.tscn` · `info_row.tscn` | **검사 5 · 변이 4/4** |

**U-1~U-10을 전부 끝냈다** (U-8이 2026-08-17에 마지막으로 닫혔다).

⚠ 한때 "U-1~U-10을 다 했다"고 적었는데 **그때는 틀렸다.** U-8은 보고서에서
대비를 재기만 했고 `theme.gd`는 안 고쳤다 — 표를 다시 세어 잡았고, 이제
실제로 고쳤다.

⚠ **가장 큰 둘은 UI 문제가 아니었다.** `injury_history`(U-1)는 읽는 쪽만 있고
채우는 쪽이 없었고, 줄 정렬(U-10)은 부품 하나의 칸 폭이었다 — **화면 대조를
하다가 나왔지 화면을 보고는 못 잡았다.**

⚠ **검사가 아무것도 안 보고 있던 자리를 변이가 두 번 잡았다.** U-1에서
실재하지 않는 부상 id를 써서 등가 변이가 됐고, U-10에서 `clip_text`가 켜지면
`get_minimum_size()`가 글자 폭을 안 본다는 걸 놓쳤다. **둘 다 통과하는
검사였다.**

### F — 동결 해제 뒤 (사용자가 2026-08-16에 풀었다)

| # | 무엇 | 상태 |
|---|---|---|
| ✅ **F-1** | **구종 습득 시스템.** `world.gd:280`이 직구 하나를 박고 끝이었고 `week_runner`는 진행률을 **아무도 안 읽는 `p["pitch_dev"]`**에 쌓았다 — 소비처(`pitch_step`·`training_growth`)는 다 있는데 생산처가 없었다. → `sim/pitch_dev.gd` + `data/pitch_catalog.json`(**02 표 그대로**) + 훈련 화면 구종 칸 | 검사 20 · **변이 8/8** · 캡처 `training-pitch` |
| ✅ **F-1b** | 구종 계측을 다른 훈련과 섞어 다시 돌렸다. **섞을 수가 없었다** — 04엔 훈련 계획 자동 추천이 아예 없어서 자동 진행이 여덟 해를 굴려도 훈련을 하나도 안 했다(F-1 계측이 `TRN_PITCH_DEV`를 손으로 박은 이유). → `sim/auto_training.gd`(02 `runAutoAdvance.ts:274-306` + `:246-272` 그대로) + `AutoAdvance.next_step`이 부른다. **그 길에 사기 축이 통째로 빈 것을 찾았다**(아래 F-1c). **결론: 값은 문제가 아니었다** — "전부 5등급"은 극단에서만 나온다 | 검사 18 · **변이 14/14** |
| ✅ **F-1c** | ⚠ **사기(`morale`) 축에 채우는 쪽이 없었다.** 읽는 쪽은 셋인데(`CoachReport`·`GameGrowth`·`AutoTraining`) `World.new_game`이 안 넣어서 `get("morale", 기본값)`에 기대고 있었다 — 오류도 로그도 안 나고, 증상은 **자동 훈련이 여덟 해 내내 "사기 낮음" 갈래만 돌아 구종을 하나도 안 배우는 것**으로 나왔다. 02 `NewGamePage:282`가 70이다. 기본값도 모듈마다 달랐다(50 vs 0) | |
| 🔶 **F-2** | **계약 협상.** 04는 "계약한다/거절한다" 둘뿐이었다 — 협상이 아니라 통보다. → `sim/negotiation.gd`(02 `ContractNegotiationModal.svelte:58-88` 값 그대로: ±20% · 문턱 1.15배 · 기간 3%/해 · 노트레이드 0.95 · 옵션 ±/∓ · 확률 95 − 초과×400). **계산은 끝났고 화면이 남았다** | 검사 16 · **변이 14/14** |
| ✅ **F-2b** | **협상 화면.** `kind: "negotiate"` + 연봉 슬라이더 · 기간/옵션 스테퍼 · 노트레이드 · 수락 가능성 막대 · 역제안. **`Relationship.effects`의 `contract_bonus`가 여기서 처음 쓰인다**(소비처 0건이었다). 붙이면서 **계약 연수가 0으로 서명되던 기존 결함**을 찾았다 — `_contract_of`가 `years`로 적는데 엔진은 `duration_years`를 읽는다 | 검사 16 · **변이 9/9** |
| ✅ **F-2c** | **관계 효과 나머지 둘.** `role_ovr_bias`는 `Rotation.assign_position`에(감독이 보는 나 = OVR + 편향, 02 `player_engine.rs:101-102` 그대로), `training_bonus`는 `WeekRunner.coach_efficiency`에 이었다 — **02는 능력치와 관계를 더한 뒤 한 번에 자른다**("각각 clamp하면 상한이 두 배가 된다"). 04엔 그 clamp가 아예 없었다. **`Relationship.effects` 셋이 전부 살았다** | 검사 15 · **변이 3/3 + 7/7** |
| ✅ **F-3** | **`TeamProfile` 초기화.** 축 열둘 중 아홉이 영원히 50이었다 — `update_all`은 둘만 갱신하고 **만드는 쪽이 없었다.** 그래서 **buyer가 구조적으로 0팀**(문턱 60, 전부 50)이라 02가 트레이드를 잃은 그 자리에 04도 있었다. → `data/team_profiles.json`(ABL 16 = 02 값 그대로) + `TeamProfile.derive`(KBL, **파생 규칙은 02 ABL 16팀에서 쟀다**) + `spread_of`(JBL, 축이 없어 02 분포를 시드로 흩는다) + `World.build`가 부른다 | 검사 17 · **변이 13/13** |
| ✅ **F-3b** | 팀 평가 **화면** — **아래 F-4b와 한 줄로 닫혔다.** 이 칸이 ⬜로 남아 열여섯 번째 "이미 끝났는데 미완으로 남은 줄"이 됐다(2026-08-17에 코드로 확인하고 고침) | ↓ F-4b 참조 |
| ✅ **F-4a** | **선수 상세.** 로스터 줄이 `HBoxContainer`라 **아무것도 안 눌렸다** — 서른 줄이 뜨는데 한 줄이 주는 게 포지션·이름·나이·OVR 넷이라 트레이드도 드래프트도 "이름과 숫자 하나"로 판단해야 했다. → `ui/player_detail_vm.gd` + `player_detail_screen`. `PlayerRow`를 `Button`으로. **주인공을 누르면 "나" 탭으로 보낸다**(두 화면이 같은 사람을 다르게 그리면 안 된다). 캡처가 **팀 이름이 `TEAM_HS_AEWOL`로 새는 것**을 잡았다 — `team_name`은 주인공만 갖는 키다 | 검사 27 · **변이 10/10 + 6/6** · 캡처 `player-detail` |
| ✅ **F-4b · F-3b** | **팀 상세 + 구단 성향 화면.** 순위표 줄이 `HBoxContainer`라 안 눌렸다 — 다른 팀이 어떤 구단인지 볼 입구가 없었다. → `ui/team_detail_vm.gd` + `team_detail_screen`. **02의 "팀 평가"는 못 옮긴다**(02는 명성 등급·팬덤·시설·분위기인데 04엔 그 데이터가 없다) — 대신 **F-3으로 살린 열두 축**을 보여준다. 팀 → 선수로 들어가고 되돌아온다 | 검사 20 · **변이 9/9** · 캡처 `team-detail` |
| ✅ **F-4c** | **국내 구장 이름이 화면에 id로 샜다.** 182팀의 `stadium`이 전부 `STADIUM_SEOUL_ROYALS` 꼴이었다 — **없는 게 아니라 안 옮긴 것**이었다. 02 `refs.json`의 `stadiums` 27개가 이름을 다 갖고 있었고 02는 `TeamDetailModal.svelte:41-42`에서 그 표를 찾아 쓴다 → `data/parks.json`의 `names` + `ParkVm.name_of()`. 임시로 두었던 ASCII id 판정(`_stadium_name`)은 걷어냈다. **02의 `id.replace(/^STADIUM_/,"")` 갈래는 안 옮겼다** — 실측 238팀 중 표에 없는 id꼴이 **0팀**이라 죽은 코드가 된다 | 검사 6+2 · **변이 3/3** · 캡처 `team-detail` |
| ✅ **F-9** | ⚠ **`GameGrowth`를 아무도 안 불렀다** — 경기 뒤 성장(능력치 XP · 사기 · 명성 · 피로)이 한 경기도 안 움직였다. **열네 번째 죽은 배선이고 제일 크다.** `Staff.mods_of`의 `morale`·`fame`도 소비처가 여기뿐이라 같이 죽어 있었다(감독 동기부여 · 구단주 홍보력). → `sim/match_outcome.gd`(02 `applyGameOutcome.ts:242-259` 그대로) + `app_root._record_match`가 한 줄로 부른다. **무승부를 승리로 안 읽는다** — 04 `to_match_result`는 무승부에 `winner_id`로 홈팀을 넣으므로 그대로 읽으면 홈 무승부마다 사기가 오른다. 시작값 셋도 02로 맞췄다(`fatigue 10`·`condition 80`·`fame 5` — **명성도 채우는 쪽이 없었다**) | 검사 16 · **변이 12/12 + 3/3** |
| ✅ **M-0** | **경기 화면 02↔04 대조표.** 02 3,482줄 vs 04 665줄 — 02의 아홉 패널을 하나씩 열어 04 상태와 **데이터 유무**를 표로 냈다(`docs/MATCH_PARITY.md`). **없는 것과 안 옮긴 것을 갈랐다** — 대부분 데이터는 04에 이미 있고 화면만 없다. 04에만 있는 것(브리핑 F-5 · 구장 27장)도 적었다 | |
| ✅ **M-1** | **투수 체력·멘탈이 화면에 없었다** — 교체 판단의 입력인데 "6.0이닝 5K"만 보였다. 데이터는 `game_loop.gd:149-151`이 이미 들고 있었다 → `MatchVm.vital_level`(02 `MatchPage:706-707` 문턱 그대로 `>60`/`>30`) + `AppTheme.vital_color` + `_build_vitals`. **축이 없으면 줄을 안 만든다** — 0으로 그리면 모르는 것이 바닥으로 보인다 | 검사 10 · **변이 8/8** · 캡처 `match-briefing` |
| ✅ **M-2** | **양 팀 타순이 없었다**(02 `lineup-panel` ×2). 브리핑은 첫 공 전에만 뜨고 사라져 투구 중엔 다음 타자를 몰랐다 → `MatchVm.lineup_rows` + 오른쪽 열에 나란히. **공격 중인 팀만 타석 표시**(02 `:144-145`)하고 인덱스는 `%`로 돌린다(한 바퀴 넘으면 아무도 타석에 없다). 02는 구장 양옆에 두는데 04는 구장이 폭을 다 써서 자리를 옮겼다 | 검사 13 · **변이 10/10** · 캡처 `match-mine` |
| ✅ **M-3** | 베이스 **다이아몬드**와 **S/B/O 램프**가 글자였다("2·3루" · "카운트 1-2") → `ui/parts/situation_board.gd`(`_draw()`). 램프 개수·색은 02 그대로(S2·B3·O2 / 초록·노랑·빨강). ⚠ **02의 다이아몬드는 대칭이 아니다**(`left:52`인데 104폭에 16짜리 베이스의 중앙은 44) — `_draw()`로 그리는 04는 **비율로 대칭**으로 뒀다. `_draw()`는 검사하기 어려우므로 좌표·색·개수를 **static으로 빼서 실제로 불러 본다** | 검사 10 · **변이 9/9** · 캡처 `match-mine` |
| ✅ **M-4** | **관전 중인데 화면이 아무 말도 안 했다.** 04는 `is_my_pitch`로 선택 화면을 접기만 하고 **접고 나서 설명을 안 했다** — 02는 그 자리에 "비활성화가 아니라 접는 것"이라는 근거까지 적어 뒀다. ⚠ **`_my_line`으로는 판단 못 한다** — 내 줄이 없으면 지금 마운드에 선 사람 것으로 폴백해서, 한 구도 안 던진 내가 "교체돼 벤치에" 있는 것이 된다(`_has_pitched`를 따로 뒀다) | 검사 9 · **변이 9/9** · 캡처 `match-watch` |
| ✅ **M-5** | 타자 정보 — **04는 이름만 보여줬다.** 브리핑(F-5)이 OVR·태그를 주지만 첫 공 전에만 뜬다 → 앞면 능력치 다섯(02 `statCard.ts:38-44` 그대로) / 뒷면 시즌 성적. ⚠ **뒷면은 실제 기록만 그린다** — 없으면 0이 아니라 "기록 없음"이다(0으로 채우면 안 뛴 것과 못 친 것이 안 갈린다). 타석은 있는데 **타수가 0인 경우**(볼넷만 골랐다)를 따로 본다. `LiveMatch`가 `ctx`에 `season_stats`를 넣는다 | 검사 9 · **변이 10/10** · 캡처 `match-mine` |
| ✅ **M-6** | 구종 **슬롯** — 04는 배운 것만 보여줘 "몇 개 더 배울 수 있나"가 안 보였다 → `PitchVm.slots_of`(02 `pitchSlots.ts:38-53`)로 빈 칸을 상한까지 잇고, 숙련도를 **점 다섯 칸**으로(02는 막대인데 04는 자리가 좁다), **선택별 소모/남은 구수**를 붙였다. ⚠ **소모 식은 `Tuning`이 정본**이고 화면은 읽기만 한다. ⚠ **02 주석 둘을 지켰다** — 스태미나로 구종을 흐리게 안 만들고(엔진이 그렇게 안 막는다), 빈 칸에 구종 이름을 안 적는다(특정 구종의 자리가 아니다) | 검사 12 · **변이 11/11** · 캡처 `match-mine` |
| ✅ **M-7** | 존에 **마지막 투구가 떨어진 점**. 착탄 좌표가 `pitch_step.step` 안에서만 쓰이고 버려졌다 → `state["last_landing"]`로 남긴다(`last_pitch_types`와 같은 방식). ⚠ **겨냥한 곳이 아니라 떨어진 곳**이다 — 겨냥을 남기면 빠진 공이 안 보이고, 변이가 실제로 그걸 뚫었다. 자리 계산은 `MatchVm.landing_pct`/`landing_point`(02 `landingToPct` `:267-272`, 캔버스 10~90% · 8~92%)가 갖고 화면은 `Zone/Box/Dot`을 옮기기만 한다. ⚠ **안 던졌으면 안 찍는다** — 한가운데에 찍으면 안 던진 것과 구분이 안 된다 | 검사 13 · **변이 10/10** · `bench:day` 재측정 |
| ✅ **P-2c** | **밴드 순서를 바로잡았다** — 38 밴드(타자에게 더 유리)에 홈런이 없어 45 밴드보다 장타가 적던 것. **3루타가 아니라 2루타를 갈랐다**(실험②가 3루타를 갈라 볼넷을 6%p 밀었다) → **홈런 −23% → −4%.** 볼넷 대가는 실험②와 거의 같은데(+9% vs +10%) 홈런은 세 배 더 왔다. **±15% 안에 든 축이 둘 → 셋.** ⚠ **볼넷이 왜 느는지는 아직 모른다** — 홈런이 늘면 주자가 사라져 `has_scoring` 가중이 줄어야 하는데 반대다. ⬜ P-2e로 남겼다 | 3시드 실측 |
| ⬜ **P-2e** | **홈런을 늘리면 볼넷이 는다** — 인과를 모른다. `dispersion_sigma`의 `has_scoring`·`is_full_base` 가중을 생각하면 반대여야 한다. 세 시드가 일관되므로 잡음이 아니다 | |
| ⬜ **P-2f** | **3루타를 세는 계측이 없다.** `player_lines`의 타자 줄에 `ab·h·hr·rbi·bb·k·sb`뿐이라 2루타·3루타를 못 센다 — 밴드를 더 만질 때 필요하다 | |
| ~~⬜ **P-2c**~~ | ~~**컨택 밴드에서 홈런 순서가 뒤집혀 있다.**~~ P-2b ①로 `contact_q >= 45` 밴드에 홈런이 생겼는데 **38 밴드(타자에게 더 유리한 쪽)에는 없다** — 유리할수록 장타가 늘어야 하는데 그 반대다. 실험 ②(38 밴드의 3루타를 갈라 홈런 0.7%)는 홈런을 3%p 당기는 대신 **볼넷을 6%p 밀어서 되돌렸다**(세 시드 모두 3.6 — 잡음이 아니다). **3루타 대신 2루타를 갈라 보는 것**이 다음 수다: 3루타는 주자를 3루에 세워 다음 타석의 압박 상황을 만들어 볼넷을 민다. 04는 3루타가 홈런보다 많을 수 있는데 그것도 같이 본다 | |
| ✅ **F-4d** | **구장 성향(파크팩터)** — 먼저 세어 보니 **02도 엔진엔 안 먹인다.** `parkFactor`는 타입 정의와 새 게임 화면 둘뿐이고 **Rust 엔진엔 0건**이다. 엔진의 `park`는 다른 축인데 **그쪽도 죽어 있다** — 값을 넘기는 `MatchPage:763`의 `matchPark`가 `"neutral"`로 시작해 snapshot으로만 갱신되므로(`:655`·`:865`) 늘 중립이고, 문자열을 `ParkType`으로 바꾸는 코드도 없다. → **표시용으로만 옮겼다**(`parks.json`의 `factors` 27개 + `ParkVm.factor_of` + 팀 상세 머리줄). **엔진에 먹이는 건 이주가 아니라 새 밸런스라 안 했고, 검사가 그 선을 지킨다**(경기 파일 넷에 `factor_of`가 없는지 본다) | 검사 7 · **변이 4/4** · 캡처 `team-detail`("로열파크(중립)") |
| ✅ **F-5** | **경기 전 브리핑.** 04는 "지금 무슨 일이 벌어지나"(점수·이닝·카운트·주자)는 다 보여주는데 **상대 타자가 이름 한 줄**이라 승부처인지를 알 수 없었다 → `ui/briefing_vm.gd`(02 `threatLevel` 문턱 그대로) + `match_screen._build_briefing()` | 검사 13 · **변이 9/9** · 캡처 `match-briefing` |
| ✅ **F-5b** | **타순에 투수가 꼈다 — 112팀 전부, 심하면 다섯 명.** 옛 주석은 "정렬이 거른다"고 했는데 **투수 타격이 야수보다 낮다는 전제가 진짜 세계에선 안 맞는다.** 검사는 통과했다 — 가짜 로스터가 투수 20 · 야수 40+이라 정렬만으로 걸러졌다. → 02 `rosterEngine.ts:406-423` 그대로 **야수 먼저, 모자라면 타격 나은 투수로 채운다.** 실측: 타율 +14% → **+2.6%** · 피안타 10.5 → 9.1 · ERA 6.17 → 5.04 | 검사 5 |
| ✅ **F-6a** | **NPC의 투구·타격 방향이 없었다.** 02 `roster_gen.rs:558`·`:727`이 투수 30% 좌완 · 야수 35% 좌타로 뽑는다 → `PlayerGen.handedness_of()` + 선수 상세 머리줄. **흐름 난수를 안 쓰고 id 해시로 뽑았다** — 02처럼 `roster()` 안에 `rng.next()`를 하나 더 끼우면 그 뒤 모든 난수가 밀려 세계가 통째로 달라진다(실측이 무효가 된다). 검사가 능력치 세 값을 못 박아 그걸 지킨다 | 검사 9 · **변이 6/6** · 캡처 `player-detail`("17세 · 우타") |
| ✅ **F-6b** | 주인공의 방향 · **투구 폼** · **생년월일** — 새 게임 화면에 입력이 없었다 → `NewGameVm`에 선택지·기본값·날짜 당김 + 씬에 위젯 다섯 + `World.new_game`이 생성값을 덮음 + "나" 탭 **선수 정보** 카드. **폼은 셋만 옮겼다** — `threeQuarter`는 02 타입·라벨엔 있는데 `formOptions`에 없어 고를 수 없다. ⚠ **02에서도 `pitchingForm`은 저장만 되고 시뮬에 안 먹인다** — 표시용으로 옮기고 그렇게 적었다. 생년 2010은 04 시작 2027·17세와도 맞는다 | 검사 10+6 · **변이 11/11** · 캡처 `new-game` · `status-profile` |
| ✅ **F-7** | **주인공 재계약 오퍼.** 금액을 내는 코드가 아예 없었고(두 곳이 이미 있는 값을 옮겨 담을 뿐), **계약이 0이 됐을 때 물어보는 코드도 없었다** — 02가 "2031년 만료 계약이 2038년까지 남은" 그 자리다. → `Contract.protagonist_offer`·`season_rating`(02 `player_engine.rs:282-311` 그대로) + `ContractDecision.ask_on_expiry` + `SeasonRunner.finish_season`이 부른다. **스태프 `budget` 계수가 여기서 처음 쓰인다** | 검사 24 · **변이 10/10 + 8/8** |
| ✅ **F-8** | **선택지가 든 소식을 만드는 곳이 없었다** — `fixtures.gd`뿐이었다. → `sim/coach_report.gd`(02 `advanceWeek.ts:1400-1484` 그대로: **3주마다** · 네 갈래 · 효과 수치까지) + `WeekRunner`가 부른다 + `CoachReport.apply`가 효과를 건다. ⚠ **진행은 안 막는다** — 02도 `pendingAction`을 안 민다. 막게 뒀더니 **같은 28일이 22일과 29일로 갈렸다**(주간 처리가 진행 뒤에 몰려 돈다) | 검사 28 · **변이 15/15** |
| ✅ **F-8b** | **소식 화면에서 답할 자리가 없었다.** `NewsVm`이 `pending`이라고 표시만 하고 **선택지 자체를 안 실었다** — 화면엔 "답해야 한다"만 뜨고 누를 게 없었다. → 행에 `prompt`·`choices`·`selected_label`을 싣고 화면이 버튼을 붙인다. **여기가 유일한 입구다**(코치 리포트는 날을 안 막으니 자동 진행이 볼 일이 없다) | 검사 6 · **변이 5/5** · 캡처 `news-decision` |
| ❓ **F-8c** | **`AutoAdvance.pick_choice`는 아직 실제 게임에서 안 돈다** — 날을 막는 소식이 0건이기 때문이다. 앞으로 "답해야만 넘어가는" 소식(02 `campusEvents` 같은 것)이 생기면 그때 살아난다. **지금 억지로 만들지 않는다** — 코치 리포트를 막게 뒀더니 같은 28일이 22일과 29일로 갈렸다 | |

**F-3 조사 (2026-08-17) — 시작하기 전에 읽는다.**

`TeamProfile.update_all`은 열둘 중 **둘만** 갱신한다(`win_now_pressure` ·
`scouting_quality`). 나머지 열은 초기화가 없어서 `of()`가 늘 중립 50을 준다.

02는 **팀 프로필을 마스터 데이터로 갖고 있다** —
`resource/data/master/teams/<리그>/<팀>.json`의 `teamProfile` 열두 축.
손으로 정한 값이고 팀 성격이 뚜렷하다(뉴욕 엠파이어는 spending 90 ·
prestige 88 · development 30, 샌디에이고는 spending 35 · development 74).

⚠ **그런데 04의 팀은 02와 같은 팀이 아니다.** 실측:

| 리그 | 04 1군 | 02에 대응 | 04가 가진 축 |
|---|---|---|---|
| ABL | 16 | **16 전부 id가 같다** | — |
| KBL | 10 | **0** (부산 웨이브스 ≠ 부산 자이언트웨일스) | `power` 2~5 |
| JBL | 12 | **0** (02엔 JBL 리그 자체가 없다) | 없음 |

그래서 한 갈래로 못 옮긴다. **셋으로 갈린다:**

1. **ABL 16 — 02 값을 그대로 가져온다.** id가 같으니 근거가 확실하다
2. **KBL 10 — `power`(2~5)에서 파생한다.** 파생 규칙을 지어내지 말고
   **02 ABL 16팀에서 재라** — 그쪽은 열두 축이 다 있으니 "강한 팀은 어느
   축이 높고 어느 축이 낮은가"를 실제로 계산할 수 있다. 눈으로 보이는
   구조: prestige가 높으면 spending·win_now·scouting·medical·market도
   높고 development_focus·patience는 낮다. stability·discipline·
   clubhouse·farm은 팀 세기와 거의 무관하다(55~70의 좁은 띠)
3. **JBL 12 — 축이 하나도 없다.** ❓ 중립 50이 유일하게 정직한 답인지,
   리그 평균을 주는 게 나은지 판단이 필요하다. **JBL만 전부 50이면
   `fa_runner`·`promotion_runner`·`release`가 그 리그에서만 다르게
   움직인다** — 지금도 그런 상태이므로 새 결함은 아니지만, 고칠 때
   같이 봐야 한다

⚠ **F-4b(팀 상세)가 여기 걸려 있다.** 축 아홉이 50인 채로 팀 평가 절을
만들면 **화면이 거짓말을 한다** — 모든 팀이 똑같아 보인다.

**F-1에서 나온 것**: 새 게임 주인공은 커맨드 48이라 **슬라이더(52)·체인지업(멘탈
50)·스플리터(제구 52)가 다 잠겨 있다.** 처음 열리는 건 커브(무브먼트 50) 하나다 —
02 문턱 그대로이고, 이제 그게 화면에 숫자로 보인다.

**실측** (`measure:pitch`, 12커리어 × 8해, 구종 개발만 1순위로):
```
보유 구종 수   최소 2 · 중앙 5 · 최대 5   ← 상한(5)에 닿는다
첫 습득 주차   전부 2주
숙련도 분포    5등급 49개                 ← 나머지 등급이 하나도 없다
```
⚠ **여덟 해 내내 구종 개발만 돌리면 전부 마스터가 된다.** 산식이 02 그대로이니
(`progress_per_week 24` · `fatigue_cost 4` — `resource/data/master/training/programs.json`
정본과 일치) 02도 같았을 것이다. **다만 실제 플레이는 이렇게 안 한다** —
한 축만 8년 돌리면 능력치가 안 큰다. 이 값이 문제인지 아닌지는 **다른 훈련과
섞어 돌려 봐야** 알 수 있다. ⬜ **F-1b로 남긴다.**

⚠ **`dist`/`release`의 `programs_pitcher.json`은 낡았다**(`progressPerWeek: 20`).
값을 대조할 땐 `resource/data/master/`를 본다 — 빌드 산출물을 보고 "이식
오류"라고 판단할 뻔했다.

---

## 모듈 하나의 리듬 (건너뛰지 않는다)

```
① 02 원본을 읽는다        — 값과 근거를 그대로 옮긴다. 밸런스 동결
② 검사를 먼저 쓴다
③ 빨간불 확인            ← 통과하면 그 검사가 아무것도 안 보는 것이다
④ 로직을 옮긴다
⑤ 배선한다               — 안 부르면 없는 것과 같다 (02가 그 결함투성이)
⑥ 실측한다               — measure:*  세계로 굴려 본다
⑦ 변이 검증              — 100% 아니면 검사를 보강한다
⑧ 전체 검사 · 고아 0
⑨ 화면을 바꿨으면 띄워서 본다
⑩ 커밋 · 이 파일 갱신
```

## 막혔을 때 — **멈추지 않는다. 적어 두고 다음으로 간다**

**02가 완제품이라 답은 거의 다 거기 있다.** 값·순서·근거가 커밋 메시지와
주석에 적혀 있으니 물을 게 없다. 그대로 옮긴다.

### **02를 실제로 돌려서 물어본다** — 문서에 없어도 답이 나온다

**02는 죽은 코드가 아니라 살아 있는 대조 장치다.** Rust 엔진이 이미 빌드돼
있고(`engine-native.win32-x64-msvc.node`) `node_modules`도 있어서, Electron
없이 `npm run ...`이 그대로 돈다:

```bash
cd 02.SvelteElectron
npm run test:fa · test:growth · test:draft · test:promotion · test:career
npm run measure:draft · measure:devplayer · measure:training · measure:relations
npm run test:rosterbalance · test:npclifecycle · test:careerpaths
node -e "const d=require('./resource/data/master/players/generation_rules.json'); ..."
```

⚠ **읽기만 한다.** 돌리는 건 소스를 안 바꾼다 — 로그·임시 DB만 쓴다.
02 파일을 **고치지는 않는다.**

실제로 이렇게 값 셋을 추측에서 실측으로 바꿨다:
`developmentPlayerRules.intakeMax` 10 · `salary` 2000 ·
대학 한 해 정원 = `rosterSize / gradeMax` = 8.

02를 돌려도 답이 안 나올 때만 아래에 ❓로 적고, 제일 그럴듯한 값을 골라
**근거를 주석에 남긴 뒤 계속 간다.** 멈추면 루프가 죽는다.

02에 답이 없는 부류는 셋뿐이다:

1. **04가 02와 다르게 정한 것의 파생** — 일 단위 진행 · 해외 풀 시뮬 ·
   1440×900 가로. 02엔 대응물이 없다
2. **02의 명백한 결함** — 그대로 옮길지 고칠지는 밸런스 판단이다.
   **기본은 그대로 옮기고 검사로 못 박는다**(대조가 깨지면 안 되니까)
3. **성능 게이트** — 02는 Electron이라 기준 자체가 다르다

### 묻어 둔 질문

- ❓ **관계 기억(`memories`)을 각인하는 곳이 아무 데도 없다 — 02에도 없었다.**
  02의 `addRelationMemory`는 호출자가 **0개**이고, 그런데도 `PeoplePage`는
  기억 목록을 그렸다(영원히 빈 목록이었다). 04도 `RelationshipRunner.add_memory`
  를 검사만 부른다. **인물 화면에서 뺐다** — 아무도 안 쓰는 데이터를 그리는
  자리를 만들면 죽은 갈래가 된다. 각인 시점(데뷔전·완봉·방출·배신)을 정하는
  건 설계 결정이라 이주 중에 만들지 않는다. 정하면 그때 화면에 붙인다

- ❓ **해외 팜 리그 연봉 배수**가 02에 없다. 국내 1군:2군 비(0.3배)로
  파생했다 — `data/salary_rules.json`. **02는 해외 팜 연봉을 아예 안 만든다**
  (`leagueMult`에 항목이 없다). 돌려도 답이 안 나오는 부류다

- ❓ **수비 훈련(`defense`)에 대응하는 코치 영역이 없다.** 02
  `relationship_rules.json`의 `training_area`가 11종을 잇는데 `defense`만
  빠져 있다 — 수비 훈련을 짠 주는 **어느 코치도 안 오른다.** 02 그대로
  옮겼다. 스태프(B-2b)에 수비 코치가 있는지 보고 다시 판단한다

- ❓ **02의 독립리그 지망 순서가 주석과 코드가 어긋난다.** 주석은 "위로
  지원할수록 조금 어렵다"인데 코드(`cut = min_ovr - penalty`, `penalty =
  [0, -2, -4]`)는 **뒤 지망이 더 어렵다.** 밸런스 동결이라 코드 쪽을 옮기고
  검사로 못 박았다(`career_path_test.test_a_later_choice_is_a_little_harder`)

- ❓ **04 학사엔 석차백분율이 없다.** 02는 석차→9등급과 석차→학점 두 표를
  따로 뒀는데 04는 학점만 만든다. 두 표를 합쳐 **학점→등급** 하나로 뒀다 —
  값은 02 그대로다(4.5→1 · 4.2→2 · … · 1.5→8). 석차 표를 그대로 옮기면
  아무도 안 먹이는 죽은 코드가 된다

- ❓ **주인공 지명률이 NPC 실측보다 높다.** 02 앵커는 "고교 3학년 투수
  4,805명 중 고졸 지명 327명 = 6.81%"인데 `measure:career` 60커리어에서
  **17%**가 나왔다. 주인공은 `World.new_game`이 만드는 사람이라 또래
  분포의 가운데가 아니다 — 산식은 NPC와 같은 것을 쓰므로 **밸런스가 아니라
  생성 분포의 문제로 본다.** 이주 후에 주인공 생성 분포를 잴 때 다시 본다

- ❓ **해외 리그 선수 이름이 한국식이다.** 02는 용병 이름을 서양식 풀
  (`namePool.western`)로 만들었는데 04의 `NameGen`엔 그 갈래가 없다 —
  ABL·JBL 선수도 한국 이름을 달고 있고, 영입하면 KBL에 한국 이름의
  "외국인"이 생긴다. 국적·출신은 기록에 남으니 판정은 맞지만 **화면에서
  어색하다.** 이름은 C(화면)에서 같이 본다

- ❓ **라이벌을 누가 지정하나.** 관계 엔진에 라이벌 갈래가 다 있는데
  (성향이 부호를 가르는 것까지) **04에 라이벌을 정하는 경로가 없다** —
  `faced_rivals`가 늘 빈 목록이다. 02는 `named_npc_registry.json`과 대회
  맞대결에서 왔다. B-4(대회)에서 붙는다

### 02가 준 기준선

`measure:devplayer` 3시즌 실측 — **04가 이 근처면 맞는 것이다**

```
진로 분포     그만둠 824~924 · 대학 400~483 · 독립 15~125 · 2군 15~37
경로별        draft_undrafted → 대학 883 · 독립 140 · 2군 52 (3시즌 합)
2군 구성      38팀 1,263명 중 육성 100명 · 육성 비중 중앙 0 · 최대 0.26
드래프트 가치  지명자 1군 도달률 0.36 · 육성선수 0.05
연봉          지명자 중앙 3,733 · 육성 2,000
```

⚠ **02도 매년 900명 가까이 야구를 그만둔다.** 04의 ~1,000명은 결함이
아니라 비슷한 수준이다 — 02 주석의 판정선이 "1,000명대면 배선이 죽은 것"이다

**04 실측 (A-3 뒤)** — 대학은 맞고, 2군·독립이 갈렸다

```
        대학      2군      독립     그만둠
04     400~416   100~101   0~7    1,020~1,068
02     400~483    15~37   15~125    824~924
```

⚠ **규칙 순서는 02 그대로인데 결과가 다르다.** 원인은 규칙이 아니라
**04의 2군 로스터가 정원(34)에 딱 맞춰져 있는 것**이다 — 육성 자리가 팀당
10개씩 그대로 남아 매년 100명을 흡수하고, 그 바람에 독립까지 내려오는
사람이 없다. 02는 2군이 [38,40,40,…,43]으로 이미 정원을 넘겨 있어서
육성 자리가 거의 없었다.

**값을 만지지 않는다** — 02 규칙(`intakeMax` 10)을 그대로 쓴 결과다.
2군 로스터 두께가 02와 달라진 이유(승강·콜업 미이관)를 먼저 봐야 한다.
**A-5 트레이드·승강이 붙은 뒤 다시 잰다.**

---

## A. 세계가 도는 데 필요한 것

- [x] **A-1** 구단 성향 (`b930e9bb0` · 변이 33)
- [x] **A-2** 계약·연봉 · FA 시장 (`cd4db0c8c` · 변이 73)
- [x] **A-3** 진로 배정 — 대학 → **2군(육성)** → 독립 → 포기 (변이 31)
      — 02를 실행해 값 셋을 받아 왔다: 육성 상한 10 · 육성 연봉 2,000 ·
        대학 한 해 8(= 정원 ÷ 학년)
      — FA 미계약자도 같은 경로를 탄다
- [x] **A-4** 2단계 방출 — 정원 안이어도 성적·연봉·뎁스·나이로 거른다
      — 방출자는 드래프트 풀로 보내 미지명자와 같은 경로를 탄다(02도 그렇다)
- [x] **A-5** 트레이드 — 다섯 갈래 · 실측 연 15건(리그당 상한 5 × 3리그)
      — 계약 만료 선점 · seller(유망주 번들) · buyer(즉시전력) · 중립 맞교환 ·
        리빌딩 번들. `TeamProfile`이 모드를 정한다
      — ⚠ **못 옮긴 갈래 둘** — 04에 데이터가 없다:
        · **부상 긴급 보강** — 자리별 부상자 목록(`injured_positions`)이 필요하다
        · **선수 야망 이적** — `personality.ambition`이 필요하다
        B 단계에서 부상·성격이 붙으면 그때 갈래를 늘린다

## B. 미이관 usecases

**주인공 커리어 — 1~68일차가 비어 있는 이유**

- [x] **B-1** `campusEvents`(412) + `weekPhases/academics`(27)
      — 학사 경고·시험. **1~68일차가 눈에 보이게 메워지는 첫 작업**
      `sim/academics.gd` · `sim/campus_events.gd` · `sim/campus_runner.gd`.
      실측 223명(02 기준 223) · 올스타 24 vs 24 · 캡 초과 0 · 포지션 10/10
- [x] **B-2** `relationships`(522)
      — 감독·동료·코치. ⚠ 02는 주 경계에서 `weekNum`을 써서 **전 커리어에 걸쳐
        한 번도 안 움직였다**(결과는 `weekNum - 1`에 있다)
      `sim/relationship.gd` · `sim/relationship_runner.gd`.
      **지금 도는 것은 팀동료뿐이다** — 감독·코치·구단주는 B-2b가 서야 한다
- [~] **B-2b 스태프(감독·코치·구단주)** — B-2를 하다 드러난 **빠진 모듈.**
      **생성·효과·관계 배선은 끝났다** — `sim/staff.gd` · `data/staff_rules.json`.
      팀마다 감독 1 · 구단주 1 · 코치 0~8(씀씀이가 정한다). 리그·전력★이
      수준을 올리고, 전문 분야가 그 코치의 두 능력치를 민다.
      15종 → 계수 10종의 **배선표를 한 곳에만** 적었다(02는 소비처가 각자
      파고들어 **15종 중 14종이 아무 계산에도 안 닿았다**).
      구단주 신뢰가 코치 실효치를 민다 — 다섯 번째 능력치의 유일한 소비처.
      ⚠ **04엔 재정 등급이 없어** `TeamProfile.owner_spending_willingness`에서
      냈다(부유 ≥75 · 안정 ≥50 · 알뜰 ≥25 · 궁핍).
      ⚠ **훈련 효율에 곱하지 않고 더한다** — 04는 덧셈으로 합치는 구조고,
      중립 코치(50)면 더하는 값이 0이라 **지금까지의 수가 그대로 남는다**.
      검사 32 · 변이 26/26 + 배선 4/4 + `app_root` 40/40.
- [x] **B-2c 스태프 생애주기** — `sim/staff_lifecycle.gd`. 늙고 · 그만두고 ·
      잘리고 · 새 사람이 온다. 나이표로 은퇴, 전성기 전후로 성장/하락
      (한 해에 능력치 **두 개만**), 구단주 인내가 몇 시즌을 기다리는지 정하고
      감독이 잘리면 코치도 34% 같이 나간다. 빈 자리는 **아래 리그에서
      먼저 데려온다**(문턱 평균 58) — 02가 용병을 무에서 찍어 출신 기록이
      없던 것과 같은 함정이라.
      ⚠ **새로 만든 사람의 id에 연도를 넣는다** — 자리 이름만으로 만들면
      스카우트로 떠난 사람과 그 자리를 메운 사람이 **같은 id**를 갖는다
      (실제로 그래서 고교 감독이 두 팀에 있었다). 검사 30 · 변이 30/30 + 배선 1/1
      **아직 안 이은 계수 둘** — 값은 이미 나오는데 읽는 자리가 없다:
      · **감독 → 보직 배정**(`Rotation.assign_position`에 `callup` 계수)
      · **구단주 → 재계약 배수**(`Contract`·`FaMarket`에 `budget` 계수)
      C에서 화면이 그 값을 보여줄 때 같이 잇는다
- [x] **B-3** `weekPhases/injuries`(273) + `injuryNews`(78) + `myBodyReport`(135)
      `sim/injury.gd` · `sim/injury_runner.gd` · `data/injury_rules.json`.
      ⚠ 02는 완치 때 `careerStatus`를 `active`로 안 되돌려 **고교 3,015명 중
        1,429명(47%)이 영구 `injured`**로 남았다 — 그 줄을 박아 뒀다.
      **`myBodyReport`의 화면 문구는 C-6("나" 탭)에서** — 여기서는
      `body_log`(경고·완치)와 `injury_log`(월간)를 쌓는 데까지다
- [~] **B-4** `tournaments`(204) + `survivalLeague`(83)
      **엔진만 끝났다** — `sim/tournament.gd` · `data/tournament_rules.json`.
      대회 8종(고교 5 · 대학 3) · 시드 배정 · 부전승 · 조별예선 · 본선.
      권역은 `teams.json`의 `stadium`에서 파생한다(02는 목록을 한 벌 더
      적어 뒀다). 검사 56 · 변이 69/69.
      **남은 것:**
      - [x] **B-4a 배선** — `sim/tournament_runner.gd`. `DayEngine.advance_day`가
            **날마다** 부른다. 실측: 한 시즌에 대회 8개가 다 열리고 다 끝난다
            (개나리 31 · 장미 31 · 무궁화 47 · 국화 101 · 패왕 23 ·
            왕중왕 7 · 은하 31 · 여명 47 = 318경기). 검사 29 · 변이 32/32
      - [x] **B-4b 대회 소식** — `sim/tournament_news.gd`. 개막·내 경기·
            라운드 명단·우승 네 갈래가 소식함까지 닿는다. ⚠ 02는 대회가
            데이터로만 돌아 **우승해도 아무 말이 없었다**. 검사 23 · 변이 34/34.
            **`tournamentView`(155)는 화면이라 C에서 한다**
      - [x] **B-4c 독립 생존리그** — `sim/survival.gd` · `sim/survival_runner.gd`
            · `data/survival_rules.json`. 4단계로 좁혀 가고 **단계마다
            순위표가 리셋된다**. 실측: 90 + 56 + 6 = 152경기, 3단계 컷,
            최종 4팀 사다리. **세계 일정에서 독립을 뺐다** — 다음 단계
            참가팀이 이전 결과에 달려 있다. 검사 42 · 변이 55/55
- [x] **B-5** `finance`(331) — `sim/finance.gd` · `data/finance_rules.json`.
      수입·누진세·스폰서·구독·투자·사치품. **자산이 매주 실제로 움직인다**
      (`p.money` + `finance_log`)이고 **구독이 훈련 효율에 얹힌다**.
      ⚠ 단위는 전부 만원 — 02는 치료비만 원 단위라 **한 주에 자산이 0이 됐다**.
      검사 45 · 변이 47/47.
      **남은 것:** 스폰서 계약 화면·투자 선택 화면은 C에서 (지금은 엔진만) — 개인 재정
- [x] **B-6** `careerDecision`(270) · `contractDecision`(260) · `militaryDecision`(168)
      — `sim/career_path.gd` · `career_decision.gd` · `career_runner.gd` ·
        `contract_decision.gd` · `military.gd` · `pending.gd` · `data/career_rules.json`.
      **주인공이 처음으로 프로에 갈 수 있게 됐다.** 02에는 `draftDrafted`를 true로
      만드는 곳이 **어디에도 없어서** 프로 콘텐츠 전부가 도달 불가였다.
      실측(`measure:career` 60커리어): 진학 75% · 지명 17% · 독립 7% · 입대 2%,
      지명 라운드 중앙 7R · 계약금 중앙 4,000만원.
      검사 52+19+23+57+26+18 · 변이 49/10/29/66/39/17 + 배선 1/1 · 38/38.
      **남은 것:**
      - `decisions`(121, 이벤트 선택지 효과 적용)은 **C에서** — 선택지 화면이
        서야 부를 데가 생긴다. 관계·사치품 효과는 `Relationship`·`Finance`에 이미 있다
      - **자동 진행 정지 목록**(02 `STOP_PENDING`)은 **B-11에서.** 04는 지금
        대기줄에 든 것이 전부 날을 멈추므로 구분이 아직 필요 없다.
        02가 여기서 잃은 것: 지명 통보·계약 셋·트레이드가 "알림성"으로 분류돼
        **조용히 버려졌다**(2031년 만료 계약이 2038년까지 남았다)
- [x] **B-7** `retirement`(228) — 주인공 은퇴. `sim/retirement.gd` ·
      `data/retirement_rules.json`. **커리어가 끝날 수 있게 됐다** —
      02는 은퇴 기록을 남기는 두 곳이 전부 NPC였고 주인공을 은퇴시키는
      코드가 어디에도 없었다(목표가 15~20시즌인 게임인데 안 끝났다).
      갈래 셋: 자발 · 노쇠·계약 불발 · 부상 강제. **판정은 NPC와 같은 표**를
      쓰고 결과를 **강제하지 않는다**(묻고 고르는 건 사용자다).
      `CareerRunner`가 매주 부른다 — 수술은 그 주에, 노쇠는 시즌 마지막 주에.
      검사 37 · 변이 39/39 + 배선 3/3.
      **남은 것:** 은퇴 확인 화면과 인생 기록 엔딩은 C에서
- [x] **B-8** `nationalTeam`(311) — `sim/national_team.gd` ·
      `sim/national_runner.gd` · `data/international_rules.json`.
      **경기는 시뮬하지 않는다**(사용자 확정) — 대표팀 전력으로 순위를 확률
      산출하고 그 순위가 병역 면제를 정한다. 4년 주기 셋(올림픽 3위 이내 ·
      아시안게임 우승만 · 월드컵 면제 없음)이 `year_mod`로 안 겹친다.
      ⚠ 02는 **국적을 안 봐서 KBL 외국인이 한국 대표로 뽑혔다**(외국인은
      병역이 "면제"라 병역 조건을 그냥 통과한다).
      ⚠ 02는 대회를 **로그로만** 내서 발탁도 메달도 면제도 화면에 없었다.
      검사 51 · 변이 53/53 + 배선 1/1.
      **남은 것:** 차출 중 자리 메우기(승강 상시 콜업)는 B-10에서 —
      `NationalRunner.is_called_up`이 그 문을 열어 뒀다
- [x] **B-9** `foreignPlayers`(343) — `sim/foreign.gd` · `data/foreign_rules.json`.
      보유 3명·투수 2명. 시즌이 바뀔 때 재계약 판정(OVR 73 · 36세) →
      못 미치면 **온 곳으로 돌려보내고** 빈 자리를 해외에서 채운다.
      ⚠ **한도 문지기는 국적이다** — "그 리그에서 외국인인가"로 물으면
      ABL 선수가 ABL에서 내국인이라 그냥 통과한다(02에서 그 자리가 다섯).
      ⚠ 02는 용병을 **무에서 찍어** 어디서 왔다는 기록이 없었다 —
      04는 ABL 팜(85%)·ABL(10%)·JBL(5%)에서 실제 선수를 데려온다.
      실측: 진짜 세계에서 KBL 10팀 × 3명 정원이 다 찬다.
      검사 32 · 변이 31/31 + 배선 1/1.
      **다르게 한 것:** 02는 새 게임 시점에 이미 외국인이 있다(`roster_gen.rs`).
      04는 **첫 시즌 롤오버가 채운다** — 초기 로스터 생성을 안 건드리므로
      밸런스 대조가 안 흔들린다. 대신 1년차 KBL엔 외국인이 없다
- [~] **B-10** `proSeason`(62) · `backgroundPostseason`(87) · `runDraftBoardBackground`(32)
      — 셋을 읽어 보니 성격이 달랐다. 나눠 적는다:
      - [x] **B-10a** `proSeason`(62) — **옮길 게 없다.** 02는 "계약이 성립했으니
            다음 시즌을 프로로 연다"가 세 곳(롤오버·지명 수락·즉시 계약)에
            복제돼 있었고 각자 리그 분기를 적어서 **한 경로로 들어간 시즌만
            일정이 비는** 결함이 났다. 04는 `SeasonRunner.roll_over` 하나가
            `World.build_schedule`을 부르는 구조라 정본이 이미 하나다 —
            같은 문제를 만들 자리가 없다
      - [x] **B-10b 프로 포스트시즌 엔진** — B-10을 하다 드러난 **빠진 모듈.**
            `sim/postseason.gd`. 1군 5강 와일드카드(4위 1승 어드밴티지) ·
            2군 단판 사다리 · 배경 리그 자동 진행.
            ⚠ **`ps_result`가 죽은 필드였다** — 읽는 곳이 셋인데(대학 입시 ·
            드래프트 판정 · 인생 기록) **쓰는 곳이 없어 전원이 미진출**이었고,
            고교 우승이 입시에 한 점도 안 실렸다. 이제
            `SeasonHistory.protagonist_record`가 프로는 포스트시즌에서,
            학교는 그 해 대회 중 **제일 멀리 간 것**에서 낸다.
            검사 47 · 변이 49/49 + 배선 2/2
      - [ ] **B-10c** `runDraftBoardBackground`(32) — **C-1(드래프트 보드) 화면
            지원이다.** 보드를 여는 그 자리에서 같이 옮긴다
- [~] **B-11** `runAutoAdvance`(491) — `sim/auto_advance.gd`.
      **정책과 반복은 끝났다.** 무엇 앞에서 멈추고(`STOPPING` 10종) 무엇을
      대신 답하며(경기·소식), 지침 정도로 선택지를 고르고(지치면 쉬는 쪽),
      정지 주차(W40·W51)를 **지나쳤는지**로 잡는다.
      `run(state, advance, answer)`이 멈출 때까지 돈다 — 진행·답변은
      부르는 쪽이 준다(화면은 화면 방식, 계측은 계측 방식).
      ⚠ 02가 여기서 결정을 조용히 버렸다: 지명 통보·계약 셋·트레이드가
      정지 목록 밖이라 **지명받고도 고교에 남고**, **2031년 만료 계약이
      2038년까지 남고**, **트레이드 통보만 사라졌다**.
      검사 35 · 변이 35/35.
      **남은 것:** 화면에 붙이는 것(진행 버튼·정지 사유 표시·로그)은
      **C에서** — 지금 붙이면 화면 구조를 두 번 만진다

## C. 화면 ← **여기서부터다** (B는 전부 끝났다, 2026-08-15)

⚠ **B가 먼저 서야 데이터가 생긴다.** 순서를 지킨다. **이제 다 섰다** —
아래 화면들이 읽을 값은 전부 상태에 쌓이고 있다.

### 화면 하나의 리듬 (엔진과 다르다)

```
① 무엇을 읽을지 정한다   — 이미 쌓이는 사전을 찾는다. 새 계산을 만들지 않는다
② ViewModel 검사를 쓴다  — 가짜 사전으로. 화면 없이 도는 부분이 여기다
③ VM을 만든다
④ .tscn 골격 + 부품      — 레이아웃을 코드로 다시 짜지 않는다
⑤ 화면 검사 (소스에 sort_custom·filter 금지를 `status_screen_test`가 본다)
⑥ 변이 검증
⑦ **띄워서 눈으로 본다** — `godot --script tools/shot.gd -- <이름>`
⑧ 커밋
```

⚠ **화면이 계산을 갖지 않는다.** 02 결함 상당수가 여기서 나왔다 —
수상 집계가 결산 모달에만, 드래프트 보드가 자기 후보 풀을 따로,
경력 조립이 모달 안에만 있었다.

⚠ **색·여백을 화면에 직접 적지 않는다.** `AppTheme`에서만 가져온다.

- [x] **C-1** 드래프트 보드 — `sim/draft_log.gd` · `ui/draft_board_vm.gd` ·
      `ui/screens/draft_board_screen.{tscn,gd}`. **B-10c도 같이 끝났다.**
      ⚠ 02는 보드가 **자기 후보 풀로 자체 시뮬을 또 돌려** 화면에서 본 지명과
      실제 소속이 어긋났다. 04는 드래프트가 하나뿐인데 결과를 **쓰고 버리고
      있었다** — `DraftLog`가 지명 당일 값을 박아 남기고 보드는 재생만 한다.
      ⚠ **화면을 띄워서 결함을 하나 잡았다** — 출신이 전원 "재수"였다.
      졸업생이 풀에 들어갈 때 `league_id`가 덮여서 고교·대학·독립 구분이
      통째로 사라졌다. `origin_league_id`를 졸업 시점에 박는다.
      실측: 후보 1,420 · 보드 220 · 지명 110(11라운드 × 10팀).
      검사 35 · 변이 17/17 + 12/12 + 1/1
- [x] **C-2** 인물(관계도) — `ui/people_vm.gd` ·
      `ui/screens/people_screen.{tscn,gd}` · `AppTheme.TONE_COLOR`.
      **"인물" 탭 자리는 있었고 안내 문구만 떠 있었다** — `MainVm`이
      `PeopleVm.build`를 실어 보내고 `MainScreen`이 "나" 탭처럼 통째로 끼운다.
      ⚠ **관계값 숫자를 화면에 안 보낸다.** VM이 내보내는 행에 `value` 자체가
      없고, 7단계 라벨 알약 색이 유일한 눈금이다(`TONE_COLOR` 일곱 색은
      서로 달라야 한다 — 검사가 본다).
      ⚠ **헤어진 사람에게는 효과 문장을 안 붙인다** — `effects_of`가 접촉
      중인 관계만 세므로 "출전 기회에서 유리합니다"를 찍으면 없는 효과를
      약속하는 것이다. 지난 인연에는 언제 만났는지를 남긴다.
      ⚠ **코치 전문 분야를 라벨에 붙인다** — 한 팀에 여덟 명까지라
      안 붙이면 여덟 줄이 전부 "코치"다.
      실측: `PeopleVm.build`가 관계 마흔 줄에 선수 9,600명 이름을 매번
      담아 **6.45ms**였다(다시 그릴 때마다 돈다) — 필요한 id만 담고 다
      찾으면 끝내서 **1.28ms**. `MainVm.build` 13.06 → 6.89ms.
      검사 55 · 변이 30/30 + 22/22 + 1/1 + 2/2
- [x] **C-3** 학사 — `sim/academics.gd`(`study_training`·`training_delta`·
      `next_exam`) · `ui/academics_vm.gd` · `StatusScreen`의 "학업" 하위 탭.
      **화면 하나가 아니었다 — 끊긴 배선이 다섯이었다**
      (C-2를 끝내고 02와 대조하며 확인. 2026-08-15)

      02에서 학업은 **"나" 탭의 하위 탭**이다(`ME_ORDER`) — 고교·대학일 때만
      보인다(`navVisibility.academics`). 04의 `StatusScreen`은 하위 탭
      셋이 **소스에 박혀 있었다** → 목록을 `StatusVm`에서 받게 바꿨다.
      **"학교에 다니나"의 정본은 `AcademicsVm.at_school` 하나다** — 탭을
      만드는 쪽이 그 값을 읽는다(두 곳이 각자 판정하면 언젠가 갈린다).

      ⚠ **이 화면이 쓰는 쪽이다.** 아래 둘은 04에서 **아무도 안 썼다** —
      읽는 코드만 있고 세우는 데가 없었다(`ps_result`와 같은 모양). 이제
      학업 탭이 고른 것을 신호로 올리고 `AppRoot`가 상태에 쓴다:
      - `school["study_mode"]` — 전 커리어가 "normal"(품질 0.55) 고정이라
        집중·휴식·수면 셋이 죽어 있었다
      - `school["major"]` — `major_of("")`가 빈 사전이라 **전공 표
        셋(체육교육·스포츠과학·일반전공)이 통째로 도달 불가**였다

      ⚠ **띄워서 결함을 둘 잡았다.**
      ① 학업에서 뭘 고르면 `StatusScreen`이 통째로 새로 만들어져 **"능력치"
      탭으로 튕겨 나갔다** — `MainScreen`이 하위 탭 자리를 기억한다.
      ② 스크린샷이 1주차 그대로였다 — `_apply_one_week`은 날짜도 사전도
      안 건드린다. `advance`로 가면 등판일마다 멈춰 중간고사(11주)에
      못 닿는다(`season-digest`와 같은 이유).

      ⚠ **검사가 엉뚱한 걸 보고 통과하고 있었다.** "훈련 효율"을 찾는
      검사였는데 **전공 선택 안내 문구에도 그 말이 있어서**, 훈련 효율을
      아예 안 찍어도 통과했다 — 변이가 잡아냈다. 숫자까지 본다.

      검사 61 · 변이 5/5 + 3/3 + 29/29 + 17/17

      - [x] **학사가 훈련에 닿는다** (2026-08-15 · 엔진 절반 끝냄).
        `Academics.training_delta`를 `AppRoot`의 훈련 효율에 더한다.
        ⚠ **학업 모드의 대가를 02에서 가져왔다** — 04는 `study_mode_gpa`만
        옮겨 와서 **집중 수업이 학점만 올리는 공짜 선택**이었다. 02
        `STUDY_MODE_EFFECTS.efficiencyMod`(focus 0.70 · normal 0.85 ·
        rest 1.00 · sleep 1.05)를 `study_mode_training`으로 그대로 옮겼다.
        ⚠ **이건 실제로 수가 움직인 변경이다.** 기본값 `normal`이 −0.15라
        **학교에 다니는 모든 주의 성장이 15% 낮아진다.** 02가 갖고 있던
        규칙을 복원한 것이라 이주로 보지만, 밸런스 동결 중에 수가 움직인
        유일한 자리이므로 여기 적어 둔다. `measure:career`는 그대로다
        (75/17/7/2 · 7R · 4,000만원) — 다만 그 계측은 `CareerRunner`만
        돌리고 주간 훈련을 안 거치므로 **이 변경을 재는 계측이 아니다**.
        (기존 구독 검사가 이때 깨졌다 — 두 쪽이 잠재력 천장에서 만나
        2.00으로 같아졌다. 천장에 여유를 줘서 고쳤고, 구독 차이는 그대로 있다)
      - ❓ **`injury_mod`·`xp_bonus`는 안 이었다 — 02도 안 썼다.**
        02가 규칙 파일과 타입에 갖고 있으면서 읽는 자리는
        `trainingEffBonus` 하나뿐이다(`advanceWeek.ts:281`). 이으면 기능
        추가이자 밸런스 변경이라 이주 중엔 안 한다
      (`can_graduate`는 `CareerDecision`이 쓴다 · `eligibility_blocked`는
      `DayEngine.appearance_gate`가 쓴다 — 이 둘은 이어져 있다)

      화면이 읽을 것: `state.school`(gpa·warning_level·major·study_mode) ·
      `state.academic_log`(학기 확정 기록) · `state.campus_log`(쇼케이스·올스타) ·
      `Academics.exam_at_day`로 다음 시험까지 D-N주.
      **04엔 과목·석차백분율이 없다** — 02의 "과목별 현황"(국영수사과)은
      옮길 대상이 아니다(묻어 둔 질문 참고)
- [x] **C-4** 재정 — `ui/finance_vm.gd` · `StatusScreen`의 "재정" 하위 탭 ·
      `Finance.sponsor_annual`/`active_sponsors`/`sign_sponsor`.
      **여기도 화면 하나가 아니었다** — 운영 코드가 부르던 `Finance` 함수는
      `weekly`·`total_training_bonus` **둘뿐**이었다.
      ⚠ **04는 스폰서를 세우는 데가 없었다.** `sponsor_offers`가 계산만 하고
      아무 데도 안 닿아서 **계약이 한 건도 안 생겼다** — `p["sponsor_annual"]`은
      읽는 곳만 있었다. 이제 계약 목록이 정본이고 합계는 거기서 낸다
      (합계를 따로 들면 계약이 끝난 해에 수입만 그대로 남는다).
      ⚠ **`training_subscriptions`도 마찬가지였다** — 읽는 곳이 둘인데
      쓰는 곳이 0이라 영영 빈 배열이었다.
      ⚠ **`finance_log`를 읽는 자리가 처음 생겼다** (자산 추이).
      ⚠ **띄워서 결함을 하나 잡았다** — "최근 8주 +1064만"인데 주당 152만이면
      8주는 1216만이다. `money`가 그 주 **끝난 뒤** 값이라 첫 줄을 기준으로
      삼으면 한 주가 빠진다.
      02는 이 화면이 OVR·사기로 수입을 즉석 계산해 **실제 `money`와 아무
      관계 없는 숫자**를 보여줬다 — 04는 `Finance`가 낸 값만 찍는다.
      검사 51 · 변이 32/32 + 10/10 + 8/8

      ❓ **투자는 안 이었다.** 02는 투자를 **시즌 종료 화면**에서 한 번
      고르고 재정 화면은 결과만 본다. 04엔 그 자리가 없어 `resolve_investment`·
      `can_invest`·`investment_options`가 아직 호출자 0이다 — 이력이 영영
      비어 있을 화면을 만들지 않으려고 투자 칸을 뺐다. 시즌 종료 화면에
      붙일 일이다.
      ❓ **`Finance.luxury`도 호출자가 없다** (02는 이벤트 선택에서 썼다).
      ❓ **명성(`fame`)을 올리는 곳이 `CampusRunner`뿐이다** — 대학 무대
      이벤트만 명성을 준다. 고교·프로에서는 커리어 내내 안 오르고, 그러면
      스폰서 문턱(명성 10~75)에 프로가 도달할 길이 좁다. B-3/경기 쪽에서
      명성을 주는 자리를 찾아 이어야 한다
- [x] **C-5** 업적 — `data/achievements.json` · `sim/achievements.gd` ·
      `ui/achievements_vm.gd` · `StatusScreen`의 "업적" 하위 탭.
      ⚠ **04엔 통째로 없었다** — 엔진도 규칙 파일도 상태도. C의 다른
      항목과 달리 화면만의 문제가 아니라 B급 이주였다.
      ⚠ **통산을 따로 세지 않는다.** 02는 `achievementMetrics` 계수기
      주머니를 들고 있었는데 `season_stats`와 정본이 둘이 된다 — 연도
      기록의 숫자를 해로 묶어 합친다. 그래서 `SeasonHistory`가 문자열
      요약만이 아니라 **숫자(`stats`)도 남기게 고쳤다**(안 그러면 해가
      바뀌는 순간 통산이 0으로 돌아간다).
      ⚠ **`training_log`로는 훈련 주를 못 센다** — 그건 "뭔가 오른 주"만
      남아서 열한 주를 훈련해도 열 주가 안 된다. 세는 자리를 따로 뒀다.
      ⚠ **순위표는 상태에 없다** — 04는 일정에서 파생한다. `state["standings"]`를
      읽었다가 "N승 달성" 여섯 개가 통째로 안 열렸다(진짜 경기를 치르는
      검사가 잡았다).
      ⚠ **죽은 갈래 둘을 안 만들었다** — 지금 `active` 중에 숨김 업적이
      없어서 `hidden` 처리를, 갈래가 표에 다 있어서 "빠뜨린 갈래 붙이기"를
      뺐다. 대신 **검사가 모든 갈래가 표에 있는지 본다**.
      검사 46 · 변이 17/17 + 3/3 + 12/12

      ❓ **02에서도 안 열리던 업적을 하나 고쳤다** — `ACH_SOCIAL_FIRST_KAKAO`가
      `active`인데 `kakaoFirstContact`를 아무도 안 만들어 늘 0이었다.
      04엔 카톡이 없어 `blocked`으로 옮겼다(id·제목·목표치는 그대로).
      ❓ **"N승 달성" 여섯 개는 고교에선 안 열린다** — 고교는 대회 중심이고
      리그 순위표에 대회 경기가 안 들어간다. 프로에 가야 열린다.
      ❓ **보상(`reward`)은 표시만 한다** — 02도 그랬다("명성 +1"이 태그일
      뿐 어디에도 안 걸린다). 이주 중에 새로 걸지 않았다.
      ❓ **새로 딴 업적을 소식으로 안 알린다** — 02도 상태에만 적었다.
- [x] **C-6** "나" 탭 보강(`me`) — 셋 다 끝났다
      - [x] **`myBodyReport`** — `sim/body_report.gd`. 04는 `body_log`를
        쌓기만 하고 읽는 곳이 진로 판정 하나뿐이라 **경고도 완치도
        플레이어에게 한 번도 안 보였다.** NPC 부상은 월간인데 내 몸만
        안 왔다 — 같은 주기로 한 통에 담는다.
        ⚠ 02는 `injuryType`을 그대로 본문에 넣어 화면에 `SHOULDER_INFLAM`이
        떴다. 이름으로 바꾸는 층을 한 겹 빠뜨리면 조용히 원문이 샌다 —
        검사가 id가 새는지 본다.
        ⚠ **심각도 한글표가 `StatusVm`에만 있었다** — 엔진이 쓰려면 표가
        둘이 된다. `Injury.SEVERITY_LABELS`로 옮기고 화면이 그걸 읽는다.
        ⚠ **C-5에서 넣은 결함을 하나 고쳤다** — `Achievements.messages_read`가
        `state["news"]`를 읽고 있었다. 04의 소식함은 `mailbox`라 늘 0이었고
        메시지 업적 셋이 통째로 안 열렸다. 이름이 비슷한 키는 조용히 틀린다.
        검사 18 · 변이 16/16 + 2/2
      - [x] **은퇴 확인 · 인생 기록** — `ui/retirement_vm.gd` ·
        `ui/screens/retirement_screen.{tscn,gd}`.
        ⚠ **`retirement_ask`를 밀어넣는 코드는 있는데 받는 자리가 없었다** —
        02가 그랬고 04도 그대로였다. 대기줄에 올라간 채 아무도 안 받아서
        자동 진행이 "은퇴 여부 결정"에서 안 풀리고, `Retirement.retire`도
        호출부가 없어 **커리어가 영영 안 끝났다.**
        ⚠ **사유에 따라 선택지가 다르다** — 부상(재기 불가)은 거절이 없다.
        누를 수 없는 버튼을 띄우지 않는다.
        ⚠ **은퇴를 누르면 그 화면이 결산으로 바뀐다** — "결산을 봤는가"
        플래그를 세이브에 안 만들려는 것이다(02 그대로).
        ⚠ **여기서는 즉시 해제가 위험하다** — 화면이 자기 신호 안에서
        지워져 잠긴 객체가 된다. 이 자리만 `queue_free`.
        검사 28 · 변이 16/16 + 7/7

      **결정 화면 넷을 이었다** (`ui/decision_vm.gd` ·
      `ui/screens/decision_screen.{tscn,gd}`) — `career_results` ·
      `draft_observe` · `draft_notification` · `trade`.
      ⚠ **종류마다 화면을 만들지 않는다.** 결정은 "무엇을 묻고 · 고를 것이
      무엇인가" 하나로 같다 — 종류마다 만들면 열 개가 되고 새 결정이
      생길 때마다 또 하나가 필요해진다. 배선표는 `DecisionVm.apply` 하나다.
      ⚠ **결정은 줄줄이 온다**(결과 → 최종 선택 → 지명 통보). 하나 답하고
      화면을 닫으면 다음 것이 대기줄에 남은 채로 진행이 막힌다 — 같은
      화면이 다음 것을 이어받는다.
      ⚠ **`HANDLED`에 없는 결정은 안 받는다** — 답을 못 하는 화면이 뜨고
      대기줄은 그대로 남는다. 검사가 `HANDLED`가 전부 `AutoAdvance.STOPPING`
      안에 있는지도 본다.
      검사 22 · 변이 14/14

      **셋을 더 얹었다** — `career_choice` · `salary_negotiation` ·
      `option_clause`. 화면은 안 고쳤다(갈래와 검사만 늘었다).
      ⚠ **붙은 곳만 선택지가 된다** — 떨어진 대학을 두면 누르면 엔진이
      거절하는 버튼이 되고 사용자는 왜 안 되는지를 모른다. 아무 데도
      안 붙어도 "지금 자리에 남는다"는 늘 있다.
      ⚠ **갈래와 팀을 한 id에 담는다**(`university:TEAM_X`) — 화면이
      선택지마다 다른 모양을 갖지 않게. 갈래를 안 읽으면 독립을 골라도
      대학에 가는데, 팀만 맞고 리그가 다르면 조용히 틀린다.
      ⚠ **보여준 조건 그대로 계약된다** — 화면이 숫자를 다시 지어내면
      "보여준 것과 다른 계약"이 된다(02가 반복해서 겪은 자리).
      검사 32 · 변이 27/27

      **아홉을 다 이었다** — `career_choice_hub` · `fa_market`까지.
      화면 모양은 **둘뿐이다**: 하나 고르기(`one`)와 여러 개 켜고
      제출하기(`many`). 결정 종류가 늘어도 이 둘을 안 넘는다.
      ⚠ **켠 것을 id에 담아 올린다**(`submit:university:U1,draft`) —
      화면이 상태를 들고 있으면 다시 그릴 때 고른 게 사라진다.
      ⚠ **은퇴는 여기서 안 받는다** — 자기 화면이 있다. 두 화면이 같은
      결정을 두고 다투면 안 된다(검사가 본다).
      검사 42 · 변이 37/37

      ❓ **FA 제안을 만드는 곳이 04에 없다.** `sign_fa_offer`는 offer를
      인자로 받는데 그걸 세우는 코드가 없다 — `fa_market`을 대기줄에
      올리는 자리만 셋이다. **없는 선택지를 지어내지 않았다**: 지금은
      "한 해 더 기다린다"만 준다(그래도 대기줄은 풀린다). 제안 생성이
      붙으면 화면은 그대로 목록을 그린다 — `state["fa_offers"]`를 읽는다
      **인생 기록을 "나" 탭 커리어에서 다시 연다** (2026-08-16).
      은퇴하는 순간이 첫 관람이고 그 뒤로는 여기가 유일한 입구다 —
      없으면 결산을 한 번 보고 못 본다. 변이 2/2

### C에서 같이 이을 것 (엔진은 이미 있다)

- [x] **B-11 자동 진행 화면 배선** (2026-08-17) — 진행 줄에 **"다음 결정까지"**.
      `AutoAdvance.run`이 검사에서만 돌던 것을 화면 → 루트 → 정책으로 이었다.
      검사 8 · **변이 6/6 + 정책 8/8 + 화면 4/4**.
      **02는 이걸 개발자 도구에만 뒀다**(`MainPage.svelte:477`, DevToolsHub 경유).
      04엔 그 허브가 없어 그대로 옮기면 화면을 하나 새로 만들어야 하고,
      실제로 쓸모 있는 자리는 진행 버튼 옆이다 — **다음 등판까지 하루씩
      누르는 게 지금 유일한 방법이었다.**
      **붙이면서 고친 것 셋:**
      - `next_step`이 **대기줄만 봤다.** 미결정 소식은 `mailbox`에, 등판은
        `schedule`에 있어서 "가도 된다"고 하는데 `DayEngine`은 하루도 안 민다 —
        **붙이자마자 상한까지 헛돌았을 자리다.** `DayEngine.stop_reason` 하나로 모았다
      - 루트가 걸음마다 `_state`를 **새 사전으로 갈아탔다.** 자동 진행은 사전
        하나를 들고 도는데 그러면 옛 사전을 계속 본다 → `_swap_state`로 참조 고정
      - 경기 결과를 남기는 길이 `_on_match_done` 안에만 있었다 → `_record_match`로
        빼서 손으로 던진 경기와 자동으로 넘긴 경기가 같은 길을 지나게 했다
- ⬜ **스태프 계수 둘** — 감독 → 보직 배정(`callup`)은 **이미 이어져 있다**
      (`promotion_runner.gd:135`). 구단주 → 재계약(`budget`)은 **붙일 자리가 없다:**
      04에 **주인공 재계약 오퍼 연봉을 계산하는 코드가 아예 없다.**
      `salary_negotiation` pending을 만드는 두 곳(`contract_decision.gd:144` ·
      `military.gd:189`)이 전부 **이미 있는 값을 그대로 옮겨 담을 뿐**이고,
      `Contract.estimate`는 NPC 계약 생성(`ensure`)에서만 쓰인다.
      02는 `player_engine.rs:296-311`에 따로 있다 —
      `market = (1800 + max(ovr-50,0)*220 + fame*28) * 리그배수 * budget.clamp(0.80,1.25)`,
      `blended = current*(1+ (rating-50)*0.012)*0.6 + market*0.4`, 최저 1500.
      **F-7로 올린다** (아래).

## D. 갚을 빚

- [x] **D-1 · D-4 (계측기)** 봤다. **두 값이 다른 게 아니라 다른 엔진이었다.**
      `bench:season`이 P0 스파이크 이식본(`MatchSim`)에 합성 로스터를 물려
      재고 있었다 — 게임이 실제로 돌리는 `GameLoop`가 아니다. `GameBench`
      주석에 **"다시 재지 않으면 그 0.659초는 아무 뜻이 없다"**고 적혀
      있었는데 그대로 남아 있었다. 이제 `bench:season`이 진짜 엔진으로
      2,612경기를 돈다. `sim/match_sim.gd`(322줄)와 `bench/fixtures.gd` 삭제.

- [x] **D-8 성능 게이트를 04의 진행 모델로 다시 정의했다** (2026-08-15, 사용자 확정)
      ⚠ **"최악의 주 98경기"는 02의 전제였다.** 02는 주 단위로 진행했지만
      04는 **하루씩** 간다 — 04에 없는 단위로 게이트를 걸고 있었다.
      ⚠ **경기 수를 곱셈으로 만들지 않는다.** 98·2,612가 어디서 온 수인지
      코드가 몰랐다. 이제 `GameBench.schedule_shape`가 **진짜 일정에서 센다** —
      일정이 바뀌면 게이트도 따라간다.
      ```
      일정 6,246경기 · 364일 · 경기당 11.6ms
      최악의 하루        83경기   0.965초  기준 1.0   ○
      가장 긴 한 번 진행 162일 2,095경기  24.355초  기준 25.0  ○
      ```
      ⚠ **여유가 없다.** 가장 긴 한 번 진행이 24.4초 / 기준 25초다 —
      프레임 쪼개기와 진행 표시가 붙지만 총 대기는 총 대기고, 코드가
      더 붙으면 바로 넘는다. **회귀가 나면 그때 최적화한다** —
      먼저 볼 곳: `GameLoop.play` 루프 안의 사전 할당 · `MatchDay.bullpen_of`
      호출 빈도 · 투구마다 `Dictionary` 조회
- [x] **D-2** 02의 `gamesPlayed`가 늘 1이라 **뛰면 성장에 손해**였다 —
      2026-08-17 고침. 성적이 **있는** 선수가 0.2×품질(≤0.28)이고 성적이
      **없는** 선수가 0.70이었다.
      **값을 새로 정한 게 아니다** — `perf_window`가 경기 줄을 순회하면서
      출전 수를 세지 않고 `1`을 박아 넣고 있었다. 04는 일정에 진짜 출전
      줄이 있다. **02의 명백한 결함까지 물려받지 않는다**(P-11과 같은 판단).
      ⚠ **재려다 계측 결함을 하나 더 찾았다.** `measure:growth`가 **경기를
      안 치르고** 돌아서 `perf`가 늘 빈 사전이었다 — 성장이 전부
      `NO_PERF_BASE` 갈래로만 돌았고, **고치기 전과 후가 한 칸도 안 달랐다.**
      `--games 1`을 붙여 진짜 경로를 켰다.
      실측(20주 · 경기 켬): **62,993칸 → 66,434칸 (+5.5%)** ·
      사람당 한 해 8.59 → 9.05칸. 검사 8 · **변이 6/6**
- [x] **D-3** NPC 구종 배열 — 2026-08-18 고침 (`sim/npc_pitch_dev.gd`)
      02 값 그대로. 실측 52주 — 투수 3317명 중 2124명이 구종 보유.
      ⚠ 29세 이상으로 시작한 투수는 영영 0개다 — **02도 그렇다**.
      검사 22 · 변이 14/14 · 주간 성장 87ms(최악 128ms).
      (아래는 옮길 때 본 근거)
      (2026-08-17 대조로 좁혔다)
      04의 NPC는 `pitches`가 없어 `PitchAi.pick_from_arsenal`이 **늘 포심을
      돌려준다** — 캡처 로그가 "1구 포심 · 2구 포심 … 6구 포심"이다. 그래서
      `pitch_pattern_modifier`(같은 구종 반복 벌점)가 **NPC 경기 내내 걸린다.**
      ⚠ **초기 생성이 문제가 아니다.** 02의 NPC도 `pitches`가 빈 채로
      시작하고 `toEngineArsenal`이 `[{fastball, 3}]`로 떨어진다 — 04와 같다.
      갈리는 건 **주간 성장**이다: 02 `npc_sim.rs:2833-2877`의
      `decide_pitch_training`이 해마다 구종을 늘린다.
      값은 02에 다 있다 — 목표 개수 `npc_pitch_target`(SP 4~5 · CP 2~4 ·
      RP 3~4, 구속으로 갈린다) · 주당 진행 `pitch_progress_per_week`
      (발견→1: 8주 · 1→2: 8주 · 2→3: 12주 · 3→4: 24주) ·
      자격(33세 미만 · `ovr/potential >= 0.70` · 새 구종은 29세 미만).
      ⚠ **밸런스에 닿는다** — 숙련도가 `grade_quality_bonus`로 결과에 걸리므로
      붙이면 `measure:engine`을 전·후로 잰다. **따로 본다**
- [x] **D-4** OVR 재계산 — 2026-08-17 고침. 🔴 **부상 후유증이 다음 성장
      주에 지워지고 있었다.** `InjuryRunner._apply_npc_penalty`가 `stats["ovr"]`
      만 깎는데 `NpcGrowth`는 능력치가 오르거나 노화가 걸리면 **`stats`에서
      OVR을 다시 만든다**(`npc_growth.gd:392-394`) — 다시 만드는 순간 후유증이
      사라지고, 후유증은 `penalty_applied`로 **한 번만** 먹이므로 영영 안
      돌아온다. 수술·중증을 겪은 NPC가 아무 흔적 없이 회복했다.
      → 02처럼 **세부 능력치를 깎고 나서 OVR을 다시 만든다**
      (`advanceWeek.ts:516-533`). ⚠ **어느 능력치를 얼마나 깎을지는 지어내지
      않았다** — `pitching_ovr`이 가중 평균이라 **전부 같은 비율로 줄이면 OVR도
      같은 비율로 준다.** 목표 OVR에 맞는 비율 하나면 되고 표가 필요 없다.
      ⚠ **죽은 가드 둘을 지웠다** — `delta == 0` 재확인(부르는 자리가 이미
      거른다)과 `ovr` 칸 건너뛰기(바로 아래에서 다시 만든다). 변이가 등가로
      나와서 알았다. 검사 7 · **변이 4/4**
- [x] **D-5** 긴 진행의 표시 — `MainScreen`에 진행 막대.
      **한 번에 162일 · 2,095경기 · 24초를 간다**(D-8 실측). 글자만
      바뀌면 그동안 숫자만 오르고 얼마나 남았는지가 안 보인다.
      죽은 줄 하나를 안 남겼다 — 끝날 때 막대를 치우는 건 `_rebuild`가
      이미 한다. 변이 4/4
- [ ] **D-6** 02 좌표의 마운드·1루·3루가 구장 그림보다 30~50px 위.
      **1:1로 띄워 확인했다** (2026-08-15) — `shot.gd -- park`가 구장을
      viewbox 크기(1000×920) 그대로 띄운다. 축소된 화면으로는 "위쪽에
      걸려 있다"까지만 보이고 px가 안 나온다.
      본 것: 그림의 1·3루 베이스는 y≈618인데 `parks.json`은 580이다(약 +38).
      마운드는 흙 원 중심이 y≈628인데 좌표는 548이다.
      1루수·3루수 스프라이트가 베이스보다 한참 위 잔디에 떠 있다.
      ⚠ **단순 오프셋이 아니다** (2026-08-16 재확인). 어긋나는 폭이
      제각각이다 — 홈 약 +10 · 1·3루 약 +38 · 마운드 약 +80.
      한 값을 더해서 맞출 수 있는 모양이 아니고, **그림의 다이아몬드
      원근 압축이 좌표 세트와 다르다**는 뜻이다. 좌표 하나씩이 아니라
      다이아몬드를 다시 잡아야 한다.
      ⚠ **눈대중으로 안 넣었다.** 스크린샷을 보고 값을 지어내면 지금보다
      더 어긋날 수 있다 — 사람이 1:1 그림을 보며 맞추는 게 빠르고 정확하다.
      도구는 있다: `godot --script tools/shot.gd -- park`.
      화면 좌표라 **밸런스 동결에는 안 걸린다**
- [x] 🔴 **D-7** 주인공 경로와 NPC 경로의 성장 표가 둘이다 —
      2026-08-19 **판정: 합치면 안 된다. 02도 둘이다**
      원래 "이주가 끝난 뒤 합친다"고 적어 뒀는데, **P-45에서 02를 다시 읽으니
      02가 일부러 갈라 놓은 것**이었다:
      · 주경 `growth_engine.rs:186` `potential_cap_factor` — **soft cap**
        (`ratio >= 1.00` 갈래가 없다 · 0.10이 끝)
      · NPC `npc_sim.rs:2676` `potential_cap` — **hard cap**(천장 0.00)
      02 검사도 그 전제 위에 선다(`startPresets.test.ts:71` "1주차부터 0.10배").
      → **합치지 않는다.** 두 표가 일부러 다르다는 것을 `growth_test.gd`가
      검사 3으로 못 박았다(변이 4/4). ⚠ 나이 계수도 같이 다시 본 뒤에 판단한다
- [x] **D-8** "화면을 모른다" 검사가 소스 **전체 문자열**을 보던 것 —
      2026-08-17 고침. `not_contains`가 **대소문자를 무시하고**(사전 키
      `"label"`을 `Label` 노드로 잡는다) **주석까지 코드로 본다**(02 심볼을
      근거로 인용하면 걸린다). F-6b에서 `new_game_vm_test`가 실제로 그렇게
      막혔고 **거기만** 고쳐 뒀었다.
      → `test/support/code_text.gd`(`CodeText.lacks`)로 **정본을 하나로**
      모으고 다섯 자리를 전부 그걸 쓰게 했다(`new_game_vm_test`의 사본도
      지웠다 — 고칠 자리가 다섯이면 그중 하나는 반드시 빠진다).
      ⚠ **양쪽을 다 확인했다**: 주석에 `sort_custom`을 인용해도 통과하고
      (거짓 실패 없음), 진짜로 `sort_custom`·`Calendar.`·`NpcStore`를 넣으면
      **3/3 잡힌다**(거짓 통과 없음)

## P. 02 대조에서 나온 것 (`docs/PARITY.md`)

- [x] **P-1 NPC 투구 선택** — 2026-08-17 이식 (`75f1a3058`).
      `MatchDay._decide`가 `PitchAi.decide`를 부른다. 존 밖 겨냥이
      한가운데로 새던 것도 같이 고쳤다(`2c1833bd4`) — **9이닝당 볼넷이
      0.0에서 3.4가 됐다**(02는 3.3).
      ⚠ **이 항목이 끝난 뒤에도 한동안 미완으로 남아 있었다** — 2026-08-17
      목록 정리에서 발견했다. 아래는 원래 기록.
- [x] ~~⚠ **P-1 NPC 투구 선택이 벤치마크용 자리표시자다**~~ (축 1)
      `MatchDay._decide`가 `location`을 **1~9(존 안)에서만** 고른다 —
      `GameBench._decide`와 같은 코드고 주석도 "성능을 재는 게 목적이라
      전술은 안 넣는다"다. 그게 **리그 경기 수천 개**에 쓰인다.
      존 밖을 안 던지니 볼이 4개 쌓일 일이 없어 **9이닝당 볼넷 0.0**
      (02는 3.3). 제구 흔들림만으로는 볼넷이 안 난다(실측).
      ⚠ **비율을 지어내면 안 된다** — 26%로 시험하니 볼넷은 0→1.2로
      늘지만 ERA 3.95→5.69, 타율 .251→.285로 같이 치솟는다.
      **02의 NPC 투구 선택을 읽어서 옮기는 일이다.** 동결 해제 후.
- [x] **P-2 · P-3** 밴드 표를 02 출력에 다시 맞췄다 (2026-08-17). **세 번 굴렸다.**
      넷 중 **볼넷(+6%)·타율(+14%)이 ±15% 안에 들었고 탈삼진(+20%)·홈런(−22%)은
      못 들었다.** 정한 대로 세 번에서 멈추고 근거를 `PARITY.md` 축 1에 적었다.
      ⚠ **탈삼진은 헛스윙이 아니라 루킹이 지배한다** — 그림자에 떨어지는 공의
      비율이 04가 높다(표는 02와 같은 값이다). ⚠ **홈런은 구조다** — 04는
      홈런이 `contact_q < 38` 두 밴드에만 있어서, 타율을 내리자 그 밴드에 닿는
      타석이 줄어 홈런도 같이 줄었다.
- [x] **P-2b 홈런 밴드** — 2026-08-17. `contact_q >= 45` 밴드에 홈런을 1%
      열어 **−36% → −23%**로 당겼다(3시드 실측 · `836a7f8ea`). 38 밴드까지
      여는 실험 ②는 볼넷을 6%p 밀어 되돌렸다 — **⬜ P-2c**로 남겼다.
      ⚠ **그림자 착탄 비율은 아직 실측이 아니다** — 아래 P-2d로 남긴다
- [x] **P-2d 탈삼진(+21%)의 원인을 실측한다** (축 1) — 2026-08-17 **닫았다**
      🔴 **대조 상대가 틀렸다.** 02를 직접 돌려서(`node scripts/audit-engine.cjs`)
      갈렸다 — 02 감사의 **① `npc_sim.rs`**(투구를 안 굴리는 시즌 모델) 값과
      04의 투구 단위 엔진을 나란히 놓고 있었다. 04에 대응하는 건 **②
      `match_engine.rs`**이고 02는 그쪽을 **비율 셋**으로만 낸다:
      | | 02 ② | 04 | 02가 적어 둔 구간 |
      |---|---|---|---|
      | 피안타/(피안타+삼진) | 0.54 | 0.51 | 0.45~0.62 ○ |
      | 볼넷/전체 | 0.13 | 0.16 | 0.08~0.20 ○ |
      | **삼진/전체** | **0.40** | **0.41** | 0.25~0.45 ○ |
      **격차는 +21%가 아니라 +2.5%다.** 셋 다 02의 기대 구간 안이다.
      `engine_measure.gd`가 이제 이 셋을 같이 내서 같은 실수를 못 하게 한다.
      **실측 하나**: 04의 탈삼진은 루킹이 지배한다 — 투구 105,423개에서
      `STRIKE_LOOK` 18.9% vs `STRIKE_SWING` 3.4%(5.6배). 주장으로만 적혀
      있던 것을 처음 숫자로 확인했다. 착탄 구역은 존 50.4% · 볼 33.5% ·
      **그림자 16.2%**.
      **추정 둘을 기각했다** — 02 원본과 한 줄씩 대조했다:
      | 의심했던 것 | 02 | 04 | 판정 |
      |---|---|---|---|
      | 착탄 분산 `DISPERSION_*` | 0.15 · 0.003 · 0.002 · 0.001 | **같다** | ✗ 원인 아님 |
      | `gaussian` 구현 | Box-Muller | **같다** | ✗ |
      | `location_q` | `INTENT_MODE = 0` → 착탄 기준 | **같다** | ✗ |
      | `pick_target` · `AUTO_CHASE_*` | 0.30 · 0.50 · 0.66 · 1.05 · 0.45 | **같다** | ✗ |
      | `pitch_quality` 전 항 | — | **같다** | ✗ |
      **남은 것은 컨택 밴드 표 하나다.** 04는 P-2b에서 이 표를 다시 잡았고
      (`pitch_outcome.gd:171-245`, 주석이 "02에서 옮긴 것이 아니다"라고 적고
      있다) **헛스윙을 파울로 옮겼다**:
      | `contact_q` | 02 헛스윙/파울/인플레이 | 04 |
      |---|---|---|
      | ≥72 | 50 / 30 / 20 | **24 / 56 / 20** |
      | ≥60 | 32 / 33 / 35 | **14 / 51 / 35** |
      | ≥52 | 22 / 35 / 43 | **9 / 48 / 43** |
      | ≥45 | 15 / 35 / 50 | **5.5 / 44.5 / 50** |
      | ≥38 | 10 / 32 / 58 | **4 / 38 / 58** |
      **인플레이 몫은 세 밴드 다 똑같고 헛스윙만 절반 이하로 줄었다.**
      "파울이 타석을 늘려 루킹·볼넷 쪽으로 민다"가 그럴듯했지만 **굴려서
      기각했다** — 02의 헛스윙 비율을 그대로 되돌려 3시드를 재니 탈삼진이
      **8.93 → 9.83으로 오히려 늘었다**(타석당 투구 3.55 → 3.46). 지금 표가
      그 축에서 더 낫다. 되돌렸다. 그리고 헛스윙을 두 배로 올려도
      `STRIKE_LOOK`은 18.8~19.0%로 **안 움직인다** — 루킹은 밴드 표가 아니라
      착탄·스윙 모형이 정하고 그 모형은 02와 같다.
      ⚠ **그럴듯한 인과를 표만 보고 적을 뻔했다.** 표는 맞았는데 방향이
      반대였다 — **굴리기 전에는 적지 않는다**

- [x] **P-4 인시즌 1군↔2군 승강** — 2026-08-16 이식
      `sim/promotion_runner.gd`를 만들고 `app_root`의 주 경계에 달았다.
      판정은 `RosterMaintenance.eval_callup`·`eval_calldown`으로 옮겼다.
      주기는 근사 없이 — 02의 "월 첫 주"를 `Calendar.date_of`의 달 변화로
      가른다(04엔 진짜 달력이 있다). 실측 26주에 140건.
      ⚠ **상시 경로가 아직 0건이다.** 부상 대체·부진 대체 사유만 받는데
      계측이 경기를 안 돌려 `npc_injuries`가 비고 `perf`가 없다 —
      **P-7이 풀어야 확인된다.**
      검사 16개 · 변이 8/8. 초과 3명으로 재다가 변이를 놓쳤다(02 결함이
      `over.max(3)`이라 초과가 3이면 결과가 같다) — 1·2·5로 고쳤다.
      근거·표는 `docs/PARITY.md` 축 6.

- [x] ~~P-4 인시즌 1군↔2군 승강이 통째로 미이관이다~~ (아래는 원래 기록)
      **동결과 무관하다** — 수치를 바꾸는 게 아니라 안 도는 시스템을 옮긴다.
      `roster_maintenance.gd`에 판정 조각(`form_score`·`is_slumping`·
      `farm_can_send_up`)과 값이 다 있는데 **부르는 곳이 검사뿐이다.**
      04 주간 경로는 `CampusRunner`·`InjuryRunner`·`RelationshipRunner`
      셋뿐이고 승강 러너가 없다. `season_runner.gd:515`의 정원 초과 강등은
      오프시즌 1회짜리라 다른 일이다.
      02는 시즌 26주에 145건을 옮긴다(640명 중 133명이 움직인다).
      **04는 0건** — 부상으로 자리가 비어도 안 메운다.
      옮길 것: `team_engine.rs:178`(콜업 189줄) · `:397`(콜다운 69줄) ·
      `market.ts:693-830`(주간 구동).
      주기 정본은 `market.ts:693-706` — 월 첫 주 정기(콜업2·콜다운2),
      나머지 주 상시(빈자리만 팀당 1).
      ⚠ 04는 일 단위라 "월 첫 주"를 그대로 못 옮긴다.
      ⚠ 투수 하한 보충은 **별도 패스**다 — 02가 `gap_fill`에 묶었다가
      회귀 4건이 깨졌다.
      근거·표는 `docs/PARITY.md` 축 6.

- [x] **P-5 FA 등급이 실제 플레이에서 A만 나온다** (축 7) — 2026-08-17 고침
      **원인은 등급 표가 아니라 분모였다.** 02는 연봉 기준선을 **프로 전체**
      에서 잡고 그 자리에 이유까지 적어 뒀다(`weekPhases/market.ts:1279`
      — "리그 하나만 보면 해외 시세가 안 잡힌다"). 04는 `league_salaries`
      **리그 하나**를 분모로 썼다. FA 자격자는 근속 쌓인 베테랑이라 자기 리그
      안에서 전부 상위 30%에 들었다 → **A만.**
      → `FaRunner.pro_salaries(world)`(`TeamProfile.PRO_LEAGUES` 셋을 합친다).
      **등급 표는 안 건드렸다** — 02와 글자 그대로 같다.
      실측(3시드, 6해):
      | 시드 | 전 | 후 | 이적 중 보상 |
      |---|---|---|---|
      | 20270101 | A:9 | **B:7 C:1** | 100% → **17%** |
      | 777 | — | **B:5 C:2** | 17% |
      | 4242 | — | **B:7 C:3** | 71% |
      **`protected_count 25`(B)·`0`(C) 갈래가 도달 가능해졌다.**
      ⚠ **이 표본에는 A가 없다** — 6해 시점의 KBL FA가 프로 전체 상위 30%에
      아직 못 든다. 한쪽 몰림이 반대로 간 것일 수 있으나 **02와 같은 구조이므로
      값을 더 만지지 않는다.** 커리어 전체(P-10)에서 다시 본다.
      ⚠ **02의 "A:19 B:18 C:23"으로 판정하지 않았다** — 그건 검사 fixture 값이다
      검사 7 · **변이 3/3**

- [x] **P-5b FA 시장이 04는 리그별 세 판, 02는 프로 전체 한 판이다** — 2026-08-19 고침
      `run_league` → `run_market` 한 판. to_league·정원·보상선수 국적까지 같이.
      실측 6해 — 계약 387명(A:164 B:153 C:70) · 이적 80% · 보상 4%.
      ⚠ P-5c의 "계약 8명"은 계측이 끝난 시장을 다시 돌린 값이었다.
      검사 30 · 변이 4/5(다섯째는 open_slots가 먼저 막아 표면화 안 됨).
      02 `market.ts:1241-1266`은 `activeProLeagues()`의 1군 팀을 **한 판에**
      넣는다 — 한국 선수가 JBL 팀과 계약하는 경로가 실제로 있고, 02는 그때
      **보상선수가 리그를 건너오는 함정**까지 적어 뒀다("같은 함정에 FA·
      드래프트·트레이드에서 걸렸다 — 문지기로 쓰던 자리가 다섯"). 04는
      `for lid in PRO_LEAGUES: run_league`라 **리그를 넘는 이적이 아예 없다.**
      ⚠ 옮기면 선수가 리그를 넘으므로 `_move`의 `to_league`·외국인 한도·
      보상선수 국적 판정이 같이 걸린다. **P-5보다 크다** — 따로 본다

- [x] **P-5c 계측이 게임 경로를 복제한다** — 2026-08-18 고침 (파일은 `bench/`다)
      ⚠ **한 번 반쪽으로 닫았다** — 함수는 냈는데 `run_league`가 여전히 제 손으로
      지었고 커밋 메시지엔 부른다고 적었다. `bench_paths_test`가 잡았다.
      `fa_measure.gd:70-93`이 `FaRunner.run_league`가 짓는 것을 **손으로 다시
      짓는다**(세계를 안 바꾸려고). 그래서 P-5에서 게임 쪽 분모를 고쳤는데
      **계측이 안 따라와 "고쳤는데 그대로"로 한 번 읽혔다.** 계측 전용으로
      `run_league`에 "적용하지 않는다" 갈래를 두거나, 짓는 부분을 함수 하나로
      빼서 둘이 같이 부르게 한다. ⚠ **같은 형태를 F-1에서도 겪었다**

- [x] ~~P-5 (원래 기록)~~ — 동결 해제 후
      등급 표는 02와 글자 그대로 같다(A 30%/보호20/보상200 · B 60%/25/100 ·
      C 100%/0/150). 갈리는 건 풀이다 — FA 자격자가 전부 근속 쌓인
      베테랑이라 연봉 백분위가 다 상위 30% 안에 든다.
      그래서 `protected_count 25`(B)와 `0`(C) 갈래가 **도달 불가**고,
      이적 중 보상선수 발생이 100%가 된다(02 검사는 59%).
      ⚠ 02의 "A:19 B:18 C:23"으로 판정하면 안 된다 — 그건 게임 값이
      아니라 검사 fixture 값이다. 02엔 진짜 세계 FA 등급 계측이 없다.
      **백분위 기준을 리그 전체가 아니라 FA 풀 안에서 잡을지**가 논점인데
      값을 건드리는 일이라 동결 해제 후에 본다.

- [x] **P-6 재정 등급이 팀 데이터에 없던 것** (축 8) — 2026-08-16 고침
      원인은 성향 생성이 아니라 **데이터 누락**이었다. 02는 재정 등급을
      `refs.json`의 `traits.resource`에 적어 두고 스태프 생성이 그걸 읽는데
      (`test-staff-gen.cjs:32`), 04 `teams.json`이 `traits`를 통째로 안
      가져왔다. 그래서 `spending_tier(willingness)`라는 대체 축을 만들었고,
      성향을 짓는 코드가 없어 **238팀 전부가 '안정' 하나**가 됐다.
      02의 `resource`를 182팀에 그대로 옮겼다 — 부유 14 · 궁핍 7 ·
      알뜰 45 · 안정 116으로 **02와 같은 분포**다.
      대체 축(`spending_tier`)은 지웠다 — 정본이 둘이면 언젠가 갈린다.
      `staff_lifecycle._make`가 50.0을 박아 두던 것도 같이 고쳤다
      (궁핍한 구단의 구단주가 은퇴하면 후임이 중립이 됐다).

- [x] **P-6b 2군에 스태프가 없던 것** — 2026-08-16 고침
      `World.teams_of`의 `_FARM` 파생 사전에 `power`·`resource`를 실었다.
      데이터의 `_2` 행과 부모를 38쌍 대조하니 두 값이 **전부 같아서**
      부모에서 실어도 어긋나지 않는다.
      `LEAGUE_KBL_FARM`을 스태프 리그에 넣고 리그 보정 +10을 적었다 —
      02는 2군이 `LEAGUE_KBL` 안의 행이라 1군과 같은 보정을 받는다.
      감독 182 · 구단주 182 · 부유 14 · 궁핍 7로 **02와 일치**한다.

- [x] ~~P-6 팀 성향이 한 번도 생성되지 않는다~~ (아래는 원래 기록)
      ⚠ **여전히 참이다** — `TeamProfile`은 아직 아무도 안 짓는다.
      다만 스태프는 이제 팀 데이터를 읽으므로 여기 안 걸린다.
      **남은 소비처는 FA 입찰 `budget_index`와 승강 `win_now_pressure`다.**
      P-4에서 같이 본다.
      `TeamProfile.of`가 세계에 성향이 없으면 `DEFAULT`(전부 50)를 준다.
      **성향을 짓는 코드가 없다** — `patch`를 부르는 곳은
      `staff_lifecycle.gd:169`의 `bad_seasons` 하나뿐이다.
      실측: 부유 구단주 0명 · 궁핍 0명 (02는 14명 · 7명).
      전 구단이 '안정' 하나라 **코치 수가 팀마다 안 갈리고**(전부 4~6)
      구단주 예산·시설이 전부 중립이다.
      ⚠ **축 7의 수수께끼가 여기서 풀린다** — FA 입찰의 `budget_index`가
      `owner_spending_willingness / 50`인데 전 팀이 50이라 **모두 1.0**이었다.
      승강 판정의 `win_now_pressure`도 전부 50이다.
      같이 볼 것: **2군에 스태프가 없다.** 02는 감독 182명(2군 포함),
      04는 172명(1군만) — 2군 선수의 훈련 보정이 통째로 중립이다.
      근거·표는 `docs/PARITY.md` 축 8.

- [x] **P-7 주간 파이프라인을 `sim/`으로** — 2026-08-16 이식
      `app_root._apply_one_week`(133줄) · `_apply_finance` ·
      `_apply_academics`를 `sim/week_runner.gd`로, `_play_game`을
      `sim/game_sim.gd`로 내렸다. 화면엔 프레임 쪼개기와 세는 것만 남았다.
      검사가 화면 메서드를 부르고 있어서 여섯 파일을 `WeekRunner.run`으로
      돌렸다 — **검사가 화면을 거쳐 로직을 보고 있었다는 뜻**이다.
      결과: 관계 계측이 처음으로 동료를 잰다(평균 0.0 → 24.1, 02는 33.0).
      라벨 분포도 02와 같은 모양이다.

- [x] **P-7b 라이벌이 0명이던 것** — 2026-08-16 고침
      `faced_rivals`가 늘 빈 배열이었다. 읽는 곳은 있는데 **채우는 곳이
      없었다** — 주석에도 "04에 라이벌 지정 경로가 없다"고 적혀 있었다.
      02(`advanceWeek.ts:1215`)를 옮겼다: **내가 던진 그 경기에서, 상대
      투수 중 이닝 최다 한 명.** 불펜까지 잡으면 관계가 폭증한다.
      행을 만드는 자리도 없었다 — `reconcile`은 같은 팀만 보므로 라이벌을
      영영 못 만든다. `_meet_rivals`를 붙였다("사건이 관계의 시작이다").
      ⚠ **진짜 원인은 따로 있었다.** 라이벌 코드를 넣어도 0명이길래 계측에
      등판 수를 찍어 보니 **주인공 등판 0경기**였다. `DayEngine`이 등판일에
      멈추는데(설계) 계측이 그 경기를 처리하지 않아 `result: null`로 남았다.
      계측이 사용자의 '자동'과 같은 자리를 대신 눌러 주게 했다 —
      1해에 등판 10경기 · 라이벌 5명(02는 커리어 전체 8명).
      검사 4개 · 변이 4/4. 불펜을 선발보다 먼저 넣어야 "이닝 최다"가
      검사에 걸린다 — 순서를 뒤집기 전엔 변이를 놓쳤다.

- [x] **P-7c 코치 관계가 낮던 것** — 2026-08-16 원인 규명
      **04 결함이 아니라 계측 fixture 문제였다.** 훈련 계획에 프로그램 id가
      아니라 focus 이름(`"velocity"`)을 넣어 `Training.program()`이 못 찾았고,
      **훈련이 통째로 안 돌았다** — 성장 임계 넘은 주 0 · 훈련 영역이 정해진
      주 0. ⚠ **계획이 비어 보이지도 않아서 `training_skip`(−2)도 안 걸렸다.**
      값이 0으로 조용히 남는 자리다.
      진짜 id를 넣으니 208주 전부 영역이 잡히고 코치 1.0 → **36.3**(02는 29.0),
      감독 0.0 → 14.0(02는 8.0), 동료 24.1 → 44.9(02는 33.0)가 됐다.

- [x] 🔴 **P-8 주인공이 두 벌로 갈리던 것** — 2026-08-16 고침
      `World.relink_protagonist(state)`를 만들고 깊은 복사를 하는 세 곳이
      부르게 했다 — `day_engine.gd:154`·`:191`, `day_runner.gd:57`.
      **정본은 로스터고 `protagonist`는 그 참조다.** 두 사전을 맞추는
      코드를 쓰면 그게 세 번째 정본이 되므로 값을 옮기지 않는다.
      고친 뒤 실측: 학년이 1→2→3으로 오르고 3학년에 `career_results`가 뜬다.
      검사 넷 · 변이 2/3(등가 하나는 죽은 가드라 지웠다 — 빈 팀 id로
      조회해도 빈 배열이라 조기 반환이 동작을 안 바꿨다).
      ⚠ **`==`로는 못 잡는다.** GDScript 사전 `==`는 값 비교라 내용이 같으면
      참이다. `is_same`으로 봐야 참조 동일성이 잡힌다 — 처음에 `==`로 써서
      검사 셋이 헛통과했다.
      **세이브 로드도 같은 자리였다(같은 날 고침).** `app_root.set_state`가
      `duplicate(true)`를 해서 불러온 게임에서 다시 갈렸다 — 거기서도
      `relink_protagonist`를 부른다. 검사 둘을 붙였다.

- [x] 🔴 **P-8b 주인공이 NPC 진로 배정을 타던 것** — 2026-08-16 고침
      `Promotion.advance_grades`가 졸업 갈래에서 주인공을 거른다.
      **학년은 그대로 올린다** — 거르는 것은 졸업 뒤 소속 배정뿐이다.
      고친 뒤 실측: 주인공이 3학년에 머물고 리그가 안 바뀌며
      `career_results` 대기가 뜬다 — **세계가 소속을 안 정하고 결정을
      기다린다.** 그게 옳은 동작이다.
      검사 셋 · 변이 2/3. 못 잡은 하나는 등가다 — `advance_grades`가
      돌려주는 `updated`를 **아무도 안 쓴다**(`season_runner.gd:60`이
      `hs_graduated`·`univ_graduated`만 읽는다). 아래 P-8d 참조.

- [x] **P-8d `advance_grades`의 `updated` 반환이 죽어 있던 것** —
      2026-08-16 지웠다. 검사 11곳을 사전 직접 보기로 바꿨다
      (`_run([n])` 뒤 `n["grade"]`). 변이 3/3.

- [x] **P-8c(a) 계측이 진로 결정에 답을 안 해 커리어가 멈추던 것** —
      2026-08-16 고침. `career_choice_hub` 하나만 답하고 있었는데
      흐름이 셋이다: 지원 → **결과 확인**(`career_results`) →
      **최종 선택**(`career_choice`). 뒤 둘이 안 풀려 3학년에 섰다.
      고친 뒤 2029년에 `university/LEAGUE_UNIVERSITY`로 **stage와 리그가
      함께** 바뀌고 대기줄도 빈다.

- [x] **P-8c(b) 등판이 10경기로 보이던 것** — 2026-08-16 계측 문제였다
      **04 결함이 아니다.** `state["schedule"]`은 시즌을 넘길 때 통째로
      덮어써지는데(`season_runner.gd:664`) 계측이 **끝나고 한 번만** 셌다 —
      그래서 늘 마지막 해 것만 남았다. **10경기는 한 시즌 등판 수**였다.
      시즌을 넘기기 전에 세어 누적하도록 고쳤다.
      ⚠ 일정은 해마다 제대로 다시 만들어진다 — `build_schedule`을 부르는
      곳을 세어 확인했다.

- [x] 🔴 **P-9 `run_season`을 아무도 안 부르던 것** — 2026-08-16 이식
      `SeasonEnd.PHASES`에 `season_relations`를 넣고 `SeasonRunner`가
      `season_history` **뒤에** 부른다 — 성적이 굳은 뒤라야 그해 것으로 잰다.
      넷을 정본에서 얻는다(지어내지 않았다): `era`는 `season_stats`의 내 줄 →
      `SeasonStats.calc_era`, `team_rank_pct`는 `Standings.from_schedule`의
      내 리그 순위(0.0=1위~1.0=꼴찌), `pitched_any`는 내 줄의 이닝.
      순위표가 없으면 0.5로 둔다 — 없는 성적을 좋게도 나쁘게도 안 읽는다.
      ⚠ **변이 검증이 무의미했다.** 셋 다 "잡힘(멈춤)"으로 나왔는데
      90초 타임아웃이라 변이 때문인지 알 수 없다 — `season_runner_test`가
      원래 오래 걸린다. **다시 재야 한다**(아래 P-9b).
      ⚠ 검사 하나를 지웠다. 관계 기록으로 배선을 보려 했는데 `_log`가
      **라벨이 바뀐 것만** 남기고, 새 게임 한 해로는 순위표가 비어 팀 등급이
      0이라 델타가 안 난다 — 그게 옳은 동작이라 볼 것이 없었다.
      배선은 `PHASES`와 `done` 목록이 지킨다.

- [x] 🔴 **P-12 승강 판정이 성적을 못 보던 것** — 2026-08-16 고침
      `PromotionRunner.attach_perf`로 시즌 누계를 붙인다. 02도 창을 안
      자른다(`market.ts:39` `seasonPerfOf`) — 최근 N경기로 바꾸면 그게
      두 번째 규칙이 된다.
      실측: 총 이동 140 → **220**, 정기 140 → 186, **상시 0 → 34**,
      왕복 0 → **9**. 상시 경로가 처음으로 돌고, 부진해서 내려갔다
      살아나 올라오는 사람이 생겼다.
      검사 넷 — 투수 줄·타자 줄이 `perf`가 되는가, 기록 없으면 빈 사전인가,
      **같은 능력치에서 부진한 1군이 호투한 2군에게 자리를 내주는가**.
      마지막 것이 이 축의 요점이다.

- [x] ~~🔴 P-12 승강 판정이 성적을 못 본다~~ (원래 기록)
      계측에 경기를 붙였는데도 **상시 승강이 0건**이었다. 원인을 팠다:
      `RosterMaintenance.form_score`가 `p["perf"]`를 읽는데
      **`sim/` 전체에서 그 키를 쓰는 곳이 `roster_maintenance.gd` 하나**다 —
      **채우는 곳이 없다.**
      · `PromotionRunner.run_team`이 `_split`으로 로스터 사전을 그대로
        넘긴다. 02는 `getTeamEntityRefs`가 라이브 스탯을 붙여 넘겼다
        (`market.ts:766`)
      · 그래서 `form_score`가 늘 0이고, `is_slumping`이 참이 안 되며,
        **상시 사유(`slump_replacement`)가 영영 안 난다**
      · 부상 사유(`injury_replacement`)도 `npc_injuries`에 중등도 이상이
        쌓여야 하는데 **26주로는 안 쌓이는지** 따로 세야 한다
      ⚠ **성적이 안 보이면 정기 승강도 능력치만 본다.** `rated()`가
        `ovr + form_score × 8`인데 뒤 항이 늘 0이다 — 02가 "성적이
        능력치를 뒤집되 완전히 무시하진 않는" 지점으로 잡은 가중 8이
        **통째로 죽어 있다.** 정기 140건도 그래서 OVR 순서대로다
      · 옮길 것: `season_stats`(또는 최근 N경기)를 로스터 사전의 `perf`로
        붙이는 자리. **02가 어디서 어떤 창으로 잘랐는지 먼저 읽는다**

- [x] **P-10 커리어 20해 대조** — 2026-08-17 돌렸다(4,377초 · 백그라운드).
      🔴 **대조를 하러 갔다가 커리어가 멈춰 있는 걸 찾았다.** 관계 크기를
      02와 비교하는 게 목적이었는데 **비교할 커리어가 없었다.**
      실측(20해 · 씨앗 20270101): 등판 189경기 · 상대 투수 줄 777개
      | 관계 | 인원 | 값 |
      |---|---|---|
      | teammate | 217명 | 0~100 (평균 41.5) |
      | **rival** | **8명** | **0~0 (평균 0.0)** |
      | coach | 7명 | −55~97 (평균 −7.3) |
      | manager | 2명 | −100~0 (평균 −50.0) |
      | owner | 2명 | 0~3 (평균 1.5) |
      라벨 각별 67 · 신뢰 50 · 우호 41 · 중립 74 · 불신 3 · 적대 1.
      **162명이 중립을 벗어났다.**
      ⚠ **라이벌 인원은 02와 같은 8명이 됐다**(P-11의 효과) — 그런데 **값이
      전부 0이다.** 잡히기만 하고 안 쌓인다. 아래 P-13으로 뗐다.
      ⚠ **아래 넷을 새로 찾았다.** P-10 자체는 닫고 항목으로 나눈다

- [x] **P-45 OVR이 잠재력을 넘는다** — 2026-08-19 **결함이 아니었다(내 오독)**
      02는 성장 엔진이 둘이다: **주인공** `growth_engine.rs:186`은 천장 갈래가
      **없고**(0.10), **NPC** `npc_sim.rs:2676`만 0.00이다. 04도 정확히 그렇게
      갈라 놓았다 — **처음부터 맞았다.** 나는 NPC 표를 주인공 표와 견줬다.
      02 검사도 그 전제다(`startPresets.test.ts:71` "1주차부터 0.10배").
      04 검사에도 이미 있었다(`growth_test.gd:50`) — 내가 안 읽었다.
      → 두 표가 일부러 다르다는 것을 검사로 박았다. 검사 3 · 변이 4/4.
      ⚠ **같은 이름의 함수가 02에 둘 있으면 어느 쪽이 주인공용인지 먼저 가른다**

- [x] **P-13 라이벌 값이 전부 0이던 것** — 2026-08-19 **판정: 02와 같다**
      20해 실측 `rival 0~0`. 배선을 다 찍어서 지운 뒤 **02와 규칙 파일을
      통째로 대조하니 차이가 0이었다**(`weekly.rival`도 `decay`도 글자 그대로).
      02 엔진(`relationship.rs:415`)도 같은 모양이다. **04의 결함이 아니라
      02에서 물려받은 성질이다** — 맞대결이 한 해 몇 번인데 시즌마다 0.9배로
      식는다. ⚠ **02 계측도 라이벌을 안 낸다** — 02에도 기준값이 없다.
      ⚠ **바꾸면 이주가 아니라 밸런스 결정이다** → 이주 뒤 사용자 결정으로.
      (아래는 처음 잡을 때의 관찰) — 2026-08-18 **P-14로 움직이기 시작했다**
      P-10 실측(20해): `rival` 8명 · **0~0 · 평균 0.0**.
      P-14 뒤 실측(6해): `rival` 6명 · **−5~4 · 평균 −0.2**.
      **먹이는 자리가 없던 게 아니라 주인공이 로스터에 없어서 안 걸렸다.**
      ⚠ **아직 작다.** 20해를 다시 굴려 teammate(평균 41.5)와 견줄 크기가
      되는지 본다 — 그때까지 열어 둔다
      🔴 **2026-08-19 P-42·43·44를 고친 뒤 20해로 다시 쟀다** (커리어가 프로
      까지 굴러간 첫 표본): `rival 21명 · 0~0 · 평균 0.0` · teammate 평균 51.6.
      **배선은 다 맞다** — 규칙 표(`faced_and_won 3`)·`weekly`의 rival 분기·
      `_meet_rivals`·순서를 찍어서 하나씩 지웠다.
      남은 후보는 **감쇠**다(12해 −2~0 → 20해 0~0으로 수렴).
      ⚠ **값을 만지기 전에 02를 돌려서 라이벌 값을 잰다** —
      `02.SvelteElectron/scripts/measure-relations.cjs`. 02 값 없이는 "작다"를
      판정할 수 없다
      🔴 **2026-08-19 20해를 굴렸는데 못 쟀다.** 주인공이 2032년부터 15해를
      대학 4학년에 멈춰 있다 — 계측이 `draft_observe`·`sports_unit_apply`·
      `military_enlist_ask`를 안 답한다. 그 상태의 rival 0.0은 판정에 못 쓴다.
      **계측이 커리어를 프로까지 밀어야 20해가 뜻을 갖는다**(P-16b 계열)

- [x] 🔴 **P-14 대학 진학 뒤 주인공이 로스터에서 사라지던 것** — 2026-08-18 고침
      `CareerDecision._move_to`가 `p["team_id"]`만 바꾸고 **로스터를 안
      건드렸다.** 04는 주인공도 로스터에 사는 구조인데(`world.gd:280`)
      `relink_protagonist`가 **새 팀 로스터에서** 찾으므로 영영 못 찾는다.
      ⚠ **02엔 대응 코드가 없다** — 02는 주인공을 로스터 밖(`g.protagonist`)에
      둔다. **04 자기 규칙을 04가 안 지키던 것**이다. → `_move_roster`.
      **양쪽 배열을 같이 고친다**(`FaRunner._move`와 같은 자리) ·
      **같은 사전을 넣는다**(복사본이면 한쪽만 자란다) ·
      **주인공만**(NPC는 `FaRunner._move`·승강이 맡는다).
      실측(6해): 로스터 속 나 **매년 찾는다**. 검사 7 · **변이 7/7**
      ⚠ **변이 둘이 처음에 안 잡혔다** — ① `relink_protagonist`를 부르고 재면
      복사본이어도 둘이 같아진다(바깥에 들고 있던 원본으로 재도록 고쳤다)
      ② 개수만 세면 "빼고 뒤에 붙이기"가 등가다(**자리**를 보도록 고쳤고
      픽스처에서 나를 가운데로 옮겼다)

- [x] 🔴 **P-15 대학 학년이 18해째 1학년이던 것** — 2026-08-18, **P-14가 원인**
      `SeasonRunner.all_players`가 **로스터만** 훑는다(`season_runner.gd:27-32`).
      주인공이 어느 로스터에도 없으니 `Promotion.advance_grades`가 **볼 수가
      없었다.** P-14를 고치자 실측에서 **1→2→3→4로 오른다**(6해).
      ⚠ **따로 고칠 게 없었다** — 원인이 하나였다

- [x] ~~🔴 P-16 20해에 성장한 주가 1주뿐이다~~ — 2026-08-18 **결함이 아니었다.
      계측이 틀린 질문을 하고 있었다**
      `relations_measure`의 `_grew_weeks`는 **"한 주에 OVR이 1칸 이상 오른
      주"**를 센다. 그런데 OVR은 가중 평균을 반올림한 값이고 성장은 스탯별
      XP로 조금씩 붙는다 — **한 주에 1칸을 넘는 일이 원래 드물다.**
      `measure:pitch`로 확인: **8해 뒤 투구 OVR 중앙 70 · 최대 78**
      (시작 50대). 주당 평균 0.05칸이니 임계를 넘는 주가 1인 게 맞다.
      ⚠ **"훈련 영역이 정해진 주 312/312"가 옆에 있었는데도 "훈련은 도는데
      능력치가 안 오른다"로 읽었다** — 두 숫자가 같은 말을 하고 있었다.
      ⬜ **P-16b로 뗀다**: 이 숫자는 오해를 부르므로 계측이 **OVR 시작→끝**을
      같이 내게 고친다

- [x] **P-16b `relations_measure`가 성장을 오해하게 내놓는다** — 2026-08-18 고침
      **04 산식은 멀쩡했다.** 계측이 훈련 계획을 한 번만 세워 부상 68%였다
      (게임은 매 걸음 `AutoTraining.apply`). 걸고 나니 부상 17% · OVR +1.0/해.
      `potential` 키도 틀렸었다(정본은 `potential_hidden`). → `bench_paths_test`
      `_grew_weeks`("한 주에 1칸 넘은 주")만 내서 **20해에 1주**로 보였고,
      그걸 "성장이 막혔다"로 읽어 P-16을 잘못 세웠다. **OVR 시작→끝**과
      **해마다의 OVR**을 같이 내면 한눈에 갈린다.
      ⚠ **계측이 세는 것과 사람이 궁금한 것이 다르면 그 숫자는 함정이다**

- [x] 🔴 **P-44 병역·전역 경로가 로스터를 안 옮긴다** — 2026-08-19 고침
      `Military.enlist`·`discharge`가 `CareerDecision.move_roster`를 부른다.
      `move_roster`는 갈 팀이 없으면 빼기만 한다(빈 이름 유령 팀 방지).
      실측 12해 — **커리어가 처음으로 프로까지 갔다**(2037~2038 KBL) ·
      manager·owner가 셋으로 늘었다. 검사 6 · 변이 4/5.
      ⬜ **rival은 아직 −2~0(평균 −0.3)** — P-13을 20해로 다시 잰다.
      (아래는 처음 잡을 때의 관찰)
      P-43을 고쳐 커리어가 움직이자 드러났다 — **병역 이후 12해 내내
      "로스터 속 나: 못 찾음"**이다(military → independent 전부).
      **P-14와 같은 계열**: 그때는 `CareerDecision._move_to`가 `team_id`만
      바꾸고 로스터를 안 건드렸고, 이번엔 `Military.enlist`·`Military.discharge`다.
      ⚠ **04는 주인공도 로스터에 산다**(`world.gd:280`). 없으면 동료·라이벌·
      성장이 그 위에 선다 — 실측에서 **rival이 −2~0(평균 −0.3)**으로 작다.
      → `CareerDecision.move_roster`를 병역/전역 경로에서도 부른다.
      ⚠ **양쪽 배열을 같이 고친다**(`FaRunner._move`가 같은 자리에 적어 뒀다)
      ⚠ **P-13(라이벌 20해)은 이걸 고친 뒤에 잰다**

- [x] 🔴 **P-43 04가 02에 없는 갈래를 갖고 있다** — 2026-08-19 고침
      `_choice`가 `continue`를 무대와 무관하게 늘 붙였는데 엔진은 이미 가른다
      (`continue_current_stage`가 고교면 false) — **화면이 엔진이 못 받는 것을
      냈다.** 02처럼 대학=진급 · 독립=계속 · 전원탈락+미필=현역 입대로 갈랐다.
      실측 12해 — 고교 → 병역(2030) → 독립리그(2031~2038) · OVR 68→76.
      검사 11 · 변이 5/5. 다음은 **P-44**.
      (아래는 처음 잡을 때의 관찰)
      **02를 읽었더니 답이 나왔다**(`CareerResultModal.svelte:75-113`).
      02의 진로 최종 선택은 이렇게만 준다:
        · `canContinue` — **대학생만** (다음 학년 진급)
        · `canContinueIndie` — **독립리그만**
        · 합격·지명이 있으면 그것들
        · **전원 탈락 + 미필 → "전원 탈락: 현역 입대"**
      04 `DecisionVm._choice`는 `continue`("지금 자리에 남는다")를 **무대와
      무관하게 항상** 붙인다. 그래서 고교 졸업반이 다 떨어지면 재수가 되고,
      계측에서 **12해 내내 고교 3학년**이었다.
      ⚠ 04에도 병역 경로는 있다(`career_decision.gd:445` "갈 곳이 없다 —
      병역이 남는다") — **선택지를 안 내주고 있을 뿐이다**(형태 ② 계열).
      ⚠ 02 주석이 그 자리의 함정도 적어 뒀다 — "군필자에게 입대를 또 권해
      한 커리어가 군 복무를 **세 번** 했다". 조건에 `militaryStatus === "미필"`이
      들어가는 이유다.
      → `_choice`가 무대를 보고 갈래를 낸다 + 전원 탈락·미필이면 현역 입대.

- [ ] **P-43b 계측 fixture가 대학을 하나만 지원한다** (2026-08-19)
      P-42를 고치고 계측이 `DecisionVm`을 타게 하니 **12해 내내 고교 3학년**
      이었다. 원서를 하나만 넣어 떨어지고, 남는 선택지가 `continue`뿐이다.
      · 계측 fixture가 대학 셋(`MAX_CHOICES`)을 다 지원해야 하는가
      · **02에 "고교 3학년 유급" 갈래가 있는가** — `_stay_label`이 무대마다
        다르게 적는데(대학은 진급·독립은 계속) 고교는 무엇인가
      ⚠ **관계 값은 이때 처음으로 02 규모로 움직였다** — teammate 평균 84.8
      (전 25.5) · rival −70~42(전 0~0) · manager 98. **예전 숫자는 계측이
      우회로 만든 세계의 값이었다.**
      ⚠ **P-13(라이벌 20해)은 이것까지 정리한 뒤에 잰다**

- [x] 🔴 **P-42 대학 4학년을 마쳐도 프로로 안 간다** — 2026-08-19 고침
      원인은 `state["career"]`가 안 지워지는 것. 고교에서 채운 `results`가
      남아 `_should_open`에 매년 걸렸다 → `_move_to`가 `reset_decision`을 부른다.
      02도 같은 증상을 겪고 적어 뒀다(`game.ts:1768`). 검사 9 · 변이 9/9.
      실측 12해 — 2030년부터 `career_choice`가 뜬다. 다음은 **P-43**.
      (아래는 처음 잡을 때의 관찰)
      계측 9해: 2032년에 대학 4학년이 되고 **2035년까지 학년 4에 머문다.**
      대기줄은 비어 있다(P-13에서 물음 셋을 답하게 고쳤다) — 즉 **아무도
      다음 진로를 안 연다.** 20해를 굴리면 15해를 그 자리에서 보낸다.
      · 고교 3학년 뒤에는 `career_choice_hub`가 열린다 — 대학 뒤에는 안 열린다
      · `SeasonRunner`의 학년 진급이 4에서 멈추고 졸업 판정이 없는 것으로 보인다
      ⚠ **P-13(라이벌)·P-10(커리어 추적)이 이것 때문에 못 잰다.** 프로 관계
      (구단주·감독)와 라이벌 표본이 대학에 갇혀 있다.
      ⚠ 02는 대학 졸업 뒤 드래프트·독립·실업을 연다 — `careerPath.ts`가 정본

- [x] **P-17 대학 리그가 해마다 9경기다** — 2026-08-18 **오해였다**
      "내 경기 9"는 **등판 수**지 팀 경기 수가 아니다. 팀은 고교 23 · 대학 16
      (리그 20·9 + 대회)이고 02도 `UNIV_TARGET_GAMES = 9`다 — 값이 맞다.
      P-10 추적: 20해 내내 `내 경기 9`다. 고교도 9였다. 등판 189경기 ÷ 20해
      ≈ 9.5. **02의 대학 일정이 몇 경기인지 확인하고 맞춘다**

- [x] **P-11 라이벌 — 아군이 라이벌로 잡히고 있었다** (2026-08-17)
      세어 보니 원인이 "같은 상대를 반복해 만나서"가 아니었다.
      한 해를 굴려 `faced_rivals`가 담은 id를 세니 **다섯 중 하나가 내 팀
      동료**였고, 그것도 **아홉 번 중 다섯 번**으로 제일 많이 잡혔다.
      · `player_lines`는 **양 팀 투수 줄을 합쳐 놓아** 어느 쪽인지가 사라진다.
        `player_id != me`인 투수 중 이닝 최다를 고르니 **내가 3이닝 던지고
        우리 불펜이 6이닝 던지면 그 불펜이 "라이벌"**이 됐다
      · **그 자리를 뺏기는 만큼 진짜 상대 선발이 안 잡혔다** — 라이벌이
        안 느는 진짜 이유가 이것이다
      ⚠ **02도 같은 결함이 있다**(`advanceWeek.ts:1215-1222`가
        `playerId !== protagonist.id`만 본다). 하지만 "라이벌"이라는 말 자체가
        상대편을 뜻한다 — **옮길 값이 아니라 명백한 결함이라 고쳤다.**
      ⚠ "로스터가 비면 안 잡는다" 가드는 **안 만들었다** — 정상 게임에서
        로스터는 최소 주인공을 담아 절대 안 빈다(죽은 코드가 된다).
        대신 **진짜 세계로 재는 검사**를 뒀다
      검사 6 · **변이 6/6**

- [x] **P-9b P-9 변이 검증을 다시 잰 것** — 2026-08-16
      느린 파일(`season_runner_test`, 한 해를 실제로 굴린다)로 재서 셋 다
      타임아웃 "잡힘"이었다. **빠른 파일로 옮겨 다시 쟀다.**
      · 단계 목록 변이 둘 → `season_end_test.gd`로 **2/2**
      · 순위 환산 변이 → `season_end_test.gd`가 그 산식을 안 봐서 **0/2**.
        **검사가 아예 없었다** — `rank_pct_of`를 작은 static 함수로 빼고
        검사 셋을 넣어 **3/3**
      ⚠ **못 잡은 변이가 "검사가 약하다"가 아니라 "검사가 없다"였다.**
        느린 파일에 묻혀 있어서 안 보였다.

- [x] ~~P-9b P-9 변이 검증을 다시 재라~~ (원래 기록)
      `season_runner_test`가 90초를 넘겨 변이 셋이 전부 타임아웃으로
      "잡힘" 처리됐다. 변이 때문인지 느려서인지 갈리지 않는다.
      ⚠ `mutate.cjs`의 대기 시간을 늘리거나, **더 빠른 검사 파일**을
      대상으로 잡는다(`season_end_test.gd`는 단계 목록만 봐서 빠르다).

- [x] ~~🔴 P-9 `RelationshipRunner.run_season`을 아무도 안 부른다~~ (원래 기록)
      **P-4·P-7b와 같은 패턴이다** — 함수와 값은 있는데 부르는 곳이 없다.
      전체를 훑어 확인했다: `sim/` · `ui/` · `bench/` 어디에도 없다.
      구단주는 **주간 항목이 없고 시즌 성적으로만 움직인다**
      (`Relationship.season`). 그래서 실측이 02의 +11.0인데 04는 0.0이다.
      · 붙일 자리는 `SeasonRunner` 안이다 — `run_season(state, era,
        team_rank_pct, pitched_any, at_day)`가 시즌 성적과 팀 순위를 받는다
      ⚠ 그 넷을 **어디서 얻는지 먼저 센다.** 계측이 지어내면 안 된다 —
        시즌 성적은 `season_stats`, 순위는 `Standings.from_schedule`이
        정본인지 확인한다
      ⚠ 검사를 먼저 써서 실패를 본 뒤 고친다

- [x] ~~P-8c(b) 등판이 여전히 10경기다~~ (원래 기록)
      진로가 풀린 뒤에도 그대로다 — 대학에 가서도 첫 해 몫만 쌓인다.
      ⚠ **입력을 센다**: `MatchDay.play`가 게이트(`skip_injury`)를 보고
      주인공을 안 내보내는지, `is_protagonist_game`이 **새 시즌 일정에
      다시 붙는지**. `World.build_schedule`이 해마다 다시 도는지부터 본다.
      라이벌이 5명에서 안 느는 것(02는 8명)이 여기 걸려 있다.

- [x] ~~P-8d `advance_grades`의 `updated` 반환이 죽어 있다~~ (원래 기록)
      **읽는 곳을 다 셌다(2026-08-16):**
      · 게임 코드 — **없다.** `season_runner.gd:60`이 `hs_graduated`와
        `univ_graduated`만 쓴다
      · 검사 — `test/promotion_test.gd`에 **11곳**
      `advance_grades`는 npc 사전을 **제자리에서** 고치므로(`npc["grade"] =
      grade + 1`) 그 배열엔 정보가 없다. 편의일 뿐이다.

      **지우려면 검사 11곳을 사전 직접 보기로 바꿔야 한다.** 한 번 시작했다가
      컨텍스트가 모자라 되돌렸다 — **반쯤 고친 채로 두는 게 더 나쁘다.**
      다음에 할 때는 이것만 한 덩어리로 잡는다:
      `_run([n])["updated"][0]["grade"]` → `_run([n])` 뒤 `n["grade"]`.
      ⚠ 검사가 인라인으로 `_run([...])["updated"][0]`을 쓰는 자리가 많아
      변수로 빼는 손질이 같이 든다.

- [x] ~~🔴 P-8b 주인공이 NPC 진로 배정을 탄다~~ (아래는 원래 기록)
      **P-8이 만든 게 아니라 P-8이 드러낸 것이다.** 전에는 로스터 쪽만
      바뀌고 `protagonist`는 안 봐서 안 보였다.

      `Promotion.advance_grades`가 **`is_protagonist`를 안 본다**
      (`promotion.gd:42~90`). 주인공이 로스터에 있으니 NPC와 똑같이 처리된다 —
      학년이 오르고(그건 맞다), 졸업하면 `league_id`를 `DRAFT_POOL`로 바꾸고,
      그다음 `Placement`가 대학으로 보낸다.

      **그 경로는 `career_stage`를 안 건드린다.** 주인공 무대를 옮기는
      정본은 `CareerDecision._move_to`(`career_decision.gd:489`)로,
      거기선 stage·league·team을 **함께** 바꾼다. NPC 경로는 리그만 바꾼다.

      그래서 2030년에 `league_id`는 `LEAGUE_UNIVERSITY`인데
      `career_stage`는 `"highschool"`로 남는다.
      ⚠ `career_stage`를 읽는 곳이 파일 9개다 — 재정 `Finance.weekly`의
      무대별 수입·지출, 학사 `SCHOOL_LEAGUES`, FA 자격.
      **대학생이 고교 생활비를 쓴다.**

      **고칠 방향**: 주인공의 진로는 `CareerRunner`/`CareerDecision`이
      정본이다(`career_choice_hub` → `career_results`). NPC 진로 배정이
      주인공을 **걸러야** 한다 — `advance_grades`와 `Placement` 양쪽.
      ⚠ **학년은 걸러선 안 된다.** 주인공도 학년은 올라야 한다 —
      거르는 것은 **졸업 뒤 소속 배정**이다. 두 일을 한 함수가 하고 있으니
      가르는 자리를 먼저 정한다.
      ⚠ `career_results` 대기가 안 풀린 채 다음 해로 넘어가는 것도 같이 본다.
      · 검사를 먼저 써서 실패를 본 뒤 고친다

- [x] ~~P-8b `career_stage`가 리그를 안 따라간다~~ (아래는 원래 기록)
      4해 실측에서 **진학은 된다** — 2030년에 `league_id`가
      `LEAGUE_UNIVERSITY`로 바뀐다. 그런데 `career_stage`는 여전히
      `"highschool"`이다.

      ```
      2029년 highschool/LEAGUE_HIGHSCHOOL · 학년 3 · 대기 ["career_results"]
      2030년 highschool/LEAGUE_UNIVERSITY · 학년 1 · 대기 ["career_results"]
      ```

      ⚠ `career_stage`를 읽는 곳이 많다(재정 `Finance.weekly`의 무대별
      수입·지출, 학사 `SCHOOL_LEAGUES`, FA 자격…). 리그와 어긋나면
      **대학생이 고교 생활비를 쓴다.**
      ⚠ **`career_results` 대기가 안 풀린 채로 다음 해로 넘어간다** —
      계측이 그 결정에 답을 안 해서인지, 결정이 소비되지 않아서인지
      먼저 가른다(입력부터 재라).

- [x] 🔴 **P-8c 프로 보직 배정 함수가 통째로 미이관이다** — 2026-08-19 고침
      02 `assign_protagonist_role`을 `Rotation.assign_pro_role`로 옮겼다
      (선발 OVR만 · `rank <= size_of` · 스윙맨/롱릴리프 · CP→마무리 ·
      RP→OVR대별). `SeasonRunner.roll_over`가 무대로 갈라 부른다.
      **실측 프로 144경기 등판 7 → 29 · role=3선발.**
      검사 11 · 변이 6/6.
      ⚠ 내 probe가 `SeasonRunner.run`만 불러 롤오버를 건너뛰고 있었다 —
      롤오버는 `finish_season`이 부른다. **probe도 게임 경로를 타야 한다.**
      (아래는 원래 기록)
      02는 보직 함수가 **둘**이다: 고교 `assign_highschool_position`(전체 투수 ·
      `higher <= 2`)와 프로 `assign_protagonist_role`(**선발 OVR만** · `rank <= 5` ·
      "N선발"/스윙맨/롱릴리프/마무리/셋업맨). **04는 고교 것만 옮겼다.**
      그래서 프로에서도 팀 3위 안을 요구하는데 로테이션은 5인이라 어긋나고,
      실측 프로 144경기에 **등판 7**(불펜 확률 5%)이 나온다.
      `rotation.gd:77` 주석이 그 어긋남을 스스로 경고하고 있었다.
      ⚠ `RELIEVER_CHANCE`에 02 역할 이름이 **이미 다 있다** — 표만 있고 배정이 없다.
      → `assign_pro_role` 신설 · 무대로 갈라 부르기 · 실측 7 → 29 확인.
      **밸런스가 아니라 이주다.**
      (아래는 이전 기록)
      🔴 **원래 증상("등판이 안 는다")은 해결됐다.** 20해 계측에서 등판이
      해마다 쌓인다(12해 154경기). **P-8·P-14·P-44가 그 사슬이었다.**
      **남은 것은 크기다**: 프로 KBL 팀이 **144경기인데 등판이 10**이다.
      ⚠ **엔진은 맞다 — 찍어서 확인했다**:
      ```
      팀 144경기 · 등판 29 · 로스터 31명 · 로테 5인 · 내가 로테에 true
      ```
      `mark_my_starts`도 `build_schedule` 안에서 제대로 불린다
      (`world.gd:249`), 시즌 롤오버도 `me["role"]`을 정해 넘긴다
      (`season_runner.gd:726-731`).
      → **남은 후보는 `Rotation.assign_position`이 주인공을 RP로 배정하는
      것**이다. RP면 로테이션에 못 들고 불펜 판정만 받아 10경기가 된다.
      ⚠ **02와 견주고 판정한다** — OVR 81이 KBL에서 불펜인 것이 02에서도
      그런지 먼저 본다. **결함이라 부르기 전에 02가 그런지 확인한다**
      (P-45에서 그 순서를 안 지켜 한 번 틀렸다)
      (아래는 원래 기록)
      ⚠ 계측은 매주 `_play_my_pending`으로 미처리 주인공 경기를 돌린다 —
      그런데도 안 는다. `MatchDay.play`가 게이트(`skip_injury`)를 보고
      주인공을 안 내보내는지, `is_protagonist_game`이 새 시즌 일정에
      다시 붙는지 **부르는 곳을 실제로 세라.**
      라이벌이 5명에서 안 느는 것(02는 8명)이 여기 걸려 있다.

- [x] ~~🔴 P-8 주인공이 두 벌로 갈린다 — 진행할 때마다~~ (아래는 원래 기록)
      **04 결함이다. 계측 fixture 문제가 아니다.** 자취 실측:

      ```
      2027년 protagonist 학년 1  → 시즌종료 ran=true · 로스터 속 나: 학년 2 (다른 사전!)
      2028년 protagonist 학년 1  → 시즌종료 ran=true · 로스터 속 나: 학년 3 (다른 사전!)
      2029년 protagonist 학년 1  → 시즌종료 ran=true · 로스터 속 나: 못 찾음(졸업)
      ```

      `World.new_game`은 `world["rosters"][team_id].append(me)`로 **같은
      참조**를 넣는다(`world.gd:253`). 그런데 진행이 상태를 깊은 복사한다 —
      `day_engine.gd:154` · `:191` · `day_runner.gd:57`의 `duplicate(true)`.
      복사 뒤에는 `state["protagonist"]`와 로스터 속 주인공이 **서로 다른
      사전**이 된다.

      **로스터 쪽만 산다.** 시즌 종료가 로스터를 훑으므로
      (`all_players` → `Promotion.advance_grades`) 학년·졸업이 거기서만
      일어나고, 화면과 판정이 읽는 `state["protagonist"]`는 **영원히
      고교 1학년**이다. 3해째엔 로스터에서 졸업해 사라지는데 주인공은 그대로다.

      ⚠ **게임 본체에도 그대로 있다.** `app_root._advance_days`가
      `_runner.run(_state, …)`의 결과로 `_state`를 갈아탄다 — 진행 버튼을
      누를 때마다 갈린다. 계측만의 문제가 아니다.
      ⚠ **오래된 세이브에도 남아 있을 수 있다.** 고칠 때 이미 갈린 상태를
      어떻게 다룰지 같이 정한다.
      ⚠ **조용히 틀리는 형태다.** 날짜는 가고 경기도 치러지는데 주인공만
      안 자란다 — `app_root` 주석이 경고한 것과 같은 종류다.
      고칠 때 검사로 못 박는다: **진행 뒤에도 `state["protagonist"]`와
      로스터 속 주인공이 같은 사람인가.**

- [x] **P-7d 학년이 안 오르던 것** — 2026-08-16 원인 규명 → P-8
      아래는 좁혀 간 기록이다. 결론은 P-8이다.

- [x] ⚠ ~~P-7d 커리어가 고교에서 안 나가고 부상이 안 낫는다~~ (축 9)
      계측에 해마다 커리어 상태를 찍어 좁혔다. 3해를 굴린 결과:

      ```
      2027년 highschool/LEAGUE_HIGHSCHOOL · 일정에 내 경기 10 · 게이트 skip_injury
      2028년 highschool/LEAGUE_HIGHSCHOOL · 일정에 내 경기 10 · 게이트 skip_injury
      2029년 highschool/LEAGUE_HIGHSCHOOL · 일정에 내 경기 10 · 게이트 skip_injury
      ```

      **대기줄과 부상을 같이 찍어 좁혔다:**

      ```
      2027년 학년 1 · 내 경기 10 · skip_injury · SHOULDER_INFLAM 1주 · 대기 없음
      2028년 학년 1 · 내 경기 10 · skip_injury · OBLIQUE_STRAIN 3주 · 대기 없음
      2029년 학년 1 · 내 경기 10 · skip_injury · SHOULDER_SURGERY 14주 · 대기 없음
      ```

      **처음 읽기 둘이 틀렸다:**
      · **대기줄은 비어 있다** — 결정이 막고 있는 게 아니다
      · **부상은 해마다 종류가 다르다** — 회복은 돌고 있고 매년 새로
        다치는 것이다. `skip_injury`는 그 시점 상태일 뿐이라
        "부상이 안 낫는다"는 틀린 읽기였다

      **남은 하나 — 학년이 1에 고정돼 있다.** 3해를 굴렸는데 `grade`가
      안 오르니 졸업 판정에 영영 안 걸리고, 그래서 고교를 못 벗어난다.

      **다음 한 걸음:** `SeasonRunner.run(s)`의 반환을 찍는다.
      `{ran: false}`면 시즌 종료가 아예 안 도는 것이고, `ran: true`인데
      학년이 그대로면 `Promotion.advance_grades`가 주인공을 못 보는 것이다.
      ⚠ `all_players`는 `world.rosters`를 훑는다(`season_runner.gd:27`).
      주인공은 `world["rosters"][team_id].append(me)`로 들어가므로
      (`world.gd:253`) 같은 참조여야 하는데, **그게 참인지부터 확인한다.**
      같이 볼 것: **구단주 −5.0**(02는 +11.0). 구단주는 주간 항목이 없고
      시즌 성적으로만 움직인다 — `run_season`이 도는지부터 확인한다.

- [x] ~~P-7c 코치 관계가 낮다~~ (아래는 원래 기록)
      코치는 담당 영역 훈련(+1)과 성장(+2)으로 오른다. 규칙 값은 02와
      같으니(축 9 표) **도는 횟수**를 볼 일이다.
      `growth_threshold 1`을 넘는 주가 몇 번인지 재 본다 — 02 주석에
      근거가 있다("2였는데 실측상 191주에 14포인트라 도달 불가였다").
      ⚠ 수치를 더 좋게 만들지 않는다. 계수를 지어내면 멈추고 올린다.

- [x] ~~P-7b 라이벌이 0명 · 코치 관계가 낮다~~ (아래는 원래 기록)
      P-7로 재게 되니 남은 차이가 드러났다.
      · **라이벌 0명** (02는 8명 평균 3.8) — `faced_and_won`이 붙는 자리를
        확인해야 한다
      · **코치 1.0** (02는 29.0) — 담당 영역 훈련(+1)과 성장(+2)으로 오르는데
        성장 임계(`growth_threshold 1`)를 넘는 주가 적으면 안 오른다
      ⚠ 규칙 값은 02와 같다(축 9 표). 산식이 아니라 **도는 횟수**를 본다.
      ⚠ `measure:relations`가 590초 걸린다 — 자주 돌릴 것은 못 된다

- [x] ~~P-7 주간 파이프라인이 화면 층에 있다~~ (아래는 원래 기록)
      `DayRunner`가 `ui/day_runner.gd`의 Node고, 그 반복을 도는 것은
      `app_root.gd`다(`auto_advance.gd` 주석이 그렇게 적어 뒀다).
      그래서 **헤드리스가 커리어 하나를 끝까지 못 굴린다.**
      실측: 관계 계측이 감독·코치·구단주는 재는데 **동료·라이벌이 0**이다.
      둘 다 `team_played`·`pitched`·`won`을 읽는데 경기가 안 돌아서다.
      02는 `advanceWeek`가 usecase라 스크립트가 그대로 불렀다.
      ⚠ 계측이 `app_root`의 주간 반복을 베끼면 안 된다 — 축 7에서 fixture를
      직접 지었다가 이적 0%가 나왔다. **주간 반복을 `sim/`으로 내리는 일이다.**
      풀리면 축 9를 잴 수 있고, 같은 이유로 지금 못 짜는 검사들도 열린다.
      근거·표는 `docs/PARITY.md` 축 9.

## E. P6 Steam 빌드·패키징

- [ ] (이주가 끝난 뒤)
