[
 ["협상 틀을 안 준다 (다시 통보가 된다)",
  "\t], \"negotiate\")\n\td[\"negotiation\"] = n\n\treturn d",
  "\t])\n\treturn d"],
 ["구단주 관계를 안 본다 (contract_bonus가 또 죽는다)",
  "\tvar owner_bonus: float = float(RelationshipRunner.effects_of(state).get(\n\t\t\"contract_bonus\", 0.0))",
  "\tvar owner_bonus: float = 0.0"],
 ["구단주 지갑을 안 본다",
  "\tvar budget: float = float(Staff.mods_of(state.get(\"world\", {}),\n\t\tString(a.get(\"team_id\", \"\"))).get(\"budget\", 1.0))",
  "\tvar budget: float = 1.0"],
 ["화면이 고른 조건을 무시한다 (슬라이더가 아무 일도 안 한다)",
  "\tt.merge(terms, true)",
  ""],
 ["역제안 버튼을 안 막는다 (눌러도 아무 일이 안 난다)",
  "\t\t{\"id\": \"counter\", \"label\": \"역제안한다\", \"enabled\": n[\"can_counter\"]},",
  "\t\t{\"id\": \"counter\", \"label\": \"역제안한다\"},"],
 ["문턱을 넘겨도 역제안이 성사된다 (화면을 우회해 통과한다)",
  "\t\t\t\tif not bool(n[\"can_counter\"]):\n\t\t\t\t\treturn false",
  ""],
 ["역제안이 요청액이 아닌 제시액으로 서명된다 (보여준 것과 다른 계약)",
  "\t\t\"salary\": int(n[\"requested\"]),",
  "\t\t\"salary\": int(a.get(\"offered_salary\", 0)),"],
 ["역제안 기간을 안 반영한다 (3년으로 합의했는데 2년 계약)",
  "\t\t\"salary\": int(n[\"requested\"]),\n\t\t\"duration_years\": int(n[\"duration_years\"]),",
  "\t\t\"salary\": int(n[\"requested\"]),\n\t\t\"duration_years\": int(a.get(\"duration_years\", 1)),"],
 ["계약 연수를 엔진이 못 읽는 키로 적는다 (계약이 0년으로 서명된다)",
  "\t\t\"salary\": int(a.get(\"offered_salary\", 0)),\n\t\t\"duration_years\": int(a.get(\"duration_years\", 1)),",
  "\t\t\"salary\": int(a.get(\"offered_salary\", 0)),\n\t\t\"years\": int(a.get(\"duration_years\", 1)),"]
]
