[
 [
  "아군을 안 거른다 (우리 불펜이 라이벌이 된다 — 02의 결함 그대로)",
  "\t\t\tif pid != me and mates.has(pid):\n\t\t\t\tcontinue",
  ""
 ],
 [
  "아군만 라이벌로 잡는다 (조건이 뒤집힌다)",
  "\t\t\tif pid != me and mates.has(pid):\n\t\t\t\tcontinue",
  "\t\t\tif pid != me and not mates.has(pid):\n\t\t\t\tcontinue"
 ],
 [
  "내 팀이 아니라 상대 로스터를 본다",
  "\t\tfor mate in World.roster_of(state.get(\"world\", {}), team):",
  "\t\tfor mate in World.roster_of(state.get(\"world\", {}), String(g.get(\"away\", \"\"))):"
 ],
 [
  "이닝 최다가 아니라 처음 나온 상대를 잡는다",
  "\t\t\t\tif outs_of > best_outs:",
  "\t\t\t\tif best_outs < 0:"
 ],
 [
  "내가 안 던진 경기도 맞대결로 친다",
  "\t\tif pitched_here and not best_id.is_empty() and not faced.has(best_id):",
  "\t\tif not best_id.is_empty() and not faced.has(best_id):"
 ],
 [
  "같은 상대를 여러 번 담는다",
  "\t\tif pitched_here and not best_id.is_empty() and not faced.has(best_id):",
  "\t\tif pitched_here and not best_id.is_empty():"
 ]
]
