[
 ["구단주 성향을 안 본다 (budget 계수가 또 죽는다)",
  "\t\t) * mult * clampf(budget_mod, OFFER_BUDGET_MIN, OFFER_BUDGET_MAX)",
  "\t\t) * mult"],
 ["구단주 계수를 안 가둔다 (지갑 큰 구단이 연봉 체계를 흔든다)",
  "clampf(budget_mod, OFFER_BUDGET_MIN, OFFER_BUDGET_MAX)",
  "budget_mod"],
 ["리그 배수를 안 곱한다 (ABL이 KBL과 같은 값이 된다)",
  "\t\t) * mult * clampf(budget_mod, OFFER_BUDGET_MIN, OFFER_BUDGET_MAX)",
  "\t\t) * clampf(budget_mod, OFFER_BUDGET_MIN, OFFER_BUDGET_MAX)"],
 ["성적을 안 본다 (잘 던져도 오퍼가 그대로다)",
  "\tvar perf: float = 1.0 + (season_rating(stats) - 50.0) \\\n\t\t* float(r.get(\"perf_step\", 0.012))",
  "\tvar perf: float = 1.0"],
 ["최저선이 없다 (못 던진 해에 0원 계약이 나온다)",
  "\treturn int(roundf(maxf(blended, float(OFFER_FLOOR))))",
  "\treturn int(roundf(blended))"],
 ["지금 연봉이 없을 때 0으로 둔다 (첫 계약이 시장가의 40%가 된다)",
  "\tif current <= 0.0:\n\t\tcurrent = market",
  ""],
 ["OVR이 낮아도 시장가가 오른다 (음수 보정을 안 막는다)",
  "\t\t+ maxf(ovr - float(r.get(\"ovr_pivot\", 50.0)), 0.0)",
  "\t\t+ (ovr - float(r.get(\"ovr_pivot\", 50.0)))"],
 ["한 이닝도 안 던진 해를 성적으로 친다 (방어율 0이 만점이 된다)",
  "\tif stats.is_empty() or ip <= 0.0:\n\t\treturn no_record",
  "\tif stats.is_empty():\n\t\treturn no_record"],
 ["평점 가중치를 뭉갠다 (ERA·WHIP·K가 같은 비중이 된다)",
  "\treturn era_score * float(r.get(\"w_era\", 0.45)) \\\n\t\t+ whip_score * float(r.get(\"w_whip\", 0.3)) \\\n\t\t+ k_score * float(r.get(\"w_k\", 0.25))",
  "\treturn (era_score + whip_score + k_score) / 3.0"],
 ["평점을 안 가둔다 (한 항이 폭주하면 오퍼가 튄다)",
  "\tvar era_score: float = clampf(float(r.get(\"era_base\", 100.0))\n\t\t- (float(stats.get(\"era\", 0.0)) - float(r.get(\"era_pivot\", 2.0)))\n\t\t* float(r.get(\"era_step\", 18.0)), lo, hi)",
  "\tvar era_score: float = float(r.get(\"era_base\", 100.0)) \\\n\t\t- (float(stats.get(\"era\", 0.0)) - float(r.get(\"era_pivot\", 2.0))) \\\n\t\t* float(r.get(\"era_step\", 18.0))"]
]
