[
 ["대기줄만 본다 — 미결정 소식·등판을 못 보고 진행하라고 한다",
  "\tvar stop = DayEngine.stop_reason(state)\n\tif stop == null:\n\t\treturn {\"kind\": \"advance\"}\n\n\tvar t: String = String(stop.get(\"type\", \"\"))\n\treturn {\"kind\": \"answer\", \"action\": stop, \"label\": label_of(t)}",
  "\tvar head: Dictionary = Pending.next(state)\n\tif head.is_empty():\n\t\treturn {\"kind\": \"advance\"}\n\tvar t: String = String(head.get(\"type\", \"\"))\n\treturn {\"kind\": \"answer\", \"action\": head, \"label\": label_of(t)}"],
 ["줄의 머리만 본다 — 결정이 알림 뒤에 숨으면 대신 답해 버린다",
  "\tvar block: Dictionary = blocking(state)\n\tif not block.is_empty():",
  "\tvar block: Dictionary = Pending.next(state)\n\tif is_stopping(String(block.get(\"type\", \"\"))):"],
 ["은퇴해도 계속 간다",
  "\tif bool(p.get(\"retired\", false)):\n\t\treturn {\"kind\": \"stop\", \"reason\": \"retired\", \"label\": label_of(\"retired\")}",
  ""],
 ["한 걸음도 안 기다린다 — 진행이 끝나기 전에 다음 걸음이 시작된다",
  "\t\tawait advance.call(state)",
  "\t\tadvance.call(state)"],
 ["답하면서 날까지 민다",
  "\t\tif kind == \"answer\":\n\t\t\tawait answer.call(state, step[\"action\"])\n\t\t\tcontinue",
  "\t\tif kind == \"answer\":\n\t\t\tawait answer.call(state, step[\"action\"])"],
 ["상한이 없다 — 줄이 안 줄면 게임이 멈춘다",
  "\twhile steps < max_steps:",
  "\twhile true:"],
 ["정지 주차를 지나쳐도 안 멈춘다",
  "\t\tvar week: int = stop_week_between(before,\n\t\t\tCalendar.week_of(int(state.get(\"day\", 1))))\n\t\tif week > 0:",
  "\t\tvar week: int = 0\n\t\tif week > 0:"],
 ["지치든 말든 첫 번째를 고른다",
  "\tif fatigue >= TIRED_AT:\n\t\tvar rest: String = _find(choices, REST_WORDS)\n\t\tif not rest.is_empty():\n\t\t\treturn rest",
  ""]
]
