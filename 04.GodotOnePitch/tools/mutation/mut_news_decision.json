[
 ["선택지를 사전에 안 싣는다 (화면이 그릴 게 없어 아무도 못 답한다)",
  "\t\trow.merge(_decision_of(m), true)",
  ""],
 ["답한 뒤에도 선택지를 준다 (두 번 눌러 회복이 공짜가 된다)",
  "\t\t\"choices\": [] if picked != null else d.get(\"choices\", []),",
  "\t\t\"choices\": d.get(\"choices\", []),"],
 ["고른 것을 안 보여준다 (뭘 골랐는지 되짚을 수 없다)",
  "\tvar picked = d.get(\"selected\", null)\n\tvar label: String = \"\"\n\tif picked != null:\n\t\tfor c in d.get(\"choices\", []):\n\t\t\tif String(c.get(\"id\", \"\")) == String(picked):\n\t\t\t\tlabel = String(c.get(\"label\", \"\"))",
  "\tvar picked = d.get(\"selected\", null)\n\tvar label: String = \"\""],
 ["결정이 없는 소식에도 선택지 칸을 만든다",
  "\tif d == null:\n\t\treturn {}",
  ""],
 ["물음을 안 싣는다 (무엇을 고르는지 모른다)",
  "\t\t\"prompt\": String(d.get(\"prompt\", \"\")),",
  "\t\t\"prompt\": \"\","]
]
