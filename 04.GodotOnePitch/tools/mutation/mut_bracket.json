[
  ["라운드를 이름순으로 정렬한다 (원본이 경고한 것)",
   "\tvar depths: Array = groups.keys()\n\tdepths.sort()\n\tdepths.reverse()",
   "\tvar depths: Array = groups.keys()\n\tdepths.sort()"],
  ["순환 참조 가드를 뺀다",
   "\t\tif not by_id.has(nid) or seen.has(nid):\n\t\t\tbreak",
   "\t\tif not by_id.has(nid):\n\t\t\tbreak"],
  ["끊긴 next_id 가드를 뺀다",
   "\t\tif not by_id.has(nid) or seen.has(nid):\n\t\t\tbreak\n\t\tseen[nid] = true\n\t\tcur = by_id[nid]",
   "\t\tif seen.has(nid):\n\t\t\tbreak\n\t\tseen[nid] = true\n\t\tcur = by_id[nid]"],
  ["한쪽만 정해져도 진행 중으로 본다",
   "\tif String(s.get(\"home_team_id\", \"\")).is_empty() or String(s.get(\"away_team_id\", \"\")).is_empty():",
   "\tif String(s.get(\"home_team_id\", \"\")).is_empty() and String(s.get(\"away_team_id\", \"\")).is_empty():"],
  ["선승 수를 반올림한다",
   "\treturn best_of / 2 + 1",
   "\treturn best_of / 2"],
  ["단판 표기를 없앤다",
   "\treturn \"단판\" if best_of == 1 else \"%d전 %d선승\" % [best_of, wins_needed(best_of)]",
   "\treturn \"%d전 %d선승\" % [best_of, wins_needed(best_of)]"],
  ["준우승을 항상 원정팀으로 적는다",
   "\treturn {\"champion\": winner, \"runner_up\": away if winner == home else home}",
   "\treturn {\"champion\": winner, \"runner_up\": away}"],
  ["결승을 깊이가 아니라 목록 첫 칸으로 찾는다",
   "\tfor s in series:\n\t\tif _depth_of(s, by_id) == 0:\n\t\t\treturn s\n\treturn {}",
   "\tif not series.is_empty():\n\t\treturn series[0]\n\treturn {}"]
]
