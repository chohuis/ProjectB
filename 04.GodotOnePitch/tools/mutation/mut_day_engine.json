[
 ["자동 회피 문턱을 올려 물어보는 구간을 없앤다",
  "const COND_AUTO_SKIP: float = 35.0",
  "const COND_AUTO_SKIP: float = 55.0"],
 ["자동 회피 문턱을 없앤다 (컨디션 0으로도 등판)",
  "const COND_AUTO_SKIP: float = 35.0",
  "const COND_AUTO_SKIP: float = 0.0"],
 ["물어보는 문턱을 없앤다",
  "const COND_ASK: float = 55.0",
  "const COND_ASK: float = 35.0"],
 ["물어보는 문턱을 올려 답이 뻔한 경기까지 창을 띄운다",
  "const COND_ASK: float = 55.0",
  "const COND_ASK: float = 80.0"],
 ["학사 경고를 부상보다 뒤로 민다",
  "\tif p.get(\"eligibility_blocked\", false):\n\t\treturn \"skip_academic\"\n\tif p.get(\"injury\", null) != null:\n\t\treturn \"skip_injury\"",
  "\tif p.get(\"injury\", null) != null:\n\t\treturn \"skip_injury\"\n\tif p.get(\"eligibility_blocked\", false):\n\t\treturn \"skip_academic\""],
 ["학사 경고를 안 본다",
  "\tif p.get(\"eligibility_blocked\", false):\n\t\treturn \"skip_academic\"",
  ""],
 ["부상을 안 본다",
  "\tif p.get(\"injury\", null) != null:\n\t\treturn \"skip_injury\"",
  ""],
 ["경계를 이상으로 바꾼다 (35이 회피가 된다)",
  "\tif cond < COND_AUTO_SKIP:",
  "\tif cond <= COND_AUTO_SKIP:"],
 ["물어보는 경계를 이상으로 바꾼다 (55가 물어보게 된다)",
  "\tif cond < COND_ASK:",
  "\tif cond <= COND_ASK:"],
 ["쌓인 pending을 안 본다 (새 정지를 그 위에 얹는다)",
  "\tvar pending: Array = s.get(\"pending\", [])\n\tif not pending.is_empty():\n\t\treturn pending[0]",
  "\tvar pending: Array = s.get(\"pending\", [])"],
 ["은퇴를 안 본다 (은퇴한 선수가 계속 등판한다)",
  "\tif p.get(\"retired\", false):\n\t\treturn {\"type\": \"retired\"}",
  ""],
 ["시즌 끝을 안 본다",
  "\tif day >= int(s.get(\"season_days\", 0)):\n\t\treturn {\"type\": \"season_end\"}",
  ""],
 ["시즌 끝을 하루 늦게 본다",
  "\tif day >= int(s.get(\"season_days\", 0)):",
  "\tif day > int(s.get(\"season_days\", 0)):"],
 ["미결정 메시지를 경기보다 뒤로 민다",
  "\tfor m in s.get(\"mailbox\", []):\n\t\tvar d = m.get(\"decision\", null)",
  "\tfor m in []:\n\t\tvar d = m.get(\"decision\", null)"],
 ["결정 없는 소식에도 멈춘다",
  "\t\tif d != null and d.get(\"selected\", null) == null:",
  "\t\tif d == null or d.get(\"selected\", null) == null:"],
 ["이미 답한 결정에도 멈춘다",
  "\t\tif d != null and d.get(\"selected\", null) == null:",
  "\t\tif d != null:"],
 ["다른 날 경기에도 멈춘다",
  "\t\tif int(g.get(\"day\", -1)) != day:\n\t\t\tcontinue",
  ""],
 ["NPC 경기에도 멈춘다",
  "\t\tif not g.get(\"is_protagonist_game\", false):\n\t\t\tcontinue",
  ""],
 ["이미 치른 경기에 또 멈춘다 (무한 루프)",
  "\t\tif g.get(\"result\", null) != null:\n\t\t\tcontinue\n\t\treturn {\"type\": \"game\", \"schedule_id\": g.get(\"id\", \"\")}",
  "\t\treturn {\"type\": \"game\", \"schedule_id\": g.get(\"id\", \"\")}"],
 ["다음 정지를 목록 첫 경기로 잡는다 (제일 가까운 게 아니라)",
  "\t\tif d < day or d > best:\n\t\t\tcontinue",
  "\t\tif d < day:\n\t\t\tcontinue"],
 ["오늘 경기를 다음 정지에서 뺀다",
  "\t\tif d < day or d > best:",
  "\t\tif d <= day or d > best:"],
 ["다음 정지가 NPC 경기도 센다",
  "\t\tif not g.get(\"is_protagonist_game\", false):\n\t\t\tcontinue\n\t\tif g.get(\"result\", null) != null:\n\t\t\tcontinue\n\t\tbest = d",
  "\t\tbest = d"],
 ["주 경계를 닫힌 구간으로 센다 (쪼갤 때마다 한 번 더)",
  "\treturn Calendar.week_ends_between(from_day, to_day - 1)",
  "\treturn Calendar.week_ends_between(from_day, to_day)"],
 ["주 경계를 한 칸 더 민다 (건너뛴다)",
  "\treturn Calendar.week_ends_between(from_day, to_day - 1)",
  "\treturn Calendar.week_ends_between(from_day + 1, to_day - 1)"],
 ["주 경계를 아예 안 센다",
  "\treturn Calendar.week_ends_between(from_day, to_day - 1)",
  "\treturn 0"],
 ["정지한 날에도 넘어간다 (경기를 못 치른 채 지나간다)",
  "\tvar stop = stop_reason(out)\n\tif stop != null:\n\t\tout[\"stopped_by\"] = stop\n\t\tout[\"games_today\"] = []\n\t\tout[\"weeks_crossed\"] = 0\n\t\treturn out",
  "\tvar stop = stop_reason(out)\n\tif stop != null:\n\t\tout[\"stopped_by\"] = stop"],
 ["정지한 날에 주 경계를 센다 (안 산 날의 성장이 붙는다)",
  "\t\tout[\"weeks_crossed\"] = 0\n\t\treturn out",
  "\t\tout[\"weeks_crossed\"] = week_boundaries_crossed(day, day + 1)\n\t\treturn out"],
 ["오늘 경기만이 아니라 전부 돌린다",
  "\t\tif int(g.get(\"day\", -1)) == day and g.get(\"result\", null) == null:",
  "\t\tif g.get(\"result\", null) == null:"],
 ["이미 치른 경기를 다시 돌린다",
  "\t\tif int(g.get(\"day\", -1)) == day and g.get(\"result\", null) == null:",
  "\t\tif int(g.get(\"day\", -1)) == day:"],
 ["하루를 안 넘긴다",
  "\tout[\"day\"] = day + 1",
  "\tout[\"day\"] = day"],
 ["여러 날 진행이 정지를 무시하고 계속 간다",
  "\t\tif step[\"day\"] == before:\n\t\t\t# 멈췄다 — 하루도 안 나아갔다\n\t\t\tout = step\n\t\t\tstop = step[\"stopped_by\"]\n\t\t\tbreak",
  "\t\tif step[\"day\"] == before:\n\t\t\tout[\"day\"] = before + 1\n\t\t\tcontinue"],
 ["여러 날 진행이 출발점이 아니라 목표로 주 경계를 센다",
  "\tout[\"weeks_crossed\"] = week_boundaries_crossed(start, out[\"day\"])",
  "\tout[\"weeks_crossed\"] = week_boundaries_crossed(start, target)"],
 ["여러 날 진행이 마지막 하루의 주 경계만 남긴다",
  "\tout[\"weeks_crossed\"] = week_boundaries_crossed(start, out[\"day\"])",
  "\tout[\"weeks_crossed\"] = out[\"weeks_crossed\"]"],
 ["여러 날 진행이 경기를 마지막 날 것만 남긴다",
  "\t\tall_games.append_array(step[\"games_today\"])",
  "\t\tall_games = step[\"games_today\"]"],
 ["여러 날 진행이 하루 더 간다",
  "\twhile out[\"day\"] < target:",
  "\twhile out[\"day\"] <= target:"]
]
