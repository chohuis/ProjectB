[
 ["존 밖을 안 던진다 (예전 상태 — 볼넷이 영원히 0)",
  "\tif rng.randf() < chase:",
  "\tif false:"],
 ["유인구 확률을 카운트와 무관하게 한다",
  "\tvar chase: float = CHASE_NEUTRAL\n\tif balls >= 3:\n\t\tchase = CHASE_BEHIND\n\telif strikes == 2:\n\t\tchase = CHASE_AHEAD",
  "\tvar chase: float = CHASE_NEUTRAL"],
 ["볼 3개와 스트라이크 2개를 뒤집는다 (야구가 거꾸로 돈다)",
  "\tif balls >= 3:\n\t\tchase = CHASE_BEHIND\n\telif strikes == 2:\n\t\tchase = CHASE_AHEAD",
  "\tif balls >= 3:\n\t\tchase = CHASE_AHEAD\n\telif strikes == 2:\n\t\tchase = CHASE_BEHIND"],
 ["유인구를 두 축 다 뺀다 (타자가 아예 안 속는다)",
  "\t\tif rng.randf() < 0.5:\n\t\t\treturn Vector2(_sign(rng) * out, other)\n\t\treturn Vector2(other, _sign(rng) * out)",
  "\t\treturn Vector2(_sign(rng) * out, _sign(rng) * out)"],
 ["볼 3개에도 코너를 노린다 (볼넷을 자초한다)",
  "\tif balls >= 3:\n\t\t# 볼넷을 피해야 한다 — 존 한복판\n\t\treturn Vector2((rng.randf() - 0.5) * MIDDLE_SPAN,\n\t\t\t(rng.randf() - 0.5) * MIDDLE_SPAN)",
  ""],
 ["구종을 보유 목록이 아니라 고정 둘에서 고른다 (예전 상태)",
  "\tvar arsenal: Array = pitcher.get(\"pitches\", [])\n\tif arsenal.is_empty():\n\t\treturn \"fastball\"",
  "\tvar arsenal: Array = []\n\tif arsenal.is_empty():\n\t\treturn \"fastball\""],
 ["볼 3개에 속구 계열을 안 고른다",
  "\t\tvar fb: Dictionary = _best(arsenal, FASTBALLS)\n\t\tif not fb.is_empty():\n\t\t\treturn String(fb[\"id\"])",
  ""],
 ["두 스트라이크 결정구 가중을 없앤다",
  "\t\tif not out_pitch.is_empty() and rng.randf() < OUT_PITCH_PROB:\n\t\t\treturn String(out_pitch[\"id\"])",
  ""],
 ["숙련도 가중을 평평하게 한다 (5등급이나 1등급이나 같다)",
  "\tif grade <= 1:\n\t\treturn 0.5",
  "\tif grade <= 5:\n\t\treturn 1.0"],
 ["존 표의 행과 열을 뒤바꾼다 (화면 코스가 거울이 된다)",
  "\treturn [[7, 8, 9], [4, 5, 6], [1, 2, 3]][row][col]",
  "\treturn [[7, 8, 9], [4, 5, 6], [1, 2, 3]][col][row]"],
 ["전략이 카운트를 안 본다",
  "\tif balls >= 3:\n\t\tstrategy = \"safe\"\n\telif strikes == 2:\n\t\tstrategy = \"aggressive\"",
  ""],
 ["좌표를 안 넘긴다 (존 대표값으로 뭉개진다)",
  "\t\t\"target\": target,",
  ""]
]
