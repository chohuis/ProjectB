[
 ["NPC 경기도 싣는다 (하루 83경기 속에서 내 등판을 못 찾는다)",
  "\t\tif not g.get(\"is_protagonist_game\", false):\n\t\t\tcontinue",
  ""],
 ["내 경기를 거꾸로 거른다",
  "\t\tif not g.get(\"is_protagonist_game\", false):",
  "\t\tif g.get(\"is_protagonist_game\", false):"],
 ["홈·원정을 뒤집는다",
  "\t\tvar is_home: bool = home == my_team",
  "\t\tvar is_home: bool = home != my_team"],
 ["상대를 자기 팀으로 적는다",
  "\t\tvar foe: String = g.get(\"away\", \"\") if is_home else home",
  "\t\tvar foe: String = home if is_home else g.get(\"away\", \"\")"],
 ["모르는 팀에 빈칸을 준다",
  "\t\t\t\"opponent\": names.get(foe, foe),",
  "\t\t\t\"opponent\": names.get(foe, \"\"),"],
 ["오늘 경기를 따로 안 본다",
  "\t\t\tstatus = \"today\" if gd == day else (\"upcoming\" if gd > day else \"missed\")",
  "\t\t\tstatus = \"upcoming\" if gd >= day else \"missed\""],
 ["지나친 경기를 예정으로 둔다 (영영 안 오는 경기를 기다린다)",
  "\t\t\tstatus = \"today\" if gd == day else (\"upcoming\" if gd > day else \"missed\")",
  "\t\t\tstatus = \"today\" if gd == day else \"upcoming\""],
 ["오늘 경기를 지나친 것으로 본다",
  "\t\t\tstatus = \"today\" if gd == day else (\"upcoming\" if gd > day else \"missed\")",
  "\t\t\tstatus = \"upcoming\" if gd > day else \"missed\""],
 ["남은 경기를 안 센다",
  "\t\t\tremaining += 1",
  "\t\t\tpass"],
 ["치른 경기도 남은 것으로 센다",
  "\t\t\tremaining += 1\n\t\telse:",
  "\t\telse:\n\t\t\tremaining += 1"],
 ["무승부를 패로 센다 (승자 id가 빈 문자열로 온다)",
  "\t\t\tvar drawn: bool = winner == null or String(winner).is_empty()",
  "\t\t\tvar drawn: bool = false"],
 ["빈 문자열 승자를 무승부로 안 본다",
  "\t\t\tvar drawn: bool = winner == null or String(winner).is_empty()",
  "\t\t\tvar drawn: bool = winner == null"],
 ["null 승자를 무승부로 안 본다",
  "\t\t\tvar drawn: bool = winner == null or String(winner).is_empty()",
  "\t\t\tvar drawn: bool = String(winner).is_empty() and winner != null"],
 ["승패를 뒤집는다",
  "\t\t\telif String(winner) == my_team:",
  "\t\t\telif String(winner) != my_team:"],
 ["점수를 뒤집어 적는다",
  "\t\t\t\tlabel = \"승 %d:%d\" % [hs, as_]",
  "\t\t\t\tlabel = \"승 %d:%d\" % [as_, hs]"],
 ["이겼는지 표시를 안 한다",
  "\t\t\t\twon = true",
  "\t\t\t\tpass"],
 ["안 치른 경기에도 결과 글자를 붙인다",
  "\t\tvar label: String = \"\"",
  "\t\tvar label: String = \"예정\""],
 ["날짜 순으로 안 준다 (일정표가 뒤죽박죽)",
  "\trows.sort_custom(func(a, b) -> bool: return a[\"day\"] < b[\"day\"])",
  ""],
 ["날짜를 거꾸로 준다",
  "\trows.sort_custom(func(a, b) -> bool: return a[\"day\"] < b[\"day\"])",
  "\trows.sort_custom(func(a, b) -> bool: return a[\"day\"] > b[\"day\"])"],
 ["월과 일을 뒤집는다",
  "\t\t\t\"date_label\": \"%d월 %d일\" % [date[\"month\"], date[\"day\"]],",
  "\t\t\t\"date_label\": \"%d월 %d일\" % [date[\"day\"], date[\"month\"]],"],
 ["요일을 달력이 아니라 주차에서 판다",
  "\t\t\t\"weekday_label\": WEEKDAY_NAMES[Calendar.weekday(year, gd)],",
  "\t\t\t\"weekday_label\": WEEKDAY_NAMES[Calendar.day_in_week(gd) % 7],"],
 ["전적에서 무승부를 뺀다",
  "\t\t\"record_label\": \"기록 없음\" if w + l + d == 0 else \"%d승 %d무 %d패\" % [w, d, l],",
  "\t\t\"record_label\": \"기록 없음\" if w + l == 0 else \"%d승 %d패\" % [w, l],"],
 ["전적의 승과 패를 뒤바꾼다",
  "\t\t\"record_label\": \"기록 없음\" if w + l + d == 0 else \"%d승 %d무 %d패\" % [w, d, l],",
  "\t\t\"record_label\": \"기록 없음\" if w + l + d == 0 else \"%d승 %d무 %d패\" % [l, d, w],"],
 ["기록이 없어도 0승 0무 0패로 적는다",
  "\t\t\"record_label\": \"기록 없음\" if w + l + d == 0 else \"%d승 %d무 %d패\" % [w, d, l],",
  "\t\t\"record_label\": \"%d승 %d무 %d패\" % [w, d, l],"],
 ["0일차를 그대로 쓴다 (달력이 뒷걸음친다)",
  "\tvar day: int = maxi(int(s.get(\"day\", 1)), 1)",
  "\tvar day: int = int(s.get(\"day\", 0))"]
]
