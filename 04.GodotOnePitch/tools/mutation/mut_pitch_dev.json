[
 ["조건이 여럿일 때 하나만 본다 (아직 못 던질 구종이 열린다)",
  "\tfor cond in e.get(\"unlock\", []):\n\t\tif float(q.get(String(cond.get(\"stat\", \"\")), 0.0)) < float(cond.get(\"value\", 0.0)):\n\t\t\treturn false\n\treturn true",
  "\tfor cond in e.get(\"unlock\", []):\n\t\treturn float(q.get(String(cond.get(\"stat\", \"\")), 0.0)) >= float(cond.get(\"value\", 0.0))\n\treturn true"],
 ["잠긴 구종도 시작된다 (화면만 막고 엔진은 통과)",
  "\t\tif not bool(c[\"can_train\"]):\n\t\t\treturn false",
  ""],
 ["다섯이 차도 새 구종을 배운다",
  "\t\telif not owned and full:",
  "\t\telif false:"],
 ["5등급을 넘긴다",
  "\t\tif g >= MAX_GRADE:",
  "\t\tif false:"],
 ["배우고도 훈련 자리를 안 비운다 (다음 주에 또 배운다)",
  "\tp[\"pitches\"] = pitches\n\tp[\"training_pitch_state\"] = {}",
  "\tp[\"pitches\"] = pitches"],
 ["새 구종을 3등급으로 준다 (02는 1이다)",
  "\t\tpitches.append({\"id\": id, \"grade\": 1})",
  "\t\tpitches.append({\"id\": id, \"grade\": 3})"],
 ["100을 안 넘어도 배운다",
  "\tif progress < 100.0:",
  "\tif false:"],
 ["훈련 중인 게 없어도 진행률을 쌓는다",
  "\tif ts.is_empty():\n\t\treturn []",
  ""]
]
