[
 ["OVR 정규화 구간을 좁혀 상위권에서 멈춘다",
  "const OVR_SPAN: float = 45.0",
  "const OVR_SPAN: float = 100.0"],
 ["OVR 정규화 바닥을 0으로 둔다",
  "const OVR_FLOOR: float = 40.0",
  "const OVR_FLOOR: float = 0.0"],
 ["OVR 정규화를 안 자른다",
  "\tvar ovr_norm: float = clampf((c.get(\"pitching_ovr\", 0.0) - OVR_FLOOR) / OVR_SPAN * 100.0,\n\t\t0.0, 100.0)",
  "\tvar ovr_norm: float = (c.get(\"pitching_ovr\", 0.0) - OVR_FLOOR) / OVR_SPAN * 100.0"],
 ["백분위와 OVR 가중을 뒤집는다",
  "\tvar base: float = pct * 0.6 + ovr_norm * 0.4",
  "\tvar base: float = pct * 0.4 + ovr_norm * 0.6"],
 ["에이스 가점을 없앤다",
  "\tvar ace_bonus: float = 8.0 if rank == 1 else (3.0 if rank == 2 else 0.0)",
  "\tvar ace_bonus: float = 0.0"],
 ["2위 에이스도 1위와 같게 본다",
  "\tvar ace_bonus: float = 8.0 if rank == 1 else (3.0 if rank == 2 else 0.0)",
  "\tvar ace_bonus: float = 8.0 if rank <= 2 else 0.0"],
 ["대회 기준점을 50으로 되돌린다 (전원이 −12를 먹는다)",
  "const TOURNAMENT_BASE: float = 20.0",
  "const TOURNAMENT_BASE: float = 50.0"],
 ["대회 비중을 키운다 (팀운이 산식을 지배한다)",
  "\tvar tour_adj: float = (c.get(\"tournament_score\", TOURNAMENT_BASE) - TOURNAMENT_BASE) * 0.20",
  "\tvar tour_adj: float = (c.get(\"tournament_score\", TOURNAMENT_BASE) - TOURNAMENT_BASE) * 1.20"],
 ["수상 항을 없앤다",
  "\tvar award_adj: float = minf(c.get(\"award_titles\", 0) * 6.0\n\t\t+ c.get(\"award_mvps\", 0) * 10.0, AWARD_CAP)",
  "\tvar award_adj: float = 0.0"],
 ["MVP를 부문 수상과 같게 본다",
  "\t\t+ c.get(\"award_mvps\", 0) * 10.0, AWARD_CAP)",
  "\t\t+ c.get(\"award_mvps\", 0) * 6.0, AWARD_CAP)"],
 ["수상 상한을 없앤다 (순위를 뒤집는다)",
  "const AWARD_CAP: float = 20.0",
  "const AWARD_CAP: float = 999.0"],
 ["부상 심각도를 안 가른다 (전부 같은 무게)",
  "\tvar injury_pen: float = minf(c.get(\"moderate_injuries\", 0) * 2.0\n\t\t+ c.get(\"severe_injuries\", 0) * 10.0\n\t\t+ c.get(\"surgery_injuries\", 0) * 18.0, INJURY_CAP)",
  "\tvar injury_pen: float = minf((c.get(\"moderate_injuries\", 0)\n\t\t+ c.get(\"severe_injuries\", 0)\n\t\t+ c.get(\"surgery_injuries\", 0)) * 12.0, INJURY_CAP)"],
 ["부상 상한을 없앤다 (감점이 −252까지 간다)",
  "const INJURY_CAP: float = 45.0",
  "const INJURY_CAP: float = 999.0"],
 ["수술 두 번으로도 지명된다",
  "const INJURY_CAP: float = 45.0",
  "const INJURY_CAP: float = 10.0"],
 ["스카우트 평가를 안 본다",
  "\tvar scout_adj: float = (c.get(\"scout_score\", 30.0) - 30.0) * 0.20",
  "\tvar scout_adj: float = 0.0"],
 ["점수를 100에서 자른다 (순서가 지워진다)",
  "\tvar total: float = maxf(base + ace_bonus + tour_adj + award_adj + scout_adj - injury_pen, 0.0)",
  "\tvar total: float = clampf(base + ace_bonus + tour_adj + award_adj + scout_adj - injury_pen, 0.0, 100.0)"],
 ["점수가 음수로 간다",
  "\tvar total: float = maxf(base + ace_bonus + tour_adj + award_adj + scout_adj - injury_pen, 0.0)",
  "\tvar total: float = base + ace_bonus + tour_adj + award_adj + scout_adj - injury_pen"],
 ["미지명 문턱을 25로 되돌린다 (아무도 안 걸러진다)",
  "const UNDRAFTED_SCORE: float = 78.0",
  "const UNDRAFTED_SCORE: float = 25.0"],
 ["라운드 기울기를 바꾼다 (앵커가 NPC 실측에서 벗어난다)",
  "const ROUND_SLOPE: float = 0.40",
  "const ROUND_SLOPE: float = 0.81"],
 ["라운드를 구간으로 나눈다 (도달 불가 라운드가 생긴다)",
  "\treturn int(clampi(int(roundf(DRAFT_ROUNDS - (total - UNDRAFTED_SCORE) * ROUND_SLOPE)),\n\t\t1, DRAFT_ROUNDS))",
  "\treturn clampi(int(ceil(4.0 + (55.0 - total) / 5.0)), 1, DRAFT_ROUNDS)"],
 ["라운드를 안 자른다",
  "\treturn int(clampi(int(roundf(DRAFT_ROUNDS - (total - UNDRAFTED_SCORE) * ROUND_SLOPE)),\n\t\t1, DRAFT_ROUNDS))",
  "\treturn int(roundf(DRAFT_ROUNDS - (total - UNDRAFTED_SCORE) * ROUND_SLOPE))"],
 ["미지명자에게도 픽을 준다",
  "\tif b[\"total\"] < UNDRAFTED_SCORE:\n\t\treturn {\"drafted\": false, \"round\": 0, \"pick\": 0, \"team_id\": \"\", \"breakdown\": b}",
  ""],
 ["픽 번호가 라운드를 안 반영한다",
  "\t\t\"pick\": (r - 1) * teams.size() + slot + 1,",
  "\t\t\"pick\": slot + 1,"],
 ["픽 번호와 팀이 어긋난다",
  "\t\t\"team_id\": teams[slot],",
  "\t\t\"team_id\": teams[(slot + 1) % teams.size()],"],
 ["빈 리그에서 죽는다",
  "\tif teams.is_empty() or rng == null:",
  "\tif rng == null:"],
 ["내역을 안 싣는다",
  "\t\t\"scout_adj\": scout_adj, \"injury_pen\": injury_pen, \"total\": total,",
  "\t\t\"total\": total,"]
]
