[
 ["관계·예산을 안 본다 (협상 폭이 사라진다)",
  "\treturn round_money(float(offered) * (1.0 + owner_bonus) * budget_mod)",
  "\treturn offered"],
 ["문턱이 배수를 안 탄다 (더 주는데 더 짠 구단이 된다)",
  "\tvar base: float = float(effective) * THRESHOLD_BASE",
  "\tvar base: float = 20000.0 * THRESHOLD_BASE"],
 ["기간이 문턱을 안 움직인다 (길게 묶어도 공짜다)",
  "\tbase *= 1.0 + float(diff) * DURATION_STEP",
  ""],
 ["노트레이드가 공짜다",
  "\tif bool(terms.get(\"no_trade\", false)):\n\t\tbase *= NO_TRADE_MULT",
  ""],
 ["구단 옵션과 선수 옵션이 같은 방향이다 (협상이 거꾸로 돈다)",
  "const PLAYER_OPTION_MULT: Array[float] = [1.0, 0.97, 0.94]",
  "const PLAYER_OPTION_MULT: Array[float] = [1.0, 1.05, 1.10]"],
 ["옵션 연수를 안 가둔다 (배열 밖을 읽고 그 자리에서 죽는다)",
  "\tbase *= TEAM_OPTION_MULT[clampi(int(terms.get(\"team_option\", 0)), 0, OPTION_MAX)]",
  "\tbase *= TEAM_OPTION_MULT[int(terms.get(\"team_option\", 0))]"],
 ["문턱 안이면 100%다 (협상에 긴장이 사라진다)",
  "\tif requested_salary <= threshold_salary:\n\t\treturn PROB_MAX",
  "\tif requested_salary <= threshold_salary:\n\t\treturn 100"],
 ["넘겨도 확률이 완만하다 (문턱이 뜻을 잃는다)",
  "\treturn maxi(0, int(roundf(float(PROB_MAX) - over * PROB_FALL)))",
  "\treturn maxi(0, int(roundf(float(PROB_MAX) - over * 20.0)))"],
 ["확률이 음수로 간다 (막대가 뒤집힌다)",
  "\treturn maxi(0, int(roundf(float(PROB_MAX) - over * PROB_FALL)))",
  "\treturn int(roundf(float(PROB_MAX) - over * PROB_FALL))"],
 ["문턱을 넘겨도 역제안을 낸다",
  "\treturn requested_salary <= threshold_salary",
  "\treturn true"],
 ["계약금을 총액에 안 넣는다",
  "\treturn requested_salary * years + signing_bonus",
  "\treturn requested_salary * years"],
 ["시장가가 0일 때 나눗셈으로 간다",
  "\tif market_salary <= 0:\n\t\treturn 100",
  ""],
 ["슬라이더 폭을 안 가둔다 (화면이 보낸 값이 그대로 통과한다)",
  "\treturn round_money(float(effective) * (1.0 + clampf(ratio, RATIO_MIN, RATIO_MAX)))",
  "\treturn round_money(float(effective) * (1.0 + ratio))"],
 ["금액을 단위로 안 끊는다",
  "\treturn int(roundf(v / float(ROUND_UNIT))) * ROUND_UNIT",
  "\treturn int(roundf(v))"]
]
