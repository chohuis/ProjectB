[
  ["이닝을 그냥 더한다 (야구식 표기 무시)",
   "\t\touts += innings_to_outs(_num(st.get(\"ip\")))",
   "\t\touts += int(_num(st.get(\"ip\")) * 3.0)"],
  ["통산 ERA를 시즌 ERA의 평균으로 낸다",
   "\t\t\"era\": (\"%.2f\" % (roundf(er * 9.0 / ip * 100.0) / 100.0)) if ip > 0.0 else \"\",",
   "\t\t\"era\": (\"%.2f\" % (roundf(er / float(seasons) * 100.0) / 100.0)) if seasons > 0 else \"\","],
  ["소수부 3 이상도 아웃으로 센다",
   "\treturn whole * 3 + (frac if frac >= 1 and frac <= 2 else 0)",
   "\treturn whole * 3 + frac"],
  ["부동소수 오차를 안 흡수한다",
   "\tvar frac: int = roundi((ip - float(whole)) * 10.0)",
   "\tvar frac: int = int((ip - float(whole)) * 10.0)"],
  ["장타율을 시즌 SLG의 평균으로 낸다",
   "\t\t\tb[\"tb\"] += roundi(_num(st.get(\"slg\")) * _num(st.get(\"ab\")))",
   "\t\t\tb[\"tb\"] += roundi(_num(st.get(\"slg\")) * 100.0)"],
  ["기록이 없어도 투수 블록을 0으로 채운다",
   "\tvar pitching: Dictionary = {}\n\tif any_p:",
   "\tvar pitching: Dictionary = {}\n\tif true:"],
  ["첫 해를 입력 첫 줄로 본다",
   "\t\tif first_year == 0 or y < first_year:\n\t\t\tfirst_year = y",
   "\t\tif first_year == 0:\n\t\t\tfirst_year = y"],
  ["ERA 커리어 하이의 이닝 문턱을 없앤다 (3이닝 무실점이 박힌다)",
   "\tif spec.has(\"gate_field\") and _num(st.get(spec[\"gate_field\"])) < spec[\"gate_min\"]:\n\t\treturn null",
   ""],
  ["동률이면 나중 해를 남긴다",
   "\t\t\tif not found or (v < best_v if lower else v > best_v):",
   "\t\t\tif not found or (v <= best_v if lower else v >= best_v):"],
  ["0도 커리어 하이로 내건다",
   "\t\tif not lower and spec.has(\"floor\") and best_v < float(spec[\"floor\"]):\n\t\t\tcontinue",
   ""],
  ["최저 ERA를 최고 ERA로 본다",
   "{\"key\": \"era\", \"label\": \"최저 ERA\", \"side\": \"pitcher\", \"field\": \"era\", \"lower\": true,",
   "{\"key\": \"era\", \"label\": \"최저 ERA\", \"side\": \"pitcher\", \"field\": \"era\", \"lower\": false,"],
  ["OVR을 stats 안에서 찾는다",
   "\tif spec[\"side\"] == \"record\":\n\t\tvar rv: float = _num(r.get(spec[\"field\"]))",
   "\tif spec[\"side\"] == \"record\":\n\t\tvar rv: float = _num(r.get(\"stats\", {}).get(spec[\"field\"]))"],
  ["같은 팀 재입단을 한 구간으로 합친다",
   "\t\tif not last.is_empty() and last[\"team_id\"] == team and year == last[\"to_year\"] + 1:",
   "\t\tif not last.is_empty() and last[\"team_id\"] == team:"],
  ["팀 이력을 입력 순서대로 본다",
   "\tfor r in _by_year(records):\n\t\tvar team: String = r.get(\"team_id\", \"\")",
   "\tfor r in records:\n\t\tvar team: String = r.get(\"team_id\", \"\")"],
  ["같은 해 두 줄이 구간을 늘린다",
   "\t\telif not last.is_empty() and last[\"team_id\"] == team and year == last[\"to_year\"]:\n\t\t\t# 같은 해 레코드가 둘(승격 등) — 구간을 늘리지 않는다\n\t\t\tpass",
   "\t\telif not last.is_empty() and last[\"team_id\"] == team and year == last[\"to_year\"]:\n\t\t\tlast[\"to_year\"] = year + 1"],
  ["수상을 횟수순으로 안 세운다",
   "\t\tif x[\"count\"] != y[\"count\"]:\n\t\t\treturn x[\"count\"] > y[\"count\"]",
   "\t\tif x[\"count\"] != y[\"count\"]:\n\t\t\treturn x[\"count\"] < y[\"count\"]"],
  ["준우승도 우승으로 센다",
   "\t\telif res == \"runner_up\":\n\t\t\trunner_up += 1",
   "\t\telif res == \"runner_up\":\n\t\t\tchampion += 1"],
  ["WHIP 분모를 이닝 대신 경기로 쓴다",
   "\t\t\t\"whip\": (\"%.2f\" % (roundf((p[\"bb\"] + p[\"h\"]) / ip_real * 100.0) / 100.0)) if ip_real > 0.0 else \"-\",",
   "\t\t\t\"whip\": (\"%.2f\" % (roundf((p[\"bb\"] + p[\"h\"]) / float(p[\"g\"]) * 100.0) / 100.0)) if ip_real > 0.0 else \"-\","]
]
