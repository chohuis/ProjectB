[
 ["XP 문턱을 고정으로 둔다 (위로 갈수록 안 느려진다)",
  "\treturn 7.5 + value * 0.35",
  "\treturn 7.5"],
 ["잠재력이 학습 속도에 안 걸린다",
  "\treturn 0.80 + (p - 60.0) / 39.0 * 0.40",
  "\treturn 1.0"],
 ["잠재력 범위를 안 자른다",
  "\tvar p: float = clampf(potential, 60.0, 99.0)",
  "\tvar p: float = potential"],
 ["천장 감쇠를 비율이 아니라 절대값으로 본다",
  "\tvar ratio: float = current / potential",
  "\tvar ratio: float = current / 100.0"],
 ["천장에 닿으면 성장을 아예 막는다",
  "\treturn 0.10",
  "\treturn 0.0"],
 ["천장 감쇠 자체를 없앤다",
  "\tif ratio < 0.75:\n\t\treturn 1.00\n\tif ratio < 0.85:\n\t\treturn 0.70\n\tif ratio < 0.95:\n\t\treturn 0.35",
  "\tif ratio < 0.95:\n\t\treturn 1.00"],
 ["잠재력 0을 0으로 나눈다",
  "\tif potential <= 0.0:\n\t\treturn 1.0",
  ""],
 ["나이가 훈련에 안 걸린다",
  "\tif age <= 29:\n\t\treturn 1.00\n\tif age <= 32:\n\t\treturn 0.85",
  "\tif age <= 32:\n\t\treturn 1.00"],
 ["노장은 훈련해도 아무 일이 없다",
  "\treturn 0.45",
  "\treturn 0.0"],
 ["탈진해도 학습이 안 준다",
  "\tif fatigue >= 85.0:\n\t\tfat_factor = 0.35\n\telif fatigue >= 70.0:\n\t\tfat_factor = 0.65",
  "\tif false:\n\t\tfat_factor = 0.35\n\telif false:\n\t\tfat_factor = 0.65"],
 ["피로 방향을 뒤집는다 (지친 선수가 더 잘 큰다)",
  "\t\tfat_factor = maxf(1.0 - fatigue / 200.0, 0.80)",
  "\t\tfat_factor = maxf(fatigue / 200.0, 0.80)"],
 ["가벼운 피로 하한을 없앤다",
  "\t\tfat_factor = maxf(1.0 - fatigue / 200.0, 0.80)",
  "\t\tfat_factor = 1.0 - fatigue / 200.0"],
 ["컨디션이 학습에 안 걸린다",
  "\tvar cond_factor: float = condition / 100.0",
  "\tvar cond_factor: float = 1.0"],
 ["성장률이 학습에 안 걸린다",
  "\tvar dev_factor: float = dev_rate / 62.0",
  "\tvar dev_factor: float = 1.0"],
 ["성실성이 학습에 안 걸린다",
  "\tvar diligence_factor: float = 0.6 + (diligence / 99.0) * 0.8",
  "\tvar diligence_factor: float = 1.0"],
 ["성실성 하한을 없앤다 (0이면 훈련이 무의미해진다)",
  "\tvar diligence_factor: float = 0.6 + (diligence / 99.0) * 0.8",
  "\tvar diligence_factor: float = (diligence / 99.0) * 1.4"],
 ["남은 XP를 버린다",
  "\t\txp -= xp_threshold(value)",
  "\t\txp = 0.0"],
 ["문턱을 넘어도 능력이 안 오른다",
  "\t\tvalue += 1.0",
  ""],
 ["올려도 문턱이 안 커진다",
  "\twhile xp >= xp_threshold(value):\n\t\txp -= xp_threshold(value)\n\t\tvalue += 1.0",
  "\tvar t: float = xp_threshold(value)\n\twhile xp >= t:\n\t\txp -= t\n\t\tvalue += 1.0"],
 ["피로 구간 승수를 평평하게 둔다",
  "\tif fatigue >= 90.0:\n\t\treturn 4.0\n\tif fatigue >= 80.0:\n\t\treturn 2.5\n\tif fatigue >= 70.0:\n\t\treturn 1.5",
  "\tif fatigue >= 70.0:\n\t\treturn 1.5"],
 ["피로 구간 승수를 선형으로 만든다",
  "\tif fatigue >= 90.0:\n\t\treturn 4.0\n\tif fatigue >= 80.0:\n\t\treturn 2.5",
  "\tif fatigue >= 90.0:\n\t\treturn 2.0\n\tif fatigue >= 80.0:\n\t\treturn 1.75"]
]
