[
 ["빈 문자열 패자를 무승부로 안 본다 (어댑터와 어긋난다)",
  "\tvar is_draw: bool = loser == null or String(loser).is_empty()",
  "\tvar is_draw: bool = loser == null"],
 ["null 패자를 무승부로 안 본다 (옛 세이브가 어긋난다)",
  "\tvar is_draw: bool = loser == null or String(loser).is_empty()",
  "\tvar is_draw: bool = String(loser).is_empty()"],
 ["무승부를 아예 안 본다",
  "\tvar is_draw: bool = loser == null or String(loser).is_empty()",
  "\tvar is_draw: bool = false"],
 ["승률 분모에 무승부를 넣는다",
  "\t\tvar decided: int = wins + losses",
  "\t\tvar decided: int = wins + losses + draws"],
 ["연승 두 자리를 못 읽는다",
  "\tvar n: int = current.substr(1).to_int()",
  "\tvar n: int = current.substr(1, 1).to_int()"],
 ["결과가 바뀌어도 연승을 잇는다",
  "\treturn result + str(n + 1) if mark == result else result + \"1\"",
  "\treturn result + str(n + 1)"],
 ["최근 10경기 압축 포맷을 못 읽는다",
  "\t\t\t\texpanded += c.repeat(digits.to_int())",
  "\t\t\t\texpanded += c"],
 ["최근 10경기를 안 자른다",
  "\treturn expanded.substr(maxi(0, expanded.length() - 10))",
  "\treturn expanded"],
 ["득실을 홈·원정 그대로 넣는다",
  "\t\tn[\"runs_against\"] = s.get(\"runs_against\", 0) + (as_ if is_home else hs)",
  "\t\tn[\"runs_against\"] = s.get(\"runs_against\", 0) + hs"],
 ["경기에 안 나온 팀도 건드린다",
  "\t\tif not involved:\n\t\t\tout.append(s)\n\t\t\tcontinue",
  ""],
 ["정렬이 원본을 제자리에서 바꾼다",
  "\tvar out: Array = standings.duplicate()",
  "\tvar out: Array = standings"],
 ["승률이 같을 때 승수를 안 본다",
  "\t\tif not is_equal_approx(pa, pb):\n\t\t\treturn pa > pb\n\t\treturn a.get(\"wins\", 0) > b.get(\"wins\", 0)",
  "\t\treturn pa > pb"]
]
