# Runtime Data

- This folder stores reference runtime schema only.
- Real runtime files should be created under `%APPDATA%/<AppName>/runtime`.
- `schema/v1_init.sql` is the baseline migration for message, messenger, and training state.
