# 기획 통합 대조 원장 (Phase 3)

> `02.SvelteElectron`(기준점, 구현됨) × `03.OnePitch`(중단, 백지 재정의) 항목별 판정 기록.
> **이 문서의 존재 이유**: 나중에 "왜 이렇게 정했더라"를 다시 묻지 않기 위해 **판정과 근거를 함께** 남긴다.
>
> **판정 표기**: `유지` = SvelteElectron 현행 그대로 · `교체` = OnePitch로 대체 · `흡수` = OnePitch를 추가
> `절충` = 둘을 섞음 · `보류` = 후속 결정 · `신규` = 양쪽에 없던 것을 새로 정함

## 상위 확정 (2026-07-29, 정리 Phase 0)

| # | 항목 | 판정 |
|---|---|---|
| 0-1 | 개발 스택 | **유지** — Svelte + Electron + Rust DLL. Flutter/frb 폐기 |
| 0-2 | 세계 규모 | **교체** — OnePitch 8권역 172팀 채택 |
| 0-3 | 매치 방식 | **유지** — 1구 인터랙티브(`match_engine.rs`) |
| 0-4 | 해외 리그 | **유지** — ABL·JBL 진출 경로 존속 |
| 0-5 | 시뮬 해상도 | **절충** — 국내 전부 풀 시뮬 / 해외만 반경 적용 |
| 0-6 | 흡수할 신규 시스템 | 팀 특성 3슬롯 · 구장 파크팩터 · 팀 컬러 · 리그 히스토리 · 개인 재정 |
| 0-7 | 세이브 | **교체** — 클린 브레이크(v4), 기존 슬롯 폐기 |
| 0-8 | 리그·대회 운영 | **교체** — 국내 전 리그 전면 |

---

## A. 세계 · 리그 · 팀 (Phase 3-2) — 판정 완료

소스: `03.OnePitch/02_기획/리그팀/` 9편 × `DESIGN.md §7` · `leagueScheduler.ts`

### A-1. 리그 구성 전면 교체 [교체]

| 리그 | 현행 | **확정** |
|---|---|---|
| 고교 | 10팀 단일리그 (주2~45, 4바퀴) | **102팀 · 8권역 주말리그 · 전국대회 5종** |
| 대학 | 7팀 + 상무 (5바퀴 2연전) | **50팀 · 5조 정규리그(225경기) · 대회 3종 + 이벤트 2종** |
| 독립 | 8팀 (8바퀴) | **10팀(시민9 + 상무1) · 4단계 생존리그** |
| 프로 1군 | KBL 8팀 (16바퀴) | **10팀 · 144경기(16차전) · 5강 WC 사다리** |
| 프로 2군 | 팜 — 경기 시뮬 없음 | **10팀 · 99경기 풀 시뮬 · 축약 PO(상위4 단판 사다리)** |
| 해외 | ABL 12 · JBL 12 | **유지** (아래 A-6) |

**국내 시즌당 경기 수: 약 2,635경기** (고교 1,020 + 대학 225 + 독립 175 + 1군 720 + 2군 495).
현행 ~778경기 대비 **3.4배**. → Phase 4 성능 목표 합의의 기준 수치.

### A-2. 프로 2군 = 풀 시뮬 [교체 — R5 결정 뒤집음]

DESIGN.md §5·§7이 "팜 리그 경기 시뮬 제거 — 로스터 상태 + 콜업 테이블 룩업만"으로 확정하고
R5에서 실제로 `DEFAULT_LEAGUE_CONFIGS`에서 팜 스케줄 생성을 제거했으나, **이 결정을 되돌린다.**

- **근거**: "국내 전부 풀 시뮬" 원칙(0-5)과 일관. 2군 개인 성적이 쌓여야 콜업 판정이 능력치만이 아니라
  성적 기반이 되고, 주인공의 2군 강등·재승격 서사가 데이터로 뒷받침된다.
- **비용**: 495경기 = 국내 전체의 19%.
- **영향**: `leagueScheduler.ts`에 팜 리그 3개 스케줄 복원 + `LeaguePage` 순위표 팜 필터 해제 +
  `SeasonEndModal` 2군 우승 발표(기존 0승0패 가드로 자동 스킵되던 것) 활성화.

### A-3. 고교 권역 = 8권역으로 통일 [절충]

OnePitch 문서 자체가 불일치했다 — 주말리그 편성표는 12권역(서울A/B·경인A/B·호남A/B·영남A/B 분할),
구장·전국대회 시드는 8권역. **8권역으로 통일한다.**

- **근거**: 구장(`stadiums.csv` 8권역 거점) · 시드 공식 · 시드 데이터가 전부 이미 8권역 기준.
  OnePitch 구현도 실측으로 "12권역은 8권역 중 4개를 쪼갠 것뿐이라 소권역당 N장 공식을 8권역에 접어도
  정보 손실 없음"을 확인하고 8권역으로 접었다.
- **부작용**: 권역별 팀 수가 6(강원·제주)~20(경기·인천)으로 편차가 크다 → 라운드로빈 **바퀴 수로 보정**
  (팀 적은 권역은 여러 바퀴). 목표 경기 수는 권역 무관 균등.
- **미결**: A/B 소권역 세부 팀 편성은 채택 안 함 → 해당 항목 자체가 소멸.

### A-4. 투구수 상한 + 의무 휴식표 전면 도입 [흡수 — 신규 시스템]

현행에는 매치엔진 soft/hard cap과 `rosterEngine.ts`의 SP 의무 휴식 "경기 수"만 있다.
**리그별 투구수 상한과 투구수별 의무 휴식일 표를 새로 도입한다.**

| 리그 | 경기당 상한 |
|---|---|
| 고교 | **105구** |
| 대학 · 독립 · 프로 | **120구** |

| 당일 투구수 | 다음 등판까지 |
|---|---|
| ~30 | 다음날 가능 |
| 31~45 | 1일 |
| 46~60 | 2일 |
| 61~75 | 3일 |
| 76~95 | 4일 |
| 96~120 | 5일 |

- 고교는 76+ = 4일까지만(105구 상한이라 96~120 구간 없음).
- **목적**: 단판 넉아웃 전국대회에서 "에이스를 아껴 쓸까 밀어붙일까" 딜레마를 만든다.
  이게 고교 대회 5종을 이식하는 실질적 이유다.
- **영향**: 로테이션 배정(`rosterEngine.ts`)·스케줄러·매치엔진 강판 판정이 전부 이 표를 참조해야 함.

### A-5. 상무 = 독립리그 10번째 팀 [교체]

현행은 대학 리그 소속(7팀 + 상무). **독립리그 "상무 피닉스"(무연고)로 이전.**

- **근거**: 병역 복무 선수가 "재도전 무대"인 독립리그에서 시민구단(언더독)과 부딪히는 비대칭 구도.
- **파생 규칙**: 복무 ~1.5년 ≈ 독립 2시즌. 매 시즌 시작 시 복무 완료자는 원 소속 프로팀 복귀,
  그 자리를 신규 선발자로 충원 → **로스터 40~50% 교체**로 상무 전력이 시즌마다 요동.
- **예산**: 상무는 `budget = NULL`(국방부 예산이라 팀 예산 밴드 미적용 — 의도된 예외).

### A-6. 해외 리그 = 현행 12팀 유지 [유지]

ABL 12(동/서 6+6) · JBL 12(CL/PL). 국내 프로 10팀보다 크지만 **"더 큰 무대로 진출"로 읽는다.**

- **근거**: 팀·일정·브래킷 코드가 이미 있어 재작업 0. FA 자격 KBL 5년 / ABL 6년 / JBL 4년도 현행 유지.
- **시뮬**: 국내와 달리 **반경 적용** — 진출 전까지 순위 드리프트만, 진출 시 풀 시뮬.

### A-7. 리그 히스토리 = 고정 세계관 [흡수]

과거 5시즌 순위 · 우승 기록 · 창단 연도 · 라이벌 페어링을 **모든 플레이에서 동일한 고정값**으로 둔다.
선수만 `worldSeed`로 달라진다.

- **근거**: "무등고 = 호남 최고 명문", "한성고 = 최고 명문"처럼 기획서가 팀마다 부여한 서사 훅이
  시드마다 뒤집히면 무의미해진다. 라이벌 구도도 안정적으로 유지된다.
- **영향**: `worldSeed` 결정성 원칙의 **예외 영역**이 생긴다 — 팀 정체성은 정적 콘텐츠(master.db),
  선수는 런타임 생성(slot.db). DESIGN §8.3의 "정의/상태/파생" 3분류에서 **정의**에 속함.

### A-8. 전력★ = 문서에서 이관 [신규 데이터 작업]

기획서 표(고교 102 · 대학 50 · 프로 10)의 ★ 값이 **CSV로 옮겨진 적이 없다**(실측 확인).
OnePitch는 과거 순위에서 역산해 썼지만, **문서 값을 정본으로 CSV에 이관한다.**

- **★의 역할**: 예산 공식(`budget = 하한 + (상한−하한) × ★/5`) · 로스터 규모 · 매치 시드 기준.
- **현재 표시용 "현재 전력"은 별개** — 로스터에서 파생 계산(`03_팀_전력_공식`). ★은 고정 정체성.
- **미이관 구간**: 독립 9개 시민구단은 문서에 ★이 없어 OnePitch가 위상·자원에서 역산(★1~4)했다.
  그 역산값을 그대로 채택한다(`03.OnePitch/03_설계/12_구현확정_결정요약.md` §A-1).

### A-9. 대학 비경기성 이벤트 2종 채택 + 고교 확장 [흡수 + 신규]

| 이벤트 | 시기 | 내용 |
|---|---|---|
| **전국대학선수쇼케이스** | 8월 초 2일 | 팀 추천(50교×4) + 주목도 상위 + 구단 지명 = ~220명. Day1 워크아웃(스탯 공개) + Day2 전시경기 |
| **올스타전(북 vs 남)** | 8월 중 1일 | 포지션 기반 24명×2, **대학당 최대 2명 캡**. 9이닝 단판 |
| **고교 유사 이벤트** | 미정 | **신규 기획 필요** — 스카우트 데이·고교 올스타 등. 단계별 비대칭 축소 목적 |

- 쇼케이스는 **주목도 급등 경로**, 올스타는 **선발되는 것 자체가 서사**.
- 고교 확장은 기획서에 없던 것 → Phase 3-3(커리어) 대조 때 고교 단계 밀도와 함께 구체화.

### A-10. 팀 식별색 = 182팀 신규 저작 [신규 데이터 작업]

`teams.csv`의 color는 **102개교가 17색을 돌려쓰고 있어**(실측) 식별색으로 쓸 수 없다.
**182팀 전체에 중복 없는 고유색을 새로 배정한다.**

- OnePitch는 team_id 해시 색으로 우회했으나, 사람이 배정한 색이 더 낫다는 판단.
- 기존 CSV의 color(남색·빨강 등)는 **"대표색 라벨"**로 남겨 텍스트 서술에 쓴다.

---

## B. 커리어 · 진로 · 병역 · 시장 · 재정 (Phase 3-3) — 판정 완료

소스: `03.OnePitch/02_기획/01~08` × `DESIGN.md §6` · `faEngine.ts` · `draftEngine.ts`

### B-1. 커리어 아크 = 6 Phase 골격 [흡수]

```
뉴게임(주인공 생성)
Phase 1 아마추어 — 고교 시작 17세
   └ 갈림길 A: 프로 지명 / 대학(최대 4년) / 독립 / 입대
Phase 2 프로 육성 — 2군                    ← B-3
Phase 3 1군 정착 & 역할 축                  ← B-4
Phase 4 주전·전성기
   └ 갈림길 B: FA / 트레이드 / 잔류·재계약
Phase 5 베테랑·노쇠
Phase 6 은퇴 → 인생 기록
```

- **목표 커리어 길이: 평균 15~20시즌** (상한 아님, 최대 ~26까지 가능). Phase 5 밸런스 하네스의 1차 통과 기준.
- **커리어 정점 = 국내 1군**이라는 OnePitch 전제는 채택하지 않는다 — 해외(ABL·JBL)를 유지하기로 했으므로
  정점은 여전히 해외 진출이다. Phase 4 갈림길 B에 **해외 진출이 선택지로 남는다.**

### B-2. 드래프트 = 11월 통합 드래프트 [교체]

현행 고교 3학년 W50→W51→W52 단발 → **연 1회 11월 통합 드래프트로 통합.**

| 경로 | 자격 |
|---|---|
| 고교 3학년 | **필수**(유일한 창구) |
| 대학 재학 중 | **1~4학년 매년 신청 가능** (4학년이 마지막 기회) |
| 독립 재적 중 | **매년 재지원 가능** |

- **주목도** = 리그 성적(경기별 S~D 등급 누적) + 대회 임팩트 + 마일스톤, **무대 수준 가중**(고교 < 대학, 독립 낮음).
- 주목도 → 라운드·픽 순위 → 계약금.
- **독립→프로 중도 지명**: 정기 드래프트 밖 경로. **7월말 트레이드 데드라인에 편승**(새 캘린더 슬롯 없음).
- 미지명 → 대학·독립·입대 재경로(현행 W52 폴백 UI 재사용 가능).

### B-3. 프로 진입 = 2군 시작 [교체]

지명 → 계약 → **2군 배치**. 특급 신인만 1군 직행.

- **근거**: A-2(2군 풀 시뮬 채택)로 2군이 실제 무대가 됐다. Phase 2→3 아크가 성립하고 콜업이 서사적 사건이 된다.
- **특급 신인 1군 직행 조건은 미확정** — 기획서가 "열린 세부"로 남긴 항목. Phase 4 구현 시 확정
  (OnePitch는 `is_elite_prospect`로 지명 확률보다 훨씬 엄격한 임계값을 D그룹 placeholder로 뒀다).

### B-4. 승강 = 상시 트리거 + 월간 정기 [절충 — 둘 다]

| 경로 | 트리거 |
|---|---|
| **상시** | 1군에 부상·부진 공백 발생 → 같은 역할 2군 상위자 콜업 후보 판정 |
| **정기** | 월 경계마다 최근 페이스 평가로 콜업·강등 확률 판정 |

- 복수 후보 경합 시 우선순위: **감독 신뢰도(관계도) → 최근 페이스 → 시즌 누적 성적**.
- OnePitch 구현은 상시 트리거를 통째로 포기하고 월간만 넣었다 — **여기서는 둘 다 넣는다.**
- ⚠ 판정 경로가 둘이라 **디버깅 시 어느 쪽이 발동했는지 로그로 구분**해야 한다(Phase 6 하네스 체크 대상).

### B-5. 역할 축 = 감독 배정 (신뢰형) [흡수]

역할(선발 / 불펜: 추격조→셋업 / 마무리 / 스윙맨)은 **감독이 배정**하고 플레이어는 성적·구위로 신뢰를 쌓아 이동.

- **감독 신뢰도 = 관계도 재사용** (−100~100, 7단계 라벨). 새 수치를 만들지 않는다.
  좋은 성적이 올리고, 감독 의견에 반하는 선택(투수 교체 강행 등)이 내린다.
- **NPC와 주인공의 비대칭**: NPC는 능력치+누적성적 서열만, 주인공은 거기에 **관계도라는 추가 렌즈**.
  S~D 등급이 주인공 전용인 것과 같은 원리.
- 구현 단계 축소 가능: 선발/중계/마무리 3분류까지가 1차, 추격조 세분화·스윙맨은 후속.

### B-6. 병역 = 3경로 + 국가대표 시스템 전체 도입 [흡수 — 대형 신규]

현행 상무/일반 2경로 → **면제 / 상무 / 현역 3경로.**

| 경로 | 조건 | 결과 |
|---|---|---|
| **면제**(예술체육요원) | 국가대표 발탁 → 지정 국제대회 성과 | 완전 면제(공백 0) + 명성 대폭↑ |
| **상무** | 상무 피닉스 선발(성적·평판) | ~1.5년, **독립리그에서 경기·성장 지속** |
| **현역/사회복무** | 위 둘 다 불가 | ~1.5~2년 공백 — 피지컬만 하락, 재기 과제 |

**국가대표 — 국제대회 3종** (OnePitch도 끝내 구현 못 한 시스템, 여기서 처음 구현):

| 대회 | 주기 | 면제 연결 |
|---|---|---|
| 아시아대회 | 4년 | **금메달 → 면제** |
| 세계게임즈 | 4년 (아시아대회와 2년 오프셋) | **메달권 → 면제** |
| 야구국가대항전 | 격년 | 면제 없음 — 명예·주목도 + 상비군 가점 |

- **캘린더 = 7월 중순**, **리그를 멈추지 않는다** — 발탁된 선수만 소속팀 로스터에서 임시 이탈(WBC 방식).
  대학은 7월이 여명기, 독립은 2차 Stage라 리그 정지가 캘린더 충돌을 일으키기 때문.
  프로는 마침 올스타 브레이크라 **"프로 선수가 국대 발탁에 가장 유리한" 자연스러운 이유**가 된다.
- **자격**: 독립·대학·프로. **고교 제외**(청소년 대표 없이 성인 국대 하나로 통일).
- **대표팀 = 합성 팀** — 상위 등급 선수 모음, 기존 팀 전력 공식으로 산출. 새 팀 데이터 불필요.
- **상대국은 익명** — "본선 상대"·"결승 상대"로만 표기(가상 세계 원칙상 실제 국명 인용 불가).
- **미이행 시**: 만 28세 상당까지 미선택 → "입영통지서" 강제 이벤트 → 현역 자동 편입.
  게임오버가 아니라 **가장 불리한 선택지로 수렴**하는 압박.

### B-7. 시장·계약 [절충]

| 항목 | 판정 |
|---|---|
| FA 자격 연수 | **유지** — KBL 5 / ABL 6 / JBL 4 리그별 차등. 진출 전략의 일부(JBL이 가장 빨리 풀림) |
| 방출 판정 | **흡수(2단계)** — 7월말 트레이드 데드라인에 "방출 경고" → 8~10월 만회 → 시즌종료 최종판정 |
| FA 등급제 | **흡수(전체)** — A/B/C 등급 + **보상선수 실제 이동까지 시뮬**. FA로 떠나면 원소속팀이 팀동료 한 명을 데려간다 |
| 계약 기간 | **흡수** — 1~5년. 단년=유연·재협상 부담 / 다년=안정·중도방출 어려움 |
| 협상 방식 | 양방향 카운터오퍼(라운드 상한 있음). OnePitch 구현은 상한 1회로 단순화 — 상한 값은 Phase 4에서 확정 |
| 계약 조항 | 구단옵션·선수옵션·인센티브·노트레이드 — **현행 `faEngine.ts`에 이미 필드 존재**, 협상 UI만 연결하면 됨 |
| 트레이드 캐던스 | 연 2회(7월말 데드라인 + 오프시즌). 현행 R5와 일치 |
| 현금 트레이드 | **흡수** — 자원=궁핍 + 주인공 말년일 때 발생. "현금 확보용으로 팔려가는" 서사 |
| 생략 유지 | 3각 트레이드 · 지명권 트레이드 · 웨이버 · 에이전트 · 연봉조정신청 |

### B-8. 개인 재정 = 전체 도입 [흡수 — 신규 시스템]

현행에 없던 시스템. **6개 요소 전부 채택.**

| 요소 | 내용 |
|---|---|
| **스폰서·CF** | 명성 등급이 높을수록 좋은 제안. 4카테고리(용품·음료식품·지역상점·디지털) — 등급이 오를수록 전국 브랜드까지 개방 |
| **누진세** | 2~3구간, **수령 시점 자동 공제**. 잔액은 항상 세후. 로그에 세전/세후 명세 |
| **사치품·라이프스타일** | 성과 후 이벤트로만 등장. **동료·스태프에게 쓰면 관계도 상승**, 자기 소비는 성격(야심가↑/안분↓)에 따라 명성이 갈림 |
| **개인 트레이닝 구독** | 3분야(피지컬·기술·멘탈) × 2단계, **1개만 활성**. 보너스가 소속팀 자원에 **반비례** — 코치 없는 학교면 사비로 공백을 메움 |
| **투자** | 시즌말 3택(저축/투자/스킵), 미응답 시 저축 처리. 수익은 과세, 손실은 세금 없이 차감 |
| **최종 자산** | 은퇴 화면에 한 줄. **등급 매김 없이 순수 숫자** — "인생은 점수로 판정하지 않는다" 원칙 유지 |

- **관리 UI = 완전 자동.** 가계부·예산배분 화면 없음, 상시 잔액만 표시.
- 저장: `protagonist.finance` JSON(잔액·세율구간·트레이너{분야,단계}·투자성향).
- ⚠ 기획서 스스로 대부분을 "스탯 스케일 확정 후"로 미뤄뒀다 → **수치는 Phase 4·5에서 확정.**

## C. 육성 코어 (Phase 3-4) — 판정 완료

소스: `03.OnePitch/02_기획/육성코어/` 10편 × `DESIGN.md §3`(보호 목록) · `save.ts` · `growth_engine.rs`

### C-0. 총론 — **보호 목록을 존중한다** [유지]

DESIGN.md §3이 "Lite 작업에서 건드리지 않는다"고 못박은 육성 코어는 **전부 현행 유지**한다.
OnePitch가 이 영역을 백지 재설계했지만(능력치 9+9 대칭 · 20~80 스케일 · 히든 3종 · 훈련 6종 카탈로그),
매치엔진·성장엔진·로스터생성기·UI가 전부 현행 스키마에 묶여 있어 교체 비용이 이득을 넘는다.

> **결과적으로 통합 기획안에서 "OnePitch를 채택한다"는 것은 세계·리그·커리어·콘텐츠 계층에 한정된다.**
> 육성 코어(선수를 기르는 부분)는 SvelteElectron이 그대로 정본이다.

### C-1. 능력치 체계 [유지]

| | 확정 |
|---|---|
| 투수 9종 | `stamina · velocity · command · control · movement · mentality · recovery · clutch · holdRunners` (+ovr) |
| 타자 11종 | `contact · power · eye · discipline · speed · baseInstinct · bunting · platoon · fielding · arm · battingClutch` (+ovr) |
| 히든 2종 | `potentialHidden` · `developmentRate` |
| 스케일 | 현행 0~100 계열 (`xp_threshold(v) = 7.5 + v × 0.35`) |

**폐기한 OnePitch 안**: 피지컬·기술·멘탈 3카테고리 × 3개 대칭 구조 · `command+control→제구` 통합 ·
`holdRunners→경기운영` 흡수 · `eye+discipline→선구안` · `fielding+arm→수비` · 리더십 신규 ·
히든 3종(천재성·인성·성실함) · 20~80 스케일(화면 3단위 반올림).

- ⚠ **파생 영향**: OnePitch의 훈련 6종 카탈로그·팀 특성 3슬롯 언어 통일·"리더십" 기반 서사가
  전부 이 스키마를 전제했다 → 아래 C-2·C-3에서 개별 판정.

### C-2. 훈련 시스템 [유지]

능력치 **3슬롯(주1 + 보조2) + 프리셋** 현행 유지.
**폐기**: 훈련종류 6종 카탈로그(근력·컨디셔닝·불펜피칭·실전형시뮬·압박대응·리더십스터디) ·
주1+보조1로 축소 · 구종 슬롯 별도 풀 분리.

- 근거: C-1에서 능력치를 유지하기로 했으므로 카탈로그가 매핑할 대상(경기운영·리더십·침착함)이 없다.
- **강도 다이얼·등판 주 상한**은 현행에 이미 있으므로 그대로.

### C-3. 스태프 능력치 = 15종 전면 배선 [흡수 — 배선 작업]

스키마는 **이미 완비돼 있다**(`entities/players/*.json`의 `details.{coach,manager,owner}.stats`).
문제는 **15종 중 2종만 실제 계산에 쓰인다**는 것 — `coachEffBonus`(투수 코치 지도)와 `ownerPatience`뿐.

| 역할 | 능력치 5종 | 배선할 곳(제안) |
|---|---|---|
| 코치 | teaching · analysis · communication · discipline · leadership | 훈련 효율 · 스카우팅 정확도 · 사기 · 부상 예방 · 관계도 |
| 감독 | tactics · decision · rotationMgmt · bullpenMgmt · moraleMgmt | 매치 전술 · 강판 판단 · 로테이션 배정 · 불펜 운용 · 팀 사기 |
| 구단주 | budgetSupport · patience · prInfluence · facilityInvestment · staffTrust | FA 오퍼 규모 · 방출 판정 · 명성/주목도 · 부상 회복 · 코치 품질 |

- **근거**: OnePitch가 §6-72에서 정확히 같은 문제를 지적받고 고쳤다 — "생성만 되고 안 쓰이는 스탯을 남기지 않는다".
  **팀 선택에 실질적 격차**가 생기는 것이 이 작업의 목적.
- 개인 트레이닝 구독(B-8)의 "코치 없는 영역을 사비로 메운다"가 성립하려면 **선행 필수**.

### C-4. 부상 전조 경고 [흡수]

피로 임계 초과 **첫 주는 경고만** → 다음 주도 초과면 실제 부상 판정.

- 현행 18종 부상 × 7종 치료 × 영구 패널티 테이블은 **그대로 유지**(OnePitch보다 상세).
- NPC는 경고 단계 없이 기존대로(반응할 의사결정 주체가 없음 — 서사적 피드백은 주인공 전용).
- A-4(투구수 의무 휴식표)와 같은 결: **"아껴 쓸까 밀어붙일까" 판단에 정보를 준다.**

### C-5. 그 외 육성 코어 [유지]

구종 시스템(카탈로그·5단계 등급·해금 조건) · S~D 경기 평가 · 성장 곡선 · 매치 엔진(1구 인터랙티브) ·
XP 성장 — 전부 현행 유지. OnePitch 대비 동등하거나 더 상세하다.

---

## D. 콘텐츠 (Phase 3-5) — 판정 완료

소스: `03.OnePitch/02_기획/콘텐츠/` 7편 × `resource/data/master/{events,messages,messenger,achievements}`

**현행 규모가 OnePitch보다 크다**: 이벤트 232파일(OnePitch 52개) · 업적 36 · 메시지 3 · 메신저 3.
따라서 콘텐츠는 **물량이 아니라 엔진 기능**만 흡수한다.

### D-1. 문장 뱅크 엔진 [흡수]

같은 이벤트가 반복돼도 매번 다른 문장이 나오도록, 이벤트 본문을 **배열**로 바꾸고
발동 시 **직전에 쓴 문장을 제외**하고 랜덤 선택한다(FM/OOTP 방식).

- 배열 길이 1이면 예외적으로 재사용. **저작 시 문장 3개 미만이면 빌드 실패**로 막는다(OnePitch의 관리 장치 재사용).
- 기존 232파일은 그대로 두고 엔진만 얹은 뒤 문장을 점진적으로 늘린다.

### D-2. 선택지 톤·힌트 자동 계산 [흡수]

저작자가 톤을 손으로 쓰지 않는다 — 선택지의 **효과 델타 부호로 런타임 자동 판정**
(positive / negative / mixed / neutral) + "사기 +10, 잔액 −500원" 형식 힌트 자동 조립.

### D-3. 뉴스·미디어 4종 [흡수 — 신규]

| 항목 | 내용 |
|---|---|
| **소속팀·라이벌 경기결과** | 내가 안 던진 날의 소속팀 경기 · 라이벌교 경기 결과를 메시지로. 새 시뮬 불필요 |
| **동료 개인기록 소식** | 팀동료가 탈삼진·안타 마일스톤에 근접하면 알림. `season_stats` 실측 기반(확률형 근사가 아님) |
| **내 팀·지역 순위 정기 요약** | 월 1회 "지역 N위 · 전국 M위" 스냅샷. **전용 순위 화면 없이 메시지로만** |
| **인접권역·먼 리그 다이제스트** | 주간, 다른 권역·다른 리그 소식을 헤드라인 수준으로 |

- **102교 세계에서 내 위치를 감잡게 하는 장치** — 리그 규모가 커진 만큼 필수.
- 메시지 본문에 **팀 이름을 넣지 않는다** — 표시명 해석은 UI 책임(현행 관례 유지).

### D-4. 그 외 콘텐츠 [유지]

캐릭터(절차적 생성) · 이벤트 트리거 유형 · 업적 · 메시지/알림 분류 · 서술 템플릿 — 현행 유지.

---

## E. UI · 화면 (Phase 3-6) — 판정 완료

소스: `03.OnePitch/04_UI기획/` 9편 × `apps/ui/src/pages/` 14개

### E-1. 내비게이션 = 현행 14페이지 유지 [유지]

`academics · achievements · finance · league · main · match · messages · new-game · records · roster ·
schedule · status · team · training`

- **폐기**: OnePitch 4허브 통합(내 선수 / 리그 / 기록 / 메시지함).
- **근거**: 4허브는 **모바일 바텀 내비 4~5개 제약** 때문에 나온 결론인데, PC 전용으로 확정했으므로
  그 제약이 사라졌다. 사이드바에 14개를 다 펼칠 수 있고 FM도 그렇게 한다.
- ✅ **다행히 `finance` 페이지가 이미 존재** — B-8(개인 재정) 흡수의 착지점이 확보돼 있다.

### E-2. 긴급 처리건 자동 진입 = 현행 유지 [유지]

정지 후 사용자가 직접 해당 화면으로 이동. **폐기**: OnePitch 결정2(자동 진입).

- 근거: 마찰은 있지만 **"내가 어디로 가는지" 통제권이 명확**한 쪽을 택함.

### E-3. 그 외 UI [유지]

화면별 상세(내 선수·리그·기록·메시지함·매치·캐릭터생성·전환화면·은퇴)는 현행 구현 유지.
표시 언어(능력치 색 스펙트럼 · 라이브상태 게이지 · 라벨 버킷 · 5★ · 1인칭 텍스트)도 현행.

---

## 판정 요약

| 영역 | 교체/흡수 | 유지 |
|---|---|---|
| **A. 세계·리그·팀** | 리그 구성 전면 · 2군 풀시뮬 · 투구수 제한 · 상무 이동 · 고정 히스토리 · 전력★ · 대학 이벤트 · 팀 컬러 | 해외 리그 12팀 |
| **B. 커리어·시장·재정** | 6 Phase 아크 · 통합 드래프트 · 2군 시작 · 승강 2경로 · 역할 축 · **국가대표/면제** · 방출 2단계 · FA 등급제 · **개인 재정 전체** | FA 자격 연수(리그별 차등) |
| **C. 육성 코어** | 스태프 15종 배선 · 부상 전조 경고 | **능력치 · 스케일 · 히든 · 훈련 슬롯 · 구종 · 평가 · 매치엔진 (보호 목록 전부)** |
| **D. 콘텐츠** | 문장 뱅크 · 톤 자동계산 · 뉴스 4종 | 이벤트 232파일 · 업적 · 캐릭터 · 템플릿 |
| **E. UI** | — | **전부** (14페이지 · 자동진입 없음 · 표시 언어) |

**총 질문 22건, 미결 0건.**

### 후속 단계로 넘긴 미확정 값

- 특급 신인 1군 직행 조건 (B-3)
- 협상 카운터오퍼 라운드 상한 (B-7)
- 개인 재정 전 수치 — 세율 구간 · 트레이닝 보너스 폭 · 투자 수익률 (B-8)
- 국가대표 발탁 커트라인 · 대회 성과 확률 (B-6)
- 스태프 15종 각각의 계수 (C-3)
- 고교 유사 이벤트 구체 기획 (A-9)

---

## 구현 중 추가 판정 (Phase 5)

기획 통합 시점에는 안 보였다가 **구현하면서 드러난 충돌**이다. 위 22건과 성격이 달라
따로 적는다 — 기획서끼리의 차이가 아니라 "기획서 수치가 v2 세계 규모와 안 맞는" 경우.

### P5-1. 주말리그 경기 수를 어떻게 균등하게 맞추나

- **기획서(02_고교.md §4-1)**: **12권역**(서울A/B·경인A/B·호남A/B·영남A/B 등 분할)에
  권역별 바퀴 수를 손으로 지정 — 16팀 3바퀴 21경기, 6팀 4바퀴 20경기 …
- **v2 세계**: 권역은 거점구장 공유로 **8권역**, 크기 6~20팀. 손으로 지정한 바퀴 표가 안 맞는다.
- **판정**: 바퀴 수를 입력으로 받지 않고 **목표 경기 수(20)를 입력으로 받아 바퀴를 역산**한다
  (`build_rounds_targeted`). 권역이 몇 팀이든 전 팀 정확히 20경기.
- **버린 안**: ① 12권역으로 쪼개기 — 거점구장이 8개뿐이라 권역의 물리적 근거가 사라진다.
  ② 기획서 바퀴 표를 8권역용으로 다시 손으로 쓰기 — 팀 수가 바뀔 때마다 또 손봐야 한다.

### P5-2. 전국대회 권역 배분을 어떻게 나누나

- **기획서(§4-2)**: "권역당 상위 2팀(=24)" — 12권역 × 2 기준.
- **v2 세계**: 8권역이라 × 2 = 16밖에 안 되고, 권역 크기가 6~20이라 **균등 배분이 왜곡을 만든다**.
  6팀 권역에서 4팀을 뽑으면 67%가 전국대회에 나가고 20팀 권역은 20%만 나간다.
- **판정**: **권역 크기 비례 배분(최대잔여법, 권역당 최소 1)**. 무궁화기 48팀 기준
  진출률 33~38%로 수렴한다. 참가 총수(32·32·48·102·24)는 기획서 그대로.
- **근거**: P5-1에서 "권역 크기 편차를 보정한다"고 정한 것과 같은 원칙이다. 크기가
  제각각인 권역을 균등 취급하면 어디선가 반드시 왜곡이 나온다.
- **버린 안**: 권역당 고정 3팀 + WC로 나머지 — 소권역 과대대표가 그대로 남는다.

### P5-3. 부전승 규칙을 따로 만들 것인가

- **기획서**: 48강 상위16 부전승 · 128대진 26 부전승 · 32대진 8 부전승 — 수치가 명시돼 있다.
- **판정**: **따로 만들지 않는다.** 표준 브래킷 시드 순서(1↔N, 2↔N-1 …)에서 빈 시드를
  부전승으로 두면 기획서 수치가 **정확히 그대로 나온다**(64−48=16, 128−102=26, 32−24=8).
  `test-tournament.cjs`가 이 일치를 검증한다.

### P5-4. 대회 성적을 리그 순위에 넣을 것인가

- **판정**: **넣지 않는다.** 개인 기록·피로도·로테이션은 리그 경기와 똑같이 쌓되
  순위표만 건너뛴다(`applyTournamentResult`).
- **근거**: 넣으면 전반기 대회 성적이 후반기 대회 시드에 섞여 들어간다.
  "주말리그 순위로 시드를 정한다"(§4-2)는 규칙 자체가 무의미해진다.

### P5-5. 대학 리그·대회 (Phase 5-5)

- **조 편성**: 기획서 A~E 5조는 **이미 데이터에 있었다** — 거점구장 5개에 정확히
  10팀씩. 고교 8권역과 같은 규칙(구장 공유)이라 5-3 스케줄러를 그대로 쓴다.
- **조별 요일**: 기획서가 조마다 다르게 정했다(A·B 화수목금 / C·D 목금 / E 4월 목금·5월 화수).
  요일을 `RegionInput.dayOffsets`로 올려 조별로 준다. E조의 시기 전환만은
  라운드 순환(`[3,4,1,2]`)으로 **근사**했다 — 라운드별 요일 전환은 구조가 커진다.
  **왜 살렸나**: 5-8 의무 휴식표가 일 단위라 요일이 등판 간격에 실제로 영향을 준다.
- **왕중왕전 참가**: 기획서 "조 1위 자동 5 + 조 2위 중 승률 상위 3". 5-4의 비례
  배분으로는 표현 불가 → **조당 고정 인원 모드**(`perGroupSlots`)와 **WC 후보
  순위 제한**(`wildcardMaxGroupRank`)을 넣었다. 대학 5조는 전부 10팀 균등이라
  비례 배분 자체가 의미가 없다. 고교는 기존 비례 배분 유지.
- **시드 정책**: 왕중왕전만 `autoSeedsFirst` — 조 1위 5팀이 상위 시드, WC 3팀이
  하위(기획서 명시). 고교는 "강한 권역 3위가 약한 권역 1위보다 셀 수 있다"는
  이유로 승률 통합 정렬을 유지한다. 같은 엔진에 두 정책이 공존한다.
- **조별예선**: 은하기(8조×3팀→조1위 8팀)·여명기(4조×5팀→조상위2 8팀)는
  넉아웃 브래킷으로 표현 불가 → `group_stage.rs` 신설. 예선이 끝나야 본선
  브래킷이 생긴다(`groupStages` → `tournaments`로 승격).
- **조 추첨 결정성**: 기획서는 "완전 랜덤 추첨(시드 없음)"이고 그게 '죽음의 조'
  드라마의 근거다. 다만 `rand::thread_rng()`를 쓰면 세이브를 다시 열 때마다 조가
  바뀐다 → **splitmix64 + `worldSeed × 대회id × 시즌`** 으로 결정적 셔플.
  플레이어에겐 여전히 예측 불가고, 세이브/로드 스컴도 막힌다.
  이를 위해 `SaveSeason.worldSeed`를 신설했다 — 주중에 뽑기가 필요한데 그때마다
  slot.db meta를 비동기로 읽을 수는 없다.
- **버린 안**: 예선 없이 24팀/20팀을 바로 넉아웃 — 코드는 0줄이지만 은하기(소수정예)와
  여명기(다전제)가 "참가 인원만 다른 같은 대회"가 된다.

### P5-6. 대회 시드용 순위 스냅샷 (Phase 5-5a)

- **문제**: 대회마다 보는 시점이 다른데(개나리기 전년 · 장미기/무궁화기 전반기 ·
  패왕기 후반기) 순위표가 하나뿐이었다. 누적 순위를 전부에 쓰면 패왕기가
  "그해 최강전"이 아니라 "3월부터 잘한 팀 대회"가 된다.
- **판정**: 전·후반기 경계를 **무궁화기 기준**으로 끊는다. 기획서 캘린더가
  "4~6월 전반기 / 7~8월 무궁화기(여름방학) / 9~11월 후반기"라고 적었다.
  후반기 순위는 스냅샷과의 **차분**이다(누적이 아니라 후반기 성적만).
- **첫 시즌**: 전년 순위가 없으니 **전력★ + history.seasonRanks**로 합성한다.
  DESIGN §7.1이 이걸 "모든 플레이에서 동일한 고정값"으로 정했으므로 근거가 된다.
  덕분에 "한성고 = 최고 명문"이 1학년 봄 개나리기부터 먹힌다.
- **버린 안**: 스냅샷 없이 대회 직전 누적 순위 — 장미기와 무궁화기가 같은 기준을
  쓰게 되고 패왕기 상위권이 시즌 초에 고정된다.

### P5-7. 시즌 결산 대회의 중복 (Phase 5-5a)

- **문제**: 고교·대학 모두 v1의 `top4 준결승/결승` 포스트시즌이 붙어 있는데,
  v2에서 시즌 결산이 **패왕기(고교 11월)** · **왕중왕전(대학 5월)** 로 옮겨갔다.
  그대로 두면 결승이 두 번 열린다.
- **판정**: 양쪽 다 `build_hs_bracket` / `build_univ_bracket`을 **폐기**한다
  (Rust 함수 · TS 래퍼 · preload · projectb.d.ts 전부).
  102팀 8권역에서 "전국 top4"를 매기는 것 자체가 맞붙은 적 없는 팀끼리
  승률만 비교하는 것이라 근거가 약하다. 패왕기는 후반기 권역 상위라는 명확한 규칙이 있다.
- 프로·독립·해외는 그대로 — 그쪽은 단일 리그라 top-N 포스트시즌이 정상이다.

### P5-8. 대회 시뮬 범위 (Phase 5-5a)

- **문제**: 5-4의 대회 진행이 "주인공이 고교일 때만" 돌았다. DESIGN §2는
  국내 전 리그 상시 풀 시뮬인데, 주인공이 대학에 가면 고교 전국대회가 멈춘다.
- **판정**: 리그 게이트를 없앤다. 주인공 소속과 무관하게 고교 5종·대학 3종이 매년 돈다.
  추가 경기 약 282(고교 233 + 대학 49) — 정규리그 2,635 대비 11%.
- 덕분에 "모교가 국화기 우승" 같은 뉴스가 프로 진출 후에도 성립한다.

### P5-9. 독립 생존리그 (Phase 5-6)

- **형식**: 리그도 대회도 아닌 제3의 구조다. 매 단계 하위권이 통째로 탈락한다
  (10→8→4). 각 단계는 **이전 대진을 이어받지 않고 생존팀끼리 새 라운드로빈**이라
  (기획서 §3) **단계마다 순위표도 리셋**한다. 누적을 쓰면 1차에서 벌어놓은 승수로
  3차 순위가 정해져 "매 단계 새 승부"가 무의미해진다.
- **일정 생성**: 5-3의 `build_rounds_targeted`를 그대로 재사용한다. 단계 = 팀 목록 +
  목표 경기 수라 새 알고리즘이 필요 없었다 (1차 18 · 2차 14 · 3차 3).
- **타이브레이크**: 기획서는 "승률 → **승자승** → 실점 최소 → 추첨"이지만
  **승자승은 구현하지 않고 다득점으로 대체**했다. 순위표(`Standing`)에 상대별 전적이
  없어 172팀 규모에서 필드를 늘리면 저장량·마이그레이션·스냅샷이 전부 따라온다.
  고교·대학 대회 시드가 이미 다득점 기준으로 돌고 있어 **리그 간 기준이 일치**한다.
  → 동률이 탈락을 가르는 상황에서 기획서와 다른 팀이 떨어질 수 있다. 감수한 차이.
- **4차 사다리**: 구 `build_ind_bracket`은 "1위 vs 2위 단판" 하나뿐이라 4팀 사다리를
  표현하지 못했다. `build_ind_ladder`로 교체 — 준PO(3vs4 단판) → PO(2위 vs 준PO승자
  단판) → 챔피언결정전(1위 vs PO승자, **3전2승**). `PostseasonSeries`가 `bestOf`와
  `nextSeriesId`를 이미 지원해 구조 변경 없이 들어갔다.
- **경기 수 정정**: DESIGN이 독립을 175로 적었으나 기획서 §3 표대로면 정규 **152**
  (90+56+6)이고 포스트시즌 최대 5를 더해 157이다. DESIGN의 총계도 대회분(318)이
  빠져 있어 2,635 → 약 2,930으로 고쳤다.
- **상무 로스터 40~50% 교체는 Phase 7로 이월** — 복무 완료자 복귀가 "원 소속 프로팀
  복귀"라 병역 트랙 전체와 묶여 있고, 신규 선발은 Phase 6 인물 재생성 뒤가 자연스럽다.

### P5-10. 프로 포스트시즌 (Phase 5-7)

- **v1 브래킷에 3위 누락 버그가 있었다.** 6강 구조로 WC(5위vs6위) → 준PO(4위) →
  PO(2위) → KS(1위)였는데 **3위가 어디에도 안 나왔다**(`t[2]` 미사용). 8팀 시절
  코드로 보인다. 기획서 5강(§4)으로 교체하면서 자연히 고쳐졌고,
  `test-pro-postseason.cjs`가 "1~5위 전원 등장"으로 회귀를 막는다.
- **WC 어드밴티지**("4위는 1승, 5위는 2승"): 새 장치를 만들지 않고
  **홈팀이 1승을 안고 시작하는 3전2승**(`bestOf: 3, homeWins: 1`)으로 표현했다.
  4위는 1승만 더하면 2승, 5위는 2연승해야 한다 — 최대 2경기로 규칙과 정확히 같다.
  `PostseasonSeries`에 이미 있던 필드라 구조 변경이 없었다.
- **`resolve_non_protagonist_series`가 어드밴티지를 삼키고 있었다.** 배경 시리즈를
  자동 처리할 때 승수를 **0부터 다시 세서** 홈의 1승 선취가 사라졌다. 시작 승수를
  이어받도록 고쳤다 — 이 결함이 없었으면 4위 통과율이 75%가 아니라 50%로 돌았다.
  (테스트가 200회 시행으로 통과율을 확인한다)
- **2군 축약 PO**: 상위 4팀 단판 사다리. 독립 사다리와 모양은 같지만 **결승도 단판**
  이라는 점이 다르다(독립 챔결은 3전2승). 기획서 §5의 "1군 5강처럼 무겁게 안 감".
- **포스트시즌 시뮬 범위**: 대회(P5-8)와 같은 기준으로 **국내 전 리그 상시**로 바꿨다.
  `injectLeaguePostseason`은 주인공 경기를 주입하며 멈춰야 해서 주인공 리그만
  처리하고, 나머지 국내 리그는 `runBackgroundPostseasons`가 통째로 정리한다.
  해외(ABL·JBL)는 Lazy 정책(DESIGN §2.2)대로 진출 전까지 제외.

### P5-11. 반경 게이트 — v1이 v2 작업을 막고 있었다 (Phase 5-8)

- **문제**: `radiusGate.ts`가 v1(Lite) 그대로였다. 커리어 단계별 표로
  주인공 리그만 반경 1이고 **나머지 국내 리그는 반경 3(비활성)** 이었다.
  `backgroundLeague.ts`가 반경 2·3을 시뮬에서 건너뛰므로,
  **5-5·5-6에서 깐 대학 225경기·독립 152경기가 생성만 되고 시뮬되지 않았다.**
  타입도 테스트도 통과하는데 경기가 안 돌던 상태 — 조용한 결함이었다.
- **판정**: DESIGN §2 v2대로 **국내 5리그(고교·대학·독립·프로1군·2군)를 항상 반경 1**,
  해외(ABL·JBL)만 반경 2로 바꿨다. 커리어 단계 분기는 해외 진출 시
  그 리그가 1이 되는 것뿐이다. 반경 3(비활성)은 이제 아무도 안 쓴다.
- **성능 영향**: 주인공이 고교여도 프로 720경기가 매주 돈다. 주당 약 56경기.
  Phase 8에서 실측한다 — 지금 되돌리면 v2 세계관 자체가 성립하지 않는다.
- **회귀 방지**: `test-pitch-rules.cjs`가 `radiusGate.ts` 소스에서 DOMESTIC 집합을
  읽어 국내 5리그가 전부 들어 있는지 검사한다. 순수 TS라 CJS 테스트에서
  import할 수 없어 소스 파싱으로 대신했다 — 완벽하진 않지만 되돌림은 잡는다.

### P5-12. 투구수 상한 · 의무 휴식표 (Phase 5-8)

- **상한을 리그별로**: `PROTAGONIST_PITCH_COUNT_HARD`가 전역 120 상수였다.
  `league_pitch_limit()`으로 바꿔 고교만 105로 낮췄다(DESIGN §7.2).
  소프트캡도 상한의 75%로 비례시켰다 — 105구 리그에서 90구 소프트캡은
  여유가 15구뿐이라 사실상 하드캡과 같아진다.
  `MatchState`에 `pitchLimit`/`pitchSoft`를 실어 경기마다 들고 다닌다.
  구 세이브는 0이라 전역 상수로 떨어진다(마이그레이션 불필요).
- **휴식표를 일 단위로**: `lastPitchedWeek`(주 단위)로는 표현이 안 됐다.
  고교 주말리그는 토·일 연전이라 **"토요일 105구 던지고 일요일 또"** 가
  주 단위 검사로 안 걸린다. `PlayerCondition`에 `lastPitchedDate`·`lastPitchCount`를
  추가하고 `rest_rules.rs`가 날짜 차이로 판정한다.
  → 5-3~5-7에서 리그별 요일(고교 주말 · 대학 평일 · 독립 화목토)을 다르게 깔아둔 게
  여기서 실제로 쓰인다.
- **구 동작 폴백**: 날짜가 없거나 파싱이 안 되면 **막지 않는다**. 데이터가 없다는
  이유로 등판을 막으면 새 세이브·마이그레이션 직후에 리그가 멈춘다.
  `reliever_would_pitch`는 날짜가 없으면 구 "같은 주 재등판 금지"로 떨어진다.
- **부수 효과**: 구 `same_week_penalty`는 "이번 주에 던졌으면 무조건 불가"라
  너무 거칠었다 — 불펜이 한 주에 두 번 못 나왔다. 일 단위로 바뀌며 이것도 풀렸다.

---

## Phase 6 판정

### P6-1. 스태프를 master.db에서 slot.db로 (Phase 6A)

- 스태프는 v1에서 **master.db(정의)** 에 있었다. `entities/players/COA_*.json` 373개를
  구워 넣고 read-only로 읽었다. 그런데 people.md §3이 요구하는 **생멸**(나이·은퇴·경질·이동)은
  전부 **상태 변화**다 — read-only 저장소에 둘 수 없다.
- **판정**: slot.db `staff` 테이블로 옮긴다. master.db에는 **생성 규칙**만 남긴다
  (`staff_rules.toml` → `players/staff_rules.json`).
- `npc_master` 테이블은 **빈 채로 남긴다.** `master:loadEntities`가 아직 SELECT하고,
  시나리오 Named NPC를 콘텐츠로 넣는 경로가 생기면 다시 쓸 자리다. 지우면 그때 되살려야 한다.

### P6-2. 감독 능력치가 매치 엔진에 전달되지 않고 있었다 (Phase 6A)

- **문제**: 구 JSON은 감독 스탯을 `tactics`/`decision`/`rotationMgmt`/`bullpenMgmt`/`moraleMgmt`로
  저장했는데 `MatchPage.svelte:695-698`은 `stats.handlePressure`/`stats.strategy`/`stats.motivation`을
  읽었다. 이름이 하나도 안 맞아 **전부 `undefined`가 Rust로 넘어가 기본값 50으로 돌았다.**
  TS 타입(`EntityManagerStats`)은 또 제3의 이름을 선언하고 있었다 — 3중 드리프트.
- **판정**: **Rust `ManagerStats`를 정본**으로 삼는다
  (`tacticalIQ`·`bullpenRead`·`offenseMind`·`motivator`·`clutchDecision`).
  실제로 값을 쓰는 쪽이 이름을 정하는 게 맞고, 매치 엔진 외에 감독 능력치의 소비자가 없다.
- 코치는 `teaching`/`analysis`/`communication`/`discipline`/`leadership`,
  구단주는 `budgetSupport`/`patience`/`prInfluence`/`facilityInvestment`/`staffTrust`.
  구단주 `patience`는 6B 경질 임계값에 쓰인다.

### P6-3. 스태프 ID 규약 — 팀 종속이되 이동해도 불변 (Phase 6A)

- `staff:<teamId>_MGR` / `_OWN` / `_COA<n>`. 레거시 `COA_00001` 류는 폐기.
- **팀 기준으로 잡되 이동해도 ID를 바꾸지 않는다** — 관계도(6C)가 ID로 연결되므로
  이적할 때마다 ID가 바뀌면 주인공이 쌓은 신뢰가 사라진다.
- 결과적으로 "TEAM_A 이름을 가진 staff가 TEAM_B에 있는" 상태가 생기는데, 그게 의도다.
  ID는 식별자일 뿐이고 소속은 `team_id` 컬럼이 말한다.

### P6-4. 팀별 독립 난수 스트림 (Phase 6A)

- 팀·역할·순번마다 `splitmix64` 스트림을 따로 판다(`teamSeed ^ hash("manager")` 등).
- **왜**: 한 스트림으로 순차 생성하면 **코치 수가 바뀌면 그 뒤 모든 팀이 흔들린다.**
  자원 등급 규칙을 조금 고쳤을 때 무관한 팀 감독이 딴 사람이 되면 디버깅이 불가능하다.
- 검증: `test-staff-gen.cjs`가 "팀 목록이 줄어도 남은 팀 스태프는 동일"을 확인한다.
  해외 Lazy 생성 시 국내가 안 흔들리는 것도 이 성질 덕이다.

### P6-5. EntityManagerModal 폐기 (Phase 6A)

- 개발자 도구 1,456줄. master.db 엔티티를 편집해 `entities/players/*.json`에 되썼다 —
  DESIGN §8.3이 폐기한 "생성 결과물을 저장해두고 스크립트로 사후 수정" 패턴 그 자체다.
- 절차 생성 세계에서는 **손 편집이 설계상 금지**다. 규칙을 고쳐야 한다.
- 함께 제거: `masterUpsertEntity`/`masterDeleteEntity` preload 브릿지(IPC 핸들러는 R3a-4d에서
  이미 죽어 있었다), `scripts/rebuild_entity_index.cjs`, `npm run gen:index`.
- `masterSave`는 남긴다 — 이벤트·업적 에디터가 쓰고, 그쪽은 진짜 ①정의 콘텐츠다.

### P6-6. 스태프 생애주기 수치 (Phase 6B)

정본은 `staff_rules.toml [lifecycle]`. 문서에 수치를 복제하지 않는다.

| 항목 | 확정 |
|---|---|
| 은퇴 곡선 | 감독 60부터 시작·72 상한 · 코치는 조금 이르게 · 구단주는 70부터·80 상한 |
| 경력 성장 | peak 50 이전 +1, 62 이후 −1. **능력치 5종 중 2종만** 적용 |
| 경질 기준 | 전력★ 기대 순위 미달 누적. 임계값은 구단주 patience (≤29→1시즌 · ≤69→2 · 그 외 3) |
| 충원 | 상위 리그·강팀부터. 하위 자리의 우수 스태프 스카우트 → 없으면 신규 생성 |

- **★1 팀은 경질이 없다.** 기대 순위 비율 `1 − (power−1)/5`이라 ★1이면 전원이 기대치다.
  약팀 감독이 하위권이라고 잘리는 건 부당하다 — 그게 그 팀의 정상이다.
- **성장은 능력치 2종만.** 5종을 전부 올리면 몇 시즌 만에 최상급이 되고 팀 격차가 무너진다
  (people.md §3-3 "완만하게").
- **이동은 위로만.** `scout_from_lower_only` — 자유 이동장이 아니다. 이동으로 생긴
  빈 자리는 **다음 시즌**에 메워진다. 같은 시즌에 연쇄를 끝까지 돌리면 리그가 한 해에 뒤집힌다.
- **신규 생성은 Rust가 하지 않는다.** `vacant` 이벤트로 남기고 TS가 `staff_gen`을 재사용한다 —
  이름 풀·생성 규칙을 생애주기 모듈이 또 들고 있지 않게.

> ⚠ **내가 사용자에게 제시한 예상치가 틀렸다.** "20시즌 후 초기 감독 생존 45~55%"라고
> 적었는데, 감독 시작 나이가 38~66(평균 52)이라 20시즌 뒤 평균 72세로 상한에 닿는다.
> 실측은 **13%** 다. 초기 감독 절반이 20시즌을 버티면 평균 재임이 40년이라는 뜻이고
> 애초에 성립하지 않는다. 곡선 자체는 사용자가 고른 "중간"이 맞으므로
> **테스트를 절대 수치 대신 "세 곡선 중 중간인가"로 바꿨다**(빠름 4% · 현행 13% · 느림 19%).

### P6-7. 566줄을 쪼개지 않은 이유 (Phase 6B)

- `processAllLeaguesSeasonEnd`(566줄)는 병역·FA·드래프트가 얽혀 있고 섹션 경계가 거의 없다.
- 스태프는 그 셋에 의존하지 않으므로 **`usecases/seasonEnd/staffLifecycle.ts`에서 독립 처리**하고
  566줄 끝에서 한 번 호출한다. 566줄 전면 분리는 별도 작업으로 남긴다 (사용자 확정).
- 호출을 `try/catch`로 감싼다 — 스태프 처리가 실패해도 시즌 종료는 끝나야 한다.
  여기서 던지면 병역·FA까지 다 처리한 시즌이 통째로 롤백된다.

### P6-8. 경질이 종착이면 세계에 아는 이름이 안 쌓인다 (Phase 6B 보강)

- **사용자 지적**: "팀 성적이 안 나오거나 특정 나이가 되면 하향도 있어야 할 것 같은데
  감독 코치도." 초기 구현은 **상향 이동만** 있었다.
- 그 결과 실제 구멍이 있었다: **경질된 스태프가 `status=fired`로 영구히 사라졌다.**
  DB에 남아 있지만 다시 등장하는 경로가 없어 매 시즌 새 사람이 생성됐다.
- **판정** (사용자 확정 3건):
  1. **경질 = FA 전환**(`status=free_agent`). 다음 빈 자리 후보에 들어간다.
     `fa_downward_only` — 마지막 소속보다 낮거나 같은 자리만. 잘린 직후 상위 팀으로
     가는 건 막는다. FA도 나이를 먹어 고령이면 은퇴 곡선이 알아서 정리한다
     (별도 "FA 잠재 기간" 카운터 불필요).
  2. **감독 경질 시 코치진 일부 동반 이탈** — 능력 하위 약 1/3. 감독 교체가 팀에
     진짜 충격이 되고, 주인공 입장에서 훈련 효율이 떨어지는 실질적 손실이 된다.
  3. **고령 하향** — 은퇴 확률이 생기는 나이부터, 상위 리그에 있으면 하향 확률 =
     은퇴 확률 × 0.5. 최하위 리그(고교)면 더 내려갈 곳이 없어 하향 없음.
     구단주는 제외 — 경영진은 리그를 옮겨 다니지 않는다.
- **충원 우선순위**: ① 현직 하위 자리 우수 스태프(상향) → ② FA 풀 → ③ 신규 생성.
- **주의한 함정**: FA가 은퇴할 때 이벤트 kind를 `retired_fa`로 갈랐다. `retired`로
  두면 이미 비어 있는 자리를 빈 자리로 또 세서 **한 자리에 두 명이 부임**한다.
- **실측 (20시즌)**: 경질 201 · 동반이탈 105 · 하향 86 · 상향 532 · FA 재취업 354 ·
  **신규 생성 0건**. 사람이 완전히 재활용된다. FA 잔류는 456명 중 5명.
  "KBL에서 잘린 감독이 고교에 부임"이 15시즌 동안 164건 — 경로가 실제로 성립한다.

### P6-9. 선수 학적 역행 — 대학 두 번 입학이 실제로 가능했다 (Phase 6B 보강)

- **사용자 지적**: "고등학교 재입학, 대학 두 번 입학 같은 케이스는 안 돼."
  (스태프 이동 로그의 `KBL→고교`를 보고 물었지만, 그쪽은 **감독** 이동이라 정상이다.
  다만 그 질문이 **선수** 경로의 실제 결함을 드러냈다.)
- **결함**: `advanceWeek`의 `isUnivResultWeek`가
  `careerStage === "university" || "independent"` 일 때도 발동하는데, 그 안에서
  `apps.universityChoices`를 조건 없이 처리했다. 지원 UI(`CareerChoiceHubModal`)도
  `!isIndependent`로만 걸러 **대학 재학생에게 "대학 진학 신청"이 보였다.**
  → `applyDraftDecision({stage:"university"})`가 호출되며 **대학 두 번 입학**이 성립.
  `독립 → 대학`도 같은 경로로 가능했다.
- **판정** (사용자 확정): **지원 단계 + 전이 가드 둘 다.** UI만 고치면 나중에 다른
  경로가 생길 때 같은 버그가 재발한다.
  1. `careerTransition.ts` 신설 — 전이 허용표가 단일 정본.
     `applyDraftDecision`이 위반 시 **상태를 건드리지 않고** `return s`.
  2. `canApplyToUniversity`/`canApplyToIndependent`로 지원 UI·결과 모달·드래프트
     알림·`advanceWeek` 결과 처리를 전부 게이트.
- **허용표**: 고교 → 대학/독립/프로/군 · 대학 → 독립/프로/군 · 독립 → 프로/군 ·
  프로 ↔ 프로/독립/군. **어떤 단계도 `highschool`로 못 간다**, `university`로 갈 수
  있는 건 `highschool`뿐. `military`에서 나가는 전이는 표에 없다 —
  전역은 `militaryHiatusStage`(입대 전 단계) 복원 전용 경로다.
- **스태프와 선수는 규칙이 다르다.** 감독·코치의 `KBL → 고교`는 정상이고 의도한
  동작이다(P6-8). 학적이 아니라 직장이므로 역행 개념이 없다. 두 규칙을 한 표로
  묶으려 하면 어느 한쪽이 망가진다.
