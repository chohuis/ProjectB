# 화면 붙이기 · 스카우팅 배선 계획 (2026-08-30)

**밸런스 잡기 전에 한다.** 근거 둘:

- **화면 없이 도는 기능은 밸런스를 재도 확인할 방법이 없다.** 이번 세션에
  만든 것 대부분(등록말소·웨이버·의료팀·전지훈련·명예의 전당·영구결번)이
  돌기는 하는데 플레이어가 볼 데가 없다.
- **스카우팅은 드래프트 결과를 바꾼다.** 밸런스 뒤에 넣으면 그 측정이
  통째로 무효가 된다.

점검 근거는 `scripts/audit-career.cjs`(60항목).

    시작    기능 없음 11건 · 보이는 데 없음 **14건**
    A~D 뒤    기능 없음 10건 · 보이는 데 없음 **3건**

남은 3건은 계획에 없던 작은 것들이다 — FA 미계약자 유입 · 스카우팅
**화면 표시**(배선은 D에서 끝났다) · 보상선수.

남은 3건: FA 미계약자 유입 · **스카우팅 잡음(D단계)** · 보상선수.

---

## 진행 현황

| 단계 | 내용 | 상태 |
|---|---|---|
| A | 소식으로 보이는 것 | ✅ 2026-08-30 — **6건 중 3건만 없었다** |
| B | 구단 정보에 얹는 것 (4건) | ✅ 2026-08-30 (`424bbb464`) |
| C | 명예의 전당 화면 | ✅ 2026-08-30 (`20ff42b4e`) |
| D | 스카우팅 잡음 배선 | ✅ 2026-08-30 (`88afe5f5c`) |
| E | 통합 점검 | ✅ **14 → 3건** |

**왜 이 순서인가**

- **A가 가장 싸다.** 소식(`NewsPage`·`DigestCards`)은 이미 있고, 이벤트를
  한 줄 더 흘리는 일이다. 새 화면이 없다.
- **B는 `TeamDetailModal`에 얹는다.** 1단계에서 연표를 붙인 자리라 구조를
  안다.
- **C만 새 화면이다.** 명예의 전당은 목록·검색이 필요해 패널로 안 된다.
- **D를 마지막에** 두는 건 드래프트 구조를 바꾸기 때문이다 — 화면 작업과
  섞으면 회귀 원인을 못 가린다.

---

## A단계 — 소식으로 보이는 것 (6건)

지금 **로그로만 남고 플레이어에게 안 간다.** `autoLog`는 개발용이다.

| 기능 | 지금 | 넣을 소식 |
|---|---|---|
| 등록말소 기간 | 로그만 | "OOO 2군 등록말소 (2주간 재등록 불가)" |
| 웨이버 공시 | 이벤트만 | "OOO 웨이버 공시 → △△ 구단이 영입" |
| FA 미계약 → 원소속 재계약 | Rust 이벤트 | "OOO 원소속 재계약 · 시장에서 못 구했다" |
| 전역·복귀 | Rust 이벤트 | "OOO 전역 · △△ 복귀" |
| 캠퍼스 이벤트 | 판정만 | 그 해 무슨 일이 있었는지 |
| 프로 올스타 초청 | 판정만 | "대학 OOO, 프로 올스타전 초청" |

**⚠ 소식 id는 유일해야 한다.** 중복이 하나만 생겨도 세이브가 안 열린다.
**이미 관행이 서 있다** — 기존 소식이 `${seasonYear}` 또는 `${Date.now()}`
를 붙인다(실측). 새로 만들 때 그걸 따르면 된다. `weekNum` 만 쓰면 안 되는
이유는 시즌마다 리셋되기 때문이다.

**⚠ 소식함 상한은 500이다** — `MAX_MAILBOX`. 약 83주라 **한 시즌 반**이다.
🔴 처음엔 이 계획서에 "200통 포화"라고 적었는데 **틀렸다.** CLAUDE.md 의
낡은 기록을 그대로 옮겼고, 실제로는 2026-08-24 에 200 → 500 으로 올렸다
(트랙 B · IPC 주당 +38 KB · 화면은 60건씩 점진 렌더링).

그래도 **한 시즌에 몇 통이 오는지는 잰다** — 500 이 여유인지 아닌지는
생산량을 봐야 안다. 늘려서 기존 소식을 밀어내면 안 하느니만 못하다.

**재는 것** 시즌당 소식 수(전후) · 종류별 분포
**검사** 각 소식이 실제로 mailbox에 들어가는가 · id 중복 0

---

## B단계 — 구단 정보에 얹는 것 (4건)

`TeamDetailModal`에 붙인다. 1단계에서 연표를 넣은 자리다.

| 기능 | 보일 것 |
|---|---|
| 의료팀 | `medicalQuality` 등급 + "부상 회복 ○○% 빠름" |
| 전지훈련 | 전훈비 규모 + 시즌 초 컨디션 효과 |
| 외국인 한도 | 보유 3명 중 몇 명 · 누구 |
| 영구결번 | 그 구단이 비운 번호와 주인 |

**⚠ 값을 화면에서 다시 계산하지 않는다.** 의료 배수는
`medicalRules.recoverySpan`이 정본이고 `injuries.ts`가 쓰는 식과 **같은
식**을 써야 한다 — 두 벌이 되면 화면과 실제가 갈린다.

**검사** 규칙 파일 값을 바꾸면 화면 표시도 바뀌는가(변이 검증)

---

## C단계 — 명예의 전당 화면 (2건)

**유일하게 새로 만드는 화면이다.** 목록·검색이 필요해 패널로 안 된다.

    헌액자 목록      이름 · 헌액 연도 · 점수 · 소속 구단
    선수 상세        수상 이력 · 통산 기록 · 영구결번 여부
    구단별 보기      그 구단 출신 헌액자

재료는 다 있다 — `hallOfFame`(선수 id → 연도·점수·구단·번호) ·
`retiredNumbers`(팀 → 번호) · `careerHistory`(수상 이력).

**⚠ 은퇴 선수는 `npcs`에서 사라지지 않는다**(`careerStatus: "retired"`).
이름·기록을 그때 조회할 수 있다. 다만 **세이브가 커지면 느려질 수 있으니**
목록을 한 번만 만들고 캐시한다.

**어디에 둘까** — `MePage` 하위 탭이 자연스럽다. 업적이 이미 거기 있고,
명예의 전당도 "커리어의 끝"이라는 같은 성격이다.

**검사** 헌액자 0명일 때 안 깨지는가 · 결번 번호가 실제 선수와 맞는가

---

## D단계 — 스카우팅 잡음 배선

🔴 **`apply_scouting_noise_native`가 Rust·preload·타입까지 다 있는데
TS가 한 번도 안 부른다.** 완성된 죽은 갈래다.

그 엔진이 이미 하는 것:

    base_noise      스카우팅 품질이 낮을수록 크다
    fame_factor     명성이 높으면 정확해진다 (−40%)
    prospect_factor 유망주는 **1.5배** 부정확
    age_factor      28세 이상 0.80 · 22세 이하 1.20
    seed_base       선수 id + 팀 + 연도 해시 — **결정적이다**

### 지금 구조가 막는 것

`runDraftSimulation`이 `candidates`(진짜 값) 하나를 받고 **전 팀이 그
목록을 공유한다.** 팀마다 다른 눈으로 볼 자리가 없다.

### 어떻게 바꾸나

1. 드래프트 순번마다 **그 팀의 스카우팅 뷰**로 후보를 평가한다
2. 뷰는 `applyScoutingNoise(팀 성향, 후보)` 한 번으로 만든다 —
   결정적 해시라 같은 팀·같은 해면 늘 같다
3. **지명은 뷰로, 실제 능력은 진짜 값으로** 들어간다 — 그게 이 기능의 요점이다

**⚠ 성능**: 팀 10 × 후보 200 = 2,000건을 드래프트마다 만든다.
한 번에 한 팀씩 만들고 버린다 — 전부 들고 있으면 메모리가 는다.

**⚠ 밸런스 파급**: 지명 순서가 바뀐다. 상위 지명자의 평균 OVR이 내려가고
하위에서 대박이 나온다. **전후를 반드시 잰다** — 그게 이 기능의 목적이다.

### ✅ 실측 결과 (씨앗 111 · 3시즌)

    span  0   R1 80.3 → R5 76.3      단조 감소 (스카우팅 없음)
    span  6   R1 80.3 → R5 76.9      순서 유지 · 팀별폭 73.9 → **71.6**
    span 14   R1 79.7 < **R2 80.1**  역전 — 라운드 순서가 의미를 잃는다

**6으로 정했다** — 순서를 흔들되 뒤엎지 않는 대역이다.

⚠ 프로브가 OVR 을 0으로 읽고 있었다(`n.ovr` 은 없다 · 정본은 `liveOvrOf`).
  **전후 비교가 통째로 무의미할 뻔했다.**

**재는 것** 라운드별 지명자 OVR 분포(전후) · 팀별 스카우팅 품질과 지명 성공률
**검사** 같은 씨앗·같은 팀이면 같은 뷰가 나오는가(결정성) ·
스카우팅 95인 팀이 5인 팀보다 잘 뽑는가

---

## E단계 — 통합 점검

- `audit-career.cjs`를 다시 돌려 **화면 없음 14 → 몇으로 줄었는지** 본다
- `probe-deadends.cjs`로 죽은 갈래가 늘지 않았는지 본다
- 소식 수가 포화 한계에 안 닿는지 재확인

---

## 매 단계 규칙

1. **재현부터** — 3회씩
2. **고치고 검사 붙이고 변이 검증**
3. **전후 실측 + 회귀**
4. **커밋**

**기준선 (`708bd051a`)**
```
vitest  142파일 1,254건
cargo   247건
tsc     0
svelte-check  34 errors / 37 warnings
```

⚠ **검사가 틀리면 없는 결함을 만든다.** 이 세션에서 네 번 그랬다 —
찾는 문자열이 실제 이름과 달랐다(`matchStart`·`signingBonus` 등).
**검사를 짤 때 표본 하나를 손으로 확인한다.**

⚠ **계획서의 "이미 있다"를 믿지 않는다.** 이 세션에서 다섯 번 틀렸다 —
등번호·연표 재료·고교 로스터·34명 출전·부상 범위. **먼저 잰다.**

---

## 다음 세션에 D단계를 온전히 (2026-08-30 정리)

**D단계만 남았다.** 컨텍스트를 다 쓰기 전에 멈췄다 — 드래프트 구조를
바꾸고 **전후를 재야** 하는 작업이라 중간에 끊기면 안 된다.

### 그대로 유효한 것

위 D단계 절의 설계·성능 주의·밸런스 경고가 전부 그대로다. 요약하면:

1. 드래프트 순번마다 **그 팀의 스카우팅 뷰**로 후보를 평가
2. 뷰는 `applyScoutingNoise` 한 번 — 결정적 해시라 같은 팀·같은 해면 같다
3. **지명은 뷰로, 실제 능력은 진짜 값으로**

### 이번 세션에서 새로 알게 된 것

- **점검기가 소식을 화면으로 안 셌다.** `news()` 를 더해 고쳤다 —
  D단계 뒤 다시 돌릴 때 그 기준으로 센다.
- **`clubEffects.ts`** 가 생겼다. 화면과 로직이 같은 식을 쓰게 하는 자리다 —
  스카우팅도 화면에 보일 값이 생기면 거기 넣는다.

### 기준선 (`20ff42b4e`)

```
vitest  147파일 1,284건
cargo   247건
tsc     0
svelte-check  34 errors / 37 warnings
소식함  MAX_MAILBOX = 1500 (3시즌 1,021통)
```

### ⚠ 이 세션에서 계측·검사가 틀린 게 **아홉 번**이었다

매번 실측으로 잡았고 커밋에 남겼다. 되풀이되는 형태는 셋이다:

1. **찾는 문자열이 실제 이름과 다르다** — `matchStart`·`signingBonus`·
   `foreignSlots.ts`. 검사를 짤 때 **표본 하나를 손으로 확인한다.**
2. **같은 문자열이 두 곳에 있어 변이가 안 잡힌다** — 그 블록에만 있는
   식별자를 함께 본다.
3. **셸이 이스케이프를 먹는다** — `
` 이 개행이 되고 치환이 조용히 실패한다.
   **파일은 Write 도구로 쓰고, 변이는 치환 여부를 확인한다.**

그리고 **커밋 전에 전체 회귀를 본다** — 이번에 부분 검사만 보고 커밋했다가
기존 검사를 깨뜨린 채 올린 적이 있다(`5ae3cb1f9` 로 고쳤다).