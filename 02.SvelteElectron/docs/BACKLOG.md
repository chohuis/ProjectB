# 남은 작업 전량 (2026-07-30 실사)

> [RESUME.md](RESUME.md)는 **"지금 당장 뭘 하나"**, 이 문서는 **"끝까지 뭐가 남았나"**다.
> 각 항목은 실사로 확인한 것만 적는다 — 로드맵 체크박스를 그대로 옮기지 않았다
> (DESIGN.md §10 R6a는 실제로 다 됐는데 체크가 안 돼 있었다. 이번에 맞췄다).

---

## 0. 즉시 다음 — 6C 관계도 (Phase 6 마지막)

설계 정본 [design/people.md](design/people.md) §4. **결정 4건은 2026-07-30에 확정됐다**:
팀 이동 = 감쇠 후 보존(×0.6) · 변동폭 = 한 시즌 ±25(라벨 1~2단계) ·
초기값 = 중립 0 + 성향 ±10 · 구 감정 9축은 관계도로 흡수·대체.

진행: **6C-1(데이터 계층)·6C-2(Rust 엔진)·6C-3(주간·시즌 배선)·6C-4(감정 폐기) 완료.**
남은 것은 6C-5(효과 배선)·6C-6(검증)이다.

구현 범위 — 스태프 전원 + 팀동료, **주인공 기준 1:N만**(NPC끼리 안 만듦),
값 −100~100 · 7단계 라벨(숫자 비노출).

| 대상 | 무엇에 영향 |
|---|---|
| 감독 | 역할 배정 · 콜업 우선순위 |
| 코치 | 담당 영역 훈련 효율 |
| 구단주 | 재계약·방출 인내심 |
| 팀동료 | 이벤트 분기 · 사기 · 뉴스 실측 근거 |

끝나면 **`feat/people` → `main` 병합**.

---

## 1. Phase 7 — 커리어·시장·신규 (R6c)

가장 덩치가 크다. 6개 묶음이고 서로 의존한다. **권장 순서는 아래 번호순** —
드래프트가 2군 배치를 부르고, 2군 배치가 승강을 부르고, 병역이 상무 로스터를 부른다.

### 7-1. 11월 통합 드래프트
- 대학 1~4학년·독립 **매년 신청** (현재는 졸업반만)
- 지명 순서·라운드·미지명자 처리
- 관련 기존 코드: `weekPhases/market.ts`(1,038줄), `draftObserve`, W47 배경 드래프트

### 7-2. 프로 진입 · 승강
- 신인은 **2군 배치**가 기본 (특급 신인 1군 직행 조건은 §11.1 미확정 — 스탯 스케일 확정 후)
- 승강 **2경로**: 상시 콜업 + 월간 정기
- 2군 스케줄·PO는 Phase 5에서 이미 복원됨 → 여기서 선수 흐름만 연결

### 7-3. 국가대표 · 병역
- 국제대회 3종 · 합성 대표팀 · 시즌 중 임시 이탈
- 병역 면제 판정
- **상무 로스터 40~50% 교체** (Phase 6에서 이월 — 복무 완료자 복귀가 병역 트랙 전체와 묶여 있어 떼어낼 수 없었다)
- 발탁 커트라인·대회 성과 확률은 §11.1 미확정

### 7-4. 방출 · FA
- 방출 **2단계 판정**
- FA **등급제** — 보상선수가 실제로 이동해야 한다
- 협상 카운터오퍼 라운드 상한 §11.1 미확정 (OnePitch는 1회로 단순화했다)

### 7-5. 개인 재정 · 스태프 배선 · 부상 전조
- 개인 재정 **전체** — 세율 구간·트레이닝 보너스·팀자원 반비례 계수·투자 수익률 전부 §11.1 미확정
- **스태프 15종 배선** — 6A에서 생성만 했다. 실제 효과 계수 15개가 미정 (§7.3)
- 부상 전조 경고

### 7-6. 문장 뱅크 · 뉴스 · 관계 연출
- 문장 뱅크 엔진 · 뉴스 4종
- **6C 관계도가 선결**이다 — "동료 상태" 뉴스가 관계 데이터를 실측 근거로 쓴다
- **팀동료 이벤트 분기** (6C 이월) — `weekPhases/events.ts`가 3줄 빈 껍데기라 붙일 데가 없다(B8)

#### 6C에서 지운 감정 메시지 12건 — 여기서 다시 짠다

Phase 6C가 `emotionEngine`(360줄)·`emotionMessageEngine`(478줄)을 폐기했다.
그 838줄은 **호출되는 곳이 없는 죽은 코드**였지만(`advanceWeek`가 유일한 호출자였고
6C-3에서 끊었다) 문구와 선택지는 쓸 만했다. 커밋 이력에서 찾을 수 있게 목록만 남긴다:
`git show 6c-4^:apps/ui/src/shared/utils/emotionMessageEngine.ts`

관계 라벨 진입을 트리거로 재작성하면 되는 7건:

| 대상 | 문구 | 트리거로 쓸 라벨 | 선택지 |
|---|---|---|---|
| 감독 | "네 투구를 눈여겨봐왔다" | 신뢰 진입 | 없음 |
| 감독 | "에이스로 기용한다" | 각별 진입 | 없음 |
| 감독 | "경고 — 태도가 마음에 안 든다" | 서먹 진입 | 수용 / 반발 |
| 감독 | "선발 박탈 통보" | 불신 진입 | 없음 |
| 코치 | "개인 훈련 제안 — 방과 후 30분" | 우호 진입 | 수락(피로+10·XP+3) / 거절 |
| 코치 | "비기 전수" | 각별 진입 + `shared_ordeal` 기억 | 수락(구속 +1) / 거절 |
| 동료 | "밥 한번 같이 먹자" | 신뢰 진입 | 함께 간다(사기+15) / 거절 |

⚠ **선행 작업**: `DecisionEffect`에 관계값 필드가 없다
(condition/fatigue/morale/money/xp/statDelta/fame/popularity/diligence/addTag뿐).
구 코드의 `effectHint`는 "resentment −10, trust +5"라고 적혀 있었지만 **실제로는
사기·피로만 움직였다** — 표시와 동작이 달랐다. 재작성 시 `relationDelta?: { personId, delta }`를
같이 만들어야 문구와 동작이 일치한다.

버린 5건 (사용자 확정 "버린다"): 코치 단기축 2건(태도 경고=pressure, 성장 감탄=excitement)과
라이벌 인지도 뉴스 3건(recognition 30/60/80). 관계도에는 단기 축과 인지도 축이 없고,
대신 만들면 "판정 근거가 다시 여러 개"가 되어 6C가 없앤 문제로 돌아간다.

### 7-7. Phase 6에서 이월된 대학 이벤트
- **쇼케이스** (감독 4명 지명) · **올스타전** (포지션별 인기도 랭킹)
- 고교 대응 이벤트(스카우트 데이 등)는 **기획 자체가 없다** — §11.1 신규 기획 필요

---

## 2. Phase 8 — 성능 (R6d)

**국내 약 2,930경기/시즌**(v1 대비 3.8배)이 설계 제약이다. 주당 약 56경기.

- [ ] **성능 목표 수치 합의** — 이게 먼저다. 추측으로 최적화하지 않는다
- [ ] 프로파일 → 진범 특정 → **그것만** 수정
- ~~`season` 블롭 → 테이블 분리~~ ✅ **이미 끝났다** — 마이그레이션 v2가
      `schedule`/`standings`/`season_stats`/`player_condition`/`team_rotation`으로 쪼개고
      구 블롭을 실제로 옮긴 뒤 `DROP TABLE season`까지 한다 (`slotdb.cjs` MIGRATIONS v2).
      **2026-07-30 첫 작성 때 이 항목을 남은 일로 적었는데 오독이었다.** R6a는 8/8 완료다
- 참고: OnePitch는 같은 규모를 Rust 단독 1시즌 24.75초로 처리했고 **최대 개선 요인은 인덱스 부재 해소**였다
  (73s→24.75s, 워스트 46s→5.1s). 현행은 주간 처리 상당부분이 TS(`advanceWeek.ts`)에 있다

⚠ **현재 성능은 측정된 적이 없다.** Phase 8은 이 숫자를 만드는 것부터 시작한다.

---

## 3. Phase 9 — 하네스 확장 · 버그 소탕 (R6e)

### 9-1. 하네스 확장
현재 `npm run harness`는 **INV1~INV8**을 본다 (나이·인원·ID정합·세이브왕복·은퇴단조·학년·스키마·고교로스터).
남은 것 — AUDIT §4가 "하네스로만 잡을 수 있다"고 분류한 것들:

- [ ] 은퇴/신인 충원 **균형** — 로스터 인원이 여러 시즌 유지되는가
- [ ] 계약 만료·FA·트레이드 **3자 일치** (npc 테이블 / transactions / 화면)
- [ ] 성적 분포 안정성 — 10시즌 누적 ERA/OPS 드리프트
- [ ] 드래프트 풀 규모 — 기존 회귀 테스트가 **시즌1만** 검증한다
- [ ] 합성 궤적(R3b)이 실제 다시즌에서 목표 분포를 유지하는가
- [ ] 부상 누적 → 은퇴 곡선 (**OnePitch는 여기서 "20/20 전원 조기 은퇴"를 발견했다**)
- [ ] `PROMPT_HARNESS.md` / `PROMPT_LOOP.md` 작성

### 9-2. 버그 B1~B11 ([AUDIT_2026-07.md](AUDIT_2026-07.md) §4)

실사한 현재 상태:

| # | 증상 | 지금 |
|---|---|---|
| B1 | 졸업 NPC의 나이·리그 변경이 slot.db에 반영 안 됨 | **미확정** — 하네스 INV1·INV4가 부분 커버. 졸업 경로 전용 검증 필요 |
| B2 | 주간 변이가 `save()` 전까지 메모리에만 | **미해결** |
| B3 | 마이그레이션 러너 부재 | ✅ **해결** — `SCHEMA_VERSION=3`, `test:migration` |
| B4 | 세이브 무결성(HMAC) 미구현, `hmac.rs` 죽은 코드 | **미해결** (DESIGN §8.4) |
| B5 | `master:loadEntities`가 `slotId`를 조용히 버림 | 미해결 (현재 무해) |
| B6 | 레거시 `"pro"` careerStage 잔존 | **미해결** — `save.ts:215`에 그대로 |
| B7 | `proTeamProfiles` 비저장 → 로드마다 초기화 | **미해결** |
| B8 | `weekPhases/events.ts`(3줄)·`academics.ts`(16)·`games.ts`(21) 빈 껍데기 | **미해결** — 줄 수 그대로 |
| B9 | 고아 스크립트 23개 | ✅ **해결** — 미등록 1개(`build_refs_from_seeds.py`, 의도적) |
| B10 | `seeds/v1/` 9파일 · `runtime/` 고아 | ✅ **해결** — 둘 다 없음 |
| B11 | vitest 미설치로 단위 테스트 실행 불가 | **미해결** — `npm test` 불가, tsc 15건 중 2건이 이것 |

---

## 4. 상시 부채 (Phase에 안 묶인 것)

| 항목 | 크기 | 메모 |
|---|---|---|
| `processAllLeaguesSeasonEnd` **566줄** 분리 | 중 | 병역·FA·드래프트가 얽혀 별도 작업 (_ledger P6-7) |
| `stores/game.ts` **2,579줄** | 대 | CLAUDE.md가 금지한 패턴의 원흉. usecases로 계속 밀어낼 것 |
| `advanceWeek.ts` **1,884줄** | 대 | Phase 8 성능의 주요 후보이기도 하다 |
| PendingAction **17종 → 8종** 통합 | 중 | R5 잔여. push/route/consume이 MainPage·runAutoAdvance·개별 모달에 흩어져 있다 |
| **히스토리/인생 기록 화면** | 중 | 원 기획 **엔딩 본체**. `careerRecords` 데이터는 이미 충분, 레이아웃·톤을 사용자와 먼저 정해야 함 |
| ABL/JBL **Lazy 게이트** | 소 | 반경 게이트가 주간 낭비는 이미 해소. 남은 건 새 게임 시 52주 스케줄 선생성 1회 비용뿐 |
| `salaryNegotiation` 타입 결함 | 소 | `CareerResultModal.svelte:79` — 필수 필드 3개 누락 |
| `EntityDetails` 미import | 소 | `TeamDetailModal.svelte` — svelte-check 7건. **화면은 동작한다** |
| 구 세이브에 스태프 없음 | — | 마이그레이션 v3가 테이블만 만든다. 사용자 확정("개발 중이라 폐기 OK") |

---

## 5. 값이 안 정해진 것 (DESIGN §11 · §11.1)

전부 **"스탯 스케일 확정 후"** 성격이라 밸런스 하네스에서 결정한다.

- 특급 신인 1군 직행 조건 · 협상 카운터오퍼 라운드 상한
- 개인 재정 전 수치 (세율 구간·보너스 폭·반비례 계수·수익률)
- 국가대표 발탁 커트라인 · 대회 성과 확률
- **스태프 15종 각각의 계수**
- 고교 유사 이벤트 구체 기획 (기획서에 없던 신규)
- 성능 목표 수치
- npc 테이블 능력치 JSON의 Rust serde 규약 (camelCase 유지 여부)
- 드리프트 리그의 "리그 리더" 생성 규칙 — **해외 전용으로 범위 축소됨**
- ~~감정 시스템 dormant 재정의~~ **소멸** — 6C가 감정 시스템을 폐기했다.
  헤어진 관계는 `contact = "apart"`로 시즌마다 감쇠하고 재회하면 그 값에서 재개된다

---

## 6. 규모 감각

| Phase | 남은 덩어리 | 상대 크기 |
|---|---|---|
| 6C | 관계도 — 6C-1~4 완료, **6C-5(효과 배선)·6C-6(검증) 남음** | 소 |
| **7** | 7묶음 (드래프트·승강·병역·FA·재정·뉴스·대학이벤트) | **대 — 여기가 본체** |
| 8 | 목표 합의 → 실측 → 진범만 수정 (블롭 분리는 이미 끝났다) | 중 |
| 9 | 하네스 6항목 + 버그 7건 | 중 |

Phase 7이 남은 일의 절반 이상이다. **하위 단계로 쪼개서 커밋 단위를 유지할 것** —
Phase 5·6에서 효과가 있었던 방식이다.
