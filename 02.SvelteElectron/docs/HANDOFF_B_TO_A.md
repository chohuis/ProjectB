# B → A 인계 5차 (2026-09-02) — 사기 손질 · 병역 · 대학 도달률

> **B-1 · B-2 · B-3 · B-4 · B-11 · B-12 · B-13 끝났다.** 병합해 달라.
>
> | 일감 | 커밋 | 어디 |
> |---|---|---|
> | B-1·B-2 사기 | `d43a50b9d` · 계기 `c11e5341b` · 정정 `ff3f05ac4` `bc2fef6ae` · 결과 `d224e22e5` | §A §B §D §E §F |
> | B-11 병역 60종 | `4649558e4` | §G |
> | B-12 통신 후반 | `e04795df6` | §G |
> | B-13 test:events 잣대 | `da622ba58` | 아래 §I |
> | B-4 소식↔선택지 7건 | `c9a7a51f0` | §I |
> | B-3 대학 도달률 | `7ea1b1a04` | §H |

## §A 🔴 `HANDOFF_A_TO_B §3.6` 의 진단이 두 군데 틀렸다

### A-1. `EVT_COND_PEAK_FORM`(×41) 은 사기를 **한 톨도 안 올린다**

`DEC_COND_PEAK_FORM` 의 두 갈래는 이것뿐이다:

```
intensive_train   fatigue:+10  xp.velocity:+3  xp.command:+3
steady            condition:+5 xp.command:+1
```

§3.6 표의 `+14 ×41` 은 **같은 주에 같이 뜬 다른 이벤트의 델타**다. A 도
"같은 주 여러 개면 나눠 못 가른다"고 표 머리에 적어 뒀는데, 1순위로 올라온
칸이 하필 그 오염된 쪽이었다. **여기에 쿨다운을 걸면 사기는 안 움직인다.**

### A-2. "`moraleDelta` 57건 · 양수 44 · 음수 13 · 합 +150" 은 **군 풀만** 센 값이다

`resource/data/master/events/**` 를 grep 하면 57건이 나오는데 그 57건이
전부 `events/pools/military*.json` 이다 — A 가 §3.6 끝에서 "군 사기는 여기서
판정하지 마라"라고 적은 바로 그 통이다. 이벤트 정의 파일 589개에는
`effects` 키 자체가 없다.

사기가 실제로 걸린 곳은 `messages/decision_templates.json` 이다:

```
결정템플릿 391 · 사기가 걸린 템플릿 226
선택지  양수 235건 합 +1571 · 음수 70건 합 −271
  그중 반복(repeatable) 이벤트가 쓰는 양수  99건 합 +603   ← 손댈 것
값 분포  15×1 12×11 10×26 9×1 8×57 7×1 6×50 5×56 4×19 3×10 2×3
```

## §B 🔴 쿨다운은 랜덤 풀에서 **한 번도 안 걸린다** — 손잡이가 아니다

풀은 이미 `maxPicksPerWeek: 1` 이고 발동률이 9~26% 다. 거기에 가중치를
곱하면 개별 이벤트는 쿨다운보다 훨씬 드물게 뜬다:

```
EVT_RAND_TEAM_MEAL       풀 26% × 가중 20% = 주당 5.2%  → 19주에 한 번   cd=2  안 걸림
EVT_RAND_LOCAL_INTERVIEW 풀 18% × 가중 29% = 주당 5.2%  → 19주에 한 번   cd=2  안 걸림
                                      (실측 ×10 / 416주 = 2.4% — 대학이 8시즌 중
                                       일부라 산식보다 낮게 나온다. 어느 쪽이든 cd 2 밖이다)
```

대학에서 닿는 랜덤 풀의 주당 사기 기여 (첫 선택지 기준 · 조건 통과분만):

```
POOL_TEAM_LIFE  26% × 3.29  +0.86     RAND_TEAM_MEAL +12 이 지분 20%
POOL_MEDIA      18% × 3.85  +0.69     LOCAL_INTERVIEW +8 두 종이 지분 48%
POOL_BODY        9% × 6.21  +0.56     UNIV_RECOVERY_DAY +12 이 지분 52%
POOL_SOCIAL     22% × 1.88  +0.41
POOL_TRAIN      13% × 1.36  +0.18                          합 +2.70/주
                                      (고교는 같은 식으로 +3.28/주)
```

⚠ **처음엔 +1.94 로 적었는데 틀렸다.** 조건을 셀 때 `career_stage` 의
`stages` 배열과 `league_id` 의 `leagueIds` 배열을 안 봐서 다른 무대 이벤트가
후보에 섞였다. 누출을 걷어내니 대학 전용 이벤트의 지분이 커져 **값이 오히려
올라갔다.** 조건을 기계로 셀 땐 `conditionEvaluator.ts:26-41` 과 같은 판정을 쓴다.

주간 회귀는 사기 95 에서 **−1.75** 다(`moraleAfterWeek` · pivot 60 · pull 0.05).
**랜덤 풀만으로 회귀를 이긴다.** 그래서 사기가 100 에 붙고, 클램프에 먹혀
델타 표에는 +0.30 으로만 보인다 — 표의 작은 숫자가 압력의 크기가 아니다.

→ 손잡이는 쿨다운(§3.6 방향 1)이 아니라 **양수 폭**(방향 2)이다. 둘 다 같은
사용자 확정 안이라 방향 2 로 간다.

## §D 전후 실측 — 목표에 닿았다 (`probe:morale --path univ` · 4시즌)

⚠ **8시즌이 아니라 4시즌이다.** 한 시즌에 10분 걸린다 — `probe:morale` 은
`runOneWeek()` 로 매주를 다 돈다(`probe:paths` 의 `autoRun` 건너뛰기와 다르다).
8시즌 판은 2시간 15분 동안 표준출력이 0바이트라 버렸고, 그 뒤에 시즌마다
한 줄 찍는 진행 표시를 넣었다.

⚠ **정책**: 헤드리스 선택은 `pickChoice` — 피로 70↑ 휴식 계열 · 30↓ 능동 계열 ·
그 사이면 `choices[0]`. 전후가 같은 정책이라 상쇄된다.

### 같은 씨앗 전·후 (20260731)

```
                 손질 전        손질 후
[university]
  이벤트주 평균   +0.53    →   +0.17     ← 목표 "0 근처"
  사기 평균       85.0     →    72.6
  사기 최소       66.7     →    45.0
  ≤60 주수         0주     →    20주 / 72주
  ≤50 주수         0주     →     5주
[highschool]
  이벤트주 평균   +0.73    →   +0.19
  사기 평균       91.7     →    93.0     ← 안 움직였다
  ≤60 주수         0주     →     0주
```

### 손질 후 · 씨앗 셋 (대학 72주 · 고교 136주)

```
씨앗        대학 평균  최소   ≤60  ≤50  ≤40   이벤트주    고교 평균  최소   ≤60
20260731     72.6   45.0   20주  5주  0주   +0.17      93.0   67.9   0주
20260802     82.5   57.5    6주  0주  0주   −0.07      89.2   67.5   0주
20260803     72.7   38.6   17주  6주  1주   +0.12      90.3   62.4   0주
```

⚠ 전측정은 **20260731 한 씨앗뿐**이다. 나머지 둘은 후측정만 있다 — 다만
손질 전 "대학 ≤60 이 0주" 는 사기가 89~98 에 붙어 있었다는 A 의 8시즌
실측과 같은 자리라, 문이 열린 것 자체는 셋이 아니다.

### 대학 `morale_lte` 여덟이 이제 닿는다

```
≤60 UNIV_MAJOR_DOUBT      ≤58 UNIV_FRIEND_QUIT       ≤55 UNIV_HOMESICK_UNIV
≤55 UNIV_Y1_DORM_HOMESICK ≤50 UNIV_BENCH_UNIV        ≤50 UNIV_DRAFT_STOCK_DOWN
≤48 UNIV_SLUMP_UNIV       ≤40 UNIV_LIFE_SLUMP_CHECK
```

씨앗 20260803 은 여덟 다 · 20260731 은 일곱 · 20260802 는 둘만 닿는다.
**플레이마다 갈리는 게 정상**이다 — 모든 판이 슬럼프를 겪을 이유는 없다.

### 문이 열렸다는 둘째 증거

이벤트 발생량이 주당 **1.95 → 2.10** 으로 늘었다. `morale_lte` 조건부가
이제 실제로 열려서다 — `EVT_UNIV_TOURNAMENT_OUT` ×6 · `EVT_UNIV_LIFE_MINOR_PAIN_01` ×7
처럼 전엔 표에 없던 것들이 올라왔다.

### 실측이 또 한 번 문서를 고쳐준 것

**이벤트가 주당 1.95건이다.** 랜덤 풀은 다 합쳐도 0.88건이 상한이니
(`maxPicksPerWeek: 1` × 발동률 9~26%) **나머지 1.07건은 조건부**다.
사기를 먹이는 몸통은 랜덤이 아니라 조건부다.

그리고 **`EVT_RAND_TEAM_MEAL` 은 자기 조건이 `morale_lte 72`** 다. 손질 전
대학 사기 평균이 85 였으니 그 +12 는 **대부분의 주에 자기 조건에 막혀
있었다** — §B 의 정적 추정 +2.70/주 는 그만큼 과대다. **실측 쪽이 맞다.**

## §E 판단 하나 남긴다 — 고교는 안 움직였다

같은 손질을 받았는데 고교 사기는 세 씨앗 전부 평균 89~93 · 최소 62~68 ·
**≤60 이 0주**다. 이유 둘:

```
고교 풀의 사기 기여가 대학보다 크다      (정적 +3.28 대 +2.70)
등판주가 136주 중 12~22주뿐이라 승패가 못 끌어내린다
```

그래서 고교 `morale_lte` 다섯(≤58 CLASSMATE_01 · ≤50 Y1_AFTER_ERROR ·
≤50 Y2_TEAM_SLUMP · ≤45 Y2_SLUMP · ≤34 LIFE_SLUMP_CHECK)은 여전히 도달 불가다.

한 단 더 조이면 움직인다. 다만 **목표는 대학이었고**(§3.6) 고교까지 내리는
건 게임이 달라지는 폭이라 **밸런스는 건마다 묻는다**는 규칙을 따랐다.

### 🛑 사용자 결정 — **1.1 로 둔다** (2026-09-02 21:50 · A 가 전달)

"등판 비중과 같이 본다." 1.0 에서는 **고교를 더 안 조인다.** 고교
`morale_lte` 다섯이 1.0 에서 안 뜨는 건 아는 채로 낸다.

⚠ 이 결정은 A 세션을 거쳐 전달받았다 — 직접 들은 게 아니다.

## §F 손질 내용 (`d43a50b9d`)

**반복(repeatable) 이벤트가 쓰는 결정템플릿만** 줄였다 · 양수 99건 합 +603 → +165

```
15→5  12→4  10→3  9→3  8→2  7→2  6→2  5→1  4→1  3→1  2→1
```

- 1회성(once_*) 66건은 **그대로 둔다** — 매주 먹이는 게 반복 쪽이고,
  인생에 한 번 오는 순간까지 깎을 이유가 없다.
- `DEC_COND_SLUMP` 는 제외 — 사기가 내려간 뒤 되돌릴 유일한 손잡이다.
- 음수 70건 불변(§3.6 지시).
- `effectHint` 숫자도 99건 전부 짝을 맞췄다 — 표시와 동작이 갈리는 게
  이 트랙의 원점이라. `check:events` 5종 통과.

---

## §G B-11 병역 — 이벤트 60종 · 기존 34종 쿨다운

A 의 캘린더 20종 위에 **조건부 16 + 일상 24** 를 얹었다. `check:militarydata` OK
(부대원 15 · 이벤트 60 · 캘린더 20주 · 실패 0 · 경고는 빈 이름뿐).

```
기존 34종     cooldownWeeks 4 를 28건에 (once:true 6건은 뺐다)
military_general  statDelta 2건 제거 — 복무 중 능력치 안 건드린다(사용자 확정)
                  훈련소 `command −1` 은 감각 −2 로 옮겼다. A 의
                  `MIL_CAL_ENTRY` 가 이미 감각 −2 로 같은 말을 한다
                  행군 `stamina +1` 은 뺐다 — 대신 줄 게 없다. 값을 지어내지 않았다
military_sports   그대로 (상무는 능력치가 움직이는 게 맞다)
```

### ⓘ 월간 부대 소식(§31)은 안 만들었다 — A 가 이미 코드에 넣었다

`militaryLife.ts:196~214` 가 4주마다 `msg-mil-digest-{year}-w{week}` 를 만들고
복무 주차·감각·피로·사기·함께한 사람·관계 상위 3·휴가/표창/징계·다음 사건까지
본문에 넣는다. **데이터 틀을 따로 두면 아무도 안 읽는 갈래가 하나 생긴다.**

### 창 감사 — 죽은 이벤트는 없다 (자원·관계 조건을 뺀 순수 창)

```
  9주   40~48  MIL_COND_SNOW              (설계대로 겨울만)
 29주    1~29  CONFLICT_SENIOR · DAY_NIGHT_DUTY · DAY_GUN_MAINT   ← MEM_GUNNER 가 W30 전출
 33주    1~33  MIL_COND_MENTOR            (MEM_SIG_SR × rank ≤1)
 59주    1~59  MIL_DAY_ERRAND             (maxRank 2)
 69주    1~69  MIL_DAY_WIRE               ← MEM_SIG_SR 가 W70 전출
 79주    1~79  MIL_DAY_KITCHEN            ← MEM_COOK 가 W80 전출
그 밖 33종은 90~100주
```

### ✅ B-12 로 닫았다 — 통신병 후반이 얇던 것 (`e04795df6`)

위 창 감사에서 나온 건이다. §36-3 의 보직 전용 일상이 ㉠ 통신 셋 · ㉡ 박격포
넷인데 통신 쪽 둘이 W59(`ERRAND` maxRank 2)·W70(`WIRE` — MEM_SIG_SR 전출)에
닫혀 **W70 뒤 통신 전용이 `MIL_DAY_OPS_NIGHT` 하나뿐**이었다. A 가 B-12 로
받아 셋을 고쳤다:

```
MIL_DAY_JR_SIGNAL_TRAIN  후임 통신병 교육 — MEM_JR_2(W62 전입) · cd 8 · 병장
MIL_DAY_OPS_HANDOVER     상황실 인수인계 — MEM_1SG · cd 6 · 병장
MIL_DAY_ERRAND           maxRank 2 → 3

W70+ 전용 일상   통신 1 → 4 · 박격포 3   (실측 · 이벤트 62종)
```

진지 공사 봄·가을의 `fatigueDelta` 도 뺐다 — A 가 캘린더를 보직별 두 줄로
갈라 거기서 피로를 주므로 이벤트에 남겨 두면 **이중 적용**이다.

⚠ 캘린더 20종의 문안은 **건드리지 않았다** — A 의 것이다(피로 두 건만 위 참고).
✅ §36-1 의 역할별 갈린 효과(W12 피로 +12(㉡)/+6(㉠))는 `choices` 에 조건을 걸
  수단이 §28 에 없어 데이터로는 표현이 안 된다 → **A 가 캘린더를 보직별 두
  줄로 갈라 풀었다.** 이벤트 쪽 피로는 B-12 에서 뺐다.

---

## §H B-3 대학 도달률 재측정 — 88% · 못 닿는 것의 정체

`measure:slotreach --path univ --seasons 8 --full` · 학습 정책 기본(normal)

```
씨앗        주    전체공용    고교         대학        KBL1군      못닿음 구성
20260731   396   15/17 88%  124/155 80%  99/113 88%  68/189 36%  GPA10 + 팀1위 + 부상불안
20260802   396   14/17 82%  126/155 81%  89/113 79%  75/189 40%  GPA10 + 사기8 + ERA2 + 부상불안
20260803   344   14/17 82%  124/155 80%  94/113 83%   0/189  0%  GPA10 + ERA2 + 지명2 + 부상불안
```

⚠ 20260803 은 프로에 안 닿았다(대학 → 입대 뒤 프로 행 0). **진로는 씨앗마다
갈린다** — 프로 도달률을 대학 경로로 재면 안 된다.
⚠ 20260802 판은 B-4(`c9a7a51f0`) **뒤**에 돌았고 나머지 둘은 앞이다. B-4 는
  `decisionTemplateId` 만 바꿔 조건을 안 건드리므로 도달률에는 안 걸린다.

### 🔴 두 프로브가 서로를 확인해 준다

20260802 만 **사기 게이트 8종**이 더 못 닿는다(MAJOR_DOUBT · FRIEND_QUIT ·
HOMESICK · Y1_DORM_HOMESICK · BENCH · DRAFT_STOCK_DOWN · SLUMP_UNIV ·
LIFE_SLUMP_CHECK). 그 씨앗이 바로 §D 에서 **사기 최소 57.5 · ≤60 이 6주뿐**이던
씨앗이다 — `probe:morale` 과 `measure:slotreach` 가 **다른 경로로 같은 말을
한다.** 하나만 맞았다면 계측을 의심했을 자리다.

### 🔴 시즌 수가 씨앗보다 중요하다 — 대학은 **8시즌**으로 잰다

```
5시즌   대학 66/113  58%      ← UNIV_Y3_* · Y4_* 가 통째로 "못닿음"
8시즌   대학 99/113  88%
```

고교 3년을 지나 대학에 붙는 게 2029 라 **5시즌이면 대학 2학년까지밖에 못 간다.**
08-30 의 26% 도, 내 첫 5시즌 58% 도 같은 함정이다. `HANDOFF_A_TO_B §1-1` 은
"씨앗 3" 만 적고 시즌을 안 적었는데 **시즌이 더 중요하다.**

### 못 닿는 것 — 두 씨앗 공통 10종이 전부 학점·학사경고다

```
gpa_lte 2     UNIV_GPA_DANGER · UNIV_GRAD_RISK · UNIV_Y3_GPA_VS_BALL
gpa_gte 3.5   UNIV_GPA_GOOD
gpa_gte 3     UNIV_SCHOLARSHIP (+ season_ip_gte 20)
경고 ≥1       UNIV_ATTENDANCE · UNIV_WARN_1 · UNIV_WARN_2 · UNIV_WARN_3 · UNIV_Y1_FIRST_WARNING
```

**게임 결함이 아니라 계측 한계다.**

```
학기 GPA = 주간 학습 품질 평균 × 4.5 × 전공배수
           focus 0.85 → 3.83    normal 0.55 → 2.48    rest 0.3 → 1.35
```

`weeklyStudyMode` 는 `"normal"` 로 시작하고 **`setStudyMode`(화면 조작)로만**
바뀐다. 헤드리스는 그걸 안 부른다. 게다가 `school.weeklyStudyMode` 를 쓰는
**선택지가 0건**이다 — `eventPaths.ts:129` 에 허용 경로로 올라 있는데 아무
이벤트도 안 쓴다. 그래서 GPA 가 2.48 에 갇혀 `gpa_lte 2` 도 `gpa_gte 3.5` 도
영영 안 닿는다.

🔴 **실측이 이 추론을 뒷받침한다** — `UNIV_GPA_GOOD`(≥3.5)도
`UNIV_GPA_DANGER`(≤2)도 **둘 다 0회**다. 한쪽만 0이면 값이 치우친 것이지만
양쪽이 다 0이면 **밴드 안에 갇힌 것**이다. 전공이 일반전공(배수 1.5 → 3.71)도
아니었다는 뜻이기도 하다.

→ A 가 `setStudyPolicy` 손잡이를 넣었으니(09-02 저녁) 다음 판은
`PB_STUDY_MODE=focus` · `rest` 로 갈라 재면 열 종이 열리는지 바로 나온다.
**그때까지 이 열 종을 "안 뜨는 콘텐츠"로 세지 마라.**

### 나머지 못닿음 — 씨앗 편차다

```
20260731 만    UNIV_GROUP_LEAD (team_rank_lte 1 — 팀이 1위를 못 했다)
20260802 만    사기 게이트 8종 (위 참고)
20260803 만    UNIV_Y3_DRAFT_STOCK_CHECK · UNIV_Y4_UNDRAFTED_FEAR
둘             UNIV_ACE_UNIV · UNIV_DRAFT_STOCK_UP (ERA 문턱 — 아래)
🔴 셋 다       UNIV_INJURY_SCARE_UNIV (피로 ≥62 & 컨디션 ≤55 & 부상 아님)
```

🔴 `UNIV_INJURY_SCARE_UNIV` 는 **씨앗 셋에서 한 번도 안 떴다.** 학점 열 종을
빼면 이것만 남는다. 조건 셋이 동시에 서는 창이 좁은지 `condition_lte 55` 가
대학 컨디션 분포 밖인지 갈라야 한다 — **B 가 다음에 볼 하나다.**

### ✅ 이닝 문턱 판정 — `HANDOFF_A_TO_B §0` 이 무효로 돌린 그 건

대학에서 `season_ip`/`season_era` 를 쓰는 건 11종이다.

```
이닝(ip) 문턱   두 씨앗 다 닿는다 — BENCH(5) CAMPUS_PAPER(10) SCOUT_CAMPUS(10)
                CONFIDENCE(15) ALLSTAR(20) SLUMP(20) Y2_FORM_STABILIZE(20)
                DRAFT_STOCK_DOWN(25)  ← ip 는 문제가 아니다
ERA 문턱        씨앗을 탄다 — ACE_UNIV(ip30 & era≤3) · DRAFT_STOCK_UP(ip25 & era≤3.2)
                20260731 닿음 · 20260803 못 닿음. **대학 4년을 평균 이하로
                보내면 안 뜨는 게 맞다** — 결함이 아니다
SCHOLARSHIP     ip 20 은 닿는다(같은 20 인 ALLSTAR 가 닿는다). 못 닿는 원인은
                같이 걸린 `gpa_gte 3` 이다 → 위 계측 한계
```

**이닝 문턱은 손댈 게 없다.** 새 세상(팀당 144경기·배경 리그 복구)에서도
대학 주인공의 시즌 이닝이 문턱 위로 올라온다.

---

## §I B-4 소식↔선택지 7건 · B-13 `test:events` 잣대

### B-4 — 버튼을 본문에 맞췄다 (`c9a7a51f0`) · **새 숫자 0**

`TRACK_B_FINDINGS §11-3` 은 길 둘을 남겼다 — ① 문구만 고치기(어긋남이 남는다)
· ② 효과를 새로 정하기(**밸런스라 사용자 확인**). **둘 다 안 탔다.**
이야기가 맞는 **기존 템플릿으로 가리키는 곳만 바꿨고**, 새로 만든 둘도
효과를 기존에서 그대로 베꼈다. 그래서 사용자 확인 없이 닫혔다.

```
회복일 운영 · 방학기 회복일   → DEC_RECOVERY_DAY    (새 · 효과는 DEC_RAND_BULLPEN_EXTRA 에서)
우천 일정 변경 · 장마         → DEC_HS_COMMON_RAIN  (기존 우천 템플릿 — 하체/제구/쉰다)
팀 분위기                     → DEC_PRO_LOCKER_MOOD (먼저 말을 꺼낸다 / 가만히 둔다)
스카우트 참관                 → DEC_SCOUT_PRESSURE  (새 · 효과는 DEC_PRO_RIVAL_MATCHUP 에서)
후배가 물어왔다               → DEC_UNIV_JUNIOR_CARE (봐준다 / 내 훈련을 한다)
```

마지막이 §11 에서 🔴 로 적힌 **역할이 정반대**였던 건이다 — 본문은 내가
가르치는 것인데 버튼은 내가 배우는 쪽이었다.

⚠ `check:playertype` ② 상한을 **307 → 316** 으로 올렸다. **콘텐츠가 는 게
아니라 가리키는 곳을 바꾼 결과다** — 회식·인터뷰 템플릿은 사기·피로만 주는데
회복일·우천·후배 템플릿은 투구 XP 를 준다(옮긴 7건 × 무가드 갈래 = 9).
이유를 검사 주석에 적었다. 타자가 열리는 날 할 일의 크기가 그만큼 늘었다는
뜻이지 새 결함이 아니다.

⚠ **이 변경은 §D 의 사기 수치를 조금 더 낮추는 쪽으로 움직인다** — 회식(+4)이
네 자리에서 빠진다. §D 표는 B-4 **전** 값이다. 마지막에 한 판으로 다시 잰다.

🔴 A 가 볼 것: `DEC_UNIV_JUNIOR_CARE` 는 **두 갈래 다 동료 관계 +6** 이다
(봐줘도 +6, 안 봐주고 내 훈련을 해도 +6). 힌트도 그렇게 적혀 있어 검사는
통과하지만 **선택이 의미가 없다.** 밸런스라 안 건드렸다 — 사용자 판단이다.

### B-13 — 빨간 줄은 콘텐츠가 아니라 잣대였다 (`da622ba58`)

`test:events` [4] 무대 판정이 `career_stage.stage` 와 `league_id.leagueId`
**단수만** 읽었다. 프로 1군 이벤트는 전부 `stages: ["pro_kbl","pro_abl","pro_jbl"]`
로 적혀 있어(2026-08-25 에 해외를 열면서 배열이 됐다) 통째로 "전체" 로 샜다.

```
전  전체 256 · 고교 155 · 대학 113 · 독립 60 · pro_kbl 5              FAIL
후  pro_abl 197 · pro_jbl 197 · pro_kbl 189 · 고교 155 · 대학 113 ·
    독립 60 · 2군 40 · 전체 17                                        ALL PASS
```

**프로 이벤트는 5종이 아니라 189종이다.** 문안을 더 지을 일이 아니었다.

같은 버그가 [3] 함의 비교(`implies`·`normCond`)에도 있었다 — 배열형끼리는
둘 다 `undefined` 라 "조건이 같다"로 통과해 다른 리그를 가리키는 쌍이 같은
조건으로 읽혔다. `setKey` 로 정규화해 같이 고쳤다(오늘 판정은 안 바뀐다 ·
잠복이었다).

🔴 **이 배열 함정에 오늘만 셋이 걸렸다** — 내 분석기 두 번(§B 의 +1.94),
이 검사 한 번. **조건을 기계로 세는 코드는 전부 `conditionEvaluator` 와 같은
판정을 써야 한다.** `scripts/` 를 훑었고 나머지엔 이 형태가 없다
(`measure-slotreach` 는 이미 배열을 본다).

---

## §C 곁가지 둘 — A 가 알아야 할 것

1. **헤드리스는 `choices[0]` 고정이 아니다.** `runAutoAdvance.ts:51` 의
   `pickChoice` 는 피로 70 이상이면 "휴식·거절·패스·쉬·무시" 라벨을,
   30 이하면 "훈련·수락·참가·도전·시작" 라벨을 먼저 고른다. `choices[0]` 은
   그 사이일 때만이다. §3.6 의 주의문을 그대로 믿으면 안 된다.
2. ~~대학 풀에 `EVT_FARM_LIFE_*` 가 섞인다~~ — **B 가 틀렸다(같은 날 정정).**
   무대 누출은 없다. FARM 계열은 `league_id`(`leagueIds`)로, PRO_LIFE 계열은
   `career_stage` 의 **`stages` 배열**로 이미 막혀 있다. 처음 분석기가
   `stage` 단수만 읽어 둘 다 후보로 셌다 — `conditionEvaluator.ts:26-41`
   이 정본이고 **배열형과 단수형을 둘 다 본다.** 조건을 기계로 셀 땐
   그 함수와 같은 판정을 써야 한다.

---

# B → A 인계 (2026-08-26)

> **트랙 B(이벤트·소식함) 정비 한 판을 끝냈다.** 병합해서 전체 검사를 돌려주면 된다.
>
> B 브랜치: `track/events` (워크트리 `ProjectB-events`)
> 지난 인계 이후 **커밋 27개** · 결과 보고서 [EVENT_REPORT_2026-08-25.md](EVENT_REPORT_2026-08-25.md)

---

## 1. ✅ A 소유 파일은 하나도 안 건드렸다

```
git diff --stat 4939ccf40 HEAD -- apps/ui/src/shared/stores/game.ts \
    apps/ui/src/shared/usecases/ apps/ui/src/pages/ packages/
→ (빈 출력)
```

B가 고친 건 **이벤트 데이터**와 **이벤트 엔진 주변**뿐이다:

| 영역 | 변경 |
|---|---|
| `resource/data/master/events/` · `messages/` | 283파일 · 이벤트 535 → **589종** |
| `utils/conditionEvaluator.ts` · `eventEngine.ts` · `eventPaths.ts` | +75줄 (아래 §3) |
| `stores/master.ts` · `types/event.ts` | 조건 검증·타입 |
| `utils/__tests__/` | 검사 766 → **802** |
| `scripts/` | 게이트 6개 · 계측 4개 |
| `scripts/perf/perfEntry.ts` | 14줄 (하네스 — 아래 §4) |

---

## 2. 🔴 A가 봐야 할 것 — 게임 쪽 결함 다섯

전부 **B가 못 고치는 자리**다. 이벤트 데이터로는 우회만 했다.

| # | 결함 | 근거 |
|---|---|---|
| 1 | **성실이 단방향이다** | 양수 보상 95 대 음수 3(전부 대학 −2). 시작 60에서 **바닥이 54**라 `diligence_lte 30`이 영원히 false. `diligenceRange.test.ts`가 못박아 뒀다 |
| 2 | ~~**사기가 좁은 띠에 붙어 있다**~~ → **2026-09-02 닫았다(§D).** `COND_SLUMP`(사기≤38)·`RAND_TEAM_MEAL`(사기≤72)이 0회였던 그 건이다. 원인은 반복 이벤트의 양수 폭이었고(`d43a50b9d`) 대학 사기 최소가 66.7 → 38.6~57.5 로 내려왔다. **고교는 아직 그대로다(§E)** |
| 3 | **`military_phase`가 스텁이다** | 항상 false를 반환한다(`conditionEvaluator.ts`). 군 서사는 `militaryStatus`/`militaryUnit`/`militaryServiceWeeks` 경로로 우회했다 — 살릴 계획이 있으면 옮긴다 |
| 4 | **`removeTag`가 없다** | 태그로 만든 연계를 닫을 수단이 없다(§5) |
| 5 | **학점을 건드릴 보상이 없다** | `universityGpa`는 조건으로 읽기만 한다. 힌트 여덟 자리가 "학점 유리"라 써놓고 **성실만 움직이고** 있었다 — 문구를 동작에 맞췄다 |


### 사용자가 이미 정한 것 셋 — 결함이 아니다 (2026-08-26)

| | 결정 |
|---|---|
| **주인공은 항상 투수다** | 의도다. `NewGamePage.svelte:271`의 하드코딩은 그대로 둔다. ⚠ 그래도 이번에 만든 71종은 **타자 갈래를 미리 넣어 뒀다** — 열리는 날 되돌아오는 것보다 쓸 때 넣는 게 싸다. 크기는 재 뒀다(`check:playertype`의 ② **306건**) |
| **해외 진출은 추후 개발** | `careerStage`를 `pro_abl`/`pro_jbl`로 쓰는 코드가 없는 건 아직 안 만든 것이다. ⚠ **해외 258종은 그때까지 도달 불가**다 — 재고만 채워 뒀다 |
| **2군에서 1군 이벤트가 다 뜨는 것** | 일단 둔다. `careerStage`가 2군에서도 `pro_kbl`이라 189종이 그대로 후보다 — 추후에 다시 본다 |

---

## 3. B가 엔진에 넣은 것 — 검토 부탁

전부 **기존 동작을 안 바꾸는 확장**이다. 단일 값은 그대로 돈다.

| 파일 | 무엇 | 왜 |
|---|---|---|
| `conditionEvaluator.ts` | `career_stage`가 **`stages` 배열**을 받는다 | 프로 세 리그를 한 번에 가리킬 수단이 없어 **해외로 나가면 1군 이야기 171종이 통째로 멈췄다** |
| 〃 | `league_id`가 **`leagueIds` 배열**을 받는다 | 같은 이유로 **ABL·JBL 2군이 0종**이었다 |
| 〃 | `player_type`이 **`twoWay`를 양쪽으로** 본다 | 정확 일치라 투타겸업에게 **투수 것도 타자 것도 안 떴다** |
| `eventEngine.ts` | `candidateByRule` 계수기 | "뽑기에서 졌다"와 "조건이 안 닿았다"를 못 갈랐다(§6) |
| `eventPaths.ts` | 경로 몇 개 추가 | `leagueYears` 등 |
| `stores/master.ts` | 배열 조건 검증 · `parseEffectsArray`에 `money`/`relation.*`/`luxury` | 문자열형 보상이 그 셋을 못 실었다 |

⚠ `stores/master.ts`는 A/B 경계에 걸쳐 있다. **로더·검증만 건드렸고 게임
로직은 안 건드렸다.**

---

## 4. 하네스(`scripts/perf/perfEntry.ts`) 14줄

계측이 안 되던 자리 둘을 고쳤다. **게임 코드가 아니라 계측 하네스다.**

| 무엇 | 왜 |
|---|---|
| 대학 지원을 **전력 낮은 쪽부터** 고른다 | 알파벳순 상위 3개를 골라 **늘 떨어졌고**, 그래서 **대학 113종이 한 번도 계측된 적이 없었다** |
| `eventRuleProbe`에 `후보` 필드 | 위 계수기를 읽는다 |

---

## 5. 알아두면 좋은 것 — 연계는 태그로만 된다

🔴 **후속 이벤트를 예약할 수단이 없다.** `nextEvents` 같은 필드가 없고,
`triggeredEvents`는 **"언제"만 남기고 무엇을 골랐는지는 안 남긴다.**

그래서 연계의 유일한 수단이 **상태에 흔적을 남기는 보상**(태그·관계·돈)이다.
사슬 셋을 그 방식으로 만들어 증명했다(`EVT_CHAIN_FORM_REBUILD_*`).

`removeTag`가 생기면 더 나은 모양으로 다시 짤 수 있다.

---

## 6. 새 게이트 여섯 — CI에 넣을지 판단 부탁

전부 **"아무도 안 죽고 로그도 안 남는" 결함**을 잡는다. 이번에 열셋을 찾았고
셋은 **B가 만들면서 낸 것을 커밋 전에 잡았다.**

```bash
npm run check:eventconditions   # 조건 타입·필드·경로
npm run check:effectkeys        # 보상 키 — 모르는 키는 조용히 버려진다
npm run check:effecthints       # 힌트가 주지 않는 보상을 약속하는가
npm run check:playertype        # 투수 전용인데 대상을 안 밝힌 것
npm run check:eventranges       # 조건값이 그 축의 눈금 안인가
npm run check:eventslots-health # 무대별 건강 진단 (게이트 아님 · 표만 찍는다)
```

계측 넷:

```bash
npm run measure:eventslots      # 재고 — 무대에 몇 개 있나
npm run measure:slotreach       # 소비 — 몇 개나 닿나  (--path indie|univ|draft|army)
npm run measure:eventrules      # 이름을 댄 규칙의 시즌별 증가분
npm run measure:mailbox         # 소식함이 무엇으로 차는가
```

---

## 7. B가 잰 것 중 A가 알아야 할 셋

### ① 소식함의 64%가 코드 소식이다

```
6시즌 292주 · 경로 indie
코드 소식 1029통 (64%) · 이벤트 578통 (36%)
보유 500/500 · 밀려남 1107통 (68.9%)
```

`msg-train`이 **매주 한 통씩 292통** — 코드 소식의 28%가 훈련 보고 하나다.
대회 소식(`msg-tour-*`)이 234통으로 그다음.

⚠ **`trimMailbox`는 두 계통을 구분하지 않는다.** 다만 미결 선택지는 상한
위로 보존하므로 **선택지 있는 이벤트는 안 밀려난다** — 밀려나는 건 대부분
읽고 넘기는 알림이다.

### ② 값을 말해야 하는데 고정 문구인 이벤트 23종

**템플릿 467개 중 치환 변수를 쓰는 게 0개다.** "4월 결산"에 4월 성적이 안
들어가고, "시상식"이 수상해도 "수상자가 아니어도 참석은 합니다"라고 한다.

같은 소재가 두 시스템에 동시에 있기도 하다 — `EVT_HS_Y1_TOP10_REPORT`가
"이름을 올렸습니다"(고정)라고 하는 동안 `msg-top10`은 **실제 순위**를 말한다.
**둘 다 뜬다.**

⚠ 사용자 판단으로 **미뤘다** — [EVENT_DEFERRED.md](EVENT_DEFERRED.md)에
선택지 셋과 판단 근거를 적어 뒀다.

### ③ 군은 이벤트 엔진이 안 돈다

`advanceWeek`가 `careerStage === "military"`면 **일찍 return한다**
(`advanceWeek.ts:1879`). 그 아래 `runEventEngine()`이 안 불린다.

B가 군 서사 14종을 **조건부 이벤트로 만들었다가** 계측 0회를 보고 알았다.
`events/pools/military_*.json`으로 옮겼다(35 → **54종**).
⚠ 계급 경계도 틀렸었다 — 12/40/70주로 근사했는데 실제는
`advanceWeek.ts:1897`의 **8/34/60**이다.

---

## 8. 병합 뒤 확인

```bash
npm run gen:manifest            # 🔴 빠뜨리면 새 규칙이 조용히 안 실린다
npm test                        # 802건
npm run check:eventconditions && npm run check:effectkeys && \
npm run check:effecthints && npm run check:playertype && npm run check:eventranges
npm run measure:eventslots      # 재고표 — 589종 · 빈 무대 없음
```

⚠ **`_manifest.json`은 gitignore다.** 새 워크트리에서는 `gen:manifest`를
반드시 먼저 돌린다 — 없으면 로더가 던진다(예전엔 19건짜리 스텁으로 조용히
폴백했고 그 상태로 잰 계측이 오진을 냈다).

---

## 9. 밸런스 — 확정을 받아야 할 값 열

전부 이번에 처음 들어간 값이라 **전례가 없다.** 보고서 §11에 표로 있다.
가장 큰 둘:

| | |
|---|---|
| **랜덤 추첨률 26% → 48%** | 42종이 35뽑기를 나누던 걸 65뽑기로. **주당 랜덤 소식 0.66 → 1.14건** |
| **돈 눈금** | 단위는 만원. 학생 15~40 띠 · 프로 150~1000 띠. 고교생 주 순현금이 5.5라 프로 값을 그대로 못 쓴다 |
