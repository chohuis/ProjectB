# 재개 지점 (2026-08-09 23:40)

## 지금 붙잡고 있는 것 — **주인공이 고교에서 경기를 못 나간다**

이게 최상위 문제다. 성장·수상·드래프트가 각각 고장난 게 아니라 **여기서 갈라져 나왔다.**

실측 (고교 3년, `scripts/perf` 하네스):

```
             이닝   등판   선발   ERA
시즌1         23     6      0    5.87
시즌2          6     2      0   19.50

또래 상위10   77~84이닝
또래 최대    107~129이닝
```

**선발 등판이 3년 내내 0이다.** 포지션은 `SP`인데 한 번도 선발로 안 나가고,
등판 자체가 6회 → 2회로 줄어든다.

### 이것 하나가 설명하는 것들

| 증상 | 이유 |
|---|---|
| 성장 +9 vs 또래 +12 | 경기 XP가 통째로 비었다 |
| 성장률 62→80으로 올려도 안 늘어남 | 훈련 XP만 늘고 경기 XP는 0이라 배수가 절반만 먹는다 |
| 개인 수상 0건 | 자격 이닝(60~70) 근처도 못 간다 |
| 드래프트 미지명 75% | 스카우트가 볼 경기가 없다 |

### ✅ 셌다 (2026-08-10) — **스케줄은 정상, 등판 판정이 문제다**

```
              내 경기   결과 있음   실제 등판   선발
시즌1           48        48          7         0
시즌2           46        46          2         0
```

경기는 46~48개가 제대로 잡히고 전부 치러진다. 내 팀 경기 = 내 경기라 일정
생성도 정상이다. **48경기 중 7번만 던지고 선발은 0이다.**

⚠ **스케줄은 `seasonStore.schedule` 최상위에 있다.** 리그 맵
(`leagueState[리그].schedule`)을 읽었더니 전체 0으로 나왔다 — 그대로 믿었으면
"경기가 아예 안 잡힌다"는 없는 결함을 보고했다.

### ✅ 하네스는 결백하다 (2026-08-10)

`runAutoAdvance`의 `conditionWarning` 갈래는 경고를 **소비한 뒤 `game`
액션을 다시 밀어넣는다** (`runAutoAdvance.ts:361`). 경기는 실제로 치러진다.
48경기 전부 결과가 있는 것과도 맞다.

### 등판 여부는 `entryReached` 하나로 갈린다

`handleGame`이 엔진에 `role: (p.position as "SP"|"RP"|"CP") ?? "SP"`를 넘기고
(`runAutoAdvance.ts:96`), 엔진이 **주인공 등판 시점까지 시뮬**한 뒤
`entryReached`로 답한다. false면 그 경기엔 안 던진다. **48경기 중 7번만
true였다.**

### ✅ 확인했다 (2026-08-10) — **RP가 맞고, 악순환이 닫혀 있다**

```
              포지션   주포지션   학년   등판   이닝   ERA
시즌 시작       SP        SP        1      —      —     —
시즌1말         RP        SP        1      7     24    8.11
시즌2말         RP        SP        2      5     16   14.90
```

`SP`로 시작해 시즌 도중 `RP`로 내려가고, **`primaryPosition`은 SP로 남아
주포지션과 실제 보직이 어긋난 채 고정된다.**

**승격 경로 자체는 있다.** 포지션은 매 시즌 W1에 다시 배정된다
(`advanceWeek.ts:119`, `assignHighschoolPosition`). 고교는 SP/RP 두 범주만
쓰고 해마다 새로 평가한다 — "한 번 RP면 영구히 RP"가 아니다.

**문제는 매년 다시 겨루는데 매년 진다는 것이다.** OVR 56~62는 또래 하위 1%라
팀 내 투수 경쟁에서 선발을 못 얻는다.

```
또래보다 약함  →  선발 못 맡음  →  경기 XP 없음  →  더 약해짐
       ↑                                              │
       └──────────────────────────────────────────────┘
```

**악순환이 닫혀 있다.** 성장률을 62→80으로 올려도 효과가 안 난 이유다 —
훈련 XP만 늘고 경기 XP는 여전히 0이다.

### 다음 한 걸음 — `assignHighschoolPosition`의 판정 기준

- **OVR만 본다면** → 위 설명이 완결이고, 고칠 곳은 "주인공을 또래 수준으로
  올리는 것" 하나다
- **다른 기준(학년·감독 관계·팀 사정)이 섞여 있다면** → 손댈 지점이 더 있다

이것만 보면 이 갈래가 끝난다.

### 구원·마무리 상 (사용자 질문, 2026-08-10)

**마무리 상은 이미 있다** — `awardRules` 투수 4부문 중 세이브왕(최소 15이닝 ·
3세이브)이라 구원 투수도 충분히 닿는다. 지금 주인공이 못 받는 건 조건이
아니라 **수상 후보 명단에 아예 없어서**다(위 결함 ①).

**빠진 건 홀드왕**(셋업맨)이다. 다만 순서가 있다:
① 주인공을 수상 후보에 넣기 → ② 선발 강등 문제 → ③ 홀드왕 신설 여부

### (참고) 원래 계획 — 주인공 position 확인

가장 유력한 가설: **주인공이 SP가 아니라 RP로 배정돼 있다.**
하네스 `boot`이 포지션을 이렇게 정한다:

```ts
position: await assignHighschoolPosition({ teamId, pitching: PITCHING }, entities)
```

팀 사정에 맞춰 배정하므로 RP가 나올 수 있다. 그러면 전부 설명된다:

| 관측 | RP 가설 |
|---|---|
| 선발 0 | RP는 선발 등판이 없다 |
| 48경기 중 7등판 | 구원은 상황이 맞아야 나간다 |
| 시즌2에 2등판으로 감소 | 성적이 나쁘면 기회가 준다 |
| 이닝 23 → 6 | 구원은 경기당 1~2이닝 |

`DEFAULT_PROTAGONIST`는 `position: "SP"`인데 하네스는 배정 함수를 쓴다 —
**두 경로가 갈린 자리가 또 하나다**(이번 세션 세 번째: 생성값 읽기,
프리셋 베끼기, 그리고 이것).

```
① 하네스가 실제 배정한 position 찍기          ← 한 줄
② 실제 새 게임(NewGamePage)은 어떻게 정하나    ← 핵심
③ 둘이 다르면 → 하네스를 게임에 맞춘다
   둘이 같은데 RP면 → **게임 설계 문제다**
```

**②가 핵심이다.** 실제 게임도 배정 함수를 쓴다면 이건 계측 문제가 아니라
"투수로 시작했는데 선발을 못 맡는다"는 설계 문제이고, 육성 시뮬로서 훨씬
큰 사안이다.

등판 판정은 `advanceWeek.ts:2190` 세 갈래:
`부상 or 컨디션<35 → 회피` / `컨디션<55 → conditionWarning 정지` / `그 외 → 등판`

컨디션으로 걸렸다면 구원 등판은 나와야 하는데 **선발이 0**이라, 그보다
위(스케줄 or 로테이션 배정)가 유력하다.

---

## 이번 세션에 고친 것 (전부 커밋됨)

| 커밋 | 내용 |
|---|---|
| `0b2a74db6` | 드래프트 5곳이 **3년 안 자란 생성값**을 읽던 배선 결함. 변이 검증 7/7 |
| `31702abd4` | 시작 프리셋 4종을 같은 총합 56으로 (46~56이라 뭘 골라도 또래 바닥이었다) |
| `197092355` | 주인공 성장률 중앙 62 → 80 |

검사 518개 통과.

### 배선 결함 상세 (`0b2a74db6`)

NPC 성장 결과는 `npcLiveStatsStore`에만 쌓이는데 드래프트 다섯 자리가
`npcs[].pitching`(생성값)을 읽고 있었다. 같은 투수 3시즌 추적:

```
npcLiveStatsStore   19→22세 OVR +9 · 31→34세 −4   (나이 곡선 정상)
npcs[].pitching     9종 전부 정확히 +0            (생성값에 고정)
```

`liveOvrOf`/`livePitchingOvrOf`(`stores/npcLiveStats.ts`)를 두고 다섯 자리가 같이 쓴다.
죽은 헬퍼 `npcCoreOvr`·`calcDraftScore`는 제거했다.

---

## 확정된 사실 (다시 재지 말 것)

- **또래(고교 3학년 투수 509명) live OVR**: 최소 56 · p25 65 · **중앙 68** · p75 71 · 최대 83
- **나이별 3년 성장폭**: 17~19세 +12 / 20~21세 +10~11 / 24세 +5 / 30세 −1 / 37세 −10.
  **NPC 성장 곡선은 건강하다 — 손댈 데 없다**
- **`devRate`는 XP에 정확히 선형**으로 먹는다 (31/62/93/124 → 주당 구속 XP 1.48/2.96/4.43/5.91).
  엔진 격리 호출로 확인. "×1.3이 안 먹는다"는 과소해석이었다
- **주인공 `potentialHidden` = 88**이고 제대로 배선돼 있다. 폴백 75로 안 떨어진다.
  감쇠(`potential_cap_factor`)는 OVR 66부터 걸린다 — 62~66 구간에선 병목이 아니다
- **고교 개인 수상 자격 조건은 문제없다** — 70이닝 이상 29~34명, 60이닝 이상 88~94명이
  매 시즌 나온다. 시스템은 잘 돈다

---

## 확정된 결함 (아직 안 고침)

### ① 주인공이 수상 후보에 아예 못 오른다

기록이 두 군데로 갈려 있다:

```
주인공   seasonStore.stats[p.id]                   ← 화면 5곳이 여기서 읽는다
NPC      seasonStore.leagueState[리그].stats[id]    ← computeAwards가 여기서 뽑는다
```

`computeAwards(rules, stats)`는 리그 맵만 받는다. `SeasonEndModal`의
`a.playerId === pid` 필터는 **영원히 빈 배열**이다.

파급: `universityUtils`의 진학 점수 `awards.length * 15`도 항상 0. 경력 화면·
인생 기록에도 주인공 수상이 한 번도 안 남았다.

고칠 자리: `computeAwards` 호출 시 `seasonStore.stats[p.id]`를 **주인공 리그 맵에만**
합쳐 넘긴다 (프로 맵에 고교 기록을 섞지 말 것).

### ② 드래프트 산식에 수상 입력이 없다

`ProtagonistDraftParams`에 `peer_ovrs`·`team_ace_rank`·`tournament_score`·
`major_injuries`·`scout_score`는 있는데 **수상만 없다**. 사용자가 원한 요소 중
유일하게 빠진 것.

### ③ 드래프트 라운드 폭이 안 나온다

최신 20회 (프리셋 56 · 새 배선 · 성장률 80):

```
성공 20/20   크래시 0
고교말 OVR   최소 57 · 중앙 62 · 최대 66     (또래 중앙 68)
라운드       7R 1 · 8R 1 · 9R 1
미지명       9회 (75%)
```

**산식 재교정은 아직 하지 말 것.** 등판 문제가 풀리면 입력 분포가 통째로
움직인다 — 지금 맞추면 두 번 한다.

---

## 사용자가 확정한 방향

- **드래프트**: 상위픽 = 팀 에이스급 + 리그 최상위 / 중간 = 리그 무난 /
  미지명 = 애매하거나 · 큰 부상 · 대회 부진. **OVR도 반영** (잘하는데 팀운 없을 수 있으니)
- **미지명부터 1R까지, 육성 결과도 최상위~하위권까지 다양하게 나와야 한다**
- **시작 프리셋은 배분만 다르고 총합은 같다** — 아키타입은 유불리가 아니라 취향
- 수상·대회활약·에이스 여부·OVR이 전부 드래프트에 반영돼야 한다

## 사용자 요청 중 아직 안 한 것

**NPC 고교 성적 → 어느 팀 몇 순위로 드래프트되는지 대응표.**
등판 문제가 풀린 뒤에 해야 기준선이 의미 있다.

---

## 보고만 하고 안 고친 것들 (사용자 판단 대기)

| | 성격 |
|---|---|
| 드래프트 보드 픽번호 중복 (56 두 줄 · 111 없음) | 110슬롯에 주인공이 얹혀서 |
| `test:rosterbalance` 4건 실패 — 시즌말 포수 0명 | KBL_2군·ABL_1군·JBL_1군, KBL_1군 야수 하한 |
| 소식함 200통 포화 (고교 36주차에) | 오래된 소식이 밀려남 |
| 이야기 이벤트 87% 유실 | 이벤트가 뜨지 못하고 버려짐 |
| 동료 8명이 떠난 뒤에도 `together` | 관계도 |

---

## 계측 도구 (이번 세션에 만든 것)

`scripts/perf/perfEntry.ts`:

- `peerProbe()` — 또래 분포·주인공 백분위 (**live를 읽는다**)
- `ageGrowthSnapshot()` — 나이별 코호트 고정 성장폭
- `statProbe()` — 주인공 능력치 9종 원값
- `growthLever(devRates[])` — **커리어 없이 엔진만 두 번 부른다.** 부상 교란 제거
- `hsAwardProbe()` — 고교 이닝 분포·자격자 수·주인공 실제 기록
- `tuneProtagonist({ovrDelta, devRateMult})` — 안(arm)별 주인공 튜닝

`scripts/study-careers.cjs --arm base|startovr|growth` — 안별 비교 조사

### 계측할 때 반복해서 밟은 함정

- **`g.npcs`가 아니라 `npcLiveStatsStore`를 읽어라.** 생성값은 안 자란다
- **시즌 롤오버 전에 재라.** 넘어가면 그 시즌 리그 기록이 초기화된다
- **코호트를 id로 고정하라.** 은퇴·신인 유입만으로 평균이 움직인다
- **`currentWeek()`는 누적이 아니다** — 시즌마다 리셋된다
- **하네스가 게임 값을 베껴 갖고 있는지 확인하라.** `perfEntry`의 `PITCHING`이
  옛 프리셋이라 "시작 49"를 게임 값으로 착각했다. 지금은 검사가 일치를 못박는다
  (`startPresets.test.ts`)
- **커리어로 레버를 재면 부상이 섞인다.** 엔진을 직접 부르면 격리된다
