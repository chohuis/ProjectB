# 미구현(A) 기획안 — 사용자 확정 2026-08-24

`BACKLOG_2026-08-24.md`의 A묶음 8건. **구현 순서는 A → B(점검) → C(보완) → D(부채)**다.

## 확정된 것

| # | 항목 | 결정 |
|---|---|---|
| A1 | 인생 기록·엔딩 | **한 장 요약 + 펼치기** · **담백한 기록부** · 네 갈래 다 싣는다 |
| A2 | 히스토리 화면 | **따로 · 상시 열람** (A1과 별개 화면) |
| A3 | 대회 개인 기록 | 기획 아님 — **점검(B)으로 옮긴다.** 부분 구현이라 재보면 답이 나온다 |
| A4 | 군 복무 문서 | 기획 아님 — 문서 작성 |
| A5 | 과거 성적 5년치 | **팀 순위만** (선수 개인 기록은 안 만든다) |
| A6 | 대학 보충 경로 | **C4(대학 1년)와 묶어 대학 4년 구조 전체를 한 번에** |
| A7 | UI 영어화 | **보류** — 콘텐츠가 굳고 나서 |
| A8 | 포수 기능 | **도루 저지 + 블로킹** (투수 리드는 안 넣는다) |

---

## A1 — 인생 기록 · 엔딩 화면

### 구조

    [ 한 장 요약 ]  은퇴 직후 자동으로 뜬다
      이름 · 활동 기간 · 포지션 · 마지막 소속
      통산 성적 (투수: 승패·ERA·탈삼진 / 타자: 타율·홈런·타점)
      대표 수상 (MVP·골든글러브·신인상 횟수)
      우승 횟수와 연도
          ↓  [ 연도별로 보기 ▼ ]
    [ 펼친 상세 ]  탭 넷
      성적   연도별 한 줄 (팀·순위·성적·수상·포스트시즌)
      경로   고교→대학→드래프트→팀→군복무→FA 이적사
      순위   그해 팀 순위와 포스트시즌 결과
      사람   감독·코치·동료 관계도 (마지막 값)

### 톤 — 담백한 기록부

숫자와 사실만 놓는다. **감정은 사용자가 넣게 둔다.**

    2016 · KBL 부산 웨이브스 · 1위
      28경기 18승 4패 ERA 2.11
      MVP · 골든글러브 · 한국시리즈 우승

⚠ **문장 뱅크를 새로 만들지 않는다.** 이벤트도 65종이 문장 하나뿐인 문제(C7)를
  안고 있는데 여기서 또 문장을 요구하면 같은 짐을 두 번 진다.

### 재료 — 다 있다

`CareerSeasonRecord`: `year` · `leagueId` · `teamId` · `rank` · `totalTeams` ·
`wins/losses/draws` · `statLine` · `ovr` · `awards` · `psResult` · `stats` · `gameLog`
`careerEvents`(드래프트·트레이드·군입대) · `achievements`

⚠ **관계(사람 탭)는 지금 숫자뿐이다.** 에피소드가 없어 얕게 보일 수 있다 —
  이름·역할·마지막 관계값만 담백하게 놓고, 서술은 안 붙인다.

### 어디서 여는가

  · 은퇴 처리 직후 **자동**
  · `StatusPage`에서 **다시 보기** (은퇴 뒤에는 언제든)

⚠ `isRetired`·`retireProtagonist`는 이미 있다(`StatusPage:624`). 새로 안 만든다.

---

## A2 — 히스토리 화면 (상시)

A1과 **성격이 다르다** — A1은 은퇴 때 한 번 보는 감정, A2는 플레이 중 언제든 여는 자료실.

    역대 순위표     연도 × 팀 (history_standings)
    역대 수상       연도별 MVP·신인상·골든글러브
    역대 선수       은퇴자 포함 조회

🔴 **D7(은퇴자 DB 누적)이 여기에 묶인다.** 이 화면이 은퇴자를 조회하므로
   **지우면 안 된다.** 대신 "현역만 로드하고 은퇴자는 조회할 때만 읽는" 쪽으로
   간다 — 정리가 아니라 **로드 분리**가 답이다.

---

## A5 — 과거 성적 5년치 (팀 순위만)

세계 생성 시 각 팀에 **직전 5년 순위·우승 이력**을 넣는다.

  · 넣을 곳은 `history_standings` — 이미 있는 테이블이다
  · 238팀 × 5년 = 약 1,190행. 가볍다
  · **선수 개인 기록은 안 만든다** — NPC 7,300명 × 5년이면 36,500행이고
    수상 이력까지 지어내야 한다. 값에 비해 비싸다

⚠ **팀 전력★과 어긋나면 안 된다.** ★5 구단이 5년 내내 꼴찌인 과거를 가지면
  세계가 자기모순이다. 방금 넣은 `power`(`ec6f6f6eb`)를 입력으로 쓴다.

---

## A6 + C4 — 대학 4년 구조 (묶음)

**지금 상태(실측)**: `universityWeek`이 52에서 멈춘다 = 1년.
대학 이벤트 93종 중 **Y2~Y4용 20종이 도달 불가**. B가 보류 중이다.

🛑 **아직 안 정한 것 셋** — 착수 전에 사용자에게 묻는다

  1. 대학은 몇 년인가 (1년 / 2년 / 4년)
  2. 중퇴·조기 드래프트가 있는가
  3. 보충(편입·재수)이 필요한가

⚠ 정해지기 전엔 **B가 Y2~Y4 20종을 건드리지 않는다.**

---

## A8 — 포수 기능 (도루 저지 + 블로킹)

  1. **도루 저지** — 포수 송구 × 주자 발 → 아웃/세이프
  2. **블로킹** — 폭투를 막아 주자 진루를 막는다

**투수 리드(볼배합)는 안 넣는다** — 구종 선택에 개입하면 경기 엔진의
핵심을 건드리게 되고, 주인공이 투수일 때 조작감과 충돌한다.

⚠ **착수 전에 확인할 것**: 경기 엔진에 도루·폭투 개념이 이미 있는가.
  없으면 그것부터 만들어야 하고 규모가 달라진다.

---

## 순서 (의존을 따라서)

```
1. A1 인생 기록 ──┬── A2 히스토리 ── D7 로드 분리
                  └── A5 과거 성적 (A2가 보여줄 재료)
2. A4 군 복무 문서            (작다 · 독립)
3. A8 포수                    (엔진 확인 먼저)
4. A6+C4 대학 4년             🛑 사용자 결정 셋이 먼저
5. A7 영어화                  보류
```

**A1부터 시작한다.** A2·A5·D7이 전부 여기 매달려 있고, 재료가 이미 다 있어
새로 만들 엔진이 없다.
