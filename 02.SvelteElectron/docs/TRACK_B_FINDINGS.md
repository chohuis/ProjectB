# 트랙 B — 실측 기록 · 1주차 (2026-09-01)

> **A 에게**: 아래 §1 은 `TRACK_B_PROMPT.md` §3 의 수치와 다르다.
> 지시서 §7 이 "네가 재서 문서와 다르면 문서가 틀린 것이다"라 해서 적는다.
> `RESUME.md` · `CLAUDE.md` 는 A 가 고친다.

---

## 1. 🔴 지시서 §3 수치 정정

| 지시서 | 실측 | 차이 |
|---|---|---|
| "프로 1군 171종이 `league_id` 를 안 쓴다" | 프로 단계 **204종** · `league_id` 쓰는 것 **0종** | 종수·전제 둘 다 |
| "문장 하나뿐인 이벤트 65종" | 한 문장 **563종** (뱅크 있는 문안 14종) | 8.7배 |
| "화면에 닿는 것 283종 = 52%" | 283/589 = **48%** | 자기모순 |

**"KBL·ABL·JBL 이 같은 글을 본다"는 결함이 아니었다.** 공용 87종의 글을
훑어 한국 전제 표현을 찾으니 3건이고 **그 셋은 의도된 것**이다
(`EVT_ABROAD_GO_BACK` 「돌아갈까」 · `EVT_ABROAD_HOME_OFFER` 「국내 복귀 제안」 ·
`EVT_ABROAD_LOCAL_FAN` 「현지 팬」).

진짜 문제는 반대였다 — §3 참고.

---

## 2. 도달률은 **경로를 안 밝히면 뜻이 없다**

단계 조건만으로 커리어 경로별 도달 가능 종수를 세면 이렇다:

```
  고교→독립            272 / 589   (46%)   ← measure:eventfunnel --nodraft 가 쓰는 경로
  고교→대학→독립        385 / 589   (65%)
  고교→프로            401 / 589   (68%)
  고교→프로→해외        416 / 589   (71%)
  고교→대학→프로        514 / 589   (87%)
```

**어느 경로로도 못 가는 것은 0종이다.** "구조적 도달 불가"는 종의 속성이
아니라 **경로의 속성**이다.

`--nodraft` 는 가장 좁은 경로를 강제한다. 그 경로에서 잰 값:

```
  뜬 종수 172.7 / 정의 589         = 29.3%
  뜬 종수 172.7 / 도달 가능 272    = 63.5%
```

⚠ 앞으로 도달률을 적을 때 **분모를 같이 적는다.** 안 그러면 29%·48%·52%·63%가
같은 문서에 섞인다 (이미 섞여 있다).

---

## 3. 해외 프로 이벤트가 국내의 47% 였다 — 데이터로 메웠다

```
                        전      후
  KBL 이 보는 것        189     189    변화 없음
  ABL·JBL 이 보는 것     89     197    2.2배
```

`pro_kbl` 전용 113종 중 **111종이 리그 중립 텍스트**였다 — 스프링캠프·개막전·
장기 원정·팬미팅처럼 어느 리그에서도 말이 되는 것들이다. 그런데 `career_stage`
가 `pro_kbl` 단수라 해외에서 안 떴다.

108종을 `stages: ["pro_kbl","pro_abl","pro_jbl"]` 로 넓혔다. **새 글은 한 줄도
안 썼다.**

### ⚠ 보류 5종 — A 확인이 필요하다

| 이벤트 | 물음 |
|---|---|
| `EVT_PRO_W47_DRAFT_WATCH` | KBO 신인 드래프트. 해외판을 따로 쓸까, KBL 전용으로 둘까 |
| `EVT_PRO_OLD_TEAMMATE` | "고교·대학 동료" — 학제가 다르다. 중립으로 고쳐 쓸까 |
| `EVT_PRO_FA_MARKET_TALK` | ABL·JBL 의 FA 제도가 KBO 와 같은 방식인가 |
| `EVT_PRO_YEAR5_FA_NEAR` | 같음 |
| `EVT_PRO_ROLE_DEMOTE` | 해외도 2군 강등이 있나 (`LEAGUE_ABL_FARM` 은 존재한다) |

---

## 4. 영구 손실의 원인 — 주인공 시작 OVR 이 콘텐츠 사다리 위에 있다

고1 이벤트 셋이 영영 못 뜨는 이유를 재니 **미디어 알림 둘이 W1·W2 를 먹고**
있었다.

```
  p745  EVT_HS_MEDIA_OVR65_ALERT   pitching_ovr >= 65
  p740  EVT_HS_MEDIA_OVR60_ALERT   pitching_ovr >= 60
```

🔴 **주인공 시작 OVR 이 68~70 이다** (프리셋 넷 전부). 고교 단계
`pitching_ovr_gte` 조건 **16개 전부**가 첫 주부터 통과한다.

### 문턱을 올리는 안은 실측으로 버렸다

```
  measure:growth  →  주인공 고교 1년: OVR 70 → 71  (+1.00)
```

`generation_rules.json` 의 `"목표 곡선(시즌당 OVR): ~18 +4.5"` 는 **NPC 값**이다.
주인공은 그 4.5분의 1이라 고교 3년에 68 → 약 71~73 밖에 안 큰다. 문턱을
시작값 위로 올리면 **안 뜨거나(75+) 여전히 곧 뜨거나(71~72)** 둘 중 하나다.

### 🔴 A 에게 — 이건 B 가 못 고치는 자리다

```
  고교 NPC OVR 범위     45~70   generation_rules.json   (A 소유)
  주인공 시작 OVR       68~70   NewGamePage.svelte      (C 소유)
  고교 콘텐츠 사다리     48~65   events/**               (B 소유)
```

**주인공이 입학 첫날부터 리그 최상위권이다.** 콘텐츠 사다리는 훨씬 낮은
데서 시작하는 주인공을 가정하고 짜였다. B 는 사다리만 갖고 있어서 증상
처치밖에 못 한다 — 셋 중 어느 쪽이 맞는지는 A 가 정해야 한다.

⚠ `EVT_HS_Y1_SLOW_GROWTH` (`pitching_ovr_lte 52`, "성장이 더디다")는
**어떤 프리셋으로도 영영 못 뜬다.**

---

## 5. 이번 주 고친 것

### 5-1. priority — 일반 미디어 기사가 입학 서사를 밀어냈다

```
  EVT_HS_MEDIA_OVR65_ALERT   745 → 42
  EVT_HS_MEDIA_OVR60_ALERT   740 → 40
```

문턱(60·65)은 그대로 뒀다. 문안이 「구종 분석 기사」·「스카우트 카메라」라
이정표가 아니라 **바닥선**이고, 그건 시작부터 통과하는 게 맞다.
`once_per_season` 이라 51주 중 언젠가는 뜬다 — 잃는 게 없다.

### 5-2. 문안 오배선 셋

`MSG_COND_STAT_MILESTONE` 「시즌 5승 달성!」 하나를 규칙 넷이 공유했다.
라이벌·드래프트·스카우트가 전부 "5승 달성" 으로 떴다.

```
  EVT_HS_Y1_RIVAL_NOTICE       또래가 이름을 외웠다       탈삼진 30↑
  EVT_HS_Y3_DRAFT_STOCK_CHECK  지명 순위 이야기가 돈다    탈삼진 80↑
  EVT_HS_Y3_SCOUT_FOCUS        스카우트가 자리를 옮겼다   OVR 55↑
```

⚠ 공유 문안 32종 중 대부분은 정상이다 — 중간고사·시즌개막이 학년별로
나뉘어 같은 글을 쓰는 것. **내용이 안 맞는 건 이 셋이었다.**

### 5-3. 고1 초반 창을 어긋나게 폈다

W1~W6 에 `once_per_career` 넷이 창을 포개고 있었다. 자리는 주당 1칸이라
priority 로는 못 푼다 — **창이 겹친 게 문제**다.

```
                    전      후
  FIRST_TRAINING   1~3  →  1~3   (그대로)
  DORM_NIGHT       1~2  →  1~5
  WELCOME_DINNER   1~4  →  1~7
  POSITION         1~6  →  3~8   ← W1·W2 를 비웠다
```

⚠ 넷 다 `week_gte` 조건이 **원래 없었다.** 창을 여는 쪽은 `week_lte` 만
있었으니, 앞으로 창을 다룰 때 그 전제를 먼저 확인해라.

### 5-4. 아무 일도 안 하는 조건 6개 제거

```
  EVT_HS_LIFE_FATIGUE_ALERT   week_gte 1 · week_lte 156
  EVT_HS_LIFE_PEAK_FORM       같음
  EVT_HS_LIFE_SLUMP_CHECK     같음
```

156 = 52×3 으로 "고교 3년" 을 의도했지만 `week_lte` 는 `currentWeek`(1~52,
시즌마다 리셋)과 비교한다. **항상 참이라 읽는 사람만 속인다.**
`career_stage: highschool` 이 이미 그 일을 한다. 동작 변화 없음.


### 5-5. 🔴 창 조정은 절반만 들었다 — 순서로 짜낼 수 있는 건 여기까지다

3회씩 잰 값이다. **목표 지표(끝내 못 뜬)는 안 내려갔다.**

```
                    끝내 못 뜬          밀린 규칙 종수
  전 (원본)          6 · 6 · 6  = 6.0      54.3
  후1 (priority)     4 · 4 · 5  = 4.3      50.0
  후2 (창 조정)      5 · 6 · 3  = 4.7      48.0  (폭 0)
```

**총량은 그대로고 구성만 바뀌었다:**

```
  전     WELCOME_DINNER · DORM_NIGHT · HOMESICK
  후1    WELCOME_DINNER · DORM_NIGHT
  후2    DORM_NIGHT · HOMESICK          ← WELCOME_DINNER 는 살고 HOMESICK 이 밀렸다
```

창을 넓히면 그 이벤트가 **더 오래 후보로 남아 다른 것을 밀어낸다.**
`WELCOME_DINNER`(p70)가 1~7 로 늘면서 `HOMESICK`(p55, 1~10)을 3주 더 눌렀다.
자리가 주당 1칸이라 **제로섬**이다.

되돌리지 않았다 — 나쁘게 하지 않았고 `밀린 규칙 종수`가 50 → 48 로
고정(폭 0)됐다. 되돌리면 그것도 잃는다.

### 🔴 A 에게 — 남은 길 둘, 둘 다 B 소유가 아니다

고1 첫 8주에 `once_per_career` 넷이 들어가야 하는데 칸이 8개이고
그 8칸을 `repeatable` 상위권과도 나눠 쓴다.

| 길 | 내용 | 소유 |
|---|---|---|
| **주당 상한** | 시즌 초 몇 주만 conditional 을 2칸으로 | 밸런스 — A |
| **시작 OVR** | 68~70 을 낮추면 미디어 계열이 후보에서 빠져 칸이 는다 | C(프리셋)·A(범위) |

⚠ track-B 문서는 반대 자리에 도달해 있었다 — *"그 주에 뽑을 새 이야기가
아예 없었다"*. **지금은 옮길 대상은 있는데 칸이 없다.** 같은 상한의
양쪽 끝이다.

---

## 6. 계측할 때 데인 것 — 다음 사람이 안 겪게

### 🔴 `--runs N` 은 회차마다 데이터를 다시 읽는다

`measure-eventfunnel.cjs:65` 가 `spawnSync` 로 자기를 자식으로 띄운다.
자식은 부팅하며 `events/**` 를 새로 읽는다.

**회차 사이에 이벤트 JSON 을 고치면 3회 평균이 무의미해진다.** 실제로
회차 2·3 사이에 고쳐서 한 번 통째로 버렸다.

→ **계측이 도는 동안 데이터를 건드리지 마라.**

### 전후 비교는 `trap` 으로 되돌린다

```bash
trap 'git checkout $FIX -- $FILES' EXIT INT TERM
```

중간에 죽으면 저장소가 옛 상태로 남는다. 되돌리기를 `trap` 에 걸어야 안전하다.

### 분모를 확인해라

`measure-eventfunnel` 의 "정의" 는 `_manifest.json` 의 **파일 수**다
(`:224~228`). 단계로 거른 도달 가능 수가 아니다.

---

## 7. 아직 못 뜨는 것

한 커리어(292주 · 고교→독립) 상세 1회 기준. **밀린 49종 중 끝내 못 뜬 6종**이다.

```
  once_per_career  2종   **영구 손실 — 그 커리어가 끝이다**
    EVT_HS_Y1_DORM_NIGHT    창을 1~5 로 넓혔는데도 못 뜬다
    EVT_HS_Y1_HOMESICK      후1 에선 살았다가 후2 에서 다시 밀렸다

  repeatable       4종   다시 오지만 **한 커리어에 한 번도 안 떴다**
    EVT_HS_MEDIA_FATIGUE_RISK
    EVT_HS_Y1_ROLE_CHECK
    EVT_HS_Y3_SCOUT_FOCUS     p800 인데 안 뜬다 — 조건이 겹치는 것으로 보인다
    EVT_IND_FORM_FOUND        컨디션 80↑ **동시에** 피로 45↓ — 그 상태가 드물다
```

⚠ `repeatable` 넷은 정책상 다시 오지만 **실제로는 안 왔다.** "영구 손실이
아니다" 와 "플레이어가 봤다" 는 다르다 — 이 구분을 지표에 넣어야 한다.

---

## 8. 🔴 대학 학년 축이 시즌과 20주 어긋난다 — A 에게 넘겼다

**[PROMPT_A_UNIV_WEEK.md](PROMPT_A_UNIV_WEEK.md) 가 본문이다.** 요약만 적는다.

```
  measure:slotreach --path univ   (씨앗 20260803 · 3시즌)
    고교  155종 중 124종 닿음  80%
    대학  113종 중  29종 닿음  26%   ← 무대 중 최악
```

진학이 시즌 **W32** 라 `universityWeek` 원점이 시즌 경계와 20주 어긋난다.

```
  연도    시즌W1의 uw   시즌W52의 uw   학년띠
  2029          21            72     Y1 ~ Y2    ← 한 시즌이 두 학년에 걸친다
  2031         125           176     Y3 ~ Y4
  2032         177           228     Y4         ← 4학년에 5번째 시즌에야 닿는다
```

- **Y4 4종은 4년제로 영영 못 뜬다** (uw 157~ 에 5시즌 걸린다)
- **Y1~Y3 는 엉뚱한 해에 뜬다** — Y1 중간고사가 2학년 때 온다

⚠ 지시서 "대학 Y2~Y4 20종을 붙여라" 도 낡았다 — **이미 38종이 있다**
(Y1 12 · Y2 13 · Y3 12 · Y4 13). 쓸 게 아니라 **뜨게 할 것**이었다.

⚠ 고교는 `grade` 를, 대학은 `school.universityWeek` 을 쓴다. 방식이 다르다
(고교 155종 중 grade 84종 · 대학 113종 중 0종).

⚠ **계측 10개 중 8개가 `university: false` 다.** `measure-eventfunnel` 포함이다.
그래서 §2 의 도달률 63.5% 는 **대학 113종이 분모 밖인 값**이고, 이 어긋남도
오래 안 보였다. `measure:slotreach --path univ` 만 대학을 탄다.

---

## §9 무대별 재고 — 상무·프로 사교가 비었다 (2026-09-01 · 데이터만으로 확인)

계측 없이 **규칙 589종·템플릿 527종을 세어서** 나온 값이다. `.node` 를
안 쓰므로 A 의 빌드와 안 겹친다.

### 9-1 🔴 random 다섯 풀의 무대별 재고

풀마다 주 1회 뽑는다(`maxPicksPerWeek: 1`). **재고가 1이면 그 하나만 반복된다.**

```
  무대           SOCIAL   BODY  TRAIN  MEDIA  TEAM_LIFE     합
  ────────────────────────────────────────────────────────────
  highschool          6      9     13     17     23        68
  university          6      3      6      8     10        33
  independent   🔴    1      3      6     10     11        31
  pro_kbl       🔴    1      8     13     24     29        75
  pro_abl       🔴    1      8     13     24     29        75
  pro_jbl       🔴    1      8     13     24     29        75
  military      🔴    1 🔴   1      4      4      5        15
```

⚠ **무대무관 규칙을 포함한 수**다. `career_stage` 조건이 없는 것은 모든
무대에 더해진다(SOCIAL 1 · BODY 1 · TRAIN 4 · MEDIA 4 · TEAM_LIFE 5).

### 9-2 🔴 프로·독립의 사교 풀은 사실상 0이다

`POOL_SOCIAL_DAILY` 은 **고교 5 · 대학 5 · 무대무관 1** 로만 짜여 있다.
프로·독립 전용이 **한 종도 없다.** 그 유일한 무대무관 1종은:

```
  EVT_RAND_SOCIAL_MEDIA_MENTION   cd4
    player_type    pitcher
    popularity_gte 15
    fame_gte       10
```

**주인공이 타자면 사교 풀이 통째로 빈다.** 투수여도 인지도 15·명성 10
아래면 같다. 프로 커리어 내내 사교 소식이 한 줄도 안 온다.

### 9-3 🔴 상무 — 이벤트는 도는데 소식함에 한 줄도 안 간다

🔴 **처음엔 "상무 이벤트 0종"이라고 적었다. 틀렸다.** `events/` 렌즈로만
세서 못 봤다 — 상무는 **별도 계통**을 쓴다.

```
  resource/data/master/events/pools/military_common.json    14종
  resource/data/master/events/pools/military_sports.json    20종
  resource/data/master/events/pools/military_general.json   20종
  resource/data/master/events/pools/military.json            5종
```

`advanceWeek.ts:2083` 이 `minRank` 로 걸러 개수를 `weekCalcMilitary` 에
넘기고, Rust(`week_engine.rs:783`)가 **매주 40%** 로 하나 뽑는다.
104주면 **약 42번 뜬다.** 계통은 멀쩡히 돈다.

⚠ **9-4 와 같은 실수다** — 2군은 리그축, 상무는 별도 풀이었다.
**무대축(`career_stage`) 하나로 재고를 세면 두 번 틀린다.**

#### 그래도 소식함은 빈다 — 이건 맞다

`advanceWeek.ts:2166` 군 복무 분기의 반환값:

```
  return {
    processedWeek: nextWeek,
    logs: [isSportsUnit ? "군 복무(체육부대)" : "군 복무(일반부대)"],
    newMessages: [],        ← 🔴 박혀 있다
    ...
  };
```

54종이 전부 `pushPendingAction` 으로 **모달만** 띄운다. 소식함에는
104주 동안 한 줄도 안 들어가고, 주간 로그도 고정 한 줄이다.

⚠ 이 54종은 `title`/`description` 이 파일에 직접 박혀 있어
**메시지 템플릿을 안 탄다.** 문장 은행(9-5)도 못 붙인다.

#### 🔴 1회성 이벤트에 1회 가드가 없다

```
  Rust    rng.gen_range(0..total)   복원추출 · 쿨다운 없음
  데이터   id title description minRank choices   ← once 필드 없음
  TS      pushPendingAction 이 중복을 안 거른다
  스토어   발동 기록이 없다
```

`MIL_COM_FIRST_DAY`(「자대 배치 첫날」)는 `minRank 0` 이라 104주 내내
후보에 남는다. 계급 구간별 기대 발동을 더하면:

```
  이병 0.16 + 일병 0.31 + 상병 0.26 + 병장 0.38 ≈ 1.1회
```

**평균 1.1회** — 상당수 커리어에서 두 번 뜨거나 한 번도 안 뜬다.
그리고 **병장 때 「자대 배치 첫날」이 뜰 수 있다.**
`MIL_SPT_FIRST_PRACTICE`(「첫 야구 훈련」)도 같다.

⚠ 뽑기·데이터 스키마를 손대야 해서 **B lane 밖이다.** A 와 상의 중.

### 9-4 ✅ 2군은 문제없다 — 확인하고 접었다

처음엔 2군도 빈 줄 알았는데 아니다. `game.ts:2297` 이 강등·이적 시
`LEAGUE_KBL_FARM` 을 **`careerStage: "pro_kbl"`** 로 떨어뜨린다(폴백).

```
  2군 선수 = careerStage "pro_kbl" + leagueId "LEAGUE_KBL_FARM"
     → 프로 random 75종을 그대로 뽑는다
     → 거기에 league_id 로 걸린 EVT_FARM_* 40종이 더해진다
```

`CareerStage` 타입에 farm 값이 없어서 빈 줄 알았던 것이다. **무대축이
아니라 리그축으로 걸려 있다.**

### 9-5 🔴 문장 은행 — 527종 중 19종만 있다

```
  템플릿 527종
    bodies 없음   508      매 발동마다 같은 문장 하나
    bodies 3+      19
```

`test:sentencebank` 는 **`bodies` 가 있는 것만** 검사한다
(`test-sentencebank.cjs:29`). 없는 508종은 검사 대상이 아니라 통과한다.

반복 노출되는 것만 추리면:

```
  oncePolicy repeatable 인데 은행 없음   273종
     conditional 132 · random 141
  random 162종은 전부 repeatable · 전부 은행 없음
```

**우선순위는 재고가 작은 풀이다.** 재고 6인 고교 SOCIAL 은 쿨다운 2주
기준으로 같은 문장을 커리어 내내 수십 번 띄운다.

⚠ `once_per_career` 는 은행이 없어도 된다 — 한 번만 뜬다. 273종만 대상이다.

### 9-6 다음에 할 것

```
  ① POOL_SOCIAL_DAILY 에 프로·독립 전용 추가          재고 1 → 6 목표
  ② 군 복무 주간을 소식함에 잇는다                    newMessages: [] 를 채운다
  ③ 재고 작은 풀부터 bodies 3~4문장                   고교/대학 SOCIAL 12종 먼저
```

⚠ ①은 `docs/EVENT_PLAN_2026-08-28.md` 의 간극 1~4 와 같은 자리다.
그 계획안은 **간극을 짐작으로 잡았는데**, 9-1 표가 실측 근거다.

⚠ ②는 상무 54종이 이미 있으니 **새로 쓰는 게 아니라 잇는 일**이다.
다만 그 54종은 템플릿을 안 타므로(9-3) 이을 자리를 A 와 정해야 한다.

🛑 **상무 이벤트를 새로 쓰지 않는다.** 처음에 0종으로 잘못 세어
"만들자"고 했는데, 54종이 이미 있다. 만들면 중복이 된다.

---

## §10 순위 조건 26종 — 판정과 **판정 방법의 실패** (2026-09-01)

`8915b2dc1`(이벤트가 주인공 리그 순위표를 읽는다) 앞뒤로 세 경로를 쟀다.

### 10-1 결과

```
                      고침 전        고침 후
  대학 GROUP_LAST(gte4)   ❌            ✅      순위축이 살았다
  대학 GROUP_LEAD(lte1)   ❌            ❌
  대학 TOURNAMENT_IN(lte2)❌            ❌
  독립 lte 3~4 넷         ❌            ❌
  독립 SURVIVE_EDGE(gte8) ✅            ✅
  프로 lte 계열 7종       ❌            **✅ 전부**
  프로 gte 계열 5종       ✅            **❌ 전부**
```

**프로에서 lte 와 gte 가 통째로 뒤집혔다.**

### 10-2 🔴 내가 못 박은 판정 기준이 틀렸다

측정 전에 이렇게 적어 뒀다:

```
  ✅ lte 계열이 못닿음에서 빠진다
  🔴 gte 계열이 새로 못닿음에 들어오면 모수가 과하게 좁혀진 것
```

**gte 다섯이 새로 못닿음에 들어왔다.** 기준대로면 🔴 인데, **결함이 아니다.**

이 실행의 주인공 팀은 **1위를 했다** (`TEAM_CHAMPION`·`TEAM_FIRST` 둘 다
`lte 1` 인데 닿았다). **1위인 팀이 8위 이하일 수는 없다.** gte 가 안
닿는 게 옳다.

🔴 **기준이 전제한 것**: 고침 전후로 *팀의 순위 분포는 같고* 조건 평가만
달라진다. **틀렸다.** 고침이 **어느 순위표를 읽는지**를 바꿨으니 측정된
순위 자체가 통째로 달라졌다.

### 10-3 🔴 더 큰 것 — **한 커리어로는 26종을 판정할 수 없다**

순위 조건은 `lte 1` 부터 `gte 8` 까지 걸쳐 있다. **한 팀은 상위권이거나
하위권이지 둘 다일 수 없다.**

```
  고침 전 실행   팀이 하위권   →  gte 전부 ✅ · lte 전부 ❌
  고침 후 실행   팀이 상위권   →  lte 전부 ✅ · gte 전부 ❌
```

**두 실행 다 "절반은 안 닿는다"가 정상이다.** 그런데 `slotreach` 의
`못닿음` 은 그걸 결함처럼 보여준다.

⚠ **"닿음/못닿음"은 순위 조건에 맞는 자다.** 필요한 건 **씨앗 여럿**이거나
**순위 궤적으로 직접 판정**하는 것이다.

### 10-4 ⚠ 같은 씨앗이 같은 세계를 안 준다 — 하네스가 다르면

```
  probe:traits      씨앗 20260803 · draft   무대: 고교 · 프로      (독립 없음)
  measure:slotreach 씨앗 20260803 · draft   무대: 고교 · **독립** · 프로
```

**같은 씨앗·같은 경로인데 커리어가 갈렸다.** 난수 소비량이 달라서다
(프로브는 `trajTick`, 계측은 조건 평가). 루트 CLAUDE.md 잔여 결함 ①
("계측이 재현되는 수준까지만")이 여기 걸린다.

🛑 **그래서 프로브의 순위 궤적으로 `slotreach` 의 도달 결과를 판정하면
안 된다.** 다른 세계다. 나는 그걸 하려다 멈췄다.

### 10-5 남은 진짜 결함 — 순위표 셋

프로브가 직접 잰 값이라 이건 유효하다(무대에 조가 없어 이벤트와 일치).

```
  🔴 독립 순위가 85주 내내 정확히 8      최소=최대=8 · 표본 85
     탈락 로그가 찍히는데 순위표팀수는 10 고정 → 승패·탈락이 안 들어온다
     정렬이 winPct→wins 라 전원 0-0 이면 배열 순서가 순위가 된다

  🔴 프로 순위표에 20팀                  1군+2군이 섞였다
     proSeason.ts:40 주석이 경고해 둔 그 함정이다 (실측 standings 20)
     PRO_TEAM_TOP3(lte 3)가 "20팀 중 3위"를 요구하게 된다

  🔴 프로 72주 중 21주는 순위가 없다     순위표팀수 0~20 · 순위없음 21
     그 주엔 gte·lte 가 **같이** 거짓이다 (conditionEvaluator.ts:157)
```

⚠ 셋 다 A 몫이다. 문턱은 하나도 안 건드린다 — **모수가 맞아야 문턱이
뜻을 갖는다.**

### 10-6 ⚠ 사기는 무대마다 다르다 — 문턱을 올리면 안 된다

```
  [pro_kbl]    최소 12.7 · ≤38 3회 · ≤60 5회      평균 회귀가 돈다
  [highschool] 최소 70   · ≤38~≤60 전부 0         안 내려온다
```

`morale_lte` 를 쓰는 규칙이 **42종**(프로 15 · 대학 9 · 고교 6 · 독립 5 …)
인데 **문턱은 하나고 분포는 무대마다 다르다.** 프로 문턱을 맞추면 고교가
죽고, 고교를 맞추면 프로가 과하게 뜬다.

🔴 **뿌리는 조건 어휘다.** 시즌 성적 축이 전부 한 방향이다:

```
  season_era_lte  season_ip_gte  season_k_gte  season_wins_gte
  → "성적이 나쁘다"를 물을 축이 하나도 없다
  → 부진·강등·기회부족 이벤트가 morale_lte 를 대역으로 쓴다
```

`season_*` 의 반대 방향을 열면 42종 중 상당수가 제 축으로 돌아간다.
