# 진행 현황 — 이어서 할 일

## 🔴 트랙이 나뉘었다 (2026-08-22)

| 트랙 | 폴더 · 브랜치 | 소유 |
|---|---|---|
| **A+C+E** | `ProjectB` · `extract-modals` | 로스터·계약 · 경기·성적 · 계측/기반 · **Rust 전부** |
| **B** | `ProjectB-events` · `track/events` | 이벤트·소식함 → [track-B-events.md](track-B-events.md) |

⚠ **B가 트렁크를 받았다 (2026-08-23 `dc88446ed`, 충돌 없음). A가 병합할 차례다** —
[HANDOFF_B_TO_A.md](HANDOFF_B_TO_A.md)에 확인할 것을 적었다. 특히 **`CLAUDE.md`의
"이벤트 로더 0건"이 사실이 아니고**, B가 A 소유 파일 셋을 최소로 건드렸다(전부 삭제).

**이 문서는 A+C+E 트랙 것이다.** B는 자기 문서를 본다.

⚠ **Rust는 A 트랙만 빌드한다.** B는 `.node`를 복사해 쓴다 —
A가 Rust를 고치면 B에게 알리고 다시 복사하게 한다.
⚠ 병합은 A가 트렁크에서 한다. B가 먼저 트렁크를 받아 충돌을 풀고 알린다.

> 이 문서가 **정본**이다. 새 대화를 열면 여기부터 읽는다.
> 마지막 갱신: 2026-08-21 · 브랜치 `extract-modals` · 마지막 커밋 `492a72ca4`

---

## 방금 닫은 것 — 로스터 회귀 (커밋 `492a72ca4`)

방출 판정에 성적·성향을 잇자 건수가 늘었고, KBL 1군 야수가
**2/10팀(최소 10) → 5/10팀(최소 8)**이 됐다. 원인은 방출이 아니라
**원래 있던 결함 둘**이 드러난 것이었다.

```
release_score/투수 37 · release_score/야수  6     ← 성적 방출은 투수를 더 자른다
release_roster/야수 25 · release_roster/투수  2     ← 정원 정리가 야수를 12배 민다
```

| # | 결함 | 왜 안 보였나 |
|---|---|---|
| ① | 정원 초과 정리가 **보직 하한을 안 봄** | 생성 시점 OVR이라 투수·야수가 고르게 섞였다 |
| ② | 방출이 `fill_first_teams`보다 **뒤** | 방출 건수가 적어 구멍이 작았다 |

②를 고치자 사슬이 맞는다 — **방출 → 2군에서 올림 → 2군은 진로 배정이 채움**.

```
                    기준선       회귀      ①만      ①+②
야수 12 미달   2/10(최소10)  5/10(8)  3/10(8)  3/10(최소 10)
타순 미달           0팀        1팀      1팀       0팀
실패 합계           4건                 6건       4건
```

2군도 안 무너졌다(포수 0팀 2→1 · 투수 최소 8→9). 새 밸런스 수치 없음.

### 🔴 커밋 전에 `test:rosterbalance`를 돌린다

로스터·방출·승강을 건드리면 **커밋 전에** 돌린다. 이번에 "느리다"고 뒤로
미뤘다가 회귀를 커밋한 뒤에 잡았다. 느린 검사는 배경 실행하면 된다.

### 🔴 같은 판정을 하는 자리를 먼저 센다

①은 `fill_first_teams`·콜다운엔 있고 정원 정리에만 없었다. 이 세션에서
같은 형태가 넷이다 — 방출 리그 목록·연봉 배선(생성 vs 유입)·표 중복
(`leagueMult`)·보직 하한. **고치기 전에 `grep -c`로 갈래를 센다.**

---

## 방금 닫은 것 — 방출 판정 (커밋 `3cf53ddb0`)

오프시즌 방출의 입력 넷이 전부 죽어 있었다. **데이터는 다 있었고 안 넘기고
있었을 뿐**이라, 새 밸런스 수치는 하나뿐이다.

| 무엇이 죽어 있었나 | 어떻게 |
|---|---|
| 능력치가 **생성 시점 값** (22곳) | `runOffseasonProcessing`이 live를 합쳐 넘기고 되돌린다 |
| 성적을 안 봄 (`ovr` 대용) | `calcNpcPerfScore` export해 재사용 · 전 리그 5,400건/시즌 |
| 방출 기준이 **전 팀 동일** | `s.proTeamProfiles` 38팀 |
| 독립리그가 목록에서 빠짐 | 넣고 + 연봉 배선 + 재계약 갱신 |

```
독립 방출        0 → 연 23건 (상한 27) · 총원 385로 회전
2군 유입         0 → 연 36~57명
육성 기준값 중앙 72 → 76   (live가 반영된다는 직접 증거)
프로 방출        KBL 37 · ABL 98 · JBL 83 (4시즌) — 폭증 없음
외국인           총원 30 / 10팀 · 2군 체류 0 — 한도 유지
```

🛑 **밸런스 수치 하나** (사용자 확정): `thresholdByLeague.LEAGUE_INDEPENDENT = 40`.
독립은 전원이 최저연봉 1200이라 과지급(+20~50)이 죽고, 성적 하위 10%(+28.8)에
뎁스(+15)를 더해도 43.8이라 공통값 55를 **구조적으로** 못 넘는다.

### ⚠ 사용자 지적이 방향을 네 번 바꿨다

```
"2군 보낼 때는 성적 보지 않나"      → promotionRules에 이미 있었다. 새 수치 0개
"나이가 아니면 계속 데리고 있어서"   → 방출 목록에 독립이 없었다. 나이는 증상
"독립도 연봉 받고 뛰어야 해"        → 유입자만 연봉 0. 같은 리그에 기준이 둘
"성적이랑 OVR에 따라 차이를 둬"     → 재계약이 _1만 돌아 연봉이 평생 고정
```

**네 번 다 내 추측이 틀렸다.** 전부 코드를 읽고 단정한 자리다.

### ⚠ 이번 회차 계측기가 여섯 번 틀렸다

| 계측기 구멍 | 낳은 착각 |
|---|---|
| `farmTeamIds` 안 넘김 | "→2군 0은 게임 결함" |
| `universityAnnualMax` 누락 | 대학 유입 481 (실제 400) |
| `off.logs`로 방출 집계 | 그 배열은 **항상 비어 있다** → 방출 0 |
| 빌드 끝나기 전에 검사 실행 | "검사 3건 실패" |
| 저장소 행에서 최상위로 읽음 | "육성선수 0명" (실제로는 `extra` 안) |
| `args[0]`을 TS 래퍼 이름으로 필터 | 페이로드 0건 (실제는 Rust export 이름) |

전부 **층 경계**에서 났다. 음성 결과가 나오면 **계측기부터 확인한다.**

⚠ `measure:draft`는 Rust만 호출해 **성장도 성적도 없다.** 거기서 독립·육성
결과를 읽으면 안 된다 — 실게임 경로는 `check:devchurn`이다.

---

## 다음 — 연봉 배수 표가 두 곳에 다르다

```
generation_rules.json   LEAGUE_INDEPENDENT 0.14   ← CLAUDE.md가 정본이라 명시
player_engine.rs        LEAGUE_INDEPENDENT 0.35
                        LEAGUE_KBL_FARM 없음 → 1.0 (규칙은 0.3)
```

`CLAUDE.md`가 경고하는 "표를 두 번 적지 말 것"이다. 고치면 **프로 2군 연봉이
3배 넘게 움직여** 밸런스라 손대지 않았다. **사용자에게 물어야 한다.**

---

## 방금 닫은 것 — 육성선수 단년 계약 만료 (커밋 `47e470749`)

미지명자가 **첫 해만 74명** 2군에 들어가고 그 뒤로 0명이었다. 뿌리는
밸런스가 아니라 배선이었다 — 육성선수는 `contract_years = 1`로 들어오는데
만료 판정이 `market.ts`에 있고 거긴 `_1`(1군)만 훑는다.

```
연도    →2군 (전→후)     포기 (전→후)
2026      74 →  79       775 → 770
2027       0 →  57       937 → 879
2030       0 →  36       971 → 929
2군 총원 중앙 34 → 36
```

사용자 확정: **성장했으면 재계약** · **풀 우선** · **KBL만**.
비교 기준은 직전 판정 시점이다(입단 대비면 어린 선수가 다 성장해 아무도 안 나간다).

⚠ **첫 시도는 회전문이었다.** 방출만 20→108로 늘고 자리는 안 났다 —
방출된 육성선수가 같은 패스의 12단계로 가서 같은 자리에 다시 들어갔다.
**검사 20개가 전부 통과한 상태였고 실측만이 잡았다.** 검사는 한 선수의
판정이 옳은지 보고 그 자리에 누가 들어가는지는 안 본다.

### 다음 — 독립리그가 2027년부터 0명이다

```
독립  10팀 · 최소 26 최대 45 평균 43.1
```

정원 45에 평균 43으로 꽉 찼는데 **최소 26인 팀도 0명을 받는다.**
2군과 같은 형태로 보인다 — 나가는 경로가 있는지 안 봤다.

---

## 지금 하던 것 — 외국인 보유 한도

KBL 외국인이 **정원의 네 배**였다. 규칙은 팀당 3명(투수 2명)인데 5시즌 뒤 113명.

```
전         총원 113명 · 2군 체류 34
FA 수정 후  총원  86명 · 2군 체류 29   ← 커밋 0132a37a6
목표        총원  30명 · 2군 체류  0
```

### 남은 결함 — 방출된 외국인이 KBL 2군으로 간다

이력이 그대로 보여준다:

```
2029:release→TEAM_KBL_BUSAN_WAVES_2
```

초과분의 사건을 세면 `release 85 · fa_signed 31 · foreign_signing 5`다.
**`release`가 최다**다.

`foreignRules._note3`가 이걸 금지한다:

> **1군만.** 2군에 두면 보유 한도 계산이 흐려지고, 실제 KBO도 외국인은
> 1군 등록이 원칙이다

`foreignPlayers.ts`의 재계약 불가 경로는 이미 **"본국 복귀"**로 처리한다.
**방출 경로만 그걸 안 따른다.**

**고칠 자리**: `npc_sim.rs:1570` `placer.place(...)` 앞, 또는 `Placer` 안.
`draft.rs:195`에 **같은 형태의 가드가 이미 있다**:

```rust
// ⚠ 국적으로 거른다 — 리그로 거르면 못 잡는다. 그 사람은 그 시점에
// 독립리그 소속이고, 독립리그는 외국인 개념이 없는 리그다.
if npc.nationality.as_deref().unwrap_or("KOR") != "KOR" { return None; }
```

드래프트는 막았는데 방출은 안 막았다 — **한 경로만 고치고 "됐다"고 읽은**
여섯 번째 사례다.

**측정**: `npm run test:foreign` (느리다 — 배경 실행). 선수별 이력은 `-- --verbose`.

---

## 사용자 답 대기 — Part 2 (착수 금지)

"좋은 외국인이 오면 기존 외국인을 밀어낸다"는 방향은 **확정**됐다(KBO 방식).
교체 틀은 `foreignPlayers.ts`(343줄)에 이미 있다 — 매년 ① 재계약 판정
(`foreignRules.renew` 미달 시 퇴출) → ② 충원.

🛑 **밀어내기 기준이 밸런스 수치라 정할 수 없다.** 물어 둔 상태:

| | 규칙 |
|---|---|
| A | 지금대로 OVR·나이만 |
| **B (권장)** | **표본이 차면 성적, 모자라면 OVR** — 승강 판정이 쓰는 방식 |
| C | 항상 성적 우선 |

B면 숫자 둘이 더 필요하다 — **표본선**(투수 N이닝·타자 N타석) ·
**성적 판정선**(ERA·OPS 얼마 아래면 재계약 불가).

⚠ 기존 주석이 성적을 안 쓴 이유를 적어 뒀다 — *"표본이 얇은 선수를 억울하게
자른다"*. 실수가 아니라 판단이었고, B가 그 걱정과 사용자 지적을 둘 다 사는
절충이다. `npcLiveStatsStore`가 이미 붙어 있어 **데이터는 문제가 아니다.**

---

## 🔴 리그를 열거나 고칠 때 — 확인할 표 목록

**이 세션에서 같은 형태를 다섯 번 만났다.** 어떤 표에는 리그가 있고 어떤
표에는 없다. 해외를 열 때 `rosterRules`엔 2군을 넣었는데 `salaryRules`엔
안 넣어서 **절반 이상이 연봉 0**이었고, 방출 목록엔 독립리그가 빠져 있었다.

### `generation_rules.json`의 리그별 표 여섯 (2026-08-22 실측)

| 표 | 지금 든 리그 | 안 든 리그가 정상인가 |
|---|---|---|
| `rosterRules` | 9개 전부 | — |
| `salaryRules.leagueMult` | 7개 | 아마추어 둘은 연봉이 없어 정상 |
| `salaryRules.minSalary` | 7개 | 위와 같다 |
| `faRules.eligibleYears` | KBL·ABL·JBL | FA는 1군만이라 정상 |
| `faRules.release.thresholdByLeague` | 독립만 | 나머지는 공통값(55)을 쓴다 |
| `foreignRules.origin.weights` | ABL·ABL_FARM·JBL | 용병 출신 리그다 |

**리그를 추가하면 여섯을 하나씩 보고 "넣는다/일부러 뺀다"를 정한다.**
빠뜨리면 그 리그만 조용히 다른 규칙으로 돈다 — 오류가 안 난다.

### 코드에 박힌 리그 목록

```
npc_sim.rs:747    은퇴·진로 판정      "LEAGUE_KBL" | "LEAGUE_ABL" | "LEAGUE_JBL"
npc_sim.rs:892    방출 2단계 대상
npc_sim.rs:1027   방출 리그 목록      ← 여기에 독립리그가 빠져 있었다
npc_sim.rs:1495~  프로 판정 셋
game.ts:1139      proLeagueSet
slotLifecycleV3:501  isPro
```

⚠ `CLAUDE.md`: **"프로 운영에 리그를 직접 적지 말 것"** — 정본은
`releaseScope.activeProLeagues()`다. 위 목록은 아직 그걸 안 쓰는 자리들이고,
새 리그를 열면 **여기부터 본다.**

### 연봉 배수는 규칙 파일이 정본이다

`player_engine.rs`에 표가 따로 박혀 있었고 어긋나 있었다(독립 0.14 vs 0.35 ·
KBL 2군 0.3 vs 없음→1.0). 지금은 지도를 받아 쓴다 — **다시 박지 않는다.**

---

## 그 밖 남은 것 (우선순위)

**재봐야 아는 것**
- 투수 공급 미달 — KBL 1군 7~10명(하한 12) · 해외 2군 투수율 0.37.
  ⚠ **`test:rosterbalance`도 집계 범위부터 의심한다**(`foreignProbe`가 그랬다)
- FA 309 → 95 → 26 급감 · loyalty 연간 감쇠 게이트(프로 W50 지나서) · 홈 승률 33%

**간단·안전**
- ~~깔때기 예외 둘~~ — **둘 다 이미 닫혀 있다** (2026-08-23 확인).
  `applyGameOutcome`은 세 경로가 배타적이다 — 앞의 둘은 `recordGameResult`를
  부르고 `return`해서 마지막 `recordGameLogs`에 안 닿는다. 중복 기록이 없다.
  그 호출은 **의도된 별도 경로**다 — 주인공 경기는 경기 엔진을 타서
  배경 시뮬도 `simulateNpcGame`도 안 거친다(옆 주석에 적혀 있다).
  `MainPage.svelte`엔 `recordGameLogs`·`recordGameResult` 직접 호출이 **0건**이다.
- ~~A7 프로 전환 헤더~~ — **재현하고 고쳤다** (6d2ac2221 · 2026-08-24).
  드래프트 결정이 먼저 `careerStage`를 pro로 바꿔 그 해 시즌 종료가 연차를 +1 했다.
  `advanceSeasonYear`가 **끝나는 시즌의 리그**도 보게 했다.
  ⚠ 연도가 되돌아간다는 건 오염 세이브 증상이었다 — 재현된 건 "2년차"뿐이다.
- ~~`test:injury` 간헐 실패~~ — **닫혔다** (07c2ab3be). 3회 통과 + 출력 해시 3회 동일.
  엔진(`calc_injury`)과 **검사 둘 다** 씨앗을 안 쓰고 있었다.
  ⚠ 씨앗을 하나로 고정하지 않았다 — 반복마다 다른 고정 씨앗이라 표본은 그대로 다양하다.
- ~~svelte-check 47건 미확인~~ — **설치해서 재보니 다른 숫자였다** (1423b978a).
  537파일 · **40 ERRORS · 51 WARNINGS · 14파일**. `npm run check:svelte`로 잰다.
    12 PreGameBriefingModal · 7 MatchPage · 6 TrainingPage · 4 MainPage ·
    4 GameStatusModal · 4 CareerResultsModal · 1씩 NewGamePage·InjuryTreatment·ContractNegotiation
  ⚠ **`tsc` 0건과 별개다** — `.svelte` 안의 스크립트·템플릿·CSS를 본다.
    첫 오류는 실제 타입 불일치다(NewGamePage가 `ProtagonistSave` 필드 11개를 안 채운다).
    WARNINGS 상당수는 "Unused CSS selector" — 무해하지만 죽은 CSS다.
  **고치지 않았다 — 목록만 넘긴다.**

**2026-08-24에 닫힌 것 (루프)**
- 시즌 종료 누락 (76ef1b7cf) — `contractDecision`의 즉시 계약이 다음 시즌을
  직접 열면서 이번 시즌을 안 닫아 **그 해 순위·수상·오프시즌이 통째로 사라졌다.**
  같은 함정이 `careerDecision`에만 고쳤어 있었다.
- 상무 Phase 1 (cc535d1fe · 05b72c752) — `&[]`가 박혔 안 돌았고, 켜자 정원 4배
  초과가 드러났다. 그 뒤 **일반병 전역자까지 상무 공백으로 센 것**도 고쳤다(43→13건).
- 구단 성향 (7882c6a7e · b91f369e4) — 새 게임이 덮어 **스토어가 항상 0개**였다.
  KBL은 데이터 자체가 없어 예산 지수에서 유도한다(지출 26~75 · 압박 35~65).
- 프로 연차 이적 리셋 (83670ba65) — `isNewTeam`이 "팀이 바뀜다"라 FA 자격에
  영영 못 닿았다. 2군 시즌도 연차로 센다(fb659e348).
- tsc 9건 → **0건** (d1b4cbb54) — 넓은 타입이 결함 셋을 가리고 있었다
  (콜업 로그 OVR이 항상 0 · 트레이드 이벤트 ID에 슬롯 누락 · 죽은 갈래).
- 슬롯 전환 가드 (573b77da1) — `resetWorldSeasonEndGuard` 호출부가 0건이었다.
- 압박 포화 (e4d622a21) — `dev`와 연속실패를 둘 다 더해 **같은 부진을 두 번** 쌀다.
  6시즌 최대가 100에서 72~87로 내려왔다.
- 재계약 성적 반영 (0c71f08b2) — FA로 올린 몸값이 이듬해 되돌아가던 것.

**2026-08-24에 새로 열린 것**
- FA 입찰 상한이 거꿔다 — `flex = (cap - 총연봉)/cap`이라 **부자 구단일수록
  적게 부른다.** 현실 야구와 반대다. `perfSpan 0.7`이 순서를 세운 건
  이 상한을 힘으로 넘은 것이지 상한이 합리적이어서가 아니다.
- 베테랑 4% — 1군 316명 중 7년차 이상이 10~15명. 원인 미상.
- 대학 진학 후에도 `seasonStore.leagueId`가 고교로 남는다 — **지금은 영향 없다**
  (481e028d7 주석). 대학 시즌을 리그별로 다룬다면 그때 본다.

**기획 판단 (루프를 안 막음)**
- 해외 팀 전력★ 값(04에도 없다 — 새로 정해야 한다) · 과거 성적 5년치
- 대학 보충 경로 · UI 영어화 범위 · 이야기 이벤트 87% 유실

**부채**
- `game.ts` 2,579줄 · `advanceWeek.ts` 1,900줄+ · 히스토리/인생 기록 화면
- 은퇴자 영구 누적(10시즌 npc +10,431행) · 폴백SIM(관측 대기)

---

## 성능 — 끝났다 (사용자 판단)

```
IPC 3.05GB → 2.12GB (-30%) · 주당 2.24초 · p95 4.38초
```

마지막 작업은 **일정 델타**(-29%) — 천장 94.4%를 **먼저 재고** -89% 달성.
다시 손대려면 `measure:perf`의 **IPC 바이트 비중 표**부터 본다(순위가 바뀌었다).

⚠ **분모를 맞춘다.** `personality`가 `syncNpcs` 페이로드의 20.1%라 "-20%"로
잡았는데 전체로는 **2.3%**였다. 채널 페이로드 비중 ≠ 전체 비중.

---

## 이 세션에서 되풀이된 형태 — 열둘

고치기 전에 이걸 먼저 읽는다. 여기 적힌 것들이 실제로 **매번** 일어났다.

1. 🔴 **고치기 전에 재현부터 확인한다.** A8·폴백SIM 둘 다 **이미 닫혀 있었다.**
   없는 결함을 고칠 뻔한 게 **다섯 번**
2. 🔴 **지표가 없는 결함을 지어낸다 — 다섯 번.** "대학 대회 23건 누락"은 내
   대조 키가 주차를 써서 생긴 착시였다(날짜로 맞추니 100%).
   다만 `foreignProbe`는 **반만** 지어냈다 — 잡음이 진짜를 덮고 있었다.
   **지표를 고치는 건 결함을 없애는 게 아니라 보이게 하는 일이다**
3. 🔴 **한 경로만 고치고 "됐다"고 읽는다 — 여섯 번.** `grep -c`로 갈래를 센다
4. 🔴 **국적 문지기 — 다섯 번.** `isForeignInQuotaLeague(국적)`은 "KBL에서
   외국인인가"라 USA·JPN이면 무조건 true다. **"이 리그에서 외국인인가"를
   물어야 할 자리**에 쓰이면 해외 로스터가 통째로 걸러진다.
   **해외가 걸린 코드를 만나면 먼저 의심한다**
5. 🔴 **타입이 좁으면 새 필드가 조용히 잘린다.** `foreignPerTeam`을 추가했는데
   중간 인자 타입이 몰라서 Rust까지 못 갔다 — **한도가 안 걸렸고 오히려 늘었다**
6. 🔴 **코드를 읽고 단정하면 틀린다 — 여섯 번.** 천장을 먼저 잰다
7. 🔴 **주석을 믿지 않는다** — 다만 주석이 **왜 그렇게 했는지**는 읽는다.
   `foreignPlayers.ts`가 성적을 안 쓴 건 실수가 아니라 판단이었다
8. 🔴 **내 시험이 틀릴 수 있다.** Ctrl+Q를 "기능 없음"으로 적을 뻔했다 —
   메인 페이지 전용인데 팀 화면에서 눌렀다. **음성 결과가 나오면 시험을 먼저 의심**
9. **소스를 정규식으로 보는 검사는 배선을 못 본다.** `fieldersPayload.test.ts`가
   통과하는데 `get is not defined`로 새 게임이 2주차에서 죽었다
10. **변이 검증을 붙인다** — 통과만 하고 아무것도 안 보는 검사가 여럿 있었다
11. **04 문서를 그대로 믿지 않는다** — 옮기기 전에 02에서 확인한다
12. **적용 전후를 잰다.** 이 세션에서 가장 값어치를 한 습관이다 —
    "고쳤다"고 보고할 뻔한 걸 두 번 잡았다

---

## 명령

```
검사  npm test (63파일 685건) · test:foreign(-- --verbose) · check:msgdup ·
      check:recentgames · check:amateurworld · check:mojibake · test:slotdb ·
      test:migration · test:newgame · diag:npcgen
계측  measure:perf(IPC 바이트 비중 표) · measure:dirty · measure:memory ·
      measure:savesize · measure:growth
빌드  npm run build · build:native(Rust 고치면 필수, 약 1분 45초)
화면  DRIVE_USER_DATA=1 node scripts/drive.mjs <명령파일>
```

⚠ **느린 명령은 배경 실행한다** — 타임아웃이 여러 번 났다
⚠ `grep -c`가 0이면 exit 1이라 `&&` 체인이 끊긴다 — `|| true`
⚠ 내 변경이 검사를 깨뜨렸는지 모르면 **stash로 baseline을 잰다**:
  `git stash push -- apps packages && <검사> && git stash pop`
  (**pop을 반드시 한다** — 이번 세션에 작업이 묻힐 뻔했다)
⚠ 긴 조각은 파일로 쓴다 · 백틱·`\n`·`\u`는 셸이 먹는다(줄 단위 치환이 안전)

## 멈추고 묻는 것

밸런스 수치(Part 2 기준·해외 전력★) · 새 기획 · 되돌릴 수 없는 파괴

⚠ **04를 지우지 않는다.** ⚠ **Steam 빌드·패키징 안 한다.**
⚠ **사용자 세이브를 지우거나 앞으로 돌리지 않는다.**
