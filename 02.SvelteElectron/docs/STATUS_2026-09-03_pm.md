# 정리 — 어디까지 왔나 (2026-09-03 14:20 · 07:30 부터의 반나절)

> 마감: 개발 마무리 **9/5(당김)** · 프리즈 **9/15** · 빌드 **9/28**. 트렁크 커밋 **92건**(07:30 이후 · 병합 18 포함).
> 순서·상태의 정본은 [PROGRESS_TREE.md](PROGRESS_TREE.md) · 일정은 [PLAN_RELEASE §2.2](PLAN_RELEASE_2026-09-28.md).
> 오전 판은 [STATUS_2026-09-03.md](STATUS_2026-09-03.md).

## 1. 한 줄

**체제가 바뀌었다.** OP(총괄)가 A(엔진)·B(이벤트)·C(화면)·D(계측)·S(문서) 다섯 워크트리를
직접 지휘하는 구조로 09-03 13:50 전환했다. 같은 시각 **출시 모델도 확정**됐다 —
"1.1" 이라 불리던 항목들은 1.1 이 아니라 **v1.0.0 의 구현 범위**다. 구현을 전부 끝내고
숫자만 남으면 v1.0.0 으로 고정하고, 수치 조정·테스트 반복 뒤 **1.0.1 로 출시**한다.

## 2. 닫힌 것 (07:30 ~ 14:20)

### 체제·출시 모델
| 항목 | 결과 |
|---|---|
| OP 체제 전환 (13:50 · `d0cb1e5b7`) | A 는 Opus·`ProjectB-engine`(`track/engine`) 로, C⑤ 는 `ProjectB-ui`(`track/ui`) 로 재개 |
| 출시 모델 확정 (14:04 · `81b65f09e`) | 구현 완주 → **v1.0.0 고정** → 수치 조정 → **1.0.1 출시**. 밸런스 값은 제안값으로 넣고 [BALANCE_BACKLOG.md](BALANCE_BACKLOG.md) 에 적는다 — 묻지 않는다 |
| v1.0.0 구현 단위 진행률 | **7 / 18 ≈ 39%**(14:2x 기준 · PROGRESS_TREE) |

### A① 보직 적합도 산식 · A② 고교 엔진
| 항목 | 결과 |
|---|---|
| A① | 세부 능력치 + 팀내 자리 경쟁으로 산식 교체 (Rust `pitcher_role.rs` · `0ae9b586d`) · `recommendRole()` 실제 교체 완료 (`8f922a58f`) |
| A② | 고교 리그 투구수 상한·선발 아웃 계수·마무리 문·의무 휴식 (`00db08993` · 상태 검사 4종 `59f493a2f`) · 호출부 셋(자동 진행·경기 화면) 배선 완료 (`660f6076d`) |
| 결함 수정 | 주인공 자신의 등판 기록이 없던 것 — 의무 휴식 재료 0 → 30 (`bc44f123b`) · 마무리 등판당 2.7~3.2이닝은 엔진이 아니라 **평균 계산이 만든 숫자**였다고 계측 정정 (`237a848ee`) |

### C①~③ · C⑤-a
| 항목 | 결과 |
|---|---|
| C① 보직 선택 인라인 | 통보 모달이 아니라 소식 안에서 고른다 (`d1dcbdac0` · 18파일 · vitest +72) |
| C② FA 제안 카드 조건 표시 | 계약금·옵션·노트레이드를 있는 줄만 표시 (`4bae911aa` · vitest +27) · 확인 단계는 비추천 버튼에만 |
| C③ 계약 협상 페이지 | 「＋ 추가」 조항·인센티브(상한 3)·최저연봉 하한·역제안 1~3회 + myBody 대시보드 갈래 + 「노트레이드」 이름표 통일 (`885662a21` · 17파일 · vitest +89 → 1,959) |
| C⑤-a | 은퇴 직후 결산 자동 열기 — 경로 셋 중 둘만 열리던 것 (`ea6b92c86`) |

### B-11 ~ B-21
| # | 항목 | 결과 |
|---|---|---|
| 11 | 계약 조건 기획 | §8 아홉 사용자 확정 → 신규 계약 페이지 시안 → **완전히 닫힘**(`9909af5b6`) — 못 잴 축(보직 미달)은 사용자가 직접 고르므로 계측 대상에서 제외 |
| 12 | 보직 선택 문안 | `role_choice.json`(굴절형 roleAs/roleObj) |
| 13 | 계약 협상·인센티브 정산 문안 | `contract_terms.json` |
| 14 | 보직 문안 합쇼체 통일 | 사용자 결정 |
| 15 | 고교 강판·불펜·마무리·의무휴식 문안 | `pitching_usage.json`(초안 · 의무 휴식은 코드 0~5일 기준) |
| 16 | 동적 치환 감사 | 529 템플릿 중 치환 40종 실측 |
| 17 | 고교 TOP10 이벤트 삭제 | 사용자 결정 · 591→588 · 도달률 분모 155→152 |
| 18 | 메시지 종류별 표시 형태 전수 | 텍스트 49자리 + 이벤트 588 · 통 수 실측 1,974건(밀려남 474 = 24%) |
| 19 | 텍스트 소식 대시보드화 기획 | 48자리 전수 → §6 여섯 확정 → §9 셋 확정 → **닫힘**(구현은 1.1 배정: A 생산부 배열화 + C StatTable·RankListPanel) |
| 20 | 병영 재회 이벤트 초안 12종 | `military_reunion.json` — 구조 질문 셋 **사용자 결정 대기**(§3 참고) |
| 21 | 대시보드 문안 데이터 | `dashboard_labels.json`(34자리) |

## 3. 열린 결정 — 재회 이벤트 범위 (사용자 결정 대기)

B-20 이 병영 재회 이벤트 초안 12종을 냈지만, v1.0.0 에 넣을지는 구조 질문 셋에 달려 있다.

| # | 무엇 |
|---|---|
| ① | 위생 검사가 군 조건 경로를 통째로 막는다 — 값 `군필` 을 열어야 한다(A 담당) |
| ② | 전역 뒤 갈 수 있는 무대는 프로 셋 + 독립뿐(대학 없음 · 설계상) |
| ③ | 조건 키 여섯 필요 — `militaryRecord` 경로 · `weeksSinceDischarge` · 부대원 relation kind · `relationDelta` personId · 문안 `{memberName}` 렌더러 |

## 4. 사용자 결정 기록 (07:30 ~ 14:20)

| 무엇 | 확정 내용 | 커밋 |
|---|---|---|
| 계약 조건 §8 아홉 | [PLAN_CONTRACT_TERMS.md](PLAN_CONTRACT_TERMS.md) §8 전부 확정 | `2b593ff76` · `ad098ba23` · `9909af5b6` |
| 보직 추천·선택 §8 열넷 | [PLAN_ROLE_RECOMMEND.md](PLAN_ROLE_RECOMMEND.md) §8 전부 확정(고교 마무리는 제안 안 둔다 · 상무는 안 묻는다 포함) | `55a050484` · `5e14b29ee` |
| 대시보드화 §6 여섯 | 기록 탭 배치·소식함 보존 기간 등 | `1484de168` |
| 대시보드화 §9 셋 | 보존은 종류 구분 없이 오래된 순 FIFO · 군 경력은 타임라인 한 줄 · 대진표는 표로 | `1db843c95` |
| 새 게임 첫 소식함 자리표시자 넷 삭제 | 삭제한다 | `07cba2343` |
| 고교 105구 결함 | 주인공 경기에만 leagueId 가 안 실리던 것 — 사용자 지시로 고침(실측 전후 분포 동일 · 밸런스 변화 없음) | `76a326d4f` |

## 5. 남은 것 — v1.0.0 구현 단위 (18 중 11)

```
A  ③ 추천 밖 불이익 depthFactor 배선
A  ④ 인센티브 시즌 끝 정산 로직 (§7⑤⑥)
A  ⑤ 소식 생산부 배열화 30자리 + 순위 스냅샷 last_digest
C  ⑨ 인센티브 정산 화면
C  ⑪ 상무 탭 §39
C  ⑫ 대시보드 컴포넌트 — StatTable·타임라인·RankListPanel 범용화
C  ⑬ 대시보드 배선 30자리 4묶음
C  ⑭ 기록 탭 카드 둘 — 계약 이력·대회 전적
C  ⑮ 소식함 FIFO 한 줄 + 군 조건 위생 검사 정정
A/C ⑰ 병영 재회 이벤트 배선 — ❓ §3 의 범위 결정 대기
전원 ⑱ 회신 결함 수정 (빌드 눈확인 뒤)
```
프리즈 전(9/8~15)은 빌드 회신 결함만 — 9/14 최종 빌드 · 9/15 프리즈(App/Depot ID 사용자).

## 6. 오늘 배운 것 (다음 세션이 또 겪지 않게)

- **워크트리 공유 파일 사고 둘** — 다른 세션이 가동 중인 워크트리의 공유 파일(`master.ts` 등)을
  같이 커밋에 실으면 미완성 편집이 섞여 들어간다. `76a326d4f` 에 C 의 미완성 import 셋이 섞여
  HEAD 가 컴파일되지 않았다(`ce51cbb47` 로 걷어냄) · `07cba2343` 에 C 의 진행 중 `migrateContract`
  편집이 섞였다(`fcef54508` 로 걷어냄). **가동 중인 세션의 공유 파일은 커밋하지 않는다.**
- **Opus 한도** — C 세션이 API 한도로 두 번 멈췄다(14:50→16:30 · ~19시→21:30 · 이전 기록).
  B·C 는 한 턴이 끝나면 멈춘다 — 인계는 문서 갱신 + 메시지 한 세트로, 재개 지시 없이는 안 돈다.
- **신뢰 대화상자** — OP 가 A·C 를 `claude remote-control` 서버 모드로 띄우려 했다.
  그런데 새 워크트리 폴더(`ProjectB-engine`·`ProjectB-ui`)는 **사람이 그 폴더에서 `claude` 를
  한 번 실행해 신뢰 대화상자를 수락해야** 서버로 뜬다 — 설정 파일에 신뢰를 직접 적어 넣는 것은
  **권한 분류기가 막아** 중단됐다. 그래서 A·C 는 서버 모드 대신 **서브에이전트(워크트리
  격리)로 대체**했다. 사용자가 돌아오면 두 폴더에서 신뢰 대화상자를 수락한 뒤 서버 모드로
  전환할 예정이다.
