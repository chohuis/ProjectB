# 문서 간 사실 불일치 — 2026-09-03 훑은 것

> S(서기)가 오늘 나온 문서(PLAN_ROLE_RECOMMEND · PLAN_CONTRACT_TERMS ·
> PLAN_MESSAGE_DASHBOARDS · PLAN_MILITARY_LIFE · HANDOFF_* · CLAUDE.md ·
> RESUME · BALANCE_*)를 훑어 같은 사실을 다르게 적은 곳을 찾았다.
> **고치지 않았다 — 표만.** 코드는 관련 파일을 읽기만 했다(안 고쳤다).

## 1. 로테이션 자리 수 — 함수 두 개 중 하나만 고쳐졌다 (A 할당)

| | 내용 |
|---|---|
| 문서 A | `PLAN_ROLE_RECOMMEND.md` §a — "로테이션 자리 수가 규칙 파일과 Rust 에서 다르다. `rosterOpsRules.rotationSize`는 고교 3·대학 3·독립 4·그 밖 5인데, `assign_protagonist_role`은 리그와 무관하게 `rank <= 5`로 5선발까지 준다" (결함으로 적음) |
| 문서 B | `PROGRESS_TREE.md` 235행 — "코드 결함 둘: ✅ 고교 105구 leagueId 누락 → 고침 · ⬜ 고교 마무리 의무 휴식 → 1.1 A②" — **로테이션 불일치는 결함 목록에 없다** (고쳐진 걸로 안 다룸) |
| 코드 실측 | `packages/engine-native/src/pitcher_role.rs:228` (오늘 신설 `recommend_role`) 은 `rank_sp <= p.rotation_size` 로 **파라미터를 받아 고쳐졌다.** 그런데 `packages/engine-native/src/player_engine.rs:114` 는 여전히 `if rank <= 5 { format!("{}선발", rank) }` — **하드코딩된 5가 그대로다.** `ROLE_ASSIGNMENT_2026-09-03.md:86` 도 이 옛 동작(`rank ≤ 5`)을 실측 결과로 적고 있다 |
| 정본 후보 | 코드(현재 상태) — **두 함수가 따로다.** 추천 산식(`pitcher_role.rs::recommend_role`)은 리그별 자리 수를 지키게 됐지만, 표시용 랭크 문구(`player_engine.rs`)는 여전히 5선발까지 붙는다. 실제 배정에 어느 쪽이 쓰이는지는 이 표에서 안 밝힌다 — A 가 확인할 것 |
| 고칠 곳 | **A 할당** — `player_engine.rs:114` 를 `pitcher_role.rs::recommend_role` 처럼 리그별 자리 수를 받게 고치거나, 어느 쪽이 실제 배정에 쓰이는지 확인해 `PLAN_ROLE_RECOMMEND.md` §a 와 `ROLE_ASSIGNMENT_2026-09-03.md` 실측을 갱신한다(둘 다 코드·계측이라 S 는 못 건드린다) |

## 2. 상무 이벤트 재고 — 34 vs 39 (✅ S 09-03 고침 · 도달 수는 여전히 미측정)

| | 내용 |
|---|---|
| 문서 A (낡음) | `BALANCE_BASELINE_2026-09-05.md` 58행 — "상무 이벤트 도달 \| 27/34 (죽은 풀 5종 → military_common 이동 뒤) \| B-10" |
| 문서 B (최신) | `EVENT_UNREACHED_2026-09-03.md` 157~158행 — "그때 상무 재고 = common 14 + sports 20 = 34 → 27/34 = 79% / 지금 상무 재고 = common 19 + sports 20 = 39 → 27/39 = 69%(같은 27 로 치면 · 실은 **미측정**)" |
| 이미 낡음 표시된 곳 | `BALANCE_BACKLOG.md` 84행 · `HANDOFF_B_TO_A.md` 565·990~993행 — 셋 다 "27/34 는 낡았다 · 지금은 39" 라고 스스로 적어 뒀다 |
| 정본 후보 | `EVENT_UNREACHED_2026-09-03.md`(가장 최근 실측 시도 · 재고 39 확인 · 도달은 **미측정**이라고 명시) |
| 고친 곳 | `BALANCE_BASELINE_2026-09-05.md` 58행을 재고 39 로 고치고, 27 은 재고 34 시절 값을 그대로 쓴 것이며 옮긴 5종은 아직 안 쟀다고 적었다. 도달 수 자체를 재는 것은 계측이라 S 는 안 했다 — 다음 상무 도달률 실측 뒤 그 값으로 다시 갱신 |

## 3. 병영 의무 휴식 일수 — 1~3일 vs 0~5일 (이미 해소됨 · 기록용)

| | 내용 |
|---|---|
| 문서 A (구전 인계) | `HANDOFF_B_TO_A.md` 1027행 — "🔴 의무 휴식 일수가 인계와 코드가 다르다. A 는 '투구수 구간별 1~3일' 이라 전했는데 `rest_rules.rs:15~24` 는 **0~5일**이다" |
| 코드 실측 | `rest_rules.rs:15~24` — "0~30구 → 0일 · 31~45 → 1 · 46~60 → 2 · 61~75 → 3 · 76~95 → 4 · 96+ → 5" |
| 이미 코드 기준으로 맞춘 곳 | `PLAN_ROLE_RECOMMEND.md` 710행("투구수별 0~5일") · `PROGRESS_TREE.md` 196행("의무 휴식은 코드 0~5일 · 기획서 숫자는 코드 기준") |
| 정본 | 코드(`rest_rules.rs`) — 문안(`pitching_usage.json`)은 `{days}` 변수만 쓰므로 숫자가 문안에 안 박혀 있어 안전하다 |
| 고칠 곳 | 없음. `HANDOFF_B_TO_A.md` 는 결함 발견 기록이라 그대로 둬도 된다(같은 문서 안에서 이미 코드 값을 적어 자체적으로 해소됐다) |

## 4. "1.1" 표기 — RESUME.md 만 출시 모델 갱신이 안 됐다 (✅ 이미 해소 · S 09-03 고침)

| | 내용 |
|---|---|
| 문서 A (안 갱신이었다) | `RESUME.md` "## 1.1 (프리즈 뒤 · 우선순위)" 절 — "병영 문안 다듬기 · 병영 밸런스 · 재회 이벤트 · 상무 탭 §39 → **보직 추천·선택(PLAN_ROLE_RECOMMEND) · 계약 조건(PLAN_CONTRACT_TERMS · 인센티브)** → 고교 사기 …" — 이 두 항목을 여전히 "프리즈 뒤 1.1" 로 적어 뒀다 |
| 문서 B (갱신됨) | `PROGRESS_TREE.md` "## 1.1 첫 묶음 — 사용자 확정 표" 바로 아래 — "🔴 **출시 모델(사용자 확정 09-03 오후)**: 이 표는 '1.1' 이 아니라 **v1.0.0 구현 범위**다"(`81b65f09e`). 보직 추천·계약 조건은 표 안에서 A①~⑤·B①~④·C①~⑤로 **지금 구현 중**이다 |
| 정본 후보 | `PROGRESS_TREE.md`(사용자 확정 시각이 박혀 있고 가장 최근) |
| 고친 곳 | `RESUME.md` "## 1.1" 절을 "## 1.0.1 뒤" 로 다시 썼다. 보직 추천·계약 조건·대시보드·재회 이벤트(축소판)·학습 강도 선택지·고교 부진 이벤트는 v1.0.0 구현 범위로 PROGRESS_TREE.md 표 링크만 남기고, 밸런스 값(병영 밸런스·고교 사기 등판 비중 포함)은 BALANCE_BACKLOG.md 링크로 옮겼다. 진짜 1.0.1 뒤로 남은 것은 이름 렌더러·인물 지정·상무 경기 같은 v1.0.0 범위 밖 구조뿐이다 |

## 5. 소식함 보존 기간 — 종류별 넷 단계 vs FIFO (이미 해소됨 · 기록용)

| | 내용 |
|---|---|
| 문서 A (앞선 제안) | `PLAN_MESSAGE_DASHBOARDS.md`·`HANDOFF_B_TO_A.md`·`PROGRESS_TREE.md` 셋 다 앞부분에 "보존 시즌 값 넷(A 2·B 3·C 4·D 영구)" 제안을 적어 뒀다 |
| 문서 B (확정) | 같은 세 문서가 **그 아래 §9 확정**에서 뒤집는다 — "① 종류별 보존 시즌? ❌ 안 나눈다 — 상한에 차면 오래된 것부터(FIFO)"(`1db843c95`) |
| 정본 | FIFO(확정) — `trimMailbox` 에서 `take` 한 줄만 지우면 끝나고, 종류표·보존 시즌·만료 판정·`MessageItem.seasonYear` 는 전부 안 만든다 |
| 고칠 곳 | 없음. 세 문서 다 제안과 확정을 **같은 문서 안에서 순서대로** 적어 뒀다 — 앞부분만 보고 인용하면(예: 표 밖으로 문장만 떼어 가면) 낡은 안으로 오해할 수 있다는 점만 다음 인용자에게 주의 |

## 요약

| # | 무엇 | 상태 | 고칠 곳 |
|---|---|---|---|
| 1 | 로테이션 자리 수 — recommend_role 은 고쳤는데 player_engine.rs 는 그대로 | 🟡 **A 할당** | player_engine.rs:114 · PLAN_ROLE_RECOMMEND.md §a · ROLE_ASSIGNMENT_2026-09-03.md |
| 2 | 상무 이벤트 재고 34 → 39 | ✅ S 09-03 고침(값은 여전히 미측정이라고 명시만) | BALANCE_BASELINE_2026-09-05.md 58행 |
| 3 | 의무 휴식 1~3일 vs 0~5일 | ✅ 이미 해소 | 없음 |
| 4 | "1.1" 표기 vs v1.0.0 범위 | ✅ S 09-03 고침 | RESUME.md "## 1.0.1 뒤" 절 |
| 5 | 소식함 보존 — 종류별 vs FIFO | ✅ 이미 해소 | 없음(인용 시 주의만) |
