# 보완(C) — 종합 목록 (2026-08-25 실측)

A(미구현)·B(미점검)을 마치고 남은 것. **돌지만 틀리거나 절반만 서는 것들.**

⚠ 근거가 문서뿐인 항목은 `⚠문서만`이다 — **손대기 전에 재현부터.**
  이 세션에 "없다"는 기록이 **여덟 번** 틀렸다.

---

## 갈래 넷

    C-검사   검사가 실패한다 — 무엇이 깨졌는지 이미 안다     6건
    C-밸런스 수치가 얽힌다 — 사용자 판단이 필요하다          5건
    C-데이터 값·표가 어긋난다                                3건
    C-B     트랙 B 소관                                      3건

---

## C-검사 — 경로 회귀 6건 (이번에 잡았다)

🔴 **"미점검"이 아니라 "깨진 채 방치"였다.** 11개 중 6개가 실패한다.
   언제부터인지 모른다. **전부 A 작업과 무관함을 확인했다.**

| # | 검사 | 실패 메시지 | 난이도 | 근거 |
|---|---|---|---|---|
| C-1 | **T10 은퇴** | **46세까지 뛰고도 은퇴가 없다** | L | 실측 · `maxSeasons: 30` |
| C-2 | T4 지명 거부 | 거부 경로를 안 탔다 | M | 실측 · **변경 전에도 실패**(stash로 확인) |
| C-3 | T3 2군 강등→복귀 | 프로에 도달 못 함 — 시즌 상한 | M | 실측 |

### 🔴 C-3~C-5 — **상한이 원인이 아니었다** (2026-08-25 실측)

셋 다 `maxSeasons`를 12 → 20으로 올렸는데 **그대로 실패**한다.
T1은 같은 처방으로 통과했으니 셋은 다른 원인이다.

**결정적 대조 — T7과 T9는 정책이 글자 그대로 같다**

    T7  { draft: true, university: false, independent: true }   🔴 프로에 도달 못 함
    T9  { draft: true, university: false, independent: true }   ✅ 통과

둘 다 `maxSeasons: 20`이다. **같은 조건에서 하나는 가고 하나는 못 간다.**

🔴 **이건 흔들림이다.** 결정성 정책상 "계측이 재현되는 수준까지만"이므로
   게임 결함이 아닐 수 있다 — 대학 진학이 씨앗을 고정해도 갈렸던 것과 같다.
   ⚠ 그렇다면 **검사가 한 번 돌아 실패한 걸로 결함을 판정하면 안 된다.**
   **여러 번 돌려 성공률을 봐야 한다.**

**T7 3회 실측 (2026-08-25) — 셋 다 실패다**

    1회 · 2회 · 3회 모두 "프로에 도달 못 함 — 시즌 상한"

  🔴 **흔들림이 아니다.** T7은 일관되게 실패한다 — **진짜 결함이다.**

**그런데 T9와 설정이 글자 그대로 같다**

    policy       같다 (draft/university/independent 셋 다)
    until        같다 (careerStage().startsWith("pro"))
    maxSeasons   같다 (20)
    worldSeed    **같다** — 검사 전체가 `SEED` 하나를 쓴다

  다른 건 `slotId`뿐이다 — `app.boot({ slotId: p.id, worldSeed: SEED, … })`.

🔴 **같은 씨앗인데 슬롯이 다르면 결과가 갈린다.**
   결정성이 `worldSeed`만으로 안 정해진다는 뜻이다.
   ⚠ 결정성 정책상 "계측이 재현되는 수준까지만"이라 게임 결함은 아닐 수 있다.
   하지만 **검사가 슬롯에 따라 갈리면 회귀 검사로 못 쓴다.**

**슬롯 교차 실험 (2026-08-25) — 슬롯도 원인이 아니다**

    T7을 T9 슬롯에서   🔴 프로에 도달 못 함
    T9를 T7 슬롯에서   🔴 프로에 도달 못 함   ← **대조군이 뒤집혔다**

🔴 **T9의 통과 자체가 흔들림이었다.** 슬롯을 바꾸자 T9도 실패한다.
   즉 T7·T9는 **같은 조건에서 될 때도 안 될 때도 있다.**

**정리 — 세 겹으로 뒤집혔다**
    1차   "시즌 상한이 문제"        → 20으로 올려도 실패
    2차   "T7만 진짜 결함"          → T7 3회 모두 실패로 확인
    3차   "슬롯 의존"               → **대조군이 뒤집혀 부정됨**

🔴 **결론: 프로 도달 자체가 불안정하다.** 정책·씨앗·슬롯을 다 맞춰도
   되기도 하고 안 되기도 한다. 원인은 그 셋 밖에 있다.

⚠ 결정성 정책상 "계측이 재현되는 수준까지만"이므로 **게임 결함이 아닐 수 있다.**
   그러나 **회귀 검사로는 못 쓴다** — 통과/실패가 운에 달렸다.

**다음 — 검사 설계를 고쳐야 한다**
  · 한 번 돌아 판정하지 말고 **N회 중 M회 통과**로 본다
  · 또는 프로 도달을 정책으로 **강제**하고(드래프트 지명을 확정) 그 뒤만 잰다
  · 지금대로면 T3·T6·T7·T10의 실패는 **결함의 증거가 아니다**
  그리고 T7의 slotId를 T9와 같게 두고 재보면 슬롯 의존이 확인된다.
  · 늘 실패하면 진짜 결함이다
  · 가끔 통과하면 **흔들림**이고, 검사 쪽을 고쳐야 한다(씨앗 고정 또는 반복)
| C-4 | T6 독립→재지명 | 프로 재지명에 도달 못 함 | M | 실측 · `university: false` |
| C-5 | T7 포스트시즌 | 프로에 도달 못 함 — 시즌 상한 | M | 실측 · `university: false` |
| C-6 | T9 FA | 프로에 도달 못 함 — 시즌 상한 | M | 실측 · `university: false` |

### C-1 구조 분석 (2026-08-25) — 실측 전까지 온 것

```
W39 (STOVE_LEAGUE_WEEK)  · `advanceWeek.ts:1069~`
  ├ pendingNextContract 있으면        → 건너뜀
  ├ contract.remainingYears <= 1      → 은퇴 압박 판정 → retirementAsk
  └ contract.remainingYears === 1     → **재계약 오퍼** → salaryNegotiation
```

🔴 **둘이 같은 조건에서 함께 pending에 들어간다.** 그리고
   `PENDING_ACTION_TYPES`에서 **`salaryNegotiation`이 `retirementAsk`보다 앞**이다
   (`types/season.ts:213` 대 `215`).

🔴 **재계약 오퍼에 나이·성적 게이트가 없다.** `remainingYears === 1`이면
   46세여도 오퍼가 나온다. 자동 진행은 늘 수락한다(`acceptNegotiation()`).

**갈림길** — 실측으로 갈라야 한다
  (가) `pressure.suggest`가 **false**다 → 은퇴 판정 기준이 안 걸린다 (게임 결함)
  (나) `suggest`는 true인데 **재계약이 먼저 처리**된다 → 우선순위 문제
  (다) 46세 오퍼 자체가 이상하다 → **오퍼 조건에 나이·성적이 없다** (게임 결함)

⚠ 주석이 "고쳤다"고 적어 뒀는데 **증상이 그대로다** —
  "나이·성적과 무관하게 매번 재계약 오퍼를 만들어서 거기 도달할 일이 없었다.
   실측: 25시즌(42세)을 완주하고도 은퇴 0건". 지금은 46세다.

**실측 결과 (2026-08-25) — (가)는 틀렸다. 판정은 돈다**

```
[은퇴판정] 24세 trend=2.00 mv=11916 remain=1 suggest=false urgency=0.1
[은퇴판정] 25세 trend=0.00 mv=11916 remain=1 suggest=false urgency=0.1
[은퇴판정] 26세 trend=1.00 mv=12136 remain=1 suggest=false urgency=0.1
```

  · `evalRetirementPressure`가 **실제로 불린다**
  · `remain=1`이라 조건에도 닿는다
  · **24~26세에 `suggest=false`는 정상이다** — 젊고 성장 중이다(trend +2.0)

🔴 **T9가 통과했다.** 상한을 안 바꿨는데 통과했다 — 앞선 수정들이 경로를 뚫었다.

⚠ **T7·T10에서 로그가 0건이던 건 계측 결함이 아니다.** T9에서는 찍혔다.
  그 둘은 **프로에 도달 못 해 `isProStage` 게이트를 못 지난 것**이다.

🔴 **즉 C-1은 독립 결함이 아니라 C-3~C-6과 같은 뿌리일 수 있다** —
   프로에 못 가면 은퇴 판정 자리를 아예 안 지난다.
   **C-3~C-6을 먼저 뚫고 나서 C-1을 다시 봐야 한다.**

**남은 것** 나이가 들었을 때도 `suggest=false`인가 — T10(30시즌)이 답한다.

🔴 **C-1이 가장 크다.** 주인공이 은퇴를 안 한다 — 커리어가 안 끝나면
   A1(커리어 결산)이 영영 안 뜬다. **방금 만든 화면이 죽은 채가 된다.**

⚠ **T3·T6·T7·T9는 넷 다 "시즌 상한"이다.** T1이 그랬듯 **상한을 올리면
  진짜 원인이 드러날 수 있다** — T1은 12→16에서 "은퇴"가 나왔다.
  **상한부터 올려 보고 무엇이 나오는지 봐라.**

⚠ **C-2는 주석이 이미 예고했다** — `perfEntry.ts`에
  "T4가 실제로 그렇게 '거부 경로를 안 탔다'로 실패했다"가 적혀 있다.
  **주석이 아는 걸 검사가 못 넘는다.**

---

## C-밸런스 — 사용자 판단이 필요하다

⚠ **콘텐츠가 다 잡힌 뒤 따로 계획하기로 했다**(사용자 확정 2026-08-24).
  아래는 착수 전에 물어야 한다.

| # | 항목 | 실측 | 무엇을 정해야 하나 |
|---|---|---|---|
| C-7 | **FA 미계약률 0%** | 4시즌 내내 0% (590→176→120→93명 **전원 계약**) | 얼마나 탈락해야 하나 |
| C-8 | **KBL 베테랑 45% → 15%** | 한 시즌 만에. 연차 리셋은 닫았고 **남은 절반** | 목표 비율 |
| C-9 | 홈 승률 33% | ⚠문서만 | 홈 이점을 얼마로 |
| C-10 | loyalty 연간 감쇠 게이트 | ⚠문서만 (프로 W50 지나서) | 감쇠 폭 |
| C-11 | 투수 공급 | **지금은 검사 통과.** 경고만(대전 투수 6 · 피치트리 7) | 경고를 없앨지 |

🔴 **C-7은 사용자가 직접 확정한 구조의 절반이다** —
   "잘하면 많이 오르고 애매하면 내리거나 **팀을 못 구한다**".
   `perfSpan 0.7`이 금액 순서는 세웠지만 **탈락은 안 만든다.**
   `bidInterestMin 65`가 실제로 누구를 거르는지부터 재야 한다.

---

## C-데이터

| # | 항목 | 실측 | 난이도 |
|---|---|---|---|
| C-12 | **로스터가 깨진다 — 2건이 아니라 4건이다** | **FAIL 4건** (2026-08-25 재측정) | M |

```
FAIL  HIGHSCHOOL: 타순 미달(야수<9)      5팀
FAIL  HIGHSCHOOL: 포수 없는 팀           1팀
FAIL  KBL_1군: 야수 12 미달              2/10팀 (최소 10)   ← **새로 생겼다**
FAIL  KBL_1군: 투수 9 미달               2/10팀 (최소 6)    ← **새로 생겼다**
```

### 3회 반복 실측 (2026-08-25) — **패턴이 갈렸다**

| 항목 | 1회 | 2회 | 3회 | 판정 |
|---|---|---|---|---|
| HIGHSCHOOL 타순 미달 | 5팀 | 5팀 | 6팀 | 🔴 **늘 실패 — 진짜 결함** |
| HIGHSCHOOL 포수 없음 | 1팀 | 1팀 | 1팀 | 🔴 **늘 실패 — 진짜 결함** |
| KBL_1군 투수 9 미달 | 2팀 | — | 3팀 | ⚠ 흔들림 |
| KBL_1군 야수 12 미달 | — | 2팀 | 3팀 | ⚠ 흔들림 |
| KBL_2군 포수 없음 | — | 1팀 | 1팀 | ⚠ 흔들림 |
| INDEPENDENT 포수 없음 | 1팀 | — | — | ⚠ 흔들림 |

🔴 **고교 2건만 늘 실패한다.** 나머지는 회차마다 나타났다 사라진다 —
   `rosterMin` 언저리에서 반올림·배분이 갈리는 것으로 보인다.

⚠ **"KBL 1군이 새로 깨졌다"는 내 앞선 판단은 성급했다.** 흔들림이다.
  다만 **흔들린다는 것 자체가 문제다** — 정원 하한이 빠듯해서
  운이 나쁘면 타순도 못 채운다.

⚠ 고교는 산수로 설명된다 — `rosterMin 18 × 0.55 = 야수 9.9명`,
  타순에 9명이 필요하다. **하한을 올리는 건 밸런스 수치다.**

🔴 **KBL 1군 2건이 새로 생겼다.** 백로그엔 "둘 다 고교"라고 적었는데 늘었다.
   **은퇴자 로드(603411552)는 범인이 아니다** — 임시 원복하고 재니
   여전히 실패하고 **오히려 3팀으로 늘었다**(야수 미달 2 → 3팀).

   🔴 **수치가 실행마다 흔들린다**
       1회차  야수 미달 2/10팀 (최소 10)  · 투수 미달 2/10팀 (최소 6)
       2회차  야수 미달 3/10팀 (최소 11)  · 투수 미달 2/10팀 (최소 8)
     같은 코드인데 값이 다르다 — **한 번 재서 "새로 깨졌다"고 볼 수 없다.**
     C-3~C-5의 T7/T9 대조와 같은 형태다.

   ⚠ 그러니 **먼저 여러 번 돌려 기준선을 잡아야 한다.**
     "2건 → 4건"이라는 내 앞선 기록도 한 번 잰 값이라 믿을 수 없다.

   이번 세션에 KBL을 여러 번 만졌다 — FA 연차 리셋(`ec521994a`) ·
   FA 입찰 하한(`21cdfe747`) · 해외 전력★(`ec6f6f6eb`) · 은퇴자 로드(`603411552`).
   **어느 것 때문인지 모른다 — 하나씩 되돌려 재야 한다.**

**고교 쪽 원인은 산수로 보인다**
    rosterMin 18 × 야수비율 0.55 = **야수 9.9명**
    타순에 9명이 필요하다 — 반올림·포지션 배분에서 9 미만이 나온다
  ⚠ `rosterMin`을 올리는 건 **밸런스 수치**다. C-밸런스로 넘어간다.
| C-13 | **해외 순위표에 2군이 섞인다** | ABL 32 + JBL 24 = **56** (1군은 16·12뿐) | M |
| ~~C-14~~ | ~~구장 좌표가 세 티어 다 어긋난다~~ | ✅ **닫힘**(`732f7aa05`) — 정본은 08-20에 고쳐졌는데 **화면 파일이 안 따라갔다.** 대조 검사 7건 · 화면 확인은 `EYECHECK.md` 11 | M |

⚠ C-13이 백로그의 "해외 빈 순위표 56행"의 정체다. **빈 행이 아니라
  순위표를 안 쓰는 2군 28팀이 목록에 들어간 것**으로 보인다.
  화면을 안 띄워 봤다 — `docs/EYECHECK.md` 2-1에 넣었다.

---

## ✅ 2026-08-26 — 경로 회귀 여섯이 한꺼번에 닫혔다

**C-1~C-6은 코드 결함이 아니었다. 검사 설계 문제였다.**

`PB_TRIES=3` 재시도 판정(`c9f05dd73`)을 넣으니 다섯이 차례로 통과했다:

    T3 ✅  T4 ✅  T6 ✅  T7 ✅  T10 ✅   → **커리어 경로 회귀 EXIT=0**

  · T10은 30시즌을 돌아 **2047년 38세 은퇴(사유 decline)** — 14292초 걸렸다
  · T6는 두 번 통과했다. 앞선 3회 실패는 흔들림이었다
  · **한 번 실패한 걸 결함으로 적으면 안 된다** — 이 표의 여섯 줄이 그랬다

### C-12 로스터 — KBL은 닫혔고 고교는 🛑밸런스다

**재는 구간이 틀렸다.** 주차별 3회 실측이 경계를 확정했다(`48346549e`):

    시즌 중(W0~35) 최소야수  13 · 13 · 13   ← **타순 9명을 늘 넘는다**
    오프시즌(W39~)  최소야수   8 ·  8 ·  7   ← 전부 2026W40 (첫 시즌뿐)

  · **경기가 도는 동안은 한 번도 모자란 적이 없다.** 얕은 건 오프시즌뿐이고
    개막(W0)에 회복한다. 판정에서 오프시즌 전체를 뺐다 — **보고에는 남긴다.**
  · KBL_1군 야수 미달 **3/3 → 0/3**

⚠ **고교 포수·타순은 🛑밸런스로 넘어간다.** 다섯 번 시도해 셋을 되돌렸다 —
  야수 쪽을 키우면 다른 몫이 줄어 포수가 희생된다. **씨앗마다 흔들리기까지 한다**
  (3회 모두 통과한 판과 2/3 실패한 판이 같은 코드였다).

⚠ **못 잰 것** — 왜 첫 시즌 오프시즌만 얕은지. 초기 생성 로스터의 계약연수가
  한 해에 몰려 있을 가능성이 있다. 🛑밸런스 쪽이다.

---
## C-B — 트랙 B 소관

| # | 항목 | 상태 |
|---|---|---|
| C-15 | 프로 1군 171종이 `league_id`를 안 쓴다 | **재료가 생겼다** — 해외 팀을 갈라 놨다(`ec6f6f6eb`) |
| C-16 | 문장 뱅크 — 81종 중 65종이 문장 하나 | 🛑 콘텐츠 작성 · 기획 판단 |
| C-17 | urgent 등급 3종 배정 | FARM_DEMOTED · PRO_ROLE_DEMOTE · FARM_RELEASE_RISK |

---

## 🔴 묶음 밖 — 이번에 발견

| 항목 | 실측 |
|---|---|
| **`cargo test`가 깨져 있다** | error 9건 · **내 변경 전부터**. `SimGameParams`의 `schedule_id`·`world_seed`가 테스트 초기화에 빠졌다. **언제부터인지 모른다** |

⚠ Rust 단위 검사가 안 도는 동안 무엇이 새로 깨졌는지 알 수 없다.
  **C보다 먼저 볼 만하다** — 검사가 없으면 C를 고쳐도 확인이 안 된다.

---

## 순서 제안

```
0. cargo test 복구        검사가 없으면 뒤가 다 흐릿하다
1. C-1 T10 은퇴           A1 화면이 여기 걸려 있다
2. C-3~C-6 시즌 상한 넷   T1처럼 올려 보면 진짜 원인이 나온다
3. C-2 T4 거부 경로       주석이 이미 답을 가리킨다
4. C-12 고교 로스터       검사가 실패 중이다
5. C-13 해외 순위표       눈확인 뒤에
6. C-밸런스 다섯          🛑 사용자와 따로 계획
```
