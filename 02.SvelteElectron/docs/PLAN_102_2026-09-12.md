# 앞으로의 계획 — 1.0.1 마감과 1.0.2 (2026-09-12)

> 사용자: 「당장 테스트 안 할 거다. 앞으로 진행할 것을 업데이트해라.」
> 정본: 평가 `PROJECT_ASSESSMENT_2026-09-11.md` · 계측 `SIM_REPORT_COMBINED_2026-09-11.md` · 죽은 칸 `CONTENT_DEAD_SLOTS_2026-09-09.md` · 밸런스 `BALANCE_PROPOSAL_101.md`

---

## 0. 지금 어디인가

| | 상태 |
|---|---|
| 1.0.1 구현·콘텐츠·계측·밸런스·껍질 | ✅ 전부 닫힘 |
| 테스터 빌드 | ✅ `OnePitch-1.0.1-test.zip` · SHA `70b014b5…` · 트렁크 `de087b19c` |
| 테스터 | ⏸ **미룸** (사용자) |
| CI | ✅ 매 push 마다 초록 · `main` = 트렁크 |
| 콘텐츠 동결 | 🔴 **아직 걸려 있다** — 「테스터 전까지」가 조건이었다 |

테스터가 미뤄지면 **회신을 기다리는 빈 시간이 없다.** 그 시간을 1.0.2 로 쓰는 것이 자연스럽고, 그러려면 아래 결정 셋이 먼저다.

---

## 1. 🔴 먼저 정할 것 셋

| # | 물음 | 권장 | 왜 |
|---|---|---|---|
| ① | **콘텐츠 동결을 푸나** | **푼다 — 단, 1.0.2 축(B) 안에서만** | 동결의 뜻은 「테스터가 보는 판을 고정」이었다. 테스터가 없으면 고정할 이유가 없다. 다만 **아무거나 늘리는 것**으로 돌아가면 9-09 이전(찾을 때마다 733→749)이 된다 — **B 축 목록에 있는 것만** |
| ② | **1.0.1 을 지금 태그하나** | **`v1.0.1-rc1` 을 `de087b19c` 에 지금** · 정식 `v1.0.1` 은 테스터 뒤 | 빌드가 어느 커밋인지 못 박아야 한다. 1.0.2 작업이 트렁크에 쌓이기 시작하면 「테스터 빌드가 어디였더라」가 된다. `package.json` 도 `1.0.1` 로 |
| ③ | **1.0.2 안에서 무엇부터** | **0단계(왜 2군·독립을 안 밟나 실측) → 1단계(군 이벤트·죽은 칸)** | 만든 게 안 도는 자리를 **원인부터** 봐야 한다. 콘텐츠 탓인지, 계측 성향 탓인지, 밸런스 탓인지 모르고 쓰면 또 안 밟힌다 |

---

## 2. 1.0.2 — B 축 「콘텐츠·무대」 (사용자 확정)

### 0단계 · 왜 안 밟히나 (D · 하루)

12판 최종: **2군 3/12 · 독립 0/12 · 군 11/12(이벤트는 절반)**. 셋 다 「만들어 뒀는데 안 돈다」인데 **원인이 다를 수 있다.**

| 무대 | 가설 | 재는 법 |
|---|---|---|
| **독립 0/12** | 고졸 직행 33% + 나머지가 전부 대학이라 **독립으로 갈 사람이 없다**(독립 = 미지명 + 대학 안 감). 12판이 전부 `growth` 라 진로 갈래가 한쪽으로 쏠렸을 수도 | 12판의 진로 사건을 세라 — 미지명 뒤 대학/독립 갈림에서 독립을 고른 판이 0 인지, 고를 기회 자체가 없었는지. **성향 셋으로** 3판씩 |
| **2군 3/12** | 시작 능력치 −9 뒤에도 최고 OVR 84~90 이라 **1군에 바로 붙는다**. 콜업·강등 통지는 있는데 강등될 만큼 못하는 판이 없다 | 12판의 `demote` 발동 수 · 프로 첫 해 ERA 분포. 강등 문턱(ERA 5.5+ · 8G+)에 닿는 판이 몇인지 |
| **군 이벤트 절반** | 콘텐츠가 모자란다(노말 24.1 · 목표 30~52). 이건 가설이 아니라 **실측**이다 | 군 무대 노말 종수 · 조건별 후보 수(창 몰림 여부 — 9-08 폴백 때 쓴 방법) |

**산출**: 무대별 「콘텐츠 / 계측 / 밸런스」 중 어느 것인지 한 줄씩. 이게 1단계의 범위를 정한다.

### 1단계 · 데이터 (B · 2~3턴)

| # | 무엇 | 크기 | 근거 |
|---|---|---|---|
| B-1 | **군 노말 이벤트 15~25종** — 목표 30~52 에 닿게. 상무·일반병 갈라서 | 중 | 노말 24.1 실측 · 0단계 후보 수 표 |
| B-2 | **히든 구종 「등급 두 단계」** 심기 — A 가 `steps` 를 붙여 뒀다 | 소 | `PLAN_REWARDS §1` · 지금 히든이 유니크와 종류가 같다 |
| B-3 | **부중대장·소대장 장면** — 「원칙대로 간다」「아직 서툴다」 | 소 | 부대원 15명 중 한 번도 안 나오는 둘 |
| B-4 | **`relationTarget` 데이터** — A 가 스키마를 열면 「후임을 챙긴다」와 「소단위를 챙긴다」가 다른 사람에게 가게 | 소 | 죽은 칸 7 |
| B-5 | 0단계 결과에 따라 **독립·2군 이벤트 손질** — 콘텐츠 탓으로 나오면 | ? | 0단계 |
| B-6 | **`startGuarantee` 쓸 자리** — 「자리를 준다」가 필요한 통지가 생기면 | 소 | 배선만 있고 데이터 0 |

⚠ **동결 규칙을 이렇게 바꾼다**: 「B 축 목록 밖의 콘텐츠 발견은 `CONTENT_DEAD_SLOTS` 에 적고 1.0.3 으로」. 목록이 늘어나는 것 자체를 막는다.

### 2단계 · 배선 (A · 1~2턴)

| # | 무엇 | 크기 | 근거 |
|---|---|---|---|
| A-1 | **조건 여덟 종을 쓸 장면** — `pitch_learned`·`injury_severity`·`relation_lte` 등 엔진엔 있고 데이터가 0 | 소 (조건은 있으니 B 가 데이터를 쓰면 됨 · A 는 검사만) | 죽은 칸 9 |
| A-2 | **`compare` 등록부** — `storyNpcs` 가 없어 `npcId` 를 적을 수 없다. 히든 「라이벌의 편지」「후배가 넘어선 날」이 여기 걸려 있다 | 중 | 죽은 칸 5 · `PLAN_EVENT_TIERS §8` |
| A-3 | **군 풀 `relationTarget` 스키마** | 소 | 죽은 칸 7 |
| A-4 | 🔴 **`advanceWeek.ts` 쪼개기 — 건드리는 자리만** | 중 | 커버리지 3.5% · 같은 결함 셋째 자리. 2군·독립을 손대며 지나는 블록을 `weekPhases/` 로 옮긴다. **통째로는 안 한다**(사용자 확정) |
| A-5 | **드라이버** — `--auto` 가 진로 허브에서 대학이 아니라 드래프트로 가는 것 · 막 넷(드래프트 참관·새 시즌·화살표 버튼·체육부대) | 소 | C 가 찾음 · 대학 경로를 도구로 재려면 필요 |
| A-6 | `.prettierignore` 59 파일 줄이기 · clippy 113 → 0 뒤 `deny` 복원 | 중 | 껍질 마무리 · 틈틈이 |

### 3단계 · 화면 (C · 1턴)

| # | 무엇 | 크기 |
|---|---|---|
| C-1 | **통지 표시** — 지금 통지는 목록에서 노말과 구분이 안 된다(칩 없음). 콜업·우승 같은 큰 순간이 노말 사이에 묻힌다. 작은 표시 하나 | 소 |
| C-2 | `bars.teamMood` 이름표(화면이 안 읽는다) | 소 |
| C-3 | 부대원 성격을 문안이 읽게 — **사용자가 「콘텐츠적으로 검토 뒤 논의」로 미룬 것.** 장면 배정은 이미 성격을 타니 **이걸 더 할지 여기서 정한다** | 중 |

### 4단계 · 재계측 (D · 하루)

12판 짝 구성 그대로. **볼 것**: 독립·2군·군 도달이 올라갔나 · 군 이벤트 노말이 30~52 에 드는가 · 폴백 · 예외 0 · 정지 0. 그리고 **1.0.1 에서 1.0.2 로 넘긴 밸런스 셋**(④ 위기집중력·견제 · ⑤ 구종 개수 · ⑨ 나머지) 중 **⑤ 구종 개수만** 여기서 잰다 — 구종이 이 게임의 축이라서. ④⑨ 는 1.0.3.

### 병행 · 문서 (OP · 틈틈이)

| # | 무엇 |
|---|---|
| 7 | ✅ **09-12** `docs/README.md` 색인 한 장. ⚠ `archive/` 이동은 **안 한다** — 참조 서른 곳(코드 주석 포함)이 깨진다 · 「옛 것」 표시는 색인이 한다 |
| 11 | ✅ **09-12** `CLAUDE.md` 는 규칙만 · 원문은 `docs/CLAUDE_HISTORY.md` 에 그대로 |

---

## 3. 1.0.3 으로 (지금 안 한다)

- **C 축 엔진** — 제구형 지명(ERA 구속 가중) · 위기집중력·견제 가중 · 좌우 상성(⑫)
- 거대 파일 **통째로** 쪼개기(`game.ts` 4,601 · `npc_sim.rs` 5,721 · `match_engine.rs` 4,953) · e2e 한 줄기(새 게임 → 3시즌 → 저장 → 다시 열기)
- 세이브 무결성 검사 · 소리·음악 · JBL 팀 데이터

---

## 4. 순서 한 줄

```
결정 셋 → v1.0.1-rc1 태그 → 0단계 실측(D) → 1단계 데이터(B) ∥ 2단계 배선(A) → 3단계 화면(C)
→ 4단계 재계측(D) → 포장 → (테스터가 되면) 회신 → v1.0.1 정식 + 1.0.2 태그
```

**A·B 는 겹치는 파일이 없어 병행이 된다**(B 는 `resource/**` · A 는 `usecases/**`·`utils/**`). 다만 A-3(스키마)이 B-4(데이터)보다 앞이고, A-2(등록부)가 히든 둘보다 앞이다.

**규모**: 0단계 하루 · 1·2단계 사흘 · 3단계 하루 · 4단계 하루 → **일주일 안쪽.**

---

## ✅ 사용자 확정 (2026-09-12)

| 물음 | 확정 | 뜻 |
|---|---|---|
| ① 콘텐츠 동결 | **전부 푼다** | B 축 목록 밖의 콘텐츠도 늘릴 수 있다. ⚠ 다만 「찾을 때마다 늘던」 9-09 이전으로 돌아가지 않게 — **발견은 우선순위 표에 먼저 적고, 재고 검사한 뒤 넣는다.** 동결이 아니라 절차로 막는다 |
| ② 태그 | **정식 `v1.0.1` 을 지금** | `package.json` 1.0.1 로 올린 이 커밋에 찍는다. ⚠ 테스터 zip(`70b014b5…`)은 한 커밋 전 `de087b19c` 에서 나왔다 — 차이는 버전 문자열과 문서뿐. 테스터를 하게 되면 태그에서 다시 굽는다(D 한 시간) |
| ③ 시작 | **0단계 실측부터** | D 가 왜 2군·독립을 안 밟나부터 잰다. 그 결과가 1단계 범위를 정한다 |

**동결 해제로 바뀌는 규칙**: `CONTENT_DEAD_SLOTS` 의 「동결 뒤 발견은 1.0.2 로」는 끝났다. 이제 발견은 **이 문서 §2 표에 줄을 더하고**, 넣을지는 0단계·4단계 계측이 정한다.
