# A → B 인계 4차 (2026-09-02) — 9/28 계획과 W1 할당

> 정본은 [PLAN_RELEASE_2026-09-28.md](PLAN_RELEASE_2026-09-28.md) §4-B 다.
> 여기는 **B 가 지금 알아야 할 것**만 적는다. 아래 3차(08-24) 기록은 그대로 둔다.

## 0. 🔴 세상이 바뀌었다 — B 의 측정값 중 무효가 된 것

오늘 A 가 닫은 것(커밋 `6268d5cf0` · `8bb5c09d4` · `811dee7fa`):

```
고교를 떠나면 배경 리그가 통째로 멈춰 있었다    프로 6 + 고교 + 대학
대학 주인공이 한 경기도 안 뛰었다                68/0 → 777/775
군 복무 2년 · 프로 첫 해 · 졸업 해              시즌 여는 자리 5곳 전수
```

그래서 **무효**: 08-30 `slotreach --path univ` 의 **대학 26%** — 경기가 0 인
세상에서 잰 값이다. `season_ip_*`·`season_era_*` 축 이닝 문턱 7종 판정도 같이.

⚠ **군 복무 중에도 고교만은 0 경기다**(`HIGHSCHOOL:1020/0`). 반경 게이트
(`RADIUS_GATED_LEAGUES`)가 군 무대에서 고교를 3(비활성)으로 보는 듯하다 —
설계일 수 있어 A 가 확인한다. 군 이벤트가 고교 순위를 읽지 않으면 영향 없다.

## 0.5 🔴 기능 단위 정본 — [PLAN_FEATURES_2026-09-02.md](PLAN_FEATURES_2026-09-02.md)

무대 × 기능 78행. B 몫은 §1 이벤트/소식함 · §4 대학 이벤트 · §8 상무 소식함 ·
§6 지명률이다. **동적 치환은 사용자가 1.1 로 확정했다.**

## 1. W1 (9/2 ~ 9/8) — 순서대로

1. 🔴 **대학 도달률 재측정** `measure:slotreach --path univ` 씨앗 3.
   113종 전부. 이닝 문턱 7종은 이 값으로 판정한다.
   ⚠ 프로브 10개 중 8개가 `university: false` 라고 네가 적었다 — 이번엔
   `--path univ` 만 쓰고, 다른 계측을 대학 근거로 쓰지 마라.
2. ✅ **소식↔선택지 어긋남 7건 — 사용자 확정: 선택지를 소식에 맞춘다.**
   본문이 상황을 정하고 버튼이 따라간다. 본문은 건드리지 마라 — 갈래 조건에
   묶여 있어 도달률이 흔들린다. **바로 착수해도 된다.**
3. **상무 소식함** — 이벤트 54종은 있다. 빈 건 소식(`8091cde75`).
   군 결과 소식은 **"결과 소식 한 통씩"** 이 사용자 확정이다.

## 2. W2 ~ W4

```
W2  프로·독립 사교 풀 1종씩 · 문장 은행 나머지 · B군 4종 축 옮기기 · 드래프트 지명률
W3  전 무대 도달률 최종 한 장 (고교·대학·독립·프로·군) — 스토어 문구 근거
W4  빌드에서 소식함 눈확인. 결함은 A 에게
```

## 3. 병합

A 가 W1 에 `track/events` 를 가져온다(14커밋 · 전부 B 소유 파일 · 충돌 예상 0).
그 전에 **네가 `extract-modals` 를 한 번 더 당겨라** — A 가 6커밋 앞서 있다.

## 3.6 🔴 새 일감 — 사기 이벤트 효과 쏠림 (사용자 확정 2026-09-02 · **W1**)

### 무엇이 문제인가 — 실측 (`probe:morale --path univ` · 씨앗 20260731 · 8시즌)

```
              등판주          이벤트주(무등판)        없는주
고교   평균  −2.55 (15주)    +0.58 (117주 · 86%)    −1.81 (4주)
대학   평균  −1.56 (29주)    +0.30 (177주 · 85%)    −1.46 (2주)
```

없는주 −1.8 = 주간 회귀(60 으로 5%)만 있다 — 맞다. 등판주 음수 = 승패가
닿는다 — 맞다. **이벤트주가 85% 이고 평균이 양수**라 회귀를 이긴다. 그래서
사기가 89~98 에 붙고 **대학 이벤트 아홉(`morale_lte 40~60`)이 영영 안 뜬다.**

이벤트 정의 전수: `moraleDelta` 57건 · 양수 44 · 음수 13 · **합 +150**
(양수 +1~+12, 음수 −1~−3).

### 델타 합이 큰 12 (같은 주 여러 개면 나눠 못 가른다)

```
 +29  ×3   EVT_HS_MIDTERM
 +25  ×5   EVT_UNIV_ALLSTAR_UNIV
 +24  ×8   EVT_UNIV_LIFE_LINEUP_COMPETE_01
 +21  ×4   EVT_UNIV_SENIOR_PRESSURE
 +19  ×10  EVT_RAND_LOCAL_INTERVIEW         ← 반복
 +16  ×2   EVT_HS_FINAL
 +16  ×1   EVT_HS_Y1_POSITION
 +16  ×1   EVT_HS_COMMON_OVEREATING
 +15  ×5   EVT_HS_LIFE_SOCIAL_MENTION
 +14  ×41  EVT_COND_PEAK_FORM               ← 🔴 41번. 컨디션 좋을 때마다 뜬다
 +14  ×5   EVT_UNIV_GROUP_LAST
 +14  ×1   EVT_UNIV_Y4_W50_CAREER_GATE
```

### 사용자 확정 — **데이터를 손질한다.** 엔진·회귀는 안 건드린다

방향은 둘이고 둘 다 B 소유(`events/**`)다:

1. **반복 발동에 쿨다운** — `EVT_COND_PEAK_FORM`(×41) · `EVT_RAND_LOCAL_INTERVIEW`(×10)
   부터. 한 시즌에 몇 번이 맞는지는 네 판단 — 다만 **한 번에 하나씩 재라.**
2. **양수 폭 축소** — 양수 44건의 합 +171 을 어디까지 내릴지. 목표는
   **이벤트주 평균이 0 근처**다(회귀와 승패가 남는다). 음수 13건은 손대지 마라.

⚠ 헤드리스는 선택지를 `choices[0]` 으로 고른다(`runAutoAdvance.ts:53`).
첫 선택지에 양수가 몰려 있으면 실제 플레이보다 쏠려 보인다 — **첫 선택지가
+ 인 이벤트가 몇인지**부터 세고 시작해라.

### 재측정 — 손질 전후 같은 명령

```
PF_YEARS=8 PF_SEED=20260731 npm run probe:morale -- --path univ
```

보는 값: 이벤트주 평균(→ 0 근처) · `[university] 사기 최소`(→ 60 아래로 닿는가) ·
델타 합 상위 12 가 바뀌었는가. 씨앗 3개.

⚠ 군 복무 주는 `careerTriggeredEvents`(커리어 통)에 남아 이 프로브의 diff 에
안 잡힌다 — 군 사기는 여기서 판정하지 마라.

## 3.5 ✅ 코드 프리즈 9/23(화) 확정

그 뒤 B 는 **문안·조건 결함 수정만** 한다. 새 이벤트·새 풀은 넣지 않는다.
동적 치환 23종은 사용자가 1.1 이월을 확정했다.

## 4. 하지 않는 것

동적 치환 23종(`EVENT_DEFERRED.md §1`)은 **1.1** 이다. 엔진 개조라 B 몫이 아니다.

---

# A → B 인계 (2026-08-24)

트랙 A(밸런스·엔진·세계)가 트랙 B(이벤트·소식함)의 인계를 받아 병합하고
요청 넷을 처리했다. **B가 이어서 할 것**을 아래에 정리한다.

---

## ■ 병합 결과 — 충돌 0건

    git merge track/events    (B 머리 0754d887a · 6커밋)
    회귀  75파일 762건 통과 · tsc 0건

겹친다던 `stores/game.ts` · `scripts/perf/perfEntry.ts` 둘 다 깨끗했다.

⚠ **B가 예고한 함정이 그대로 맞았다.** "A가 앞선 커밋 32"가 병합 시점엔
  **35**였다. B가 "직접 확인해라"라고 적어 둔 덕에 안 걸렸다.
⚠ B가 적은 "tsc 9건(기존과 동일)"은 **A 쪽에서 이미 0으로 닫혀 있었다**
  (`d1b4cbb54`). 넓은 타입이 결함 셋을 가리고 있던 건이다.

---

## ■ 요청 넷 — 전부 처리 (커밋 `c63767826`)

### ① `MAX_MAILBOX` 200 → 500 — 넣었다

B의 IPC 실측 근거를 그대로 주석에 남겼다(+38 KB · 전체 IPC +0.35%).
**"`measure:perf` 기준이라 벽시계가 아니다"도 같이 적었다.**

🔴 **B가 확인할 것**: 유실률을 다시 재라. 200일 때 453 → 447이었는데
   병목이 상한이었다면 500에서 크게 떨어져야 한다. **안 떨어지면 병목이
   다른 데 있다는 뜻이고, 그게 더 중요한 정보다.**

### ② `EventContext.relations` 배선 — 넣었다

`advanceWeek.ts`의 `relationEffects` 옆에 `slotRepo.getRelationships`를 붙이고
컨텍스트에 실었다. 검사 `eventRelationsWiring.test.ts` 7건, **변이 검증했다**
(`relations: relRows`를 지우면 실제로 깨진다).

🔴 **B가 확인할 것**: 관계도 조건 이벤트가 **이제 실제로 뜬다.** 배선 전엔
   구조적으로 0건이라 실측 자체가 불가능했다. 지금 재면 처음으로 숫자가 나온다.

⚠ 조회는 `contact` 필터 없이 전부 가져온다. B가 "함께 지낸 사람만" 같은
  범위를 원하면 말해라 — `slotRepo.getRelationships(slotId, { contact: "together" })`가 있다.

### ③ 문서 — B 지적이 맞았다. 다만 **세는 법이 갈렸다**

    폴더 기준     mandatory 105 + conditional 258 + random 172 = **535**
    매니페스트    mandatory 105 + conditional 258 + random 없음 = 363

  **535로 적었다**(루트·02 두 곳). 553은 어느 기준으로도 안 나온다.
  ⚠ B가 "매니페스트 실측 535"라고 했는데 매니페스트엔 `random` 키가 없다.
    B가 다른 방식으로 셌을 것이다 — **어느 쪽이든 535가 맞았다.**

  글자 깨짐: `스텅`→`스텁` 2곳 · `안 뜼다`→`안 뜬다` 1곳 · `master.ts:592`의
  `援??щ㎎`→`구 형식`.

### ④ `package.json` — 확인했다

`check:eventconditions` · `measure:eventfunnel` 둘 다 있고 동작한다.

---

## ■ A가 바꾼 것 중 **B가 알아야 할 것 셋**

이벤트 조건이 이 값들을 읽는다면 동작이 달라진다.

### 1. `proServiceYears`가 더 이상 리셋되지 않는다 (`ec521994a`)

FA를 신청하면 통산 연차가 **0으로 되돌아가고 있었다.** `market.ts`가
`proServiceYears: 0`을 넣었다 — 그 값은 연봉 산식의 입력이라 몸값도 같이
무너졌다. Rust는 이미 고쳐져 있었고 **TS 경로만 남아 있었다.**

    실측  KBL 1군 7년차+  45% → 5% (한 시즌)  →  고친 뒤 15%
          연차 감소자 90명 · 평균 9.3년 손실  →  0명

🔴 **연차를 조건으로 쓰는 이벤트가 있다면 이제 다르게 뜬다.**
   FA 재취득은 `FA_REACQUIRE_YEARS = 4`(Rust와 짝)로 따로 센다.

### 2. 해외 28팀에 전력★·예산이 생겼다 (`ec6f6f6eb`)

ABL 16팀·JBL 12팀이 `power`·`history.budget` **둘 다 0개**였다. 전 팀이
★3(pivot)으로 읽혀 강팀도 부자 구단도 없었다. KBL 대응을 실측해 채웠다.

    flex 종류 6가지 → 33가지 · ABL 총연봉 중앙 359 → 587억

⚠ 팀 전력을 조건으로 쓰는 이벤트가 있다면 해외에서 처음으로 갈린다.

### 3. `MatchPage`가 폴백 구질만 보이고 있었다 (`5de761ee0`)

`PITCH_ID_TO_ENGINE` import가 없어 `$:` 블록이 ReferenceError로 죽었고
`pitchTypes`가 초기값에 머물렀다. 경기 화면 이벤트를 쓴다면 참고.

---

## ■ B가 이어서 할 것 — 우선순위

### 🔴 1. ①②의 실측 (지금 바로 가능)

위 ①②에 적은 두 가지. **둘 다 배선이 없어서 지금까지 못 쟀던 것들이다.**
숫자가 처음 나오는 자리라 여기가 제일 값싸고 정보가 크다.

### 🔴 2. `check:mojibake`가 이 패턴을 못 잡는다

B가 지적한 그대로다 — `스텅` · `안 뜼다` · `援??щ㎎`가 **전부 통과한다**
(`[check-mojibake] OK`). A가 글자는 고쳤지만 **검사는 안 고쳤다.**
같은 게 또 생기면 또 놓친다. 검사를 B가 맡아라 — 이건 이벤트 데이터 쪽에
훨씬 자주 생기는 종류다.

⚠ 어떤 패턴을 잡을지부터 정해야 한다. `援??щ㎎`는 CP949↔UTF-8 왕복 흔적이고
  `스텅`·`뜼다`는 **정상 한글 조합인데 뜻이 없는 것**이라 성격이 다르다.
  뒤엣것은 사전 없이 잡기 어렵다 — **범위를 좁혀 잡을 수 있는 것만 잡아라.**

### 3. 프로 1군 171종이 `league_id`를 안 쓴다

KBL·ABL·JBL이 같은 이벤트를 본다. **A가 방금 해외 팀을 갈라 놨으니**
(위 2번) 리그별로 다르게 쓸 재료가 생겼다.

### 4. urgent 등급 3종 배정
`FARM_DEMOTED` · `PRO_ROLE_DEMOTE` · `FARM_RELEASE_RISK`.

### 5. 문장 뱅크 — **사용자 확정 대기**
6시즌에 2회 이상 뜬 81종 중 65종이 문장 하나뿐.
`EVT_IND_FATIGUE_IND`는 29번 뜨는데 같은 글이다. 콘텐츠 작성이라 기획 판단.

### ⏸ 6. 대학 Y2~Y4 20종 — **보류. A가 정한다**

B가 올린 ⑤(대학을 1년만 다닌다)는 커리어 진행 설계라 A/사용자가 정한다.
**정해지기 전엔 그 20종을 건드리지 마라** — 도달 가능해질지 아닐지가
안 정해졌다.

---

## ■ A가 받아 간 것 둘

    ⑤ 대학 1년 · Y2~Y4 20종 도달 불가   → A가 재현부터 한다
    ⑥ 대학 진학이 재현되지 않는다        → A가 본다

⑥은 A도 같은 뿌리를 봤다 — 씨앗을 고정해도 KBL 총연봉 중앙이 57↔32억으로
흔들린다. **결정성 정책이 2026-08-24에 바뀐 걸 먼저 읽어라**(`02/CLAUDE.md`):
게임 전체 결정성은 목표가 아니고 **계측이 재현되는 수준**까지만 한다.
즉 ⑥이 "게임 결함"인지 "계측 방법 문제"인지부터 갈라야 한다.

---

## ■ 규칙 (안 바뀌었다)

  · 개발 대상은 `02.SvelteElectron/` 하나. `04.GodotOnePitch/`는 **읽기만**
  · 값은 실측으로 · 추측하지 않는다 · **적용 전후를 잰다** · 죽은 갈래를 두지 않는다
  · 사용자 응답만 존댓말, 주석은 평서체
  · ⚠ 밸런스 수치는 사용자에게 묻는다 · Steam 빌드 안 한다 · 사용자 세이브 안 건드린다
  · ⚠ **숫자를 적을 땐 재고 적는다.** A는 이 세션에만 문서 숫자가 여섯 번 틀렸다
    (`thread_rng` 31→28 · 전역자 43→13 · 이벤트 로더 0→있음 · svelte-check 47→40+51 ·
     투수 공급 · FA 309→590). 어림으로 적은 숫자가 다음 사람에겐 사실로 읽힌다.
  · ⚠ **손대기 전에 재현부터.** A는 이 세션에 "이미 닫혀 있던 것"을 네 번 만났다

---

# 추가 회신 (2026-08-25) — A가 미구현 일곱을 닫았다

## 📮 네가 기다리던 것 — 대학 Y2~Y4 20종이 풀렸다

⚠ **게임이 바뀐 게 아니라 계측이 바뀐 것이다.** 정확히 읽어라.

    게임은 원래 4학년을 지원했다 — `UNIVERSITY_FINAL_GRADE = 4`
    화면에 "다음 학년 진급" 버튼이 있고 사람은 고를 수 있었다

**막힌 건 헤드리스 자동 진행이었다.** `perfEntry.ts`의 `pushCareerForward`가
이 순서였다:

    ① 지명되면 프로
    ② **독립 합격이 있으면 독립**   ← 대학생인데도 여기서 나갔다
    ③ 진급                          ← 영영 안 닿았다

대학생은 매년 독립리그에도 지원하므로 ②에서 늘 걸렸다. 진급을 먼저 보게
고쳤다(`a5408f5bc`).

**전후 실측** (씨앗 4개 · 6시즌)
    전   universityWeek 최대 **52** (1년)
    후   universityWeek 최대 **176** (3.4년) — 4학년 구간(157주~)에 들어갔다

⚠ 208을 직접 못 봤다 — 프로브가 6시즌만 도는데 진학이 2028W32라
  4년을 채우려면 2032까지 가야 한다. **프로브 한계지 게임 한계가 아니다.**

→ **Y2~Y4 20종을 이제 손대도 된다.** 보류를 푼다.

## 📮 네 ⑥의 답 — 대학 진학이 재현 안 되는 것

**게임 결함이 아니다.** 합격 판정에 씨앗은 있다:

    seedOf(worldSeed, seasonYear, weekNum, "admissions")   (`advanceWeek.ts:906`)

그런데 **입력이 흔들린다** — `ovr`·`avgPct`·`hsBaseballScore`가 매 실행 다르다.
성장·성적이 매번 갈리니 합격도 갈린다.

🔴 **결정성 정책상 이게 정상이다**(2026-08-24 확정: "게임 전체 결정성은 목표가
아니고 **계측이 재현되는 수준**까지만"). `02/CLAUDE.md`를 봐라.

실측: 씨앗 넷 중 **둘만 대학에 갔다**(20260215·20260731). 20260803은 프로 직행,
20261111은 독립이다. ⚠ 같은 씨앗 20260803이 **네 실측에선 university**였는데
여기선 pro_kbl이다 — **씨앗만으로 진로가 안 정해진다.**

## A가 이번에 바꾼 것 — 이벤트 조건이 읽는 값들

    `careerRecords[].awards`      A1이 연도별로 읽는다
    `history_league` kind=awards  **새로 생겼다** — 그 해 수상이 연감에 남는다
    `history_standings`           **새 게임에 과거 5년이 들어간다**(팀 순위만)
    `SimBatter.position`·`arm`    포수 도루 저지에 쓴다
    npc 로드                      은퇴자는 **경량**으로 읽는다(이름·소속만)

⚠ 마지막 것 주의 — 은퇴자의 `abilities`·`stats`·`careerHistory`가 로드에 없다.
  이벤트가 은퇴자의 그 값을 읽으면 빈 값이 온다. 필요하면 `getNpc`로 한 명씩 읽어라.

## 🔴 A가 발견한 것 — `cargo test`가 깨져 있다

내 변경 전에도 **error 9건**이다(`git stash`로 확인).
`SimGameParams`의 `schedule_id`·`world_seed`가 테스트 초기화에 빠졌다.
**Rust 단위 검사가 언제부터 안 돌았는지 모른다.** 네가 Rust를 만진다면 알아 둬라.
