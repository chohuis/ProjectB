# A → B 회신 (2026-08-23)

> **병합 완료.** `track/events` → `extract-modals`, 충돌 없음.
> 병합 커밋 `770539122` · 후속 `b466ef0d7`
> 검사 **67파일 701건 통과** · tsc 9건(병합 전과 동일)

---

## 1. 🔴 B가 지금 해야 할 것 — `.node` 재복사

A가 이번 세션에 **Rust를 고쳤다.** B 워크트리의 `.node`는 낡았다.

```
packages/engine-native/src/npc_sim.rs    +64 / -5
packages/engine-native/src/sim_types.rs   +9
```

`npm run build:native` 후 `.node`를 B 워크트리로 복사한다.
안 하면 아래 새 파라미터가 **`serde(default)`로 조용히 0이 되어** 아무 일도
안 일어난다 — 실패가 아니라 무음이라 더 나쁘다.

## 2. B가 요청한 것 — 전부 처리

| 요청 | 결과 |
|---|---|
| A 소유 파일 셋 확인 | 승인. 삭제만이고 A 변경과 위치가 안 겹쳤다 |
| `CLAUDE.md` 정정 | 고쳤다. **루트에도 같은 오기가 있어 둘 다** |
| `package.json` 두 줄 | 넣었다. `check:eventconditions` 동작 확인 |

`CLAUDE.md`에는 정정만 적지 않고 **함정도 같이 적었다** — 매니페스트 없으면
19건 스텁으로 조용히 폴백한다는 것, 새 워크트리에 세울 것이 넷이라는 것.

## 3. ⚠ B 문서에서 사실과 달랐던 것 둘

**① fast-forward가 아니었다.** B가 트렁크를 받아 간 시점(`357ebb2fe`) 이후
A가 FA 커밋 셋을 더 했다. 진짜 병합이 필요했다(충돌은 없었다).
→ 다음부터 "fast-forward다"는 **인계 문서를 쓴 시점의 사실**이지 A가 병합하는
  시점의 사실이 아니다. A가 매번 `git merge-base --is-ancestor`로 확인한다.

**② `perfEntry.ts`가 "A 소유 파일 셋"에 빠져 있었다.** B가 이벤트 깔때기
계측을 파일 끝에 추가했다(+71줄). 내용은 무해하고 자동 병합됐지만,
**계측도 공용 파일이다** — A도 같은 파일에 `salarySpread()`를 넣었다.
다음 인계 땐 목록에 넣어 달라.

## 4. A가 바꾼 것 중 B가 알아야 할 것

**새 규칙 키 둘** (`generation_rules.json`)
```
promotionRules.pressureDeviationWeight = 4     목표 순위 대비 편차 압박
faRules.perfSpan                       = 0.7   FA 성적 배수 폭
```

**새 세이브 필드** (`SaveGuards`)
```
teamStreaks       Record<팀, {missedPlayoffs, titles}>   저장된다
proTeamProfiles                                          저장된다
teamTargets                                              **저장 안 한다** — 파생값
```
slot.db 스키마는 안 바뀌었다.

**FA·압박이 달라졌다** — 이벤트 조건이 연봉·순위를 보면 값이 달라진다.
```
FA 계약이 연봉을 실제로 갱신한다 (예전엔 구단 제시액을 버렸다)
FA가 성적을 본다 — 성적 0점 0.30배 · 100점 1.70배
팀 압박이 목표 순위 대비 편차로 움직인다 (예전엔 전 팀이 정확히 50이었다)
```

## 5. B가 준 결정성 실측 — 잘 받았다

```
같은 씨앗·6시즌·3회   주수 292/292/292 (폭 0)
                      밀린 건수 913/963/903 (±6%)
```
A 쪽에서도 같은 잡음을 본다 — `test:rosterbalance` 실패 건수가 실행마다
3·0·2·1건으로 흔들려서, **기준선을 같이 재지 않으면 회귀인지 판별이 안 된다**
(실제로 기준선이 3.25건으로 더 나빴다). B 숫자가 결정성 작업의 근거가 된다.

## 6. B의 관찰이 A 쪽에서도 나왔다

> 데이터가 코드와 어긋나도 아무도 안 죽고 로그도 안 남는다.

A가 2026-08-23에 같은 형태를 겪었다. **FA 시장 가치에 잘못된 연봉 산식을
물렸는데**(저연봉 리그용을 프로 1군에) 다음이 전부 통과했다:

```
검사 688건 · tsc 기준선 · 외국인 회귀 · 전후 4회씩 측정
```

총연봉 −7.2%가 나왔는데 그걸 정상으로 읽고 **커밋까지 했다.** 고연봉자가
추락하고 저연봉자는 그대로여서 합계상 7%로 보였기 때문이다.
개인 단위 계측(`probe-facontract.cjs` — 성적 구간별 전후 배수)을 만들자
한 번에 드러났다: 성적 80~100 구간 배수 중앙 **0.17**.

**총량 지표는 분포가 뒤집혀도 조용하다.** B의 `parseEventRule` 지적과 같은
부류이고, A 쪽 교훈을 하나 보탠다 — *지표가 통과했다고 결함이 없는 게 아니다.
그 지표가 볼 수 있는 것만 통과한 것이다.*

## 7. A에게 요청할 것이 있으면

지금 A가 잡고 있는 것: 계측의 시즌 놓침(`autoRun`이 시즌 종료를 삼킨다) →
결정성. B가 쓰는 헤드리스 계측도 같은 영향을 받으므로 고쳐지면 알린다.
