# C63 눈확인 — 대학 학점 칸 (엔진 정본 `schoolState.universityGpa`)
#
#   DEV_PORT=5178 DRIVE_USER_DATA=1 VITE_DEV_SERVER_URL=http://localhost:5178 #     SCREENSHOT_DIR=docs/screens node scripts/drive.mjs scripts/c63-univgpa.txt
#
# ⚠ **새 게임**으로 본다 · 임시 저장소(`DRIVE_USER_DATA=1`). 고교 3년을 미느라
#   **한 판에 45분**이 든다 — 중간에 막히면 임시 저장소(`%TEMP%/drive-udd-*`)의
#   `saves/` 만 **복사해** 이어 보는 게 빠르다(원본을 넘기면 앱이 열면서 고친다).
#
# 🔴 **진로 허브를 `--auto` 에 맡기면 안 된다.** `advance` 의 HUB_TOGGLE 은
#   `.opt-btn` 중 첫 비-danger 를 켜는데 그게 **드래프트 신청**이다
#   (`CareerChoiceHubModal` 144줄) — 대학이 아니라 프로로 간다.
#   그래서 허브(`button.submit`)에서 멈춰 대학을 손으로 고른다.
#
# 🔴 **`drive.mjs` 의 확인 정규식(`^(확인|닫기|계속|시작|넘어가기)$`)이 모르는 막이
#   넷 있다** (2026-09-11 · 판마다 하나씩 걸려 네 번 다시 돌렸다).
#   드라이버는 안 건드리고 여기 `unblock` 이 치운다:
#     ① 「20XX 드래프트 — 참관/**스킵**」            매년 W32 · 첫 판이 GO×18 로 멈췄다
#     ② 시즌 종료 — 출구가 「**새 시즌 시작**」뿐(`.btn-next`) · 둘째 판이 W52 에서 GO×20
#        (투자 선택 `.invest-opt` 이 떠 있으면 그것부터)
#     ③ 「W47 지원 결과 확인」 — 버튼이 「결과 확인 →」·「진로 선택으로 →」라
#        **화살표 때문에** 정규식에 안 걸린다(`.reveal-btn` · `.actions`)
#     ④ 「체육부대 입대 신청」 — 「이번엔 아니오」
#   ⚠ 못 알아본 막은 `NOTHING [버튼 목록]` 으로 **이름을 뱉는다** — 다음 사람이
#     화면을 다시 띄워 손으로 뒤지지 않게.
#
# 🔴 **eval 줄에 `
` 을 쓰지 마라.** 생성기에서 한 번 진짜 줄바꿈이 되어 eval 이
#   두 줄로 갈렸고 드라이버가 `SyntaxError` 만 뱉었다 — 판 하나를 통째로 날렸다.
#   문자열은 `replace(/\s+/g,' ')` 로 접는다.
#
# 본 것 (2026-09-11 실측 · 영천대 1학년):
#   W33~37 첫 학기 진행 중 → 학점 「—·첫 학기 진행 중」 · 이번 학기 「—·성적 발표 전」
#           졸업선 2.00 (충족/미달 안 붙인다 — 이력이 없으면 판단하지 않는다)
#   W38    기말 정산 직후  → 학점 2.47/4.5 「보통」(g-low) · 이번 학기 「기말 2.47」
#           졸업선 2.00 「충족」
launch
resize 1440x900
sleep 3000
click-text 새 게임
sleep 1500
wait .slot-info
click .slot-info
sleep 3000
fill .field input|학점확인
sleep 800
click .btn.next
sleep 4000
wait .region-item
click .region-item
sleep 2000
wait .team-list-item
click .team-list-item
sleep 2000
click .btn.next
sleep 2500
click .btn.next
sleep 2500
click .btn.start
sleep 30000
ss 00-start
advance 40 --auto button.submit
sleep 1500
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2500
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2000
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
advance 40 --auto button.submit
sleep 1500
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2500
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2000
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
advance 40 --auto button.submit
sleep 1500
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2500
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2000
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
advance 40 --auto button.submit
sleep 1500
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2500
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2000
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
advance 40 --auto button.submit
sleep 1500
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2500
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2000
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
advance 40 --auto button.submit
sleep 1500
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2500
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2000
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
advance 40 --auto button.submit
sleep 1500
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2500
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2000
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
advance 40 --auto button.submit
sleep 1500
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2500
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2000
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
advance 40 --auto button.submit
sleep 1500
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2500
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2000
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
advance 40 --auto button.submit
sleep 1500
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2500
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2000
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
advance 40 --auto button.submit
sleep 1500
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2500
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2000
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
advance 40 --auto button.submit
sleep 1500
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2500
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2000
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
advance 40 --auto button.submit
sleep 1500
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2500
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2000
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
advance 40 --auto button.submit
sleep 1500
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2500
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2000
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
advance 40 --auto button.submit
sleep 1500
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2500
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2000
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
advance 40 --auto button.submit
sleep 1500
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2500
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2000
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
advance 40 --auto button.submit
sleep 1500
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2500
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2000
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
advance 40 --auto button.submit
sleep 1500
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2500
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2000
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
advance 40 --auto button.submit
sleep 1500
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2500
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2000
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
advance 40 --auto button.submit
sleep 1500
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2500
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2000
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
advance 40 --auto button.submit
sleep 1500
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2500
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2000
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
advance 40 --auto button.submit
sleep 1500
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2500
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2000
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
advance 40 --auto button.submit
sleep 1500
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2500
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2000
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
advance 40 --auto button.submit
sleep 1500
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2500
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2000
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
advance 40 --auto button.submit
sleep 1500
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2500
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2000
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
advance 40 --auto button.submit
sleep 1500
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2500
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2000
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
advance 40 --auto button.submit
sleep 1500
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2500
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2000
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
advance 40 --auto button.submit
sleep 1500
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2500
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2000
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
advance 40 --auto button.submit
sleep 1500
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2500
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2000
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
advance 40 --auto button.submit
sleep 1500
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2500
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2000
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
advance 40 --auto button.submit
sleep 1500
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2500
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2000
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
advance 40 --auto button.submit
sleep 1500
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2500
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2000
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
advance 40 --auto button.submit
sleep 1500
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2500
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2000
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
advance 40 --auto button.submit
sleep 1500
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2500
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2000
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
advance 40 --auto button.submit
sleep 1500
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2500
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2000
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
advance 40 --auto button.submit
sleep 1500
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2500
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2000
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
advance 40 --auto button.submit
sleep 1500
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2500
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2000
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
advance 40 --auto button.submit
sleep 1500
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2500
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2000
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
advance 40 --auto button.submit
sleep 1500
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2500
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2000
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
advance 40 --auto button.submit
sleep 1500
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2500
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2000
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2000
advance 40 --auto button.submit
sleep 1500
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2500
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2000
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2000
advance 40 --auto button.submit
sleep 1500
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2500
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2000
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2000
advance 40 --auto button.submit
sleep 1500
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2500
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2000
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2000
advance 40 --auto button.submit
sleep 1500
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2500
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2000
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2000
advance 40 --auto button.submit
sleep 1500
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2500
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2000
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2000
advance 40 --auto button.submit
sleep 1500
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2500
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2000
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2000
advance 40 --auto button.submit
sleep 1500
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2500
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2000
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2000
advance 40 --auto button.submit
sleep 1500
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2500
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2000
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2000
advance 40 --auto button.submit
sleep 1500
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2500
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2000
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2000
advance 40 --auto button.submit
sleep 1500
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2500
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2000
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2000
advance 40 --auto button.submit
sleep 1500
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2500
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2000
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2000
advance 40 --auto button.submit
sleep 1500
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2500
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2000
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2000
advance 40 --auto button.submit
sleep 1500
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2500
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2000
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2000
ss 01-hub
eval document.querySelector('.wk')?.innerText ?? '(주차 없음)'
eval (()=>{const b=[...document.querySelectorAll('.opt-btn')].find(x=>x.innerText.includes('대학 진학'));if(!b)return 'NO_UNIV_BTN:'+[...document.querySelectorAll('.opt-btn')].map(x=>x.innerText.trim().replace(/\s+/g,' ')).join(' / ');b.click();return 'OK 대학 진학 신청 열었다'})()
sleep 9000
ss 02-univ-modal
eval (()=>{const rows=[...document.querySelectorAll('.rows button')];if(!rows.length)return 'NO_ROWS';const pass=rows.filter(r=>r.querySelector('.elig-status.pass'));const pick=pass[pass.length-1]??rows[rows.length-1];pick.click();return (pass.length?'자격 통과 '+pass.length+'팀 · 고른 것: ':'자격 0팀 · 그냥 마지막: ')+pick.innerText.trim().replace(/\s+/g,' ')})()
sleep 2000
click .pick-btn
sleep 1200
eval (()=>{const b=[...document.querySelectorAll('.actions .btns button')].find(x=>x.innerText.trim()==='확인');if(!b)return 'NO_CONFIRM';if(b.disabled)return 'CONFIRM_DISABLED';b.click();return 'OK 확인'})()
sleep 2500
eval (()=>{const s=document.querySelector('button.submit');if(!s)return 'NO_SUBMIT';if(s.disabled)return 'SUBMIT_DISABLED';s.click();return 'OK 신청 완료'})()
sleep 5000
ss 03-submitted
advance 20 --auto
sleep 1500
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2500
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2000
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2000
advance 20 --auto
sleep 1500
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2500
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2000
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2000
advance 20 --auto
sleep 1500
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2500
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2000
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2000
advance 20 --auto
sleep 1500
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2500
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2000
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2000
advance 20 --auto
sleep 1500
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2500
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2000
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2000
advance 20 --auto
sleep 1500
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2500
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2000
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2000
advance 20 --auto
sleep 1500
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2500
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2000
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2000
advance 20 --auto
sleep 1500
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2500
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2000
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2000
eval (()=>{const t=[...document.querySelectorAll('nav.nav .tab')].find(b=>b.innerText.trim().startsWith('나'));if(!t)return 'NO_ME_TAB';t.click();return 'OK me'})()
sleep 2000
eval (()=>{const t=[...document.querySelectorAll('.me .tabs .tab')];const a=t.find(b=>/학업/.test(b.innerText));if(!a)return 'NO_ACADEMICS_TAB:'+t.map(b=>b.innerText.trim()).join(' / ');a.click();return 'OK 학업'})()
sleep 2500
ss 04-univ-academics-first
eval document.querySelector('.wk')?.innerText ?? '(주차 없음)'
eval [...document.querySelectorAll('.summary-row .summary-item')].map(i=>i.innerText.replace(/\s+/g,' │ '))
eval (()=>{const g=[...document.querySelectorAll('.summary-item')].find(i=>i.querySelector('.lbl')?.innerText.trim()==='학점');if(!g)return 'NO_GPA_CELL';const s=g.querySelector('strong');return {val:s.innerText.replace(/\s+/g,' '),cls:s.className,sub:g.querySelector('.sub')?.innerText??''}})()
advance 8 --auto
sleep 1500
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2500
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2000
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2000
advance 8 --auto
sleep 1500
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2500
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2000
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2000
advance 8 --auto
sleep 1500
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2500
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2000
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2000
advance 8 --auto
sleep 1500
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2500
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2000
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2000
advance 8 --auto
sleep 1500
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2500
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2000
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2000
eval (()=>{const t=[...document.querySelectorAll('nav.nav .tab')].find(b=>b.innerText.trim().startsWith('나'));if(!t)return 'NO_ME_TAB';t.click();return 'OK me'})()
sleep 2000
eval (()=>{const t=[...document.querySelectorAll('.me .tabs .tab')];const a=t.find(b=>/학업/.test(b.innerText));if(!a)return 'NO_ACADEMICS_TAB:'+t.map(b=>b.innerText.trim()).join(' / ');a.click();return 'OK 학업'})()
sleep 2500
ss 05-univ-academics-midterm
eval document.querySelector('.wk')?.innerText ?? '(주차 없음)'
eval [...document.querySelectorAll('.summary-row .summary-item')].map(i=>i.innerText.replace(/\s+/g,' │ '))
eval (()=>{const g=[...document.querySelectorAll('.summary-item')].find(i=>i.querySelector('.lbl')?.innerText.trim()==='학점');if(!g)return 'NO_GPA_CELL';const s=g.querySelector('strong');return {val:s.innerText.replace(/\s+/g,' '),cls:s.className,sub:g.querySelector('.sub')?.innerText??''}})()
advance 10 --auto
sleep 1500
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2500
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2000
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2000
advance 10 --auto
sleep 1500
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2500
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2000
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2000
advance 10 --auto
sleep 1500
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2500
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2000
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2000
advance 10 --auto
sleep 1500
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2500
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2000
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2000
advance 10 --auto
sleep 1500
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2500
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2000
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2000
advance 10 --auto
sleep 1500
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2500
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2000
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2000
advance 10 --auto
sleep 1500
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2500
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2000
eval (()=>{const vis=el=>!!el&&!el.disabled&&el.offsetParent!==null;const rv=[...document.querySelectorAll('.reveal-btn')].filter(vis);if(rv.length){rv[0].click();return 'REVEAL';}const act=[...document.querySelectorAll('.actions button')].filter(vis).find(b=>b.innerText.includes('진로 선택으로'));if(act){act.click();return 'TO_CHOICE';}const mil=[...document.querySelectorAll('button')].filter(vis).find(b=>/이번엔 아니오/.test(b.innerText));if(mil){mil.click();return 'MIL_NO';}const inv=[...document.querySelectorAll('.invest-opt')].filter(vis);if(inv.length){inv[0].click();return 'INVEST';}const ns=document.querySelector('.btn-next');if(vis(ns)){ns.click();return 'NEW_SEASON';}const bs=[...document.querySelectorAll('button')].filter(vis);const t=bs.find(b=>/^(스킵|건너뛰기|확인|닫기|계속|시작|넘어가기|나중에|새 시즌 시작)$/.test(b.innerText.trim()));if(t){t.click();return 'BTN:'+t.innerText.trim();}return 'NOTHING ['+bs.slice(0,10).map(b=>b.innerText.trim().replace(/\s+/g,' ').slice(0,14)).join('|')+']';})()
sleep 2000
eval (()=>{const t=[...document.querySelectorAll('nav.nav .tab')].find(b=>b.innerText.trim().startsWith('나'));if(!t)return 'NO_ME_TAB';t.click();return 'OK me'})()
sleep 2000
eval (()=>{const t=[...document.querySelectorAll('.me .tabs .tab')];const a=t.find(b=>/학업/.test(b.innerText));if(!a)return 'NO_ACADEMICS_TAB:'+t.map(b=>b.innerText.trim()).join(' / ');a.click();return 'OK 학업'})()
sleep 2500
ss 06-univ-academics-final
eval document.querySelector('.wk')?.innerText ?? '(주차 없음)'
eval [...document.querySelectorAll('.summary-row .summary-item')].map(i=>i.innerText.replace(/\s+/g,' │ '))
eval (()=>{const g=[...document.querySelectorAll('.summary-item')].find(i=>i.querySelector('.lbl')?.innerText.trim()==='학점');if(!g)return 'NO_GPA_CELL';const s=g.querySelector('strong');return {val:s.innerText.replace(/\s+/g,' '),cls:s.className,sub:g.querySelector('.sub')?.innerText??''}})()
quit
