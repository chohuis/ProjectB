node_modules/
dist/
.vite/
release/

# Rust 빌드 아티팩트
packages/engine-native/target/
packages/engine-native/*.node
# 그 산물이 **어느 소스에서 나왔는지** 적어 둔 도장 (scripts/native-stamp.cjs).
# 산물과 짝이라 산물처럼 무시한다 — 커밋하면 남의 빌드 도장을 물려받는다
packages/engine-native/.native-stamp.json

# 빌드 시 자동 생성되는 마스터 데이터 인덱스
resource/data/master/_manifest.json
resource/data/master/entities/players/_index.json

# 사용자 저장 데이터
userData/

# 로컬 설정
.env
.env.local

# 하네스 실행 결과 (생성물 — DATA_POLICY §1 ③파생)
docs/reports/harness_last.json

# 드라이버가 남기는 스크린샷 — 검증용이지 산출물이 아니다
resource/logs/shots/
# UI 순회기(scripts/ui-walk.mjs)가 남기는 캡처. 한 번 돌 때 수십 장이라
# 커밋에 섞이면 diff가 안 읽힌다
resource/logs/uiwalk/

# 계측 덤프 (probe-npc-dump.cjs 가 남기는 생성물 — DATA_POLICY §1 ③파생)
resource/logs/d-regress/

# D 세이브 산출물(probe-make-c9b-save.cjs 등) — slot.db 사본이라 판마다 수백 MB.
# C 눈확인용이라 절대경로로 전달하지 커밋하지 않는다
resource/logs/d-saves/
resource/master.db
resource/master.db-shm
resource/master.db-wal
